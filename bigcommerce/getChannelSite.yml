apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Channel Site
  description: >-
    Alias of `GET /sites?channel_id=channel_id`Returns site data for the
    specified channel.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Channel Site
      description: >-
        Alias of `GET /sites?channel_id=channel_id`Returns site data for the
        specified channel.
    paths:
      /channels/{channel_id}/site:
        get:
          summary: Get A Channel Site
          description: >-
            Alias of `GET /sites?channel_id=channel_id`Returns site data for the
            specified channel.
          operationId: getChannelSite
          responses:
            '200':
              $ref: '#/components/responses/site_Resp'
