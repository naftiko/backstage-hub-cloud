apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Activate A Theme
  description: Actives a store *Theme*.This returns a 204 response upon success.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Activate A Theme
      description: Actives a store *Theme*.This returns a 204 response upon success.
    paths:
      /themes/actions/activate:
        post:
          summary: Activate A Theme
          description: Actives a store *Theme*.This returns a 204 response upon success.
          operationId: activateStoreTheme
          requestBody:
            description: Request parameters.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/Activate'
          responses:
            default:
              description: Error message.
              content:
                application/json:
                  schema:
                    allOf:
                      - type: object
                        description: |
                          Error payload for the BigCommerce API.
                        properties:
                          status:
                            description: |
                              The HTTP status code.
                            type: integer
                          title:
                            description: |
                              The error title describing the particular error.
                            type: string
                          type:
                            type: string
                          instance:
                            type: string
                        title: Base Error
                      - type: object
                        properties:
                          errors:
                            type: object
                            properties: {}
                            additionalProperties: true
                            title: Detailed Errors
                    title: Error Response
