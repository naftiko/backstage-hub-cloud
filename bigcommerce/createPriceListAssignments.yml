apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Price List Assignments
  description: >-
    Creates a batch of `Price List Assignments`. **Note:** The batch limit for
    `Price List Assignments` is 25.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Price List Assignments
      description: >-
        Creates a batch of `Price List Assignments`. **Note:** The batch limit for
        `Price List Assignments` is 25.
    paths:
      /pricelists/assignments:
        post:
          summary: Create Price List Assignments
          description: >-
            Creates a batch of `Price List Assignments`. **Note:** The batch limit
            for `Price List Assignments` is 25.
          operationId: createPriceListAssignments
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/CreateBatchPriceListAssignmentsRequest'
          responses:
            '422':
              description: Error response. Includes the errors for each reference ID.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/PriceListAssignmentsBatchErrorResponse'
