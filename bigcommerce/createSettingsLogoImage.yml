apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Logo Image
  description: >-
    Uploads an image file to use as the storefront logo. Supported MIME types
    include GIF, JPEG, and PNG.  - Channel ID can be used as a query parameter
    for updating channel-specific setting. If omitted, you will interact with
    the global setting only.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Logo Image
      description: >-
        Uploads an image file to use as the storefront logo. Supported MIME types
        include GIF, JPEG, and PNG.  - Channel ID can be used as a query parameter
        for updating channel-specific setting. If omitted, you will interact with
        the global setting only.
    paths:
      /settings/logo/image:
        post:
          summary: Create Logo Image
          description: >-
            Uploads an image file to use as the storefront logo. Supported MIME
            types include GIF, JPEG, and PNG.  - Channel ID can be used as a query
            parameter for updating channel-specific setting. If omitted, you will
            interact with the global setting only.
          operationId: createSettingsLogoImage
          requestBody:
            content:
              multipart/form-data:
                schema:
                  type: object
                  properties:
                    LogoFile:
                      type: string
                      format: binary
          responses:
            '204':
              description: OK
