apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Categories
  description: >-
    Returns a list of categories. To get a specific category in a tree, provide
    a category ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Categories
      description: >-
        Returns a list of categories. To get a specific category in a tree, provide
        a category ID.
    paths:
      /catalog/trees/categories:
        get:
          summary: Get All Categories
          description: >-
            Returns a list of categories. To get a specific category in a tree,
            provide a category ID.
          operationId: getAllCategories
          responses:
            '500':
              description: Internal Server Error
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ErrorRequest'
