apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Check Zone Given An Address.
  description: Check the applicable tax zone for a given address and customer group.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Check Zone Given An Address.
      description: Check the applicable tax zone for a given address and customer group.
    paths:
      /tax/zonecheck:
        post:
          summary: Check Zone Given An Address.
          description: Check the applicable tax zone for a given address and customer group.
          operationId: zonecheck
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  type: array
                  items:
                    $ref: '#/components/schemas/ZoneCheck'
          responses:
            '422':
              description: The request body does not meet specifications.
