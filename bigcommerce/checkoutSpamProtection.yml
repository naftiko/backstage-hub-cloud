apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Checkout Spam Protection
  description: >-
    Verifies if checkout is created by human.> #### Note> * Substitute your
    storefront domain for `yourstore.example.com`. > * The Send a Test Request
    feature is not currently supported for this endpoint.  
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Checkout Spam Protection
      description: >-
        Verifies if checkout is created by human.> #### Note> * Substitute your
        storefront domain for `yourstore.example.com`. > * The Send a Test Request
        feature is not currently supported for this endpoint.  
    paths:
      /checkouts/{checkoutId}/spam-protection:
        post:
          summary: Checkout Spam Protection
          description: >-
            Verifies if checkout is created by human.> #### Note> * Substitute your
            storefront domain for `yourstore.example.com`. > * The Send a Test
            Request feature is not currently supported for this endpoint.  
          operationId: checkoutSpamProtection
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/SpamProtectionRequest'
          responses:
            '200':
              description: Returns Checkout Object.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/checkout_Full'
