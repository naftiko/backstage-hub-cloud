apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get System Timestamp
  description: >-
    Returns the system timestamp at the time of the request. The time resource
    is useful for validating API authentication details and testing client
    connections.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get System Timestamp
      description: >-
        Returns the system timestamp at the time of the request. The time resource
        is useful for validating API authentication details and testing client
        connections.
    paths:
      /time:
        get:
          summary: Get System Timestamp
          description: >-
            Returns the system timestamp at the time of the request. The time
            resource is useful for validating API authentication details and testing
            client connections.
          operationId: getSystemTimestamp
          responses:
            '200':
              description: ''
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/timeStamp_Full'
                  examples:
                    response:
                      value:
                        time: 1529512970
