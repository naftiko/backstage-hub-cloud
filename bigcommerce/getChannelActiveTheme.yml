apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Channel Active Theme
  description: >-
    Returns details of the theme active on the specified channel.Does not
    support active Blueprint (legacy) themes.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Channel Active Theme
      description: >-
        Returns details of the theme active on the specified channel.Does not
        support active Blueprint (legacy) themes.
    paths:
      /channels/{channel_id}/active-theme:
        get:
          summary: Get A Channel Active Theme
          description: >-
            Returns details of the theme active on the specified channel.Does not
            support active Blueprint (legacy) themes.
          operationId: getChannelActiveTheme
          responses:
            '404':
              $ref: '#/components/responses/active_theme_not_found_resp'
