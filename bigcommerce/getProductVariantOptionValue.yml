apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Product Variant Option Value
  description: >-
    Returns a single *Variant Option Value*. Optional parameters can be passed
    in.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Product Variant Option Value
      description: >-
        Returns a single *Variant Option Value*. Optional parameters can be passed
        in.
    paths:
      /catalog/products/{product_id}/options/{option_id}/values/{value_id}:
        get:
          summary: Get A Product Variant Option Value
          description: >-
            Returns a single *Variant Option Value*. Optional parameters can be
            passed in.
          operationId: getProductVariantOptionValue
          responses:
            '404':
              description: |
                The resource was not found.
              content:
                application/json:
                  schema:
                    title: Not Found
                    type: object
                    properties:
                      status:
                        type: integer
                        description: |
                          404 HTTP status code.
                      title:
                        type: string
                        description: The error title describing the particular error.
                      type:
                        type: string
                      instance:
                        type: string
                    description: Error payload for the BigCommerce API.
