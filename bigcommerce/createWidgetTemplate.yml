apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Widget Template
  description: >-
    Creates a **Widget Template**.***Note:*** *There is a limit of 1000 custom
    widget templates per store.***Required Fields*** name* template
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Widget Template
      description: >-
        Creates a **Widget Template**.***Note:*** *There is a limit of 1000 custom
        widget templates per store.***Required Fields*** name* template
    paths:
      /content/widget-templates:
        post:
          summary: Create A Widget Template
          description: >-
            Creates a **Widget Template**.***Note:*** *There is a limit of 1000
            custom widget templates per store.***Required Fields*** name* template
          operationId: createWidgetTemplate
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/widgetTemplate_Post'
          responses:
            '422':
              $ref: '#/components/responses/Error422_Resp'
