apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update Tax Properties
  description: Update one or more tax properties. Only fields specified will be adjusted.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update Tax Properties
      description: Update one or more tax properties. Only fields specified will be adjusted.
    paths:
      /tax/properties:
        put:
          summary: Update Tax Properties
          description: >-
            Update one or more tax properties. Only fields specified will be
            adjusted.
          operationId: updateTaxProperties
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  type: array
                  items:
                    $ref: '#/components/schemas/PropertyPUT'
          responses:
            '422':
              description: The request body does not meet specifications.
