apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Product Custom Field
  description: >-
    Returns a *Custom Field*.**Note:**The default rate limit for this endpoint
    is 40 concurrent requests.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Product Custom Field
      description: >-
        Returns a *Custom Field*.**Note:**The default rate limit for this endpoint
        is 40 concurrent requests.
    paths:
      /catalog/products/{product_id}/custom-fields/{custom_field_id}:
        get:
          summary: Get A Product Custom Field
          description: >-
            Returns a *Custom Field*.**Note:**The default rate limit for this
            endpoint is 40 concurrent requests.
          operationId: getProductCustomField
          responses:
            '405':
              $ref: '#/components/responses/MethodNotAllowedError'
