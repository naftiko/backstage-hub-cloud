apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Site’s Ssl/tls Certificate Information
  description: Obtain information about a site’s SSL/TLS certificate.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Site’s Ssl/tls Certificate Information
      description: Obtain information about a site’s SSL/TLS certificate.
    paths:
      /sites/{site_id}/certificate:
        get:
          summary: Get A Site’s Ssl/tls Certificate Information
          description: Obtain information about a site’s SSL/TLS certificate.
          operationId: getSiteCertificate
          responses:
            '200':
              description: OK
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/CertificateResponse'
                  examples: {}
