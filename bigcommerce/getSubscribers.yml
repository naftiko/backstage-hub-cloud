apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Subscribers
  description: >-
    Returns a list of *Subscribers*. Optional filter parameters can be passed
    in.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Subscribers
      description: >-
        Returns a list of *Subscribers*. Optional filter parameters can be passed
        in.
    paths:
      /customers/subscribers:
        get:
          summary: Get Subscribers
          description: >-
            Returns a list of *Subscribers*. Optional filter parameters can be
            passed in.
          operationId: getSubscribers
          responses:
            '200':
              description: ''
              content:
                application/json:
                  schema:
                    title: Subscriber Collection Response
                    type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/subscriber_Full'
                      meta:
                        $ref: '#/components/schemas/CollectionMeta'
                    description: |
                      Response payload for the BigCommerce API.
