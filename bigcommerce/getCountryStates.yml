apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Countryʼs States
  description: >-
    Returns a list of *States* belonging to a *Country*. A state or province,
    identifiable by an ISO 3166 subdivision code.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Countryʼs States
      description: >-
        Returns a list of *States* belonging to a *Country*. A state or province,
        identifiable by an ISO 3166 subdivision code.
    paths:
      /countries/{country_id}/states:
        get:
          summary: Get All Countryʼs States
          description: >-
            Returns a list of *States* belonging to a *Country*. A state or
            province, identifiable by an ISO 3166 subdivision code.
          operationId: getCountryStates
          responses:
            '200':
              $ref: '#/components/responses/countriesStatesCollectionResponse'
