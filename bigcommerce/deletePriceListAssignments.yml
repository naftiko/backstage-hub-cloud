apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Price List Assignments
  description: >-
    Deletes one or more `Price List Assignments` objects from BigCommerce using
    a query parameter. You must use at least one query parameter. 
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Price List Assignments
      description: >-
        Deletes one or more `Price List Assignments` objects from BigCommerce using
        a query parameter. You must use at least one query parameter. 
    paths:
      /pricelists/assignments:
        delete:
          summary: Delete Price List Assignments
          description: >-
            Deletes one or more `Price List Assignments` objects from BigCommerce
            using a query parameter. You must use at least one query parameter. 
          operationId: deletePriceListAssignments
          responses:
            '204':
              description: No Content.
