apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Count Of All States
  description: Returns a count of all states.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Count Of All States
      description: Returns a count of all states.
    paths:
      /countries/states/count:
        get:
          summary: Get A Count Of All States
          description: Returns a count of all states.
          operationId: getStatesCount
          responses:
            '200':
              $ref: '#/components/responses/countResponse'
