apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Site Certificates
  description: Return all SSL certificates connected to domains within a store.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Site Certificates
      description: Return all SSL certificates connected to domains within a store.
    paths:
      /sites/certificates:
        get:
          summary: Get Site Certificates
          description: Return all SSL certificates connected to domains within a store.
          operationId: getSitesCertificates
          responses:
            '200':
              description: OK
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/GetCertificatesResponse'
