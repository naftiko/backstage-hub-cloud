apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Web Analytics Providers
  description: Returns a list of web analytics providers.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Web Analytics Providers
      description: Returns a list of web analytics providers.
    paths:
      /settings/analytics:
        get:
          summary: Get All Web Analytics Providers
          description: Returns a list of web analytics providers.
          operationId: getAnalyticsProviders
          responses:
            '200':
              description: OK
              content:
                application/json:
                  schema:
                    type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/AnalyticsProviders'
                      meta:
                        $ref: '#/components/schemas/MetaOpen'
