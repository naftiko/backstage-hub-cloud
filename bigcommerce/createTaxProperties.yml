apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Tax Properties
  description: >-
    Create one or more tax properties. A **code** and a **display name** must be
    included when creating tax properties.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Tax Properties
      description: >-
        Create one or more tax properties. A **code** and a **display name** must be
        included when creating tax properties.
    paths:
      /tax/properties:
        post:
          summary: Create Tax Properties
          description: >-
            Create one or more tax properties. A **code** and a **display name**
            must be included when creating tax properties.
          operationId: createTaxProperties
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  type: array
                  items:
                    $ref: '#/components/schemas/PropertyPOST'
          responses:
            '422':
              description: The request body does not meet specifications.
