apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Currency
  description: >-
    Deletes a *Currency*.If a currencyʼs `is_default` property is set to true,
    this currency cannot be deleted. 
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Currency
      description: >-
        Deletes a *Currency*.If a currencyʼs `is_default` property is set to true,
        this currency cannot be deleted. 
    paths:
      /currencies/{id}:
        delete:
          summary: Delete A Currency
          description: >-
            Deletes a *Currency*.If a currencyʼs `is_default` property is set to
            true, this currency cannot be deleted. 
          operationId: deleteCurrency
          responses:
            '204':
              description: ''
              content:
                application/json:
                  schema:
                    type: object
