apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Parameters Tax Providers Provider_id Connection
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Parameters Tax Providers Provider_id Connection
      description: Needs a more full description created.
    paths:
      /tax/providers/{provider_id}/connection:
        parameters:
          summary: Parameters Tax Providers Provider_id Connection
          description: Needs a more full description created.
          operationId: parameterstaxprovidersprovideridconnection
