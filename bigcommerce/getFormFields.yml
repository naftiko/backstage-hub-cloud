apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Form Fields
  description: >-
    Gets form fields.> #### Note> * Substitute your storefront domain for
    `yourstore.example.com`. > * The Send a Test Request feature is not
    currently supported for this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Form Fields
      description: >-
        Gets form fields.> #### Note> * Substitute your storefront domain for
        `yourstore.example.com`. > * The Send a Test Request feature is not
        currently supported for this endpoint.
    paths:
      /form-fields:
        get:
          summary: Get Form Fields
          description: >-
            Gets form fields.> #### Note> * Substitute your storefront domain for
            `yourstore.example.com`. > * The Send a Test Request feature is not
            currently supported for this endpoint.
          operationId: getFormFields
          responses:
            '200':
              description: Returns an object with form fields groups.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/FormFieldGroups'
