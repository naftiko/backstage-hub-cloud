apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Price Record By Currency Code
  description: >-
    Deletes a *Price List Record* using the currency code.**Note:*** Supports up
    to 25 simultaneous DELETE requests. Running more than the allowed number of
    requests concurrently on the same store will result in a `429` status error,
    and your additional requests will fail.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Price Record By Currency Code
      description: >-
        Deletes a *Price List Record* using the currency code.**Note:*** Supports up
        to 25 simultaneous DELETE requests. Running more than the allowed number of
        requests concurrently on the same store will result in a `429` status error,
        and your additional requests will fail.
    paths:
      /pricelists/{price_list_id}/records/{variant_id}/{currency_code}:
        delete:
          summary: Delete A Price Record By Currency Code
          description: >-
            Deletes a *Price List Record* using the currency code.**Note:***
            Supports up to 25 simultaneous DELETE requests. Running more than the
            allowed number of requests concurrently on the same store will result in
            a `429` status error, and your additional requests will fail.
          operationId: deletePriceListRecord
          responses:
            '204':
              description: ''
              content: {}
