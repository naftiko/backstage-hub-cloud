apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Subscriber
  description: Creates a *Subscriber*.**Required Fields*** email**Read Only Fields*** id
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Subscriber
      description: Creates a *Subscriber*.**Required Fields*** email**Read Only Fields*** id
    paths:
      /customers/subscribers:
        post:
          summary: Create A Subscriber
          description: >-
            Creates a *Subscriber*.**Required Fields*** email**Read Only Fields***
            id
          operationId: createSubscriber
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/subscriber_Post'
          responses:
            '422':
              description: >
                The `Subscriber` was not valid. This is the result of missing
                required fields, or of invalid data. See the response for more
                details.
              content:
                application/json:
                  schema:
                    title: Error Response
                    type: object
                    properties:
                      errors:
                        title: Detailed Errors
                        type: object
                        properties: {}
                        additionalProperties: true
                      instance:
                        type: string
                      status:
                        type: integer
                        description: |
                          The HTTP status code.
                      title:
                        type: string
                        description: |
                          The error title describing the particular error.
                      type:
                        type: string
