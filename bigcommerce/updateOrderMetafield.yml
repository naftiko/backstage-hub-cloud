apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update A Metafield
  description: >-
    Updates a `Metafield` object.The maxiumum number of metafields allowed on
    each order, product, category, variant, or brand is 250 per client ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update A Metafield
      description: >-
        Updates a `Metafield` object.The maxiumum number of metafields allowed on
        each order, product, category, variant, or brand is 250 per client ID.
    paths:
      /orders/{order_id}/metafields/{metafield_id}:
        put:
          summary: Update A Metafield
          description: >-
            Updates a `Metafield` object.The maxiumum number of metafields allowed
            on each order, product, category, variant, or brand is 250 per client
            ID.
          operationId: updateOrderMetafield
          requestBody:
            description: |
              A `Metafield` object.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/MetafieldPut'
          responses:
            '404':
              description: |
                The resource was not found.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/NotFound'
