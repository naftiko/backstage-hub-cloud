apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Token
  description: >-
    Creates a Storefront API token.**Required Scopes*** `Manage` `Storefront API
    Tokens`
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Token
      description: >-
        Creates a Storefront API token.**Required Scopes*** `Manage` `Storefront API
        Tokens`
    paths:
      /storefront/api-token:
        post:
          summary: Create A Token
          description: >-
            Creates a Storefront API token.**Required Scopes*** `Manage` `Storefront
            API Tokens`
          operationId: createToken
          requestBody:
            content:
              application/json:
                schema:
                  allOf:
                    - $ref: '#/components/schemas/TokenPostSimple'
                    - $ref: '#/components/schemas/TokenPostImpersonation'
          responses:
            '422':
              description: Invalid JSON request body - missing or invalid data.
              content: {}
