apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update A Cart Metafield
  description: Update a `Metafield`, by `cart_id`.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update A Cart Metafield
      description: Update a `Metafield`, by `cart_id`.
    paths:
      /carts/{cart_id}/metafields/{metafield_id}:
        put:
          summary: Update A Cart Metafield
          description: Update a `Metafield`, by `cart_id`.
          operationId: updateCartMetafield
          requestBody:
            description: |
              A `Metafield` object.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/MetafieldBase_Post'
                examples: {}
          responses:
            '404':
              description: |
                The resource was not found.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/NotFound'
                  examples:
                    example-1:
                      value:
                        status: 404
                        title: There was no order found with ID 1010
                        type: >-
                          https://developer.bigcommerce.com/api-docs/getting-started/api-status-codes
