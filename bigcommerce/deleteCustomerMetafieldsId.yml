apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Customer Metafields
  description: >-
    Deletes customer metafields. To delete customer metafields, use 'customerId'
    and 'metafieldId' in the query parameters.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Customer Metafields
      description: >-
        Deletes customer metafields. To delete customer metafields, use 'customerId'
        and 'metafieldId' in the query parameters.
    paths:
      /customers/{customerId}/metafields/{metafieldId}:
        delete:
          summary: Delete Customer Metafields
          description: >-
            Deletes customer metafields. To delete customer metafields, use
            'customerId' and 'metafieldId' in the query parameters.
          operationId: deleteCustomerMetafieldsId
          requestBody:
            content:
              application/json:
                schema:
                  type: array
                  items:
                    type: integer
          responses:
            '204':
              description: |
                Response object for customer metafields deletion with success.
