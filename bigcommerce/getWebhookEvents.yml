apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Events
  description: >-
    Get a list of events that were sent but not successfully received. Events
    are stored for not less than one week.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Events
      description: >-
        Get a list of events that were sent but not successfully received. Events
        are stored for not less than one week.
    paths:
      /hooks/events:
        get:
          summary: Get Events
          description: >-
            Get a list of events that were sent but not successfully received.
            Events are stored for not less than one week.
          operationId: getWebhookEvents
          responses:
            '401':
              $ref: '#/components/responses/401_Unauthorized'
