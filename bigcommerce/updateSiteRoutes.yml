apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update A Site’s Routes
  description: >-
    Upsert routes for site with ID `{site_id}`.## Usage Notes* `id` is required
    when updating an existing route.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update A Site’s Routes
      description: >-
        Upsert routes for site with ID `{site_id}`.## Usage Notes* `id` is required
        when updating an existing route.
    paths:
      /sites/{site_id}/routes:
        put:
          summary: Update A Site’s Routes
          description: >-
            Upsert routes for site with ID `{site_id}`.## Usage Notes* `id` is
            required when updating an existing route.
          operationId: updateSiteRoutes
          requestBody:
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/siteRoute_Full'
          responses:
            '422':
              $ref: '#/components/responses/BulkErrorResponse'
