apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Price List Assignments
  description: >-
    Fetches an array of `Price List Assignments` matching a particular Customer
    Group and Price List and Channel.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Price List Assignments
      description: >-
        Fetches an array of `Price List Assignments` matching a particular Customer
        Group and Price List and Channel.
    paths:
      /pricelists/assignments:
        get:
          summary: Get Price List Assignments
          description: >-
            Fetches an array of `Price List Assignments` matching a particular
            Customer Group and Price List and Channel.
          operationId: getListOfPriceListAssignments
          responses:
            '200':
              description: An array of price list assignments and metadata.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/AssignmentsForGetResponse'
