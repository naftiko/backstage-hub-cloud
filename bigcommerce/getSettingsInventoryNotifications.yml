apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Inventory Notifications Settings
  description: >-
    Returns inventory notification settings. * `channel_id` can be used as a
    query parameter to get inventory notification settings per channel. If
    omitted, you will interact with the global setting only.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Inventory Notifications Settings
      description: >-
        Returns inventory notification settings. * `channel_id` can be used as a
        query parameter to get inventory notification settings per channel. If
        omitted, you will interact with the global setting only.
    paths:
      /settings/inventory/notifications:
        get:
          summary: Get Inventory Notifications Settings
          description: >-
            Returns inventory notification settings. * `channel_id` can be used as a
            query parameter to get inventory notification settings per channel. If
            omitted, you will interact with the global setting only.
          operationId: getSettingsInventoryNotifications
          responses:
            '200':
              description: >-
                OK. `null` indicates that a particular field has not been overridden
                on a channel level when channel level settings are requested
              content:
                application/json:
                  examples: {}
                  schema:
                    type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/InventoryNotificationsSettings'
                      meta:
                        $ref: '#/components/schemas/MetaOpen'
