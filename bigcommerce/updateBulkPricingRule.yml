apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update A Bulk Pricing Rule
  description: >-
    Updates a *Bulk Pricing Rule*.**Required Fields*** none**Read-Only Fields**-
    id
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update A Bulk Pricing Rule
      description: >-
        Updates a *Bulk Pricing Rule*.**Required Fields*** none**Read-Only Fields**-
        id
    paths:
      /catalog/products/{product_id}/bulk-pricing-rules/{bulk_pricing_rule_id}:
        put:
          summary: Update A Bulk Pricing Rule
          description: >-
            Updates a *Bulk Pricing Rule*.**Required Fields*** none**Read-Only
            Fields**- id
          operationId: updateBulkPricingRule
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  allOf:
                    - type: object
                      properties:
                        id:
                          type: integer
                          description: Unique ID of the *Bulk Pricing Rule*. Read-Only.
                          readOnly: true
                      required:
                        - id
                    - $ref: '#/components/schemas/bulkPricingRule_Full'
          responses:
            '422':
              description: >
                The `BulkPricingRule` was not valid. This is the result of missing
                required fields, or of invalid data. See the response for more
                details.
              content:
                application/json:
                  schema:
                    title: Error Response
                    type: object
                    properties:
                      errors:
                        title: Detailed Errors
                        type: object
                        properties: {}
                        additionalProperties: true
                      instance:
                        type: string
                      status:
                        type: integer
                        description: |
                          The HTTP status code.
                      title:
                        type: string
                        description: |
                          The error title describing the particular error.
                      type:
                        type: string
