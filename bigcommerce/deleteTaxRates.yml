apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Tax Rates
  description: >-
    Delete one or more tax rates.> You must specify which rate(s) to delete
    using the `id:in` query parameter.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Tax Rates
      description: >-
        Delete one or more tax rates.> You must specify which rate(s) to delete
        using the `id:in` query parameter.
    paths:
      /tax/rates:
        delete:
          summary: Delete Tax Rates
          description: >-
            Delete one or more tax rates.> You must specify which rate(s) to delete
            using the `id:in` query parameter.
          operationId: deleteTaxRates
          responses:
            '204':
              description: No Content
