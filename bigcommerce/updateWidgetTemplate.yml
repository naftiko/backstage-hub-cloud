apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update A Widget Template
  description: Updates a **Widget Template**.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update A Widget Template
      description: Updates a **Widget Template**.
    paths:
      /content/widget-templates/{uuid}:
        put:
          summary: Update A Widget Template
          description: Updates a **Widget Template**.
          operationId: updateWidgetTemplate
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/widgetTemplate_Put'
          responses:
            '422':
              $ref: '#/components/responses/Error422_Resp'
