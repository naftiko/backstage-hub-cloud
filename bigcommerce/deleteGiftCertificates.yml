apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete All Gift Certificates
  description: By default, it deletes all *Gift Certificates*.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete All Gift Certificates
      description: By default, it deletes all *Gift Certificates*.
    paths:
      /gift_certificates:
        delete:
          summary: Delete All Gift Certificates
          description: By default, it deletes all *Gift Certificates*.
          operationId: deleteGiftCertificates
          responses:
            '204':
              description: ''
              content:
                application/json:
                  schema:
                    type: object
