apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Checkout
  description: >-
    Returns a *Checkout*.The cart ID and checkout ID are the same.> #### Note> *
    Substitute your storefront domain for `yourstore.example.com`. > * The Send
    a Test Request feature is not currently supported for this endpoint.  
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Checkout
      description: >-
        Returns a *Checkout*.The cart ID and checkout ID are the same.> #### Note> *
        Substitute your storefront domain for `yourstore.example.com`. > * The Send
        a Test Request feature is not currently supported for this endpoint.  
    paths:
      /checkouts/{checkoutId}:
        get:
          summary: Get A Checkout
          description: >-
            Returns a *Checkout*.The cart ID and checkout ID are the same.> ####
            Note> * Substitute your storefront domain for `yourstore.example.com`. >
            * The Send a Test Request feature is not currently supported for this
            endpoint.  
          operationId: getCheckout
          responses:
            '400':
              description: When a problem arises, returns a generic response.
              content:
                application/json:
                  schema:
                    title: Checkout Error
                    type: object
                    properties:
                      errors:
                        type: array
                        description: ''
                        items:
                          title: Error Inner
                          type: object
                          properties:
                            status:
                              type: integer
                              description: ''
                              format: int32
                            title:
                              type: string
                              description: ''
                            type:
                              type: string
                              description: ''
                            detail:
                              type: string
                              description: ''
