apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Categories
  description: >-
    Deletes categories. To delete a specific category in a tree, provide a
    category ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Categories
      description: >-
        Deletes categories. To delete a specific category in a tree, provide a
        category ID.
    paths:
      /catalog/trees/categories:
        delete:
          summary: Delete Categories
          description: >-
            Deletes categories. To delete a specific category in a tree, provide a
            category ID.
          operationId: deleteTreeCategories
          responses:
            '500':
              description: Server error
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ErrorRequest'
