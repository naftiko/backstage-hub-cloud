apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Cart Metafields
  description: Get a cart's metafields.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Cart Metafields
      description: Get a cart's metafields.
    paths:
      /carts/{cart_id}/metafields:
        get:
          summary: Get Cart Metafields
          description: Get a cart's metafields.
          operationId: getCartMetafields
          responses:
            '422':
              description: >
                The `Metafield` is not valid. This is the result of missing required
                fields or of invalid data. See the response for more details.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ErrorResponse'
