apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Countryʼs State
  description: >-
    Returns a *State*. A state or province, identifiable by an ISO 3166
    subdivision code.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Countryʼs State
      description: >-
        Returns a *State*. A state or province, identifiable by an ISO 3166
        subdivision code.
    paths:
      /countries/{country_id}/states/{id}:
        get:
          summary: Get A Countryʼs State
          description: >-
            Returns a *State*. A state or province, identifiable by an ISO 3166
            subdivision code.
          operationId: getCountryState
          responses:
            '200':
              $ref: '#/components/responses/countriesStatesResponse'
