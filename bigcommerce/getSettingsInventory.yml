apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Inventory Settings
  description: Get Inventory Settings
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Inventory Settings
      description: Get Inventory Settings
    paths:
      /settings/inventory:
        get:
          summary: Get Inventory Settings
          description: Get Inventory Settings
          operationId: getSettingsInventory
          responses:
            '422':
              description: >-
                Provided settings could not be applied for some reason - detailed
                errors in the response.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ErrorResponse'
