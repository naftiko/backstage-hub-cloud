apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Categories
  description: >-
    Creates new categories. Creating a category requires: - `name` - `tree_id`
    or `parent_id` 
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Categories
      description: >-
        Creates new categories. Creating a category requires: - `name` - `tree_id`
        or `parent_id` 
    paths:
      /catalog/trees/categories:
        post:
          summary: Create Categories
          description: >-
            Creates new categories. Creating a category requires: - `name` -
            `tree_id` or `parent_id` 
          operationId: createCategories
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/CreateCategories'
          responses:
            '500':
              description: Internal Server Error
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ErrorRequest'
