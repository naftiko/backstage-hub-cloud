apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Theme Configuration
  description: >-
    Returns a list of theme's configurations.**Usage Notes**:* At least one
    filter must be provided.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Theme Configuration
      description: >-
        Returns a list of theme's configurations.**Usage Notes**:* At least one
        filter must be provided.
    paths:
      /themes/{uuid}/configurations:
        get:
          summary: Get Theme Configuration
          description: >-
            Returns a list of theme's configurations.**Usage Notes**:* At least one
            filter must be provided.
          operationId: getThemeConfiguration
          responses:
            '200':
              $ref: '#/components/responses/themesConfigurations_RespCollection'
