apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Customer Attribute Values
  description: >-
    Returns a list of Customer Attribute Values. Optional filter parameters can
    be passed in.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Customer Attribute Values
      description: >-
        Returns a list of Customer Attribute Values. Optional filter parameters can
        be passed in.
    paths:
      /customers/attribute-values:
        get:
          summary: Get All Customer Attribute Values
          description: >-
            Returns a list of Customer Attribute Values. Optional filter parameters
            can be passed in.
          operationId: getCustomersAttributeValues
          responses:
            '200':
              $ref: '#/components/responses/CustomerAttributeValueCollectionResponse'
