apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: googleDriveDrivesUpdate
  description: Updates the metadate for a shared drive.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Patch Drives Driveid
      description: Updates the metadate for a shared drive.
    paths:
      /drives/{driveId}:
        patch:
          summary: Patch Drives Driveid
          description: Updates the metadate for a shared drive.
          operationId: googleDriveDrivesUpdate
          requestBody:
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/Drive'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/Drive'
              description: Successful response
