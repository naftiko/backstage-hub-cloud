apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: googleGetPOPSettings
  description: Gets POP settings.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Pop Settings
      description: Gets POP settings.
    paths:
      /gmail/v1/users/{userId}/settings/pop:
        get:
          summary: Get Pop Settings
          description: Gets POP settings.
          operationId: googleGetPOPSettings
          responses:
            '200':
              description: OK
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    accessWindow: fromNowOn
                    disposition: dispositionUnspecified
