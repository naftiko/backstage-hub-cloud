apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: retrieve-name
  description: Gets the user capabilities.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieve Name
      description: Gets the user capabilities.
    paths:
      /v2/{name}:
        get:
          summary: Retrieve Name
          description: Gets the user capabilities.
          operationId: drivelabelsUsersGetCapabilities
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/GoogleAppsDriveLabelsV2UserCapabilities'
              description: Successful response
