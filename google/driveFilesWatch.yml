apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Files Fileid Watch
  description: Subscribes to changes to a file.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Files Fileid Watch
      description: Subscribes to changes to a file.
    paths:
      /files/{fileId}/watch:
        post:
          summary: Post Files Fileid Watch
          description: Subscribes to changes to a file.
          operationId: driveFilesWatch
          requestBody:
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/Channel'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/Channel'
              description: Successful response
