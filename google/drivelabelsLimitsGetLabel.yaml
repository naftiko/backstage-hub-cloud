apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: retrieve-limits-label
  description: >-
    Get the constraints on the structure of a Label; such as, the maximum number
    of Fields allowed and maximum length of the label title.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieve Limits Label
      description: >-
        Get the constraints on the structure of a Label; such as, the maximum number
        of Fields allowed and maximum length of the label title.
    paths:
      /v2/limits/label:
        get:
          summary: Retrieve Limits Label
          description: >-
            Get the constraints on the structure of a Label; such as, the maximum
            number of Fields allowed and maximum length of the label title.
          operationId: drivelabelsLimitsGetLabel
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/GoogleAppsDriveLabelsV2LabelLimits'
              description: Successful response
