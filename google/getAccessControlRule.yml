apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Access Control Rule
  description: Returns an access control rule.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Access Control Rule
      description: Returns an access control rule.
    paths:
      /calendars/{calendarId}/acl/{ruleId}:
        get:
          summary: Get Access Control Rule
          description: Returns an access control rule.
          operationId: getAccessControlRule
          responses:
            '200':
              description: OK
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    etag: velit eu voluptate
                    id: ex dolore in sint
                    kind: calendar#aclRule
                    role: et
                    scope:
                      type: sit eiusmod culpa do
                      value: quis esse
