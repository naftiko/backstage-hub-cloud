apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: googleDriveTeamdrivesDelete
  description: 'Deprecated: Use `drives.delete` instead.'
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Teamdrives Teamdriveid
      description: 'Deprecated: Use `drives.delete` instead.'
    paths:
      /teamdrives/{teamDriveId}:
        delete:
          summary: Delete Teamdrives Teamdriveid
          description: 'Deprecated: Use `drives.delete` instead.'
          operationId: googleDriveTeamdrivesDelete
          responses:
            '200':
              description: Successful response
