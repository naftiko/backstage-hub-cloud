apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: googleDriveRevisionsGet
  description: Gets a revision's metadata or content by ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Files Fileid Revisions Revisionid
      description: Gets a revision's metadata or content by ID.
    paths:
      /files/{fileId}/revisions/{revisionId}:
        get:
          summary: Get Files Fileid Revisions Revisionid
          description: Gets a revision's metadata or content by ID.
          operationId: googleDriveRevisionsGet
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/Revision'
              description: Successful response
