apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Files Fileid Comments Commentid Replies Replyid
  description: Gets a reply by ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Files Fileid Comments Commentid Replies Replyid
      description: Gets a reply by ID.
    paths:
      /files/{fileId}/comments/{commentId}/replies/{replyId}:
        get:
          summary: Get Files Fileid Comments Commentid Replies Replyid
          description: Gets a reply by ID.
          operationId: driveRepliesGet
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/Reply'
              description: Successful response
