apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-teamdrives
  description: 'Deprecated: Use `drives.create` instead.'
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Teamdrives
      description: 'Deprecated: Use `drives.create` instead.'
    paths:
      /teamdrives:
        post:
          summary: Post Teamdrives
          description: 'Deprecated: Use `drives.create` instead.'
          operationId: driveTeamdrivesCreate
          requestBody:
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/TeamDrive'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/TeamDrive'
              description: Successful response
