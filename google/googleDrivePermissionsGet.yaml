apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: googleDrivePermissionsGet
  description: Gets a permission by ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Files Fileid Permissions Permissionid
      description: Gets a permission by ID.
    paths:
      /files/{fileId}/permissions/{permissionId}:
        get:
          summary: Get Files Fileid Permissions Permissionid
          description: Gets a permission by ID.
          operationId: googleDrivePermissionsGet
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/Permission'
              description: Successful response
