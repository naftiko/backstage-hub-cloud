apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-forwarding-addresses
  description: Gets the specified forwarding address.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Forwarding Addresses
      description: Gets the specified forwarding address.
    paths:
      /gmail/v1/users/{userId}/settings/forwardingAddresses/{forwardingEmail}:
        get:
          summary: Get Forwarding Addresses
          description: Gets the specified forwarding address.
          operationId: getForwardingAddresses
          responses:
            '200':
              description: OK
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    forwardingEmail: ad tempor dolor
                    verificationStatus: pending
