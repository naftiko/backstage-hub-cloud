apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: googleDriveDrivesHide
  description: Hides a shared drive from the default view.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Drives Driveid Hide
      description: Hides a shared drive from the default view.
    paths:
      /drives/{driveId}/hide:
        post:
          summary: Post Drives Driveid Hide
          description: Hides a shared drive from the default view.
          operationId: googleDriveDrivesHide
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/Drive'
              description: Successful response
