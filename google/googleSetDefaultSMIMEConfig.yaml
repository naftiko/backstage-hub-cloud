apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: googleSetDefaultSMIMEConfig
  description: Sets the default S/MIME config for the specified send-as alias.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Set Default S Mime Config
      description: Sets the default S/MIME config for the specified send-as alias.
    paths:
      /gmail/v1/users/{userId}/settings/sendAs/{sendAsEmail}/smimeInfo/{id}/setDefault:
        post:
          summary: Set Default S Mime Config
          description: Sets the default S/MIME config for the specified send-as alias.
          operationId: googleSetDefaultSMIMEConfig
          requestBody:
            content: {}
          responses:
            '204':
              description: No Content
              content:
                text/plain:
                  schema:
                    type: string
                  example: ''
