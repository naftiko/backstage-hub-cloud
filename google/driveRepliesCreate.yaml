apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-files-fileid-comments-commentid-replies
  description: Creates a reply to a comment.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Files Fileid Comments Commentid Replies
      description: Creates a reply to a comment.
    paths:
      /files/{fileId}/comments/{commentId}/replies:
        post:
          summary: Post Files Fileid Comments Commentid Replies
          description: Creates a reply to a comment.
          operationId: driveRepliesCreate
          requestBody:
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/Reply'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/Reply'
              description: Successful response
