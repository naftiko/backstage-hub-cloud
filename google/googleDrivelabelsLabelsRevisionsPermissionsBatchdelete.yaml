apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: googleDrivelabelsLabelsRevisionsPermissionsBatchdelete
  description: >-
    Deletes Label permissions. Permissions affect the Label resource as a whole,
    are not revisioned, and do not require publishing.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Parent Permissions:batchdelete
      description: >-
        Deletes Label permissions. Permissions affect the Label resource as a whole,
        are not revisioned, and do not require publishing.
    paths:
      /v2/{parent}/permissions:batchDelete:
        post:
          summary: Create Parent Permissions:batchdelete
          description: >-
            Deletes Label permissions. Permissions affect the Label resource as a
            whole, are not revisioned, and do not require publishing.
          operationId: googleDrivelabelsLabelsRevisionsPermissionsBatchdelete
          requestBody:
            content:
              application/json:
                schema:
                  $ref: >-
                    #/components/schemas/GoogleAppsDriveLabelsV2BatchDeleteLabelPermissionsRequest
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/GoogleProtobufEmpty'
              description: Successful response
