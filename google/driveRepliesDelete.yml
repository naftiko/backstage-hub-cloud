apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Files Fileid Comments Commentid Replies Replyid
  description: Deletes a reply.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Files Fileid Comments Commentid Replies Replyid
      description: Deletes a reply.
    paths:
      /files/{fileId}/comments/{commentId}/replies/{replyId}:
        delete:
          summary: Delete Files Fileid Comments Commentid Replies Replyid
          description: Deletes a reply.
          operationId: driveRepliesDelete
          responses:
            '200':
              description: Successful response
