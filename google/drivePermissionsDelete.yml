apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Files Fileid Permissions Permissionid
  description: >-
    Deletes a permission. **Warning:** Concurrent permissions operations on the
    same file are not supported; only the last update is applied.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Files Fileid Permissions Permissionid
      description: >-
        Deletes a permission. **Warning:** Concurrent permissions operations on the
        same file are not supported; only the last update is applied.
    paths:
      /files/{fileId}/permissions/{permissionId}:
        delete:
          summary: Delete Files Fileid Permissions Permissionid
          description: >-
            Deletes a permission. **Warning:** Concurrent permissions operations on
            the same file are not supported; only the last update is applied.
          operationId: drivePermissionsDelete
          responses:
            '200':
              description: Successful response
