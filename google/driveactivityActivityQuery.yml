apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Activity:query
  description: Query past activity in Google Drive.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Activity:query
      description: Query past activity in Google Drive.
    paths:
      /v2/activity:query:
        post:
          summary: Post Activity:query
          description: Query past activity in Google Drive.
          operationId: driveactivityActivityQuery
          requestBody:
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/QueryDriveActivityRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/QueryDriveActivityResponse'
              description: Successful response
