apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: googleGETCSEIdenetites
  description: Retrieves a client-side encryption identity configuration.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Cse Idenetites
      description: Retrieves a client-side encryption identity configuration.
    paths:
      /gmail/v1/users/{userId}/settings/cse/identities/{cseEmailAddress}:
        get:
          summary: Get Cse Idenetites
          description: Retrieves a client-side encryption identity configuration.
          operationId: googleGETCSEIdenetites
          responses:
            '200':
              description: OK
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    emailAddress: aliquip quis id
                    primaryKeyPairId: in exercitation esse
                    signAndEncryptKeyPairs:
                      signingKeyPairId: est deserunt
                      encryptionKeyPairId: elit nulla esse ut
