apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Teamdrives
  description: 'Deprecated: Use `drives.list` instead.'
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Teamdrives
      description: 'Deprecated: Use `drives.list` instead.'
    paths:
      /teamdrives:
        get:
          summary: Get Teamdrives
          description: 'Deprecated: Use `drives.list` instead.'
          operationId: driveTeamdrivesList
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/TeamDriveList'
              description: Successful response
