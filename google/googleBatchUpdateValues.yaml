apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: googleBatchUpdateValues
  description: >-
    Sets values in one or more ranges of a spreadsheet. The caller must specify
    the spreadsheet ID, a valueInputOption, and one or more ValueRanges.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Batch Update Values
      description: >-
        Sets values in one or more ranges of a spreadsheet. The caller must specify
        the spreadsheet ID, a valueInputOption, and one or more ValueRanges.
    paths:
      /v4/spreadsheets/{spreadsheetId}/values:batchUpdate:
        post:
          summary: Batch Update Values
          description: >-
            Sets values in one or more ranges of a spreadsheet. The caller must
            specify the spreadsheet ID, a valueInputOption, and one or more
            ValueRanges.
          operationId: googleBatchUpdateValues
          requestBody:
            content:
              application/json:
                schema:
                  type: object
                  example: null
          responses:
            '200':
              description: OK
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  examples:
                    example-0:
                      summary: Successful response
                      value: null
                    example-1:
                      summary: Successful response
                      value: null
