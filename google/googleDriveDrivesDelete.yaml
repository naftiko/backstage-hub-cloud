apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: googleDriveDrivesDelete
  description: >-
    Permanently deletes a shared drive for which the user is an `organizer`. The
    shared drive cannot contain any untrashed items.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Drives Driveid
      description: >-
        Permanently deletes a shared drive for which the user is an `organizer`. The
        shared drive cannot contain any untrashed items.
    paths:
      /drives/{driveId}:
        delete:
          summary: Delete Drives Driveid
          description: >-
            Permanently deletes a shared drive for which the user is an `organizer`.
            The shared drive cannot contain any untrashed items.
          operationId: googleDriveDrivesDelete
          responses:
            '200':
              description: Successful response
