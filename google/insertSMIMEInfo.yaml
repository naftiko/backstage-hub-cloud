apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: insert-smime-info
  description: >-
    Insert (upload) the given S/MIME config for the specified send-as alias.
    Note that pkcs12 format is required for the key.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Insert Smime Info
      description: >-
        Insert (upload) the given S/MIME config for the specified send-as alias.
        Note that pkcs12 format is required for the key.
    paths:
      /gmail/v1/users/{userId}/settings/sendAs/{sendAsEmail}/smimeInfo:
        post:
          summary: Insert Smime Info
          description: >-
            Insert (upload) the given S/MIME config for the specified send-as alias.
            Note that pkcs12 format is required for the key.
          operationId: insertSMIMEInfo
          requestBody:
            content:
              application/json:
                schema:
                  type: object
                  example:
                    id: id
                    issuerCn: ipsum qui s
                    isDefault: false
                    expiration: dolore magna
                    pem: 'cillum labore '
                    pkcs12: in aute et
                    encryptedKeyPassword: cillum ut
          responses:
            '200':
              description: OK
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    id: id
                    issuerCn: ipsum qui s
                    isDefault: false
                    expiration: dolore magna
                    pem: 'cillum labore '
                    pkcs12: in aute et
                    encryptedKeyPassword: cillum ut
