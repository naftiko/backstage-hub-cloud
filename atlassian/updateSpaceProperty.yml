apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update Space Property
  description: >-
    Deprecated, use [Confluence's v2
    API](https://developer.atlassian.com/cloud/confluence/rest/v2/intro/).Updates
    a space property. Note, you cannot update the key of a spaceproperty, only
    the value.**[Permissions
    required](https://confluence.atlassian.com/x/_AozKw)**:‘Admin’ permission
    for the space.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update Space Property
      description: >-
        Deprecated, use [Confluence's v2
        API](https://developer.atlassian.com/cloud/confluence/rest/v2/intro/).Updates
        a space property. Note, you cannot update the key of a spaceproperty, only
        the value.**[Permissions
        required](https://confluence.atlassian.com/x/_AozKw)**:‘Admin’ permission
        for the space.
    paths:
      /wiki/rest/api/space/{spaceKey}/property/{key}:
        put:
          summary: Update Space Property
          description: >-
            Deprecated, use [Confluence's v2
            API](https://developer.atlassian.com/cloud/confluence/rest/v2/intro/).Updates
            a space property. Note, you cannot update the key of a spaceproperty,
            only the value.**[Permissions
            required](https://confluence.atlassian.com/x/_AozKw)**:‘Admin’
            permission for the space.
          operationId: updateSpaceProperty
          requestBody:
            description: The space property being updated.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/SpacePropertyUpdate'
          responses:
            '413':
              description: Returned if the value of the property is too long.
              content: {}
