apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get User Membership For A Workspace
  description: >-
    Returns the workspace membership, which includesa `User` object for the
    member and a `Workspace` objectfor the requested workspace.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get User Membership For A Workspace
      description: >-
        Returns the workspace membership, which includesa `User` object for the
        member and a `Workspace` objectfor the requested workspace.
    paths:
      /workspaces/{workspace}/members/{member}:
        get:
          summary: Get User Membership For A Workspace
          description: >-
            Returns the workspace membership, which includesa `User` object for the
            member and a `Workspace` objectfor the requested workspace.
          operationId: getusermembershipforaworkspace
          responses:
            '404':
              description: >-
                A workspace cannot be found, or a user cannot be found, or the user
                is not a a member of the workspace.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
