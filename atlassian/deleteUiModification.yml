apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Ui Modification
  description: >-
    Deletes a UI modification. All the contexts that belong to the UI
    modification are deleted too. UI modification can only be deleted by Forge
    apps.**[Permissions](#permissions) required:** None.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Ui Modification
      description: >-
        Deletes a UI modification. All the contexts that belong to the UI
        modification are deleted too. UI modification can only be deleted by Forge
        apps.**[Permissions](#permissions) required:** None.
    paths:
      /rest/api/3/uiModifications/{uiModificationId}:
        delete:
          summary: Delete Ui Modification
          description: >-
            Deletes a UI modification. All the contexts that belong to the UI
            modification are deleted too. UI modification can only be deleted by
            Forge apps.**[Permissions](#permissions) required:** None.
          operationId: deleteUiModification
          responses:
            '404':
              description: Returned if the UI modification is not found.
