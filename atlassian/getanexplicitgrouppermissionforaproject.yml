apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get An Explicit Group Permission For A Project
  description: >-
    Returns the group permission for a given group and project.Only users with
    admin permission for the project may access this resource.Permissions can
    be:* `admin`* `create-repo`* `write`* `read`* `none`
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get An Explicit Group Permission For A Project
      description: >-
        Returns the group permission for a given group and project.Only users with
        admin permission for the project may access this resource.Permissions can
        be:* `admin`* `create-repo`* `write`* `read`* `none`
    paths:
      /workspaces/{workspace}/projects/{project_key}/permissions-config/groups/{group_slug}:
        get:
          summary: Get An Explicit Group Permission For A Project
          description: >-
            Returns the group permission for a given group and project.Only users
            with admin permission for the project may access this
            resource.Permissions can be:* `admin`* `create-repo`* `write`* `read`*
            `none`
          operationId: getanexplicitgrouppermissionforaproject
          responses:
            '404':
              description: >-
                One or more of the workspace, project, and group doesn't
                exist                               for the given identifiers or the
                requesting user is not                               authenticated
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
