apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Linker Value
  description: >-
    Delete a single [linker](/cloud/bitbucket/modules/linker/) valueof the
    authenticated application.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Linker Value
      description: >-
        Delete a single [linker](/cloud/bitbucket/modules/linker/) valueof the
        authenticated application.
    paths:
      /addon/linkers/{linker_key}/values/{value_id}:
        delete:
          summary: Delete A Linker Value
          description: >-
            Delete a single [linker](/cloud/bitbucket/modules/linker/) valueof the
            authenticated application.
          operationId: deletealinkervalue
          responses:
            '404':
              description: The linker value does not exist.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
