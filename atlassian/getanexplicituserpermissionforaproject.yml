apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get An Explicit User Permission For A Project
  description: >-
    Returns the explicit user permission for a given user and project.Only users
    with admin permission for the project may access this resource.Permissions
    can be:* `admin`* `create-repo`* `write`* `read`* `none`
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get An Explicit User Permission For A Project
      description: >-
        Returns the explicit user permission for a given user and project.Only users
        with admin permission for the project may access this resource.Permissions
        can be:* `admin`* `create-repo`* `write`* `read`* `none`
    paths:
      /workspaces/{workspace}/projects/{project_key}/permissions-config/users/{selected_user_id}:
        get:
          summary: Get An Explicit User Permission For A Project
          description: >-
            Returns the explicit user permission for a given user and project.Only
            users with admin permission for the project may access this
            resource.Permissions can be:* `admin`* `create-repo`* `write`* `read`*
            `none`
          operationId: getanexplicituserpermissionforaproject
          responses:
            '404':
              description: >-
                One or more of the workspace, project, and selected user doesn't
                exist for the given identifiers or the requesting user is not
                authenticated
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
