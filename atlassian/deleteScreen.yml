apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Screen
  description: >-
    Deletes a screen. A screen cannot be deleted if it is used in a screen
    scheme, workflow, or workflow draft.Only screens used in classic projects
    can be deleted.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Screen
      description: >-
        Deletes a screen. A screen cannot be deleted if it is used in a screen
        scheme, workflow, or workflow draft.Only screens used in classic projects
        can be deleted.
    paths:
      /rest/api/3/screens/{screenId}:
        delete:
          summary: Delete Screen
          description: >-
            Deletes a screen. A screen cannot be deleted if it is used in a screen
            scheme, workflow, or workflow draft.Only screens used in classic
            projects can be deleted.
          operationId: deleteScreen
          responses:
            '404':
              content:
                application/json:
                  example: '{"errorMessages":["The screen was not found."],"errors":{}}'
              description: Returned if the screen is not found.
