apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete App Property (forge)
  description: >-
    Deletes a Forge app's property.**[Permissions](#permissions) required:**
    Only Forge apps can make this request.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete App Property (forge)
      description: >-
        Deletes a Forge app's property.**[Permissions](#permissions) required:**
        Only Forge apps can make this request.
    paths:
      /rest/forge/1/app/properties/{propertyKey}:
        delete:
          summary: Delete App Property (forge)
          description: >-
            Deletes a Forge app's property.**[Permissions](#permissions) required:**
            Only Forge apps can make this request.
          operationId: addonPropertiesResource.DeleteAppPropertydelete
          responses:
            '404':
              content:
                application/json:
                  example:
                    message: Property with key not found.
                    statusCode: 404
                  schema:
                    $ref: '#/components/schemas/OperationMessage'
              description: Returned if the property isn't found or doesn't belong to the app.
