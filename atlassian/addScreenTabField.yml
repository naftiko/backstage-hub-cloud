apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Add Screen Tab Field
  description: >-
    Adds a field to a screen tab.**[Permissions](#permissions) required:**
    *Administer Jira* [global
    permission](https://confluence.atlassian.com/x/x4dKLg).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Add Screen Tab Field
      description: >-
        Adds a field to a screen tab.**[Permissions](#permissions) required:**
        *Administer Jira* [global
        permission](https://confluence.atlassian.com/x/x4dKLg).
    paths:
      /rest/api/3/screens/{screenId}/tabs/{tabId}/fields:
        post:
          summary: Add Screen Tab Field
          description: >-
            Adds a field to a screen tab.**[Permissions](#permissions) required:**
            *Administer Jira* [global
            permission](https://confluence.atlassian.com/x/x4dKLg).
          operationId: addScreenTabField
          requestBody:
            required: true
            content:
              application/json:
                example:
                  fieldId: summary
                schema:
                  $ref: '#/components/schemas/AddFieldBean'
          responses:
            '404':
              description: Returned if the screen, screen tab, or field is not found.
