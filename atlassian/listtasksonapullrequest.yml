apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Tasks On A Pull Request
  description: >-
    Returns a paginated list of the pull request's tasks.This endpoint supports
    filtering and sorting of the results by the 'task' field.See [filtering and
    sorting](/cloud/bitbucket/rest/intro/#filtering) for more details.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Tasks On A Pull Request
      description: >-
        Returns a paginated list of the pull request's tasks.This endpoint supports
        filtering and sorting of the results by the 'task' field.See [filtering and
        sorting](/cloud/bitbucket/rest/intro/#filtering) for more details.
    paths:
      /repositories/{workspace}/{repo_slug}/pullrequests/{pull_request_id}/tasks:
        get:
          summary: List Tasks On A Pull Request
          description: >-
            Returns a paginated list of the pull request's tasks.This endpoint
            supports filtering and sorting of the results by the 'task' field.See
            [filtering and sorting](/cloud/bitbucket/rest/intro/#filtering) for more
            details.
          operationId: listtasksonapullrequest
          responses:
            '404':
              description: If the pull request does not exist.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
