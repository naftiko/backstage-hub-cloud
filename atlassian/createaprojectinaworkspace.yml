apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Project In A Workspace
  description: >-
    Creates a new project.Note that the avatar has to be embedded as either a
    data-urlor a URL to an external image as shown in the examples below:```$
    body=$(cat 
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Project In A Workspace
      description: >-
        Creates a new project.Note that the avatar has to be embedded as either a
        data-urlor a URL to an external image as shown in the examples below:```$
        body=$(cat 
    paths:
      /workspaces/{workspace}/projects:
        post:
          summary: Create A Project In A Workspace
          description: >-
            Creates a new project.Note that the avatar has to be embedded as either
            a data-urlor a URL to an external image as shown in the examples
            below:```$ body=$(cat 
          operationId: createaprojectinaworkspace
          requestBody: {}
          responses:
            '404':
              description: A workspace doesn't exist at this location.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
