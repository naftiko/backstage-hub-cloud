apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get An Explicit Group Permission For A Repository
  description: >-
    Returns the group permission for a given group slug and repositoryOnly users
    with admin permission for the repository may access this
    resource.Permissions can be:* `admin`* `write`* `read`* `none`
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get An Explicit Group Permission For A Repository
      description: >-
        Returns the group permission for a given group slug and repositoryOnly users
        with admin permission for the repository may access this
        resource.Permissions can be:* `admin`* `write`* `read`* `none`
    paths:
      /repositories/{workspace}/{repo_slug}/permissions-config/groups/{group_slug}:
        get:
          summary: Get An Explicit Group Permission For A Repository
          description: >-
            Returns the group permission for a given group slug and repositoryOnly
            users with admin permission for the repository may access this
            resource.Permissions can be:* `admin`* `write`* `read`* `none`
          operationId: getanexplicitgrouppermissionforarepository
          responses:
            '404':
              description: The given user, workspace, and/or repository could not be found
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
