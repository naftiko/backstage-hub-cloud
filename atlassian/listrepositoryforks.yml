apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Repository Forks
  description: Returns a paginated list of all the forks of the specifiedrepository.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Repository Forks
      description: Returns a paginated list of all the forks of the specifiedrepository.
    paths:
      /repositories/{workspace}/{repo_slug}/forks:
        get:
          summary: List Repository Forks
          description: Returns a paginated list of all the forks of the specifiedrepository.
          operationId: listrepositoryforks
          responses:
            '200':
              description: All forks.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/paginated_repositories'
