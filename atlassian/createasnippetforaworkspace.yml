apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Snippet For A Workspace
  description: >-
    Identical to
    [`/snippets`](/cloud/bitbucket/rest/api-group-snippets/#api-snippets-post),
    except that the new snippet will becreated under the workspace specified in
    the path parameter`{workspace}`.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Snippet For A Workspace
      description: >-
        Identical to
        [`/snippets`](/cloud/bitbucket/rest/api-group-snippets/#api-snippets-post),
        except that the new snippet will becreated under the workspace specified in
        the path parameter`{workspace}`.
    paths:
      /snippets/{workspace}:
        post:
          summary: Create A Snippet For A Workspace
          description: >-
            Identical to
            [`/snippets`](/cloud/bitbucket/rest/api-group-snippets/#api-snippets-post),
            except that the new snippet will becreated under the workspace specified
            in the path parameter`{workspace}`.
          operationId: createasnippetforaworkspace
          requestBody: {}
          responses:
            '403':
              description: >-
                If the authenticated user does not have permission to create
                snippets in the specified workspace.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
