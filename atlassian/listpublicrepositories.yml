apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Public Repositories
  description: >-
    Returns a paginated list of all public repositories.This endpoint also
    supports filtering and sorting of the results. See[filtering and
    sorting](/cloud/bitbucket/rest/intro/#filtering) for more details.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Public Repositories
      description: >-
        Returns a paginated list of all public repositories.This endpoint also
        supports filtering and sorting of the results. See[filtering and
        sorting](/cloud/bitbucket/rest/intro/#filtering) for more details.
    paths:
      /repositories:
        get:
          summary: List Public Repositories
          description: >-
            Returns a paginated list of all public repositories.This endpoint also
            supports filtering and sorting of the results. See[filtering and
            sorting](/cloud/bitbucket/rest/intro/#filtering) for more details.
          operationId: listpublicrepositories
          responses:
            '200':
              description: All public repositories.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/paginated_repositories'
