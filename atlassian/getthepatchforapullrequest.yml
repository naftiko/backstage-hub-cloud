apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get The Patch For A Pull Request
  description: >-
    Redirects to the [repository
    patch](/cloud/bitbucket/rest/api-group-commits/#api-repositories-workspace-repo-slug-patch-spec-get)with
    the revspec that corresponds to pull request.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get The Patch For A Pull Request
      description: >-
        Redirects to the [repository
        patch](/cloud/bitbucket/rest/api-group-commits/#api-repositories-workspace-repo-slug-patch-spec-get)with
        the revspec that corresponds to pull request.
    paths:
      /repositories/{workspace}/{repo_slug}/pullrequests/{pull_request_id}/patch:
        get:
          summary: Get The Patch For A Pull Request
          description: >-
            Redirects to the [repository
            patch](/cloud/bitbucket/rest/api-group-commits/#api-repositories-workspace-repo-slug-patch-spec-get)with
            the revspec that corresponds to pull request.
          operationId: getthepatchforapullrequest
          responses:
            '302':
              description: >
                Redirects to the [repository
                patch](/cloud/bitbucket/rest/api-group-commits/#api-repositories-workspace-repo-slug-patch-spec-get)
                with

                the revspec that corresponds to pull request.
