apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Branch Restrictions
  description: Returns a paginated list of all branch restrictions on therepository.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Branch Restrictions
      description: Returns a paginated list of all branch restrictions on therepository.
    paths:
      /repositories/{workspace}/{repo_slug}/branch-restrictions:
        get:
          summary: List Branch Restrictions
          description: Returns a paginated list of all branch restrictions on therepository.
          operationId: listbranchrestrictions
          responses:
            '404':
              description: If the repository does not exist
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
