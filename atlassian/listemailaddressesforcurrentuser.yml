apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Email Addresses For Current User
  description: >-
    Returns all the authenticated user's email addresses. Bothconfirmed and
    unconfirmed.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Email Addresses For Current User
      description: >-
        Returns all the authenticated user's email addresses. Bothconfirmed and
        unconfirmed.
    paths:
      /user/emails:
        get:
          summary: List Email Addresses For Current User
          description: >-
            Returns all the authenticated user's email addresses. Bothconfirmed and
            unconfirmed.
          operationId: listemailaddressesforcurrentuser
          responses:
            default:
              description: Unexpected error.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
