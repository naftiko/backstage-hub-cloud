apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Set Default Share Scope
  description: >-
    Sets the default sharing for new filters and dashboards for a
    user.**[Permissions](#permissions) required:** Permission to access Jira.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Set Default Share Scope
      description: >-
        Sets the default sharing for new filters and dashboards for a
        user.**[Permissions](#permissions) required:** Permission to access Jira.
    paths:
      /rest/api/3/filter/defaultShareScope:
        put:
          summary: Set Default Share Scope
          description: >-
            Sets the default sharing for new filters and dashboards for a
            user.**[Permissions](#permissions) required:** Permission to access
            Jira.
          operationId: setDefaultShareScope
          requestBody:
            required: true
            content:
              application/json:
                example:
                  scope: GLOBAL
                schema:
                  $ref: '#/components/schemas/DefaultShareScope'
          responses:
            '401':
              description: Returned if the authentication credentials are incorrect or missing.
