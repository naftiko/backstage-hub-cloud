apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Add Member To Group By Groupid
  description: >-
    Adds a user as a member in a group represented by its
    groupId**[Permissions](https://confluence.atlassian.com/x/_AozKw)
    required**:User must be a site admin.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Add Member To Group By Groupid
      description: >-
        Adds a user as a member in a group represented by its
        groupId**[Permissions](https://confluence.atlassian.com/x/_AozKw)
        required**:User must be a site admin.
    paths:
      /wiki/rest/api/group/userByGroupId:
        post:
          summary: Add Member To Group By Groupid
          description: >-
            Adds a user as a member in a group represented by its
            groupId**[Permissions](https://confluence.atlassian.com/x/_AozKw)
            required**:User must be a site admin.
          operationId: addUserToGroupByGroupId
          requestBody:
            description: AccountId of the user who needs to be added as member.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/AccountId'
          responses:
            '404':
              description: If no user group by the give name exists.
              content: {}
