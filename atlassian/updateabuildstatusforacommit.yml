apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update A Build Status For A Commit
  description: >-
    Used to update the current status of a build status object on thespecific
    commit.This operation can also be used to change other properties of
    thebuild status:* `state`* `name`* `description`* `url`* `refname`The `key`
    cannot be changed.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update A Build Status For A Commit
      description: >-
        Used to update the current status of a build status object on thespecific
        commit.This operation can also be used to change other properties of
        thebuild status:* `state`* `name`* `description`* `url`* `refname`The `key`
        cannot be changed.
    paths:
      /repositories/{workspace}/{repo_slug}/commit/{commit}/statuses/build/{key}:
        put:
          summary: Update A Build Status For A Commit
          description: >-
            Used to update the current status of a build status object on
            thespecific commit.This operation can also be used to change other
            properties of thebuild status:* `state`* `name`* `description`* `url`*
            `refname`The `key` cannot be changed.
          operationId: updateabuildstatusforacommit
          requestBody:
            description: The updated build status object
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/commitstatus'
          responses:
            '404':
              description: If the repository or build does not exist
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
