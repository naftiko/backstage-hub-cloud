apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Add The Specific User As A Default Reviewer For The Project
  description: >-
    Adds the specified user to the project's list of default reviewers. The
    method isidempotent. Accepts an optional body containing the `uuid` of the
    user to be added.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Add The Specific User As A Default Reviewer For The Project
      description: >-
        Adds the specified user to the project's list of default reviewers. The
        method isidempotent. Accepts an optional body containing the `uuid` of the
        user to be added.
    paths:
      /workspaces/{workspace}/projects/{project_key}/default-reviewers/{selected_user}:
        put:
          summary: Add The Specific User As A Default Reviewer For The Project
          description: >-
            Adds the specified user to the project's list of default reviewers. The
            method isidempotent. Accepts an optional body containing the `uuid` of
            the user to be added.
          operationId: addthespecificuserasadefaultreviewerfortheproject
          responses:
            '404':
              description: If the specified user, project, or workspace does not exist
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
