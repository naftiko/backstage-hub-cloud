apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Share Permission
  description: >-
    Deletes a share permission from a filter.**[Permissions](#permissions)
    required:** Permission to access Jira and the user must own the filter.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Share Permission
      description: >-
        Deletes a share permission from a filter.**[Permissions](#permissions)
        required:** Permission to access Jira and the user must own the filter.
    paths:
      /rest/api/3/filter/{id}/permission/{permissionId}:
        delete:
          summary: Delete Share Permission
          description: >-
            Deletes a share permission from a filter.**[Permissions](#permissions)
            required:** Permission to access Jira and the user must own the filter.
          operationId: deleteSharePermission
          responses:
            '404':
              description: |-
                Returned if:

                 *  the filter is not found.
                 *  the user does not own the filter.
