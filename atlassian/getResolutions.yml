apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Resolutions
  description: >-
    Returns a list of all issue resolution values.**[Permissions](#permissions)
    required:** Permission to access Jira.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Resolutions
      description: >-
        Returns a list of all issue resolution values.**[Permissions](#permissions)
        required:** Permission to access Jira.
    paths:
      /rest/api/3/resolution:
        get:
          summary: Get Resolutions
          description: >-
            Returns a list of all issue resolution
            values.**[Permissions](#permissions) required:** Permission to access
            Jira.
          operationId: getResolutions
          responses:
            '401':
              description: Returned if the authentication credentials are incorrect or missing.
