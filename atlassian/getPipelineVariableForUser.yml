apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Variable For A User
  description: >-
    Retrieve a user level variable.This endpoint has been deprecated, and you
    should use the new workspaces endpoint. For more information, see [the
    announcement](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-teams-deprecation/).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Variable For A User
      description: >-
        Retrieve a user level variable.This endpoint has been deprecated, and you
        should use the new workspaces endpoint. For more information, see [the
        announcement](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-teams-deprecation/).
    paths:
      /users/{selected_user}/pipelines_config/variables/{variable_uuid}:
        get:
          summary: Get A Variable For A User
          description: >-
            Retrieve a user level variable.This endpoint has been deprecated, and
            you should use the new workspaces endpoint. For more information, see
            [the
            announcement](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-teams-deprecation/).
          operationId: getPipelineVariableForUser
          responses:
            '404':
              description: The account or variable with the given UUID was not found.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
