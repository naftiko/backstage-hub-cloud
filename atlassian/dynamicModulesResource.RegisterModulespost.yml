apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Register Modules
  description: >-
    Registers a list of modules.**[Permissions](#permissions) required:** Only
    Connect apps can make this request.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Register Modules
      description: >-
        Registers a list of modules.**[Permissions](#permissions) required:** Only
        Connect apps can make this request.
    paths:
      /rest/atlassian-connect/1/app/module/dynamic:
        post:
          summary: Register Modules
          description: >-
            Registers a list of modules.**[Permissions](#permissions) required:**
            Only Connect apps can make this request.
          operationId: dynamicModulesResource.RegisterModulespost
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ConnectModules'
          responses:
            '401':
              content:
                application/json:
                  example:
                    message: The request is not from a Connect app.
                  schema:
                    $ref: '#/components/schemas/ErrorMessage'
              description: Returned if the call is not from a Connect app.
