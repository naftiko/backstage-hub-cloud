apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Snippets In A Workspace
  description: >-
    Identical to
    [`/snippets`](/cloud/bitbucket/rest/api-group-snippets/#api-snippets-get),
    except that the result is further filteredby the snippet owner and only
    those that are owned by `{workspace}` arereturned.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Snippets In A Workspace
      description: >-
        Identical to
        [`/snippets`](/cloud/bitbucket/rest/api-group-snippets/#api-snippets-get),
        except that the result is further filteredby the snippet owner and only
        those that are owned by `{workspace}` arereturned.
    paths:
      /snippets/{workspace}:
        get:
          summary: List Snippets In A Workspace
          description: >-
            Identical to
            [`/snippets`](/cloud/bitbucket/rest/api-group-snippets/#api-snippets-get),
            except that the result is further filteredby the snippet owner and only
            those that are owned by `{workspace}` arereturned.
          operationId: listsnippetsinaworkspace
          responses:
            '404':
              description: If the user does not exist.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
