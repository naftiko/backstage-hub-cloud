apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Summary Of Test Reports For A Given Step Of A Pipeline.
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Summary Of Test Reports For A Given Step Of A Pipeline.
      description: Needs a more full description created.
    paths:
      /repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}/test_reports:
        get:
          summary: Get A Summary Of Test Reports For A Given Step Of A Pipeline.
          description: Needs a more full description created.
          operationId: getPipelineTestReports
          responses:
            '404':
              description: >-
                No account, repository, pipeline, step or test reports exist for the
                provided path.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
