apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Repository
  description: >-
    Deletes the repository. This is an irreversible operation.This does not
    affect its forks.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Repository
      description: >-
        Deletes the repository. This is an irreversible operation.This does not
        affect its forks.
    paths:
      /repositories/{workspace}/{repo_slug}:
        delete:
          summary: Delete A Repository
          description: >-
            Deletes the repository. This is an irreversible operation.This does not
            affect its forks.
          operationId: deletearepository
          responses:
            '404':
              description: If the repository does not exist.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
