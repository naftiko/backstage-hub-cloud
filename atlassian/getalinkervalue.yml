apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Linker Value
  description: >-
    Get a single [linker](/cloud/bitbucket/modules/linker/) valueof the
    authenticated application.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Linker Value
      description: >-
        Get a single [linker](/cloud/bitbucket/modules/linker/) valueof the
        authenticated application.
    paths:
      /addon/linkers/{linker_key}/values/{value_id}:
        get:
          summary: Get A Linker Value
          description: >-
            Get a single [linker](/cloud/bitbucket/modules/linker/) valueof the
            authenticated application.
          operationId: getalinkervalue
          responses:
            '404':
              description: The linker value does not exist.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
