apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update An Explicit Group Permission For A Repository
  description: >-
    Updates the group permission, or grants a new permission if one does not
    already exist.Only users with admin permission for the repository may access
    this resource.The only authentication method supported for this endpoint is
    via app passwords.Permissions can be:* `admin`* `write`* `read`
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update An Explicit Group Permission For A Repository
      description: >-
        Updates the group permission, or grants a new permission if one does not
        already exist.Only users with admin permission for the repository may access
        this resource.The only authentication method supported for this endpoint is
        via app passwords.Permissions can be:* `admin`* `write`* `read`
    paths:
      /repositories/{workspace}/{repo_slug}/permissions-config/groups/{group_slug}:
        put:
          summary: Update An Explicit Group Permission For A Repository
          description: >-
            Updates the group permission, or grants a new permission if one does not
            already exist.Only users with admin permission for the repository may
            access this resource.The only authentication method supported for this
            endpoint is via app passwords.Permissions can be:* `admin`* `write`*
            `read`
          operationId: updateanexplicitgrouppermissionforarepository
          requestBody: {}
          responses:
            '404':
              description: >-
                The workspace does not exist, the repository does not exist, or the
                group does not exist.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
