apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Snippet's Raw File At Head
  description: >-
    Convenience resource for getting to a snippet's raw files without theneed
    for first having to retrieve the snippet itself and having to pullout the
    versioned file links.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Snippet's Raw File At Head
      description: >-
        Convenience resource for getting to a snippet's raw files without theneed
        for first having to retrieve the snippet itself and having to pullout the
        versioned file links.
    paths:
      /snippets/{workspace}/{encoded_id}/files/{path}:
        get:
          summary: Get A Snippet's Raw File At Head
          description: >-
            Convenience resource for getting to a snippet's raw files without
            theneed for first having to retrieve the snippet itself and having to
            pullout the versioned file links.
          operationId: getasnippet'SrawfileatHEAD
          responses:
            '404':
              description: If the snippet does not exist.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
