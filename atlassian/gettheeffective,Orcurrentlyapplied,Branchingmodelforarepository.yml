apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get The Effective, Or Currently Applied, Branching Model For A Repository
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get The Effective, Or Currently Applied, Branching Model For A Repository
      description: Needs a more full description created.
    paths:
      /repositories/{workspace}/{repo_slug}/effective-branching-model:
        get:
          summary: >-
            Get The Effective, Or Currently Applied, Branching Model For A
            Repository
          description: Needs a more full description created.
          operationId: gettheeffective,Orcurrentlyapplied,Branchingmodelforarepository
          responses:
            '404':
              description: If the repository does not exist
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
