apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Deploy Key From A Project
  description: This deletes a deploy key from a project.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Deploy Key From A Project
      description: This deletes a deploy key from a project.
    paths:
      /workspaces/{workspace}/projects/{project_key}/deploy-keys/{key_id}:
        delete:
          summary: Delete A Deploy Key From A Project
          description: This deletes a deploy key from a project.
          operationId: deleteadeploykeyfromaproject
          responses:
            '404':
              description: >-
                If the specified workspace, project, or project deploy key does not
                exist
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
