apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Trunks Trunksid Ipaccesscontrollists
  description: Associate an IP Access Control List with a Trunk
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Trunks Trunksid Ipaccesscontrollists
      description: Associate an IP Access Control List with a Trunk
    paths:
      /v1/Trunks/{TrunkSid}/IpAccessControlLists:
        post:
          summary: Post Trunks Trunksid Ipaccesscontrollists
          description: Associate an IP Access Control List with a Trunk
          operationId: createIpAccessControlList
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateIpAccessControlListRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trunking.v1.trunk.ip_access_control_list'
              description: Created
