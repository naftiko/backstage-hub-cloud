apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Chatservicesid Conversations Conversationsid Participants
  description: Add a new participant to the conversation in a specific service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Chatservicesid Conversations Conversationsid Participants
      description: Add a new participant to the conversation in a specific service
    paths:
      /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants:
        post:
          summary: Post Services Chatservicesid Conversations Conversationsid Participants
          description: Add a new participant to the conversation in a specific service
          operationId: createServiceConversationParticipant
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateServiceConversationParticipantRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.service.service_conversation.service_conversation_participant
              description: Created
