apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Subscriptions Sid
  description: Fetch a specific Subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Subscriptions Sid
      description: Fetch a specific Subscription.
    paths:
      /v1/Subscriptions/{Sid}:
        get:
          summary: Get Subscriptions Sid
          description: Fetch a specific Subscription.
          operationId: fetchSubscription
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/events.v1.subscription'
              description: OK
