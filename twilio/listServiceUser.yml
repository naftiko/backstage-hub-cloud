apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Chatservicesid Users
  description: Retrieve a list of all conversation users in your service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Chatservicesid Users
      description: Retrieve a list of all conversation users in your service
    paths:
      /v1/Services/{ChatServiceSid}/Users:
        get:
          summary: Get Services Chatservicesid Users
          description: Retrieve a list of all conversation users in your service
          operationId: listServiceUser
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListServiceUserResponse'
              description: OK
