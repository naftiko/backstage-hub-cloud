apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Documents Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Documents Sid
      description: Needs a more full description created.
    paths:
      /v1/Services/{ServiceSid}/Documents/{Sid}:
        post:
          summary: Post Services Servicesid Documents Sid
          description: Needs a more full description created.
          operationId: updateDocument
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateDocumentRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/sync.v1.service.document'
              description: OK
