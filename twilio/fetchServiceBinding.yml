apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Chatservicesid Bindings Sid
  description: Fetch a push notification binding from the conversation service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Chatservicesid Bindings Sid
      description: Fetch a push notification binding from the conversation service
    paths:
      /v1/Services/{ChatServiceSid}/Bindings/{Sid}:
        get:
          summary: Get Services Chatservicesid Bindings Sid
          description: Fetch a push notification binding from the conversation service
          operationId: fetchServiceBinding
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/conversations.v1.service.service_binding'
              description: OK
