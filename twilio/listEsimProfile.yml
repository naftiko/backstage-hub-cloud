apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Esimprofiles
  description: Retrieve a list of eSIM Profiles.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Esimprofiles
      description: Retrieve a list of eSIM Profiles.
    paths:
      /v1/ESimProfiles:
        get:
          summary: Get Esimprofiles
          description: Retrieve a list of eSIM Profiles.
          operationId: listEsimProfile
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListEsimProfileResponse'
              description: OK
