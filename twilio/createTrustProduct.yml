apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Trustproducts
  description: Create a new Customer-Profile.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Trustproducts
      description: Create a new Customer-Profile.
    paths:
      /v1/TrustProducts:
        post:
          summary: Post Trustproducts
          description: Create a new Customer-Profile.
          operationId: createTrustProduct
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateTrustProductRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trusthub.v1.trust_product'
              description: Created
