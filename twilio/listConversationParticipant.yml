apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Conversations Conversationsid Participants
  description: Retrieve a list of all participants of the conversation
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Conversations Conversationsid Participants
      description: Retrieve a list of all participants of the conversation
    paths:
      /v1/Conversations/{ConversationSid}/Participants:
        get:
          summary: Get Conversations Conversationsid Participants
          description: Retrieve a list of all participants of the conversation
          operationId: listConversationParticipant
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListConversationParticipantResponse'
              description: OK
