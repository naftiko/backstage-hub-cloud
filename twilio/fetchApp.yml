apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Apps Sid
  description: Fetch a specific App.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Apps Sid
      description: Fetch a specific App.
    paths:
      /v1/Apps/{Sid}:
        get:
          summary: Get Apps Sid
          description: Fetch a specific App.
          operationId: fetchApp
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/microvisor.v1.app'
              description: OK
