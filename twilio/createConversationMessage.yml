apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Conversations Conversationsid Messages
  description: Add a new message to the conversation
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Conversations Conversationsid Messages
      description: Add a new message to the conversation
    paths:
      /v1/Conversations/{ConversationSid}/Messages:
        post:
          summary: Post Conversations Conversationsid Messages
          description: Add a new message to the conversation
          operationId: createConversationMessage
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateConversationMessageRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.conversation.conversation_message
              description: Created
