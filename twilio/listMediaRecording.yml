apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Mediarecordings
  description: Returns a list of MediaRecordings.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Mediarecordings
      description: Returns a list of MediaRecordings.
    paths:
      /v1/MediaRecordings:
        get:
          summary: Get Mediarecordings
          description: Returns a list of MediaRecordings.
          operationId: listMediaRecording
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListMediaRecordingResponse'
              description: OK
