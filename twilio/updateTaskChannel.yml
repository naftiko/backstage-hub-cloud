apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Workspaces Workspacesid Taskchannels Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Workspaces Workspacesid Taskchannels Sid
      description: Needs a more full description created.
    paths:
      /v1/Workspaces/{WorkspaceSid}/TaskChannels/{Sid}:
        post:
          summary: Post Workspaces Workspacesid Taskchannels Sid
          description: Needs a more full description created.
          operationId: updateTaskChannel
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateTaskChannelRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/taskrouter.v1.workspace.task_channel'
              description: OK
