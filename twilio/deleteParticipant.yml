apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Services Servicesid Sessions Sessionsid Participants Sid
  description: >-
    Delete a specific Participant. This is a soft-delete. The participant
    remains associated with the session and cannot be re-added. Participants are
    only permanently deleted when the
    [Session](https://www.twilio.com/docs/proxy/api/session) is deleted.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Services Servicesid Sessions Sessionsid Participants Sid
      description: >-
        Delete a specific Participant. This is a soft-delete. The participant
        remains associated with the session and cannot be re-added. Participants are
        only permanently deleted when the
        [Session](https://www.twilio.com/docs/proxy/api/session) is deleted.
    paths:
      /v1/Services/{ServiceSid}/Sessions/{SessionSid}/Participants/{Sid}:
        delete:
          summary: Delete Services Servicesid Sessions Sessionsid Participants Sid
          description: >-
            Delete a specific Participant. This is a soft-delete. The participant
            remains associated with the session and cannot be re-added. Participants
            are only permanently deleted when the
            [Session](https://www.twilio.com/docs/proxy/api/session) is deleted.
          operationId: deleteParticipant
          responses:
            '204':
              description: The resource was deleted successfully.
