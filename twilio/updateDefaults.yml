apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Assistants Assistantsid Defaults
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Assistants Assistantsid Defaults
      description: Needs a more full description created.
    paths:
      /v1/Assistants/{AssistantSid}/Defaults:
        post:
          summary: Post Assistants Assistantsid Defaults
          description: Needs a more full description created.
          operationId: updateDefaults
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateDefaultsRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/autopilot.v1.assistant.defaults'
              description: OK
