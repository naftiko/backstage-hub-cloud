apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Configs Key
  description: Retrieve a Config for an Account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Configs Key
      description: Retrieve a Config for an Account.
    paths:
      /v1/Configs/{Key}:
        get:
          summary: Get Configs Key
          description: Retrieve a Config for an Account.
          operationId: fetchAccountConfig
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/microvisor.v1.account_config'
              description: OK
