apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Ipcommands Sid
  description: Fetch IP Command instance from your account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Ipcommands Sid
      description: Fetch IP Command instance from your account.
    paths:
      /v1/IpCommands/{Sid}:
        get:
          summary: Get Ipcommands Sid
          description: Fetch IP Command instance from your account.
          operationId: fetchIpCommand
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/supersim.v1.ip_command'
              description: OK
