apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Configs Key
  description: Update a config for an Account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Configs Key
      description: Update a config for an Account.
    paths:
      /v1/Configs/{Key}:
        post:
          summary: Post Configs Key
          description: Update a config for an Account.
          operationId: updateAccountConfig
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateAccountConfigRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/microvisor.v1.account_config'
              description: OK
