apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Hostednumber Orders Sid
  description: >-
    Cancel the HostedNumberOrder (only available when the status is in
    `received`).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Hostednumber Orders Sid
      description: >-
        Cancel the HostedNumberOrder (only available when the status is in
        `received`).
    paths:
      /v2/HostedNumber/Orders/{Sid}:
        delete:
          summary: Delete Hostednumber Orders Sid
          description: >-
            Cancel the HostedNumberOrder (only available when the status is in
            `received`).
          operationId: deleteHostedNumberOrder
          responses:
            '204':
              description: The resource was deleted successfully.
