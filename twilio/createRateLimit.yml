apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Ratelimits
  description: Create a new Rate Limit for a Service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Ratelimits
      description: Create a new Rate Limit for a Service
    paths:
      /v2/Services/{ServiceSid}/RateLimits:
        post:
          summary: Post Services Servicesid Ratelimits
          description: Create a new Rate Limit for a Service
          operationId: createRateLimit
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateRateLimitRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.rate_limit'
              description: Created
