apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Configs Key
  description: Delete a config for an Account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Configs Key
      description: Delete a config for an Account.
    paths:
      /v1/Configs/{Key}:
        delete:
          summary: Delete Configs Key
          description: Delete a config for an Account.
          operationId: deleteAccountConfig
          responses:
            '204':
              description: The resource was deleted successfully.
