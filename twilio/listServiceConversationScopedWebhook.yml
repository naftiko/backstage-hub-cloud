apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Chatservicesid Conversations Conversationsid Webhooks
  description: Retrieve a list of all webhooks scoped to the conversation
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Chatservicesid Conversations Conversationsid Webhooks
      description: Retrieve a list of all webhooks scoped to the conversation
    paths:
      /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks:
        get:
          summary: Get Services Chatservicesid Conversations Conversationsid Webhooks
          description: Retrieve a list of all webhooks scoped to the conversation
          operationId: listServiceConversationScopedWebhook
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/ListServiceConversationScopedWebhookResponse
              description: OK
