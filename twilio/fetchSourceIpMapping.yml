apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Sourceipmappings Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Sourceipmappings Sid
      description: Needs a more full description created.
    paths:
      /v1/SourceIpMappings/{Sid}:
        get:
          summary: Get Sourceipmappings Sid
          description: Needs a more full description created.
          operationId: fetchSourceIpMapping
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/voice.v1.source_ip_mapping'
              description: OK
