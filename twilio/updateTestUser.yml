apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Flows Sid Testusers
  description: Update flow test users
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Flows Sid Testusers
      description: Update flow test users
    paths:
      /v2/Flows/{Sid}/TestUsers:
        post:
          summary: Post Flows Sid Testusers
          description: Update flow test users
          operationId: updateTestUser
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateTestUserRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/studio.v2.flow.test_user'
              description: OK
