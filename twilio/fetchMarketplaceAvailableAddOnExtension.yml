apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Marketplace Availableaddons Availableaddonsid Extensions Sid
  description: Fetch an instance of an Extension for the Available Add-on.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Marketplace Availableaddons Availableaddonsid Extensions Sid
      description: Fetch an instance of an Extension for the Available Add-on.
    paths:
      /marketplace/AvailableAddOns/{AvailableAddOnSid}/Extensions/{Sid}:
        get:
          summary: Get Marketplace Availableaddons Availableaddonsid Extensions Sid
          description: Fetch an instance of an Extension for the Available Add-on.
          operationId: fetchMarketplaceAvailableAddOnExtension
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/preview.marketplace.available_add_on.available_add_on_extension
              description: OK
