apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Entities Identity Challenges Sid
  description: Verify a specific Challenge.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Entities Identity Challenges Sid
      description: Verify a specific Challenge.
    paths:
      /v2/Services/{ServiceSid}/Entities/{Identity}/Challenges/{Sid}:
        post:
          summary: Post Services Servicesid Entities Identity Challenges Sid
          description: Verify a specific Challenge.
          operationId: updateChallenge
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateChallengeRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.entity.challenge'
              description: OK
