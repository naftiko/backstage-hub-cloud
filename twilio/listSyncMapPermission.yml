apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Maps Mapsid Permissions
  description: Retrieve a list of all Permissions applying to a Sync Map.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Maps Mapsid Permissions
      description: Retrieve a list of all Permissions applying to a Sync Map.
    paths:
      /v1/Services/{ServiceSid}/Maps/{MapSid}/Permissions:
        get:
          summary: Get Services Servicesid Maps Mapsid Permissions
          description: Retrieve a list of all Permissions applying to a Sync Map.
          operationId: listSyncMapPermission
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListSyncMapPermissionResponse'
              description: OK
