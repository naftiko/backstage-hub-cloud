apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Entities Identity
  description: Fetch a specific Entity.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Entities Identity
      description: Fetch a specific Entity.
    paths:
      /v2/Services/{ServiceSid}/Entities/{Identity}:
        get:
          summary: Get Services Servicesid Entities Identity
          description: Fetch a specific Entity.
          operationId: fetchEntity
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.entity'
              description: OK
