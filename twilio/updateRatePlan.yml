apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Rateplans Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Rateplans Sid
      description: Needs a more full description created.
    paths:
      /v1/RatePlans/{Sid}:
        post:
          summary: Post Rateplans Sid
          description: Needs a more full description created.
          operationId: updateRatePlan
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateRatePlanRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/wireless.v1.rate_plan'
              description: OK
