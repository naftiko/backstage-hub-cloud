apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Streams
  description: Create a new Stream.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Streams
      description: Create a new Stream.
    paths:
      /v1/Services/{ServiceSid}/Streams:
        post:
          summary: Post Services Servicesid Streams
          description: Create a new Stream.
          operationId: createSyncStream
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateSyncStreamRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/sync.v1.service.sync_stream'
              description: Created
