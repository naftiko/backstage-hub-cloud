apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Attempts Summary
  description: Get a summary of how many attempts were made and how many were converted.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Attempts Summary
      description: Get a summary of how many attempts were made and how many were converted.
    paths:
      /v2/Attempts/Summary:
        get:
          summary: Get Attempts Summary
          description: >-
            Get a summary of how many attempts were made and how many were
            converted.
          operationId: fetchVerificationAttemptsSummary
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.verification_attempts_summary'
              description: OK
