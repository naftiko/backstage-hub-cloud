apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Conversations Conversationsid Messages Sid
  description: Fetch a message from the conversation
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Conversations Conversationsid Messages Sid
      description: Fetch a message from the conversation
    paths:
      /v1/Conversations/{ConversationSid}/Messages/{Sid}:
        get:
          summary: Get Conversations Conversationsid Messages Sid
          description: Fetch a message from the conversation
          operationId: fetchConversationMessage
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.conversation.conversation_message
              description: OK
