apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Rooms Roomsid Participants Participantsid Subscribedtracks Sid
  description: >-
    Returns a single Track resource represented by `track_sid`.  Note: This is
    one resource with the Video API that requires a SID, be Track Name on the
    subscriber side is not guaranteed to be unique.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Rooms Roomsid Participants Participantsid Subscribedtracks Sid
      description: >-
        Returns a single Track resource represented by `track_sid`.  Note: This is
        one resource with the Video API that requires a SID, be Track Name on the
        subscriber side is not guaranteed to be unique.
    paths:
      /v1/Rooms/{RoomSid}/Participants/{ParticipantSid}/SubscribedTracks/{Sid}:
        get:
          summary: Get Rooms Roomsid Participants Participantsid Subscribedtracks Sid
          description: >-
            Returns a single Track resource represented by `track_sid`.  Note: This
            is one resource with the Video API that requires a SID, be Track Name on
            the subscriber side is not guaranteed to be unique.
          operationId: fetchRoomParticipantSubscribedTrack
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/video.v1.room.room_participant.room_participant_subscribed_track
              description: OK
