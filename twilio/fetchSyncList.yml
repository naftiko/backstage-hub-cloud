apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Lists Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Lists Sid
      description: Needs a more full description created.
    paths:
      /v1/Services/{ServiceSid}/Lists/{Sid}:
        get:
          summary: Get Services Servicesid Lists Sid
          description: Needs a more full description created.
          operationId: fetchSyncList
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/sync.v1.service.sync_list'
              description: OK
