apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Marketplace Installedaddons Installedaddonsid Extensions Sid
  description: Update an Extension for an Add-on installation.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Marketplace Installedaddons Installedaddonsid Extensions Sid
      description: Update an Extension for an Add-on installation.
    paths:
      /marketplace/InstalledAddOns/{InstalledAddOnSid}/Extensions/{Sid}:
        post:
          summary: Post Marketplace Installedaddons Installedaddonsid Extensions Sid
          description: Update an Extension for an Add-on installation.
          operationId: updateMarketplaceInstalledAddOnExtension
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: >-
                    #/components/schemas/UpdateMarketplaceInstalledAddOnExtensionRequest
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/preview.marketplace.installed_add_on.installed_add_on_extension
              description: OK
