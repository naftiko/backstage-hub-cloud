apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Configuration Addresses Sid
  description: Remove an existing address configuration
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Configuration Addresses Sid
      description: Remove an existing address configuration
    paths:
      /v1/Configuration/Addresses/{Sid}:
        delete:
          summary: Delete Configuration Addresses Sid
          description: Remove an existing address configuration
          operationId: deleteConfigurationAddress
          responses:
            '204':
              description: The resource was deleted successfully.
