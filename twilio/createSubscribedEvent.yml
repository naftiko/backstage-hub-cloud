apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Subscriptions Subscriptionsid Subscribedevents
  description: Add an event type to a Subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Subscriptions Subscriptionsid Subscribedevents
      description: Add an event type to a Subscription.
    paths:
      /v1/Subscriptions/{SubscriptionSid}/SubscribedEvents:
        post:
          summary: Post Subscriptions Subscriptionsid Subscribedevents
          description: Add an event type to a Subscription.
          operationId: createSubscribedEvent
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateSubscribedEventRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/events.v1.subscription.subscribed_event'
              description: Created
