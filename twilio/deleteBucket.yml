apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Services Servicesid Ratelimits Ratelimitsid Buckets Sid
  description: Delete a specific Bucket.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Services Servicesid Ratelimits Ratelimitsid Buckets Sid
      description: Delete a specific Bucket.
    paths:
      /v2/Services/{ServiceSid}/RateLimits/{RateLimitSid}/Buckets/{Sid}:
        delete:
          summary: Delete Services Servicesid Ratelimits Ratelimitsid Buckets Sid
          description: Delete a specific Bucket.
          operationId: deleteBucket
          responses:
            '204':
              description: The resource was deleted successfully.
