apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Accesstokens
  description: Create a new enrollment Access Token for the Entity
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Accesstokens
      description: Create a new enrollment Access Token for the Entity
    paths:
      /v2/Services/{ServiceSid}/AccessTokens:
        post:
          summary: Post Services Servicesid Accesstokens
          description: Create a new enrollment Access Token for the Entity
          operationId: createAccessToken
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateAccessTokenRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.access_token'
              description: Created
