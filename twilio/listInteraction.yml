apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Sessions Sessionsid Interactions
  description: >-
    Retrieve a list of all Interactions for a Session. A maximum of 100 records
    will be returned per page.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Sessions Sessionsid Interactions
      description: >-
        Retrieve a list of all Interactions for a Session. A maximum of 100 records
        will be returned per page.
    paths:
      /v1/Services/{ServiceSid}/Sessions/{SessionSid}/Interactions:
        get:
          summary: Get Services Servicesid Sessions Sessionsid Interactions
          description: >-
            Retrieve a list of all Interactions for a Session. A maximum of 100
            records will be returned per page.
          operationId: listInteraction
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListInteractionResponse'
              description: OK
