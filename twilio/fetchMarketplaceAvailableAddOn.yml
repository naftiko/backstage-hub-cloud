apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Marketplace Availableaddons Sid
  description: Fetch an instance of an Add-on currently available to be installed.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Marketplace Availableaddons Sid
      description: Fetch an instance of an Add-on currently available to be installed.
    paths:
      /marketplace/AvailableAddOns/{Sid}:
        get:
          summary: Get Marketplace Availableaddons Sid
          description: Fetch an instance of an Add-on currently available to be installed.
          operationId: fetchMarketplaceAvailableAddOn
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/preview.marketplace.available_add_on'
              description: OK
