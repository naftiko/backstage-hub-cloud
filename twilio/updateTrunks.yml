apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Trunks Siptrunkdomain
  description: Assign an Inbound Processing Region to a SIP Trunk
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Trunks Siptrunkdomain
      description: Assign an Inbound Processing Region to a SIP Trunk
    paths:
      /v2/Trunks/{SipTrunkDomain}:
        post:
          summary: Post Trunks Siptrunkdomain
          description: Assign an Inbound Processing Region to a SIP Trunk
          operationId: updateTrunks
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateTrunksRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/routes.v2.trunks'
              description: OK
