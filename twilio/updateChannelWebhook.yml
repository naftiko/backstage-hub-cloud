apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Channels Channelsid Webhooks Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Channels Channelsid Webhooks Sid
      description: Needs a more full description created.
    paths:
      /v2/Services/{ServiceSid}/Channels/{ChannelSid}/Webhooks/{Sid}:
        post:
          summary: Post Services Servicesid Channels Channelsid Webhooks Sid
          description: Needs a more full description created.
          operationId: updateChannelWebhook
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateChannelWebhookRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/ip_messaging.v2.service.channel.channel_webhook
              description: OK
