apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Flows Sid Revisions
  description: Retrieve a list of all Flows revisions.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Flows Sid Revisions
      description: Retrieve a list of all Flows revisions.
    paths:
      /v2/Flows/{Sid}/Revisions:
        get:
          summary: Get Flows Sid Revisions
          description: Retrieve a list of all Flows revisions.
          operationId: listFlowRevision
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListFlowRevisionResponse'
              description: OK
