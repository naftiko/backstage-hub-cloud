apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Lists Listsid Permissions Identity
  description: Update an identity's access to a specific Sync List.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Lists Listsid Permissions Identity
      description: Update an identity's access to a specific Sync List.
    paths:
      /v1/Services/{ServiceSid}/Lists/{ListSid}/Permissions/{Identity}:
        post:
          summary: Post Services Servicesid Lists Listsid Permissions Identity
          description: Update an identity's access to a specific Sync List.
          operationId: updateSyncListPermission
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateSyncListPermissionRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/sync.v1.service.sync_list.sync_list_permission
              description: OK
