apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Networkaccessprofiles
  description: Create a new Network Access Profile
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Networkaccessprofiles
      description: Create a new Network Access Profile
    paths:
      /v1/NetworkAccessProfiles:
        post:
          summary: Post Networkaccessprofiles
          description: Create a new Network Access Profile
          operationId: createNetworkAccessProfile
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateNetworkAccessProfileRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/supersim.v1.network_access_profile'
              description: Created
