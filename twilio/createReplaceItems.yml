apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Regulatorycompliance Bundles Bundlesid Replaceitems
  description: >-
    Replaces all bundle items in the target bundle (specified in the path) with
    all the bundle items of the source bundle (specified by the from_bundle_sid
    body param)
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Regulatorycompliance Bundles Bundlesid Replaceitems
      description: >-
        Replaces all bundle items in the target bundle (specified in the path) with
        all the bundle items of the source bundle (specified by the from_bundle_sid
        body param)
    paths:
      /v2/RegulatoryCompliance/Bundles/{BundleSid}/ReplaceItems:
        post:
          summary: Post Regulatorycompliance Bundles Bundlesid Replaceitems
          description: >-
            Replaces all bundle items in the target bundle (specified in the path)
            with all the bundle items of the source bundle (specified by the
            from_bundle_sid body param)
          operationId: createReplaceItems
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateReplaceItemsRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/numbers.v2.regulatory_compliance.bundle.replace_items
              description: Created
