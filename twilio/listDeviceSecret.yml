apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Devices Devicesid Secrets
  description: Retrieve a list of all Secrets for a Device.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Devices Devicesid Secrets
      description: Retrieve a list of all Secrets for a Device.
    paths:
      /v1/Devices/{DeviceSid}/Secrets:
        get:
          summary: Get Devices Devicesid Secrets
          description: Retrieve a list of all Secrets for a Device.
          operationId: listDeviceSecret
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListDeviceSecretResponse'
              description: OK
