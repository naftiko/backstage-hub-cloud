apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Chatservicesid Conversations Conversationsid Messages Sid
  description: Fetch a message from the conversation
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Chatservicesid Conversations Conversationsid Messages Sid
      description: Fetch a message from the conversation
    paths:
      /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages/{Sid}:
        get:
          summary: Get Services Chatservicesid Conversations Conversationsid Messages Sid
          description: Fetch a message from the conversation
          operationId: fetchServiceConversationMessage
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.service.service_conversation.service_conversation_message
              description: OK
