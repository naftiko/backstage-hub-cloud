apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Chatservicesid Conversations Conversationsid Messages
  description: Retrieve a list of all messages in the conversation
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Chatservicesid Conversations Conversationsid Messages
      description: Retrieve a list of all messages in the conversation
    paths:
      /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages:
        get:
          summary: Get Services Chatservicesid Conversations Conversationsid Messages
          description: Retrieve a list of all messages in the conversation
          operationId: listServiceConversationMessage
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListServiceConversationMessageResponse'
              description: OK
