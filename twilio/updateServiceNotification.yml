apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Chatservicesid Configuration Notifications
  description: Update push notification service settings
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Chatservicesid Configuration Notifications
      description: Update push notification service settings
    paths:
      /v1/Services/{ChatServiceSid}/Configuration/Notifications:
        post:
          summary: Post Services Chatservicesid Configuration Notifications
          description: Update push notification service settings
          operationId: updateServiceNotification
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateServiceNotificationRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.service.service_configuration.service_notification
              description: OK
