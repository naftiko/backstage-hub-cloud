apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Chatservicesid Configuration
  description: Fetch the configuration of a conversation service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Chatservicesid Configuration
      description: Fetch the configuration of a conversation service
    paths:
      /v1/Services/{ChatServiceSid}/Configuration:
        get:
          summary: Get Services Chatservicesid Configuration
          description: Fetch the configuration of a conversation service
          operationId: fetchServiceConfiguration
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.service.service_configuration
              description: OK
