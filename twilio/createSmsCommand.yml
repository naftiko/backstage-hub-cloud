apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Smscommands
  description: Send SMS Command to a Sim.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Smscommands
      description: Send SMS Command to a Sim.
    paths:
      /v1/SmsCommands:
        post:
          summary: Post Smscommands
          description: Send SMS Command to a Sim.
          operationId: createSmsCommand
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateSmsCommandRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/supersim.v1.sms_command'
              description: Created
