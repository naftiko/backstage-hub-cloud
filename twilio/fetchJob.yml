apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Exports Jobs Jobsid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Exports Jobs Jobsid
      description: Needs a more full description created.
    paths:
      /v1/Exports/Jobs/{JobSid}:
        get:
          summary: Get Exports Jobs Jobsid
          description: Needs a more full description created.
          operationId: fetchJob
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/bulkexports.v1.export.job'
              description: OK
