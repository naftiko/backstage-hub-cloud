apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Chatservicesid Bindings
  description: >-
    Retrieve a list of all push notification bindings in the conversation
    service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Chatservicesid Bindings
      description: >-
        Retrieve a list of all push notification bindings in the conversation
        service
    paths:
      /v1/Services/{ChatServiceSid}/Bindings:
        get:
          summary: Get Services Chatservicesid Bindings
          description: >-
            Retrieve a list of all push notification bindings in the conversation
            service
          operationId: listServiceBinding
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListServiceBindingResponse'
              description: OK
