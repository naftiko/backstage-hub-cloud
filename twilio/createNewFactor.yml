apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Entities Identity Factors
  description: Create a new Factor for the Entity
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Entities Identity Factors
      description: Create a new Factor for the Entity
    paths:
      /v2/Services/{ServiceSid}/Entities/{Identity}/Factors:
        post:
          summary: Post Services Servicesid Entities Identity Factors
          description: Create a new Factor for the Entity
          operationId: createNewFactor
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateNewFactorRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.entity.new_factor'
              description: Created
