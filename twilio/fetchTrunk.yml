apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Trunks Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Trunks Sid
      description: Needs a more full description created.
    paths:
      /v1/Trunks/{Sid}:
        get:
          summary: Get Trunks Sid
          description: Needs a more full description created.
          operationId: fetchTrunk
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trunking.v1.trunk'
              description: OK
