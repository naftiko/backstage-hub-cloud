apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Hostednumber Orders Bulk Bulkhostingsid
  description: Fetch a specific BulkHostedNumberOrder.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Hostednumber Orders Bulk Bulkhostingsid
      description: Fetch a specific BulkHostedNumberOrder.
    paths:
      /v2/HostedNumber/Orders/Bulk/{BulkHostingSid}:
        get:
          summary: Get Hostednumber Orders Bulk Bulkhostingsid
          description: Fetch a specific BulkHostedNumberOrder.
          operationId: fetchBulkHostedNumberOrder
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/numbers.v2.bulk_hosted_number_order'
              description: OK
