apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Rooms Roomsid Participants Sid Anonymize
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Rooms Roomsid Participants Sid Anonymize
      description: Needs a more full description created.
    paths:
      /v1/Rooms/{RoomSid}/Participants/{Sid}/Anonymize:
        post:
          summary: Post Rooms Roomsid Participants Sid Anonymize
          description: Needs a more full description created.
          operationId: updateRoomParticipantAnonymize
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/video.v1.room.room_participant.room_participant_anonymize
              description: OK
