apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Entities
  description: Retrieve a list of all Entities for a Service.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Entities
      description: Retrieve a list of all Entities for a Service.
    paths:
      /v2/Services/{ServiceSid}/Entities:
        get:
          summary: Get Services Servicesid Entities
          description: Retrieve a list of all Entities for a Service.
          operationId: listEntity
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListEntityResponse'
              description: OK
