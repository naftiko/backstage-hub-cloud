apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Streams
  description: Retrieve a list of all Streams in a Service Instance.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Streams
      description: Retrieve a list of all Streams in a Service Instance.
    paths:
      /v1/Services/{ServiceSid}/Streams:
        get:
          summary: Get Services Servicesid Streams
          description: Retrieve a list of all Streams in a Service Instance.
          operationId: listSyncStream
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListSyncStreamResponse'
              description: OK
