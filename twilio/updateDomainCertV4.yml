apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Linkshortening Domains Domainsid Certificate
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Linkshortening Domains Domainsid Certificate
      description: Needs a more full description created.
    paths:
      /v1/LinkShortening/Domains/{DomainSid}/Certificate:
        post:
          summary: Post Linkshortening Domains Domainsid Certificate
          description: Needs a more full description created.
          operationId: updateDomainCertV4
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateDomainCertV4Request'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/messaging.v1.domain_cert_v4'
              description: Created
