apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Chatservicesid Participantconversations
  description: >-
    Retrieve a list of all Conversations that this Participant belongs to by
    identity or by address. Only one parameter should be specified.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Chatservicesid Participantconversations
      description: >-
        Retrieve a list of all Conversations that this Participant belongs to by
        identity or by address. Only one parameter should be specified.
    paths:
      /v1/Services/{ChatServiceSid}/ParticipantConversations:
        get:
          summary: Get Services Chatservicesid Participantconversations
          description: >-
            Retrieve a list of all Conversations that this Participant belongs to by
            identity or by address. Only one parameter should be specified.
          operationId: listServiceParticipantConversation
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/ListServiceParticipantConversationResponse
              description: OK
