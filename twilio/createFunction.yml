apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Functions
  description: Create a new Function resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Functions
      description: Create a new Function resource.
    paths:
      /v1/Services/{ServiceSid}/Functions:
        post:
          summary: Post Services Servicesid Functions
          description: Create a new Function resource.
          operationId: createFunction
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateFunctionRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/serverless.v1.service.function'
              description: Created
