apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Regulatorycompliance Bundles Sid
  description: Updates a Bundle in an account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Regulatorycompliance Bundles Sid
      description: Updates a Bundle in an account.
    paths:
      /v2/RegulatoryCompliance/Bundles/{Sid}:
        post:
          summary: Post Regulatorycompliance Bundles Sid
          description: Updates a Bundle in an account.
          operationId: updateBundle
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateBundleRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/numbers.v2.regulatory_compliance.bundle'
              description: OK
