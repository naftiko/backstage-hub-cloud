apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Exports Resourcetype Configuration
  description: Update a specific Export Configuration.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Exports Resourcetype Configuration
      description: Update a specific Export Configuration.
    paths:
      /v1/Exports/{ResourceType}/Configuration:
        post:
          summary: Post Exports Resourcetype Configuration
          description: Update a specific Export Configuration.
          operationId: updateExportConfiguration
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateExportConfigurationRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/bulkexports.v1.export_configuration'
              description: OK
