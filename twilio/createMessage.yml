apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Channels Channelsid Messages
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Channels Channelsid Messages
      description: Needs a more full description created.
    paths:
      /v2/Services/{ServiceSid}/Channels/{ChannelSid}/Messages:
        post:
          summary: Post Services Servicesid Channels Channelsid Messages
          description: Needs a more full description created.
          operationId: createMessage
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateMessageRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ip_messaging.v2.service.channel.message'
              description: Created
