apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Mediaprocessors
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Mediaprocessors
      description: Needs a more full description created.
    paths:
      /v1/MediaProcessors:
        post:
          summary: Post Mediaprocessors
          description: Needs a more full description created.
          operationId: createMediaProcessor
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateMediaProcessorRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/media.v1.media_processor'
              description: Created
