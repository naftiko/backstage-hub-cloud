apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Playerstreamers Sid Playbackgrant
  description: >-
    **This method is not enabled.** Returns a single PlaybackGrant resource
    identified by a SID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Playerstreamers Sid Playbackgrant
      description: >-
        **This method is not enabled.** Returns a single PlaybackGrant resource
        identified by a SID.
    paths:
      /v1/PlayerStreamers/{Sid}/PlaybackGrant:
        get:
          summary: Get Playerstreamers Sid Playbackgrant
          description: >-
            **This method is not enabled.** Returns a single PlaybackGrant resource
            identified by a SID.
          operationId: fetchPlayerStreamerPlaybackGrant
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/media.v1.player_streamer.player_streamer_playback_grant
              description: OK
