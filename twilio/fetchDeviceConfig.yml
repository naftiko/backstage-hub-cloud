apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Devices Devicesid Configs Key
  description: Retrieve a Config for a Device.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Devices Devicesid Configs Key
      description: Retrieve a Config for a Device.
    paths:
      /v1/Devices/{DeviceSid}/Configs/{Key}:
        get:
          summary: Get Devices Devicesid Configs Key
          description: Retrieve a Config for a Device.
          operationId: fetchDeviceConfig
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/microvisor.v1.device.device_config'
              description: OK
