apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Ratelimits
  description: Retrieve a list of all Rate Limits for a service.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Ratelimits
      description: Retrieve a list of all Rate Limits for a service.
    paths:
      /v2/Services/{ServiceSid}/RateLimits:
        get:
          summary: Get Services Servicesid Ratelimits
          description: Retrieve a list of all Rate Limits for a service.
          operationId: listRateLimit
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListRateLimitResponse'
              description: OK
