apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Maps Mapsid Items Key
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Maps Mapsid Items Key
      description: Needs a more full description created.
    paths:
      /v1/Services/{ServiceSid}/Maps/{MapSid}/Items/{Key}:
        post:
          summary: Post Services Servicesid Maps Mapsid Items Key
          description: Needs a more full description created.
          operationId: updateSyncMapItem
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateSyncMapItemRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/sync.v1.service.sync_map.sync_map_item'
              description: OK
