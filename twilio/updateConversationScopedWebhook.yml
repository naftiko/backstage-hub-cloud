apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Conversations Conversationsid Webhooks Sid
  description: Update an existing conversation-scoped webhook
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Conversations Conversationsid Webhooks Sid
      description: Update an existing conversation-scoped webhook
    paths:
      /v1/Conversations/{ConversationSid}/Webhooks/{Sid}:
        post:
          summary: Post Conversations Conversationsid Webhooks Sid
          description: Update an existing conversation-scoped webhook
          operationId: updateConversationScopedWebhook
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateConversationScopedWebhookRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.conversation.conversation_scoped_webhook
              description: OK
