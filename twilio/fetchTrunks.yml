apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Trunks Siptrunkdomain
  description: Fetch the Inbound Processing Region assigned to a SIP Trunk.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Trunks Siptrunkdomain
      description: Fetch the Inbound Processing Region assigned to a SIP Trunk.
    paths:
      /v2/Trunks/{SipTrunkDomain}:
        get:
          summary: Get Trunks Siptrunkdomain
          description: Fetch the Inbound Processing Region assigned to a SIP Trunk.
          operationId: fetchTrunks
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/routes.v2.trunks'
              description: OK
