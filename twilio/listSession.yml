apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Sessions
  description: >-
    Retrieve a list of all Sessions for the Service. A maximum of 100 records
    will be returned per page.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Sessions
      description: >-
        Retrieve a list of all Sessions for the Service. A maximum of 100 records
        will be returned per page.
    paths:
      /v1/Services/{ServiceSid}/Sessions:
        get:
          summary: Get Services Servicesid Sessions
          description: >-
            Retrieve a list of all Sessions for the Service. A maximum of 100
            records will be returned per page.
          operationId: listSession
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListSessionResponse'
              description: OK
