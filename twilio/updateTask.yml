apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Workspaces Workspacesid Tasks Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Workspaces Workspacesid Tasks Sid
      description: Needs a more full description created.
    paths:
      /v1/Workspaces/{WorkspaceSid}/Tasks/{Sid}:
        post:
          summary: Post Workspaces Workspacesid Tasks Sid
          description: Needs a more full description created.
          operationId: updateTask
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateTaskRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/taskrouter.v1.workspace.task'
              description: OK
