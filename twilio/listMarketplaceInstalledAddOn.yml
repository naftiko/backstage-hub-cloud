apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Marketplace Installedaddons
  description: Retrieve a list of Add-ons currently installed on this Account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Marketplace Installedaddons
      description: Retrieve a list of Add-ons currently installed on this Account.
    paths:
      /marketplace/InstalledAddOns:
        get:
          summary: Get Marketplace Installedaddons
          description: Retrieve a list of Add-ons currently installed on this Account.
          operationId: listMarketplaceInstalledAddOn
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListMarketplaceInstalledAddOnResponse'
              description: OK
