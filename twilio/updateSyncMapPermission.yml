apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Maps Mapsid Permissions Identity
  description: Update an identity's access to a specific Sync Map.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Maps Mapsid Permissions Identity
      description: Update an identity's access to a specific Sync Map.
    paths:
      /v1/Services/{ServiceSid}/Maps/{MapSid}/Permissions/{Identity}:
        post:
          summary: Post Services Servicesid Maps Mapsid Permissions Identity
          description: Update an identity's access to a specific Sync Map.
          operationId: updateSyncMapPermission
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateSyncMapPermissionRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/sync.v1.service.sync_map.sync_map_permission
              description: OK
