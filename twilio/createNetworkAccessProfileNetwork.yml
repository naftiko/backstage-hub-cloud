apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Networkaccessprofiles Networkaccessprofilesid Networks
  description: Add a Network resource to the Network Access Profile resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Networkaccessprofiles Networkaccessprofilesid Networks
      description: Add a Network resource to the Network Access Profile resource.
    paths:
      /v1/NetworkAccessProfiles/{NetworkAccessProfileSid}/Networks:
        post:
          summary: Post Networkaccessprofiles Networkaccessprofilesid Networks
          description: Add a Network resource to the Network Access Profile resource.
          operationId: createNetworkAccessProfileNetwork
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateNetworkAccessProfileNetworkRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/supersim.v1.network_access_profile.network_access_profile_network
              description: Created
