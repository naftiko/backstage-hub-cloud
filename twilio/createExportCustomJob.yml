apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Exports Resourcetype Jobs
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Exports Resourcetype Jobs
      description: Needs a more full description created.
    paths:
      /v1/Exports/{ResourceType}/Jobs:
        post:
          summary: Post Exports Resourcetype Jobs
          description: Needs a more full description created.
          operationId: createExportCustomJob
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateExportCustomJobRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/bulkexports.v1.export.export_custom_job'
              description: Created
