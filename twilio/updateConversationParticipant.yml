apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Conversations Conversationsid Participants Sid
  description: Update an existing participant in the conversation
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Conversations Conversationsid Participants Sid
      description: Update an existing participant in the conversation
    paths:
      /v1/Conversations/{ConversationSid}/Participants/{Sid}:
        post:
          summary: Post Conversations Conversationsid Participants Sid
          description: Update an existing participant in the conversation
          operationId: updateConversationParticipant
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateConversationParticipantRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.conversation.conversation_participant
              description: OK
