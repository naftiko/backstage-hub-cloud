apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Ipcommands
  description: Send an IP Command to a Super SIM.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Ipcommands
      description: Send an IP Command to a Super SIM.
    paths:
      /v1/IpCommands:
        post:
          summary: Post Ipcommands
          description: Send an IP Command to a Super SIM.
          operationId: createIpCommand
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateIpCommandRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/supersim.v1.ip_command'
              description: Created
