apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Credentials Publickeys Sid
  description: Fetch the public key specified by the provided Credential Sid
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Credentials Publickeys Sid
      description: Fetch the public key specified by the provided Credential Sid
    paths:
      /v1/Credentials/PublicKeys/{Sid}:
        get:
          summary: Get Credentials Publickeys Sid
          description: Fetch the public key specified by the provided Credential Sid
          operationId: fetchCredentialPublicKey
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/accounts.v1.credential.credential_public_key
              description: OK
