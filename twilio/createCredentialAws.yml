apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Credentials Aws
  description: Create a new AWS Credential
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Credentials Aws
      description: Create a new AWS Credential
    paths:
      /v1/Credentials/AWS:
        post:
          summary: Post Credentials Aws
          description: Create a new AWS Credential
          operationId: createCredentialAws
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateCredentialAwsRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/accounts.v1.credential.credential_aws'
              description: Created
