apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Schemas Id Versions
  description: Retrieve a paginated list of versions of the schema.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Schemas Id Versions
      description: Retrieve a paginated list of versions of the schema.
    paths:
      /v1/Schemas/{Id}/Versions:
        get:
          summary: Get Schemas Id Versions
          description: Retrieve a paginated list of versions of the schema.
          operationId: listSchemaVersion
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListSchemaVersionResponse'
              description: OK
