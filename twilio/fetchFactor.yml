apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Entities Identity Factors Sid
  description: Fetch a specific Factor.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Entities Identity Factors Sid
      description: Fetch a specific Factor.
    paths:
      /v2/Services/{ServiceSid}/Entities/{Identity}/Factors/{Sid}:
        get:
          summary: Get Services Servicesid Entities Identity Factors Sid
          description: Fetch a specific Factor.
          operationId: fetchFactor
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.entity.factor'
              description: OK
