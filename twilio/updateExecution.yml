apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Flows Flowsid Executions Sid
  description: Update the status of an Execution to `ended`.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Flows Flowsid Executions Sid
      description: Update the status of an Execution to `ended`.
    paths:
      /v2/Flows/{FlowSid}/Executions/{Sid}:
        post:
          summary: Post Flows Flowsid Executions Sid
          description: Update the status of an Execution to `ended`.
          operationId: updateExecution
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateExecutionRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/studio.v2.flow.execution'
              description: OK
