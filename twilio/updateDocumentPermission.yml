apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Documents Documentsid Permissions Identity
  description: Update an identity's access to a specific Sync Document.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Documents Documentsid Permissions Identity
      description: Update an identity's access to a specific Sync Document.
    paths:
      /v1/Services/{ServiceSid}/Documents/{DocumentSid}/Permissions/{Identity}:
        post:
          summary: Post Services Servicesid Documents Documentsid Permissions Identity
          description: Update an identity's access to a specific Sync Document.
          operationId: updateDocumentPermission
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateDocumentPermissionRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/sync.v1.service.document.document_permission
              description: OK
