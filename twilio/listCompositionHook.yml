apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Compositionhooks
  description: List of all Recording CompositionHook resources.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Compositionhooks
      description: List of all Recording CompositionHook resources.
    paths:
      /v1/CompositionHooks:
        get:
          summary: Get Compositionhooks
          description: List of all Recording CompositionHook resources.
          operationId: listCompositionHook
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListCompositionHookResponse'
              description: OK
