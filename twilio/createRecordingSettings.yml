apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Recordingsettings Default
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Recordingsettings Default
      description: Needs a more full description created.
    paths:
      /v1/RecordingSettings/Default:
        post:
          summary: Post Recordingsettings Default
          description: Needs a more full description created.
          operationId: createRecordingSettings
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateRecordingSettingsRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/video.v1.recording_settings'
              description: Created
