apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Chatservicesid Roles Sid
  description: Update an existing user role in your service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Chatservicesid Roles Sid
      description: Update an existing user role in your service
    paths:
      /v1/Services/{ChatServiceSid}/Roles/{Sid}:
        post:
          summary: Post Services Chatservicesid Roles Sid
          description: Update an existing user role in your service
          operationId: updateServiceRole
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateServiceRoleRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/conversations.v1.service.service_role'
              description: OK
