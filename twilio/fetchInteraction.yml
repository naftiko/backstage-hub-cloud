apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Sessions Sessionsid Interactions Sid
  description: >-
    Retrieve a list of Interactions for a given
    [Session](https://www.twilio.com/docs/proxy/api/session).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Sessions Sessionsid Interactions Sid
      description: >-
        Retrieve a list of Interactions for a given
        [Session](https://www.twilio.com/docs/proxy/api/session).
    paths:
      /v1/Services/{ServiceSid}/Sessions/{SessionSid}/Interactions/{Sid}:
        get:
          summary: Get Services Servicesid Sessions Sessionsid Interactions Sid
          description: >-
            Retrieve a list of Interactions for a given
            [Session](https://www.twilio.com/docs/proxy/api/session).
          operationId: fetchInteraction
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/proxy.v1.service.session.interaction'
              description: OK
