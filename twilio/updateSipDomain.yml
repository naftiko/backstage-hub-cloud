apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Sipdomains Sipdomain
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Sipdomains Sipdomain
      description: Needs a more full description created.
    paths:
      /v2/SipDomains/{SipDomain}:
        post:
          summary: Post Sipdomains Sipdomain
          description: Needs a more full description created.
          operationId: updateSipDomain
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateSipDomainRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/routes.v2.sip_domain'
              description: OK
