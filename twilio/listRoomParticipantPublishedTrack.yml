apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Rooms Roomsid Participants Participantsid Publishedtracks
  description: >-
    Returns a list of tracks associated with a given Participant. Only
    `currently` Published Tracks are in the list resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Rooms Roomsid Participants Participantsid Publishedtracks
      description: >-
        Returns a list of tracks associated with a given Participant. Only
        `currently` Published Tracks are in the list resource.
    paths:
      /v1/Rooms/{RoomSid}/Participants/{ParticipantSid}/PublishedTracks:
        get:
          summary: Get Rooms Roomsid Participants Participantsid Publishedtracks
          description: >-
            Returns a list of tracks associated with a given Participant. Only
            `currently` Published Tracks are in the list resource.
          operationId: listRoomParticipantPublishedTrack
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListRoomParticipantPublishedTrackResponse'
              description: OK
