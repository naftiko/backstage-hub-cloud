apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Esimprofiles Sid
  description: Fetch an eSIM Profile.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Esimprofiles Sid
      description: Fetch an eSIM Profile.
    paths:
      /v1/ESimProfiles/{Sid}:
        get:
          summary: Get Esimprofiles Sid
          description: Fetch an eSIM Profile.
          operationId: fetchEsimProfile
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/supersim.v1.esim_profile'
              description: OK
