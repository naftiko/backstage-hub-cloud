apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Secrets Key
  description: Retrieve a Secret for an Account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Secrets Key
      description: Retrieve a Secret for an Account.
    paths:
      /v1/Secrets/{Key}:
        get:
          summary: Get Secrets Key
          description: Retrieve a Secret for an Account.
          operationId: fetchAccountSecret
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/microvisor.v1.account_secret'
              description: OK
