apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Rooms Roomsid Participants Participantsid Subscriberules
  description: Update the Subscribe Rules for the Participant
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Rooms Roomsid Participants Participantsid Subscriberules
      description: Update the Subscribe Rules for the Participant
    paths:
      /v1/Rooms/{RoomSid}/Participants/{ParticipantSid}/SubscribeRules:
        post:
          summary: Post Rooms Roomsid Participants Participantsid Subscriberules
          description: Update the Subscribe Rules for the Participant
          operationId: updateRoomParticipantSubscribeRule
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateRoomParticipantSubscribeRuleRequest'
          responses:
            '202':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/video.v1.room.room_participant.room_participant_subscribe_rule
              description: Accepted
