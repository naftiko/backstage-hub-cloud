apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Endusers Sid
  description: Update an existing End User.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Endusers Sid
      description: Update an existing End User.
    paths:
      /v1/EndUsers/{Sid}:
        post:
          summary: Post Endusers Sid
          description: Update an existing End User.
          operationId: updateEndUser
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateEndUserRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trusthub.v1.end_user'
              description: OK
