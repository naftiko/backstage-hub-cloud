apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Supportingdocuments
  description: Create a new Supporting Document.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Supportingdocuments
      description: Create a new Supporting Document.
    paths:
      /v1/SupportingDocuments:
        post:
          summary: Post Supportingdocuments
          description: Create a new Supporting Document.
          operationId: createSupportingDocument
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateSupportingDocumentRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trusthub.v1.supporting_document'
              description: Created
