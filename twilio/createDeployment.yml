apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Environments Environmentsid Deployments
  description: Create a new Deployment.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Environments Environmentsid Deployments
      description: Create a new Deployment.
    paths:
      /v1/Services/{ServiceSid}/Environments/{EnvironmentSid}/Deployments:
        post:
          summary: Post Services Servicesid Environments Environmentsid Deployments
          description: Create a new Deployment.
          operationId: createDeployment
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateDeploymentRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/serverless.v1.service.environment.deployment
              description: Created
