apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Conversations Sid
  description: Fetch a conversation from your account's default service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Conversations Sid
      description: Fetch a conversation from your account's default service
    paths:
      /v1/Conversations/{Sid}:
        get:
          summary: Get Conversations Sid
          description: Fetch a conversation from your account's default service
          operationId: fetchConversation
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/conversations.v1.conversation'
              description: OK
