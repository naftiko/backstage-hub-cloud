apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Supportingdocuments Sid
  description: Update an existing Supporting Document.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Supportingdocuments Sid
      description: Update an existing Supporting Document.
    paths:
      /v1/SupportingDocuments/{Sid}:
        post:
          summary: Post Supportingdocuments Sid
          description: Update an existing Supporting Document.
          operationId: updateSupportingDocument
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateSupportingDocumentRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trusthub.v1.supporting_document'
              description: OK
