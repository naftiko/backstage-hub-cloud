apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Fleets Sid
  description: Fetch a Fleet instance from your account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Fleets Sid
      description: Fetch a Fleet instance from your account.
    paths:
      /v1/Fleets/{Sid}:
        get:
          summary: Get Fleets Sid
          description: Fetch a Fleet instance from your account.
          operationId: fetchFleet
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/supersim.v1.fleet'
              description: OK
