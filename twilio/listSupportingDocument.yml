apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Supportingdocuments
  description: Retrieve a list of all Supporting Document for an account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Supportingdocuments
      description: Retrieve a list of all Supporting Document for an account.
    paths:
      /v1/SupportingDocuments:
        get:
          summary: Get Supportingdocuments
          description: Retrieve a list of all Supporting Document for an account.
          operationId: listSupportingDocument
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListSupportingDocumentResponse'
              description: OK
