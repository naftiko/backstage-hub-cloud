apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Sessions Sessionsid Participants
  description: Retrieve a list of all Participants in a Session.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Sessions Sessionsid Participants
      description: Retrieve a list of all Participants in a Session.
    paths:
      /v1/Services/{ServiceSid}/Sessions/{SessionSid}/Participants:
        get:
          summary: Get Services Servicesid Sessions Sessionsid Participants
          description: Retrieve a list of all Participants in a Session.
          operationId: listParticipant
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListParticipantResponse'
              description: OK
