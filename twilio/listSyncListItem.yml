apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Lists Listsid Items
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Lists Listsid Items
      description: Needs a more full description created.
    paths:
      /v1/Services/{ServiceSid}/Lists/{ListSid}/Items:
        get:
          summary: Get Services Servicesid Lists Listsid Items
          description: Needs a more full description created.
          operationId: listSyncListItem
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListSyncListItemResponse'
              description: OK
