apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Chatservicesid Users
  description: Add a new conversation user to your service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Chatservicesid Users
      description: Add a new conversation user to your service
    paths:
      /v1/Services/{ChatServiceSid}/Users:
        post:
          summary: Post Services Chatservicesid Users
          description: Add a new conversation user to your service
          operationId: createServiceUser
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateServiceUserRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/conversations.v1.service.service_user'
              description: Created
