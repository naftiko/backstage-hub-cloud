apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Messagingconfigurations
  description: Create a new MessagingConfiguration for a service.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Messagingconfigurations
      description: Create a new MessagingConfiguration for a service.
    paths:
      /v2/Services/{ServiceSid}/MessagingConfigurations:
        post:
          summary: Post Services Servicesid Messagingconfigurations
          description: Create a new MessagingConfiguration for a service.
          operationId: createMessagingConfiguration
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateMessagingConfigurationRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.messaging_configuration'
              description: Created
