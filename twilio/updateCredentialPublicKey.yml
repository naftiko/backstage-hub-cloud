apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Credentials Publickeys Sid
  description: Modify the properties of a given Account
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Credentials Publickeys Sid
      description: Modify the properties of a given Account
    paths:
      /v1/Credentials/PublicKeys/{Sid}:
        post:
          summary: Post Credentials Publickeys Sid
          description: Modify the properties of a given Account
          operationId: updateCredentialPublicKey
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateCredentialPublicKeyRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/accounts.v1.credential.credential_public_key
              description: OK
