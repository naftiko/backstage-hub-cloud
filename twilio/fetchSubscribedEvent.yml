apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Subscriptions Subscriptionsid Subscribedevents Type
  description: Read an Event for a Subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Subscriptions Subscriptionsid Subscribedevents Type
      description: Read an Event for a Subscription.
    paths:
      /v1/Subscriptions/{SubscriptionSid}/SubscribedEvents/{Type}:
        get:
          summary: Get Subscriptions Subscriptionsid Subscribedevents Type
          description: Read an Event for a Subscription.
          operationId: fetchSubscribedEvent
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/events.v1.subscription.subscribed_event'
              description: OK
