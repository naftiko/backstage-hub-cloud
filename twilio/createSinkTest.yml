apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Sinks Sid Test
  description: Create a new Sink Test Event for the given Sink.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Sinks Sid Test
      description: Create a new Sink Test Event for the given Sink.
    paths:
      /v1/Sinks/{Sid}/Test:
        post:
          summary: Post Sinks Sid Test
          description: Create a new Sink Test Event for the given Sink.
          operationId: createSinkTest
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/events.v1.sink.sink_test'
              description: Created
