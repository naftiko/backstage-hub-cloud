apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Customerprofiles Sid
  description: Updates a Customer-Profile in an account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Customerprofiles Sid
      description: Updates a Customer-Profile in an account.
    paths:
      /v1/CustomerProfiles/{Sid}:
        post:
          summary: Post Customerprofiles Sid
          description: Updates a Customer-Profile in an account.
          operationId: updateCustomerProfile
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateCustomerProfileRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trusthub.v1.customer_profile'
              description: OK
