apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Voice Settings
  description: Update a specific Voice Insights Setting.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Voice Settings
      description: Update a specific Voice Insights Setting.
    paths:
      /v1/Voice/Settings:
        post:
          summary: Post Voice Settings
          description: Update a specific Voice Insights Setting.
          operationId: updateAccountSettings
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateAccountSettingsRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/insights.v1.account_settings'
              description: OK
