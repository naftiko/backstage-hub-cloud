apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Sessions Sid
  description: Update a specific Session.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Sessions Sid
      description: Update a specific Session.
    paths:
      /v1/Services/{ServiceSid}/Sessions/{Sid}:
        post:
          summary: Post Services Servicesid Sessions Sid
          description: Update a specific Session.
          operationId: updateSession
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateSessionRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/proxy.v1.service.session'
              description: OK
