apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Marketplace Installedaddons Sid
  description: Update an Add-on installation for the Account specified.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Marketplace Installedaddons Sid
      description: Update an Add-on installation for the Account specified.
    paths:
      /marketplace/InstalledAddOns/{Sid}:
        post:
          summary: Post Marketplace Installedaddons Sid
          description: Update an Add-on installation for the Account specified.
          operationId: updateMarketplaceInstalledAddOn
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateMarketplaceInstalledAddOnRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/preview.marketplace.installed_add_on'
              description: OK
