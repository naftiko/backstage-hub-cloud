apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Devices Devicesid Secrets Key
  description: Update a secret for a Microvisor Device.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Devices Devicesid Secrets Key
      description: Update a secret for a Microvisor Device.
    paths:
      /v1/Devices/{DeviceSid}/Secrets/{Key}:
        post:
          summary: Post Devices Devicesid Secrets Key
          description: Update a secret for a Microvisor Device.
          operationId: updateDeviceSecret
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateDeviceSecretRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/microvisor.v1.device.device_secret'
              description: OK
