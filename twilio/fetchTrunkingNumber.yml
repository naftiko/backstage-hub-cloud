apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Trunking Numbers Destinationnumber
  description: >-
    Fetch pricing information for a specific destination and, optionally,
    origination phone number.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Trunking Numbers Destinationnumber
      description: >-
        Fetch pricing information for a specific destination and, optionally,
        origination phone number.
    paths:
      /v2/Trunking/Numbers/{DestinationNumber}:
        get:
          summary: Get Trunking Numbers Destinationnumber
          description: >-
            Fetch pricing information for a specific destination and, optionally,
            origination phone number.
          operationId: fetchTrunkingNumber
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/pricing.v2.trunking_number'
              description: OK
