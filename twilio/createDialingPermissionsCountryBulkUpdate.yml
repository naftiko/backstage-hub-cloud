apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Dialingpermissions Bulkcountryupdates
  description: >-
    Create a bulk update request to change voice dialing country permissions of
    one or more countries identified by the corresponding [ISO country
    code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Dialingpermissions Bulkcountryupdates
      description: >-
        Create a bulk update request to change voice dialing country permissions of
        one or more countries identified by the corresponding [ISO country
        code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)
    paths:
      /v1/DialingPermissions/BulkCountryUpdates:
        post:
          summary: Post Dialingpermissions Bulkcountryupdates
          description: >-
            Create a bulk update request to change voice dialing country permissions
            of one or more countries identified by the corresponding [ISO country
            code](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)
          operationId: createDialingPermissionsCountryBulkUpdate
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: >-
                    #/components/schemas/CreateDialingPermissionsCountryBulkUpdateRequest
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/voice.v1.dialing_permissions.dialing_permissions_country_bulk_update
              description: Created
