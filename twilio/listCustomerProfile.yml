apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Customerprofiles
  description: Retrieve a list of all Customer-Profiles for an account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Customerprofiles
      description: Retrieve a list of all Customer-Profiles for an account.
    paths:
      /v1/CustomerProfiles:
        get:
          summary: Get Customerprofiles
          description: Retrieve a list of all Customer-Profiles for an account.
          operationId: listCustomerProfile
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListCustomerProfileResponse'
              description: OK
