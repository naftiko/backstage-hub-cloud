apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    Post Services Servicesid Entities Identity Challenges Challengesid
    Notifications
  description: Create a new Notification for the corresponding Challenge
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Post Services Servicesid Entities Identity Challenges Challengesid
        Notifications
      description: Create a new Notification for the corresponding Challenge
    paths:
      /v2/Services/{ServiceSid}/Entities/{Identity}/Challenges/{ChallengeSid}/Notifications:
        post:
          summary: >-
            Post Services Servicesid Entities Identity Challenges Challengesid
            Notifications
          description: Create a new Notification for the corresponding Challenge
          operationId: createNotification
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateNotificationRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/verify.v2.service.entity.challenge.notification
              description: Created
