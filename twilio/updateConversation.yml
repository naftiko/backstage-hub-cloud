apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Conversations Sid
  description: Update an existing conversation in your account's default service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Conversations Sid
      description: Update an existing conversation in your account's default service
    paths:
      /v1/Conversations/{Sid}:
        post:
          summary: Post Conversations Sid
          description: Update an existing conversation in your account's default service
          operationId: updateConversation
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateConversationRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/conversations.v1.conversation'
              description: OK
