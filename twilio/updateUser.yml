apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Users Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Users Sid
      description: Needs a more full description created.
    paths:
      /v2/Services/{ServiceSid}/Users/{Sid}:
        post:
          summary: Post Services Servicesid Users Sid
          description: Needs a more full description created.
          operationId: updateUser
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateUserRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ip_messaging.v2.service.user'
              description: OK
