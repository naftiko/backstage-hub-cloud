apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Iprecords Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Iprecords Sid
      description: Needs a more full description created.
    paths:
      /v1/IpRecords/{Sid}:
        post:
          summary: Post Iprecords Sid
          description: Needs a more full description created.
          operationId: updateIpRecord
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateIpRecordRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/voice.v1.ip_record'
              description: OK
