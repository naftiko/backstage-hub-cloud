apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Forms Formtype
  description: Fetch the forms for a specific Form Type.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Forms Formtype
      description: Fetch the forms for a specific Form Type.
    paths:
      /v2/Forms/{FormType}:
        get:
          summary: Get Forms Formtype
          description: Fetch the forms for a specific Form Type.
          operationId: fetchForm
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.form'
              description: OK
