apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Complianceinquiries Tollfree Initialize
  description: >-
    Create a new Compliance Tollfree Verification Inquiry for the authenticated
    account. This is necessary to start a new embedded session.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Complianceinquiries Tollfree Initialize
      description: >-
        Create a new Compliance Tollfree Verification Inquiry for the authenticated
        account. This is necessary to start a new embedded session.
    paths:
      /v1/ComplianceInquiries/Tollfree/Initialize:
        post:
          summary: Post Complianceinquiries Tollfree Initialize
          description: >-
            Create a new Compliance Tollfree Verification Inquiry for the
            authenticated account. This is necessary to start a new embedded
            session.
          operationId: createComplianceTollfreeInquiry
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateComplianceTollfreeInquiryRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trusthub.v1.compliance_tollfree_inquiry'
              description: Created
