apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Webhooks
  description: Create a new Webhook for the Service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Webhooks
      description: Create a new Webhook for the Service
    paths:
      /v2/Services/{ServiceSid}/Webhooks:
        post:
          summary: Post Services Servicesid Webhooks
          description: Create a new Webhook for the Service
          operationId: createWebhook
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateWebhookRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.webhook'
              description: Created
