apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Lists Listsid Permissions
  description: Retrieve a list of all Permissions applying to a Sync List.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Lists Listsid Permissions
      description: Retrieve a list of all Permissions applying to a Sync List.
    paths:
      /v1/Services/{ServiceSid}/Lists/{ListSid}/Permissions:
        get:
          summary: Get Services Servicesid Lists Listsid Permissions
          description: Retrieve a list of all Permissions applying to a Sync List.
          operationId: listSyncListPermission
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListSyncListPermissionResponse'
              description: OK
