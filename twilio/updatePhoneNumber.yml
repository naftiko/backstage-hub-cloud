apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Phonenumbers Phonenumber
  description: Assign an Inbound Processing Region to a phone number.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Phonenumbers Phonenumber
      description: Assign an Inbound Processing Region to a phone number.
    paths:
      /v2/PhoneNumbers/{PhoneNumber}:
        post:
          summary: Post Phonenumbers Phonenumber
          description: Assign an Inbound Processing Region to a phone number.
          operationId: updatePhoneNumber
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdatePhoneNumberRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/routes.v2.phone_number'
              description: OK
