apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Environments Environmentsid Variables Sid
  description: Update a specific Variable.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Environments Environmentsid Variables Sid
      description: Update a specific Variable.
    paths:
      /v1/Services/{ServiceSid}/Environments/{EnvironmentSid}/Variables/{Sid}:
        post:
          summary: Post Services Servicesid Environments Environmentsid Variables Sid
          description: Update a specific Variable.
          operationId: updateVariable
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateVariableRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/serverless.v1.service.environment.variable
              description: OK
