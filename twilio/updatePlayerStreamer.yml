apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Playerstreamers Sid
  description: Updates a PlayerStreamer resource identified by a SID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Playerstreamers Sid
      description: Updates a PlayerStreamer resource identified by a SID.
    paths:
      /v1/PlayerStreamers/{Sid}:
        post:
          summary: Post Playerstreamers Sid
          description: Updates a PlayerStreamer resource identified by a SID.
          operationId: updatePlayerStreamer
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdatePlayerStreamerRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/media.v1.player_streamer'
              description: OK
