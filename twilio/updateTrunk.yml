apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Trunks Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Trunks Sid
      description: Needs a more full description created.
    paths:
      /v1/Trunks/{Sid}:
        post:
          summary: Post Trunks Sid
          description: Needs a more full description created.
          operationId: updateTrunk
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateTrunkRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trunking.v1.trunk'
              description: OK
