apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Ratelimits Sid
  description: Update a specific Rate Limit.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Ratelimits Sid
      description: Update a specific Rate Limit.
    paths:
      /v2/Services/{ServiceSid}/RateLimits/{Sid}:
        post:
          summary: Post Services Servicesid Ratelimits Sid
          description: Update a specific Rate Limit.
          operationId: updateRateLimit
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateRateLimitRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.rate_limit'
              description: OK
