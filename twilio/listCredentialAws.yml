apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Credentials Aws
  description: >-
    Retrieves a collection of AWS Credentials belonging to the account used to
    make the request
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Credentials Aws
      description: >-
        Retrieves a collection of AWS Credentials belonging to the account used to
        make the request
    paths:
      /v1/Credentials/AWS:
        get:
          summary: Get Credentials Aws
          description: >-
            Retrieves a collection of AWS Credentials belonging to the account used
            to make the request
          operationId: listCredentialAws
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListCredentialAwsResponse'
              description: OK
