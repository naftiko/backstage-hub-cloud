apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Sinks Sid
  description: Fetch a specific Sink.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Sinks Sid
      description: Fetch a specific Sink.
    paths:
      /v1/Sinks/{Sid}:
        get:
          summary: Get Sinks Sid
          description: Fetch a specific Sink.
          operationId: fetchSink
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/events.v1.sink'
              description: OK
