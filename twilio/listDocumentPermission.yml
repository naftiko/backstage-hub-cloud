apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Documents Documentsid Permissions
  description: Retrieve a list of all Permissions applying to a Sync Document.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Documents Documentsid Permissions
      description: Retrieve a list of all Permissions applying to a Sync Document.
    paths:
      /v1/Services/{ServiceSid}/Documents/{DocumentSid}/Permissions:
        get:
          summary: Get Services Servicesid Documents Documentsid Permissions
          description: Retrieve a list of all Permissions applying to a Sync Document.
          operationId: listDocumentPermission
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListDocumentPermissionResponse'
              description: OK
