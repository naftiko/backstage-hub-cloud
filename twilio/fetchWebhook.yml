apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Webhooks Sid
  description: Fetch a specific Webhook.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Webhooks Sid
      description: Fetch a specific Webhook.
    paths:
      /v2/Services/{ServiceSid}/Webhooks/{Sid}:
        get:
          summary: Get Services Servicesid Webhooks Sid
          description: Fetch a specific Webhook.
          operationId: fetchWebhook
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.webhook'
              description: OK
