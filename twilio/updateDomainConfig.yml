apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Linkshortening Domains Domainsid Config
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Linkshortening Domains Domainsid Config
      description: Needs a more full description created.
    paths:
      /v1/LinkShortening/Domains/{DomainSid}/Config:
        post:
          summary: Post Linkshortening Domains Domainsid Config
          description: Needs a more full description created.
          operationId: updateDomainConfig
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateDomainConfigRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/messaging.v1.domain_config'
              description: Created
