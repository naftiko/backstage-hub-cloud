apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Services Servicesid Maps Mapsid Permissions Identity
  description: Delete a specific Sync Map Permission.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Services Servicesid Maps Mapsid Permissions Identity
      description: Delete a specific Sync Map Permission.
    paths:
      /v1/Services/{ServiceSid}/Maps/{MapSid}/Permissions/{Identity}:
        delete:
          summary: Delete Services Servicesid Maps Mapsid Permissions Identity
          description: Delete a specific Sync Map Permission.
          operationId: deleteSyncMapPermission
          responses:
            '204':
              description: The resource was deleted successfully.
