apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Users Usersid Conversations
  description: Retrieve a list of all User Conversations for the User.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Users Usersid Conversations
      description: Retrieve a list of all User Conversations for the User.
    paths:
      /v1/Users/{UserSid}/Conversations:
        get:
          summary: Get Users Usersid Conversations
          description: Retrieve a list of all User Conversations for the User.
          operationId: listUserConversation
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListUserConversationResponse'
              description: OK
