apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Rooms Roomsid Participants Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Rooms Roomsid Participants Sid
      description: Needs a more full description created.
    paths:
      /v1/Rooms/{RoomSid}/Participants/{Sid}:
        post:
          summary: Post Rooms Roomsid Participants Sid
          description: Needs a more full description created.
          operationId: updateRoomParticipant
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateRoomParticipantRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/video.v1.room.room_participant'
              description: OK
