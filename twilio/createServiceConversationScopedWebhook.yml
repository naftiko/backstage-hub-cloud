apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Chatservicesid Conversations Conversationsid Webhooks
  description: Create a new webhook scoped to the conversation in a specific service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Chatservicesid Conversations Conversationsid Webhooks
      description: Create a new webhook scoped to the conversation in a specific service
    paths:
      /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks:
        post:
          summary: Post Services Chatservicesid Conversations Conversationsid Webhooks
          description: Create a new webhook scoped to the conversation in a specific service
          operationId: createServiceConversationScopedWebhook
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: >-
                    #/components/schemas/CreateServiceConversationScopedWebhookRequest
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.service.service_conversation.service_conversation_scoped_webhook
              description: Created
