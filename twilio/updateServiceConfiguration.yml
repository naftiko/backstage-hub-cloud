apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Chatservicesid Configuration
  description: Update configuration settings of a conversation service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Chatservicesid Configuration
      description: Update configuration settings of a conversation service
    paths:
      /v1/Services/{ChatServiceSid}/Configuration:
        post:
          summary: Post Services Chatservicesid Configuration
          description: Update configuration settings of a conversation service
          operationId: updateServiceConfiguration
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateServiceConfigurationRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.service.service_configuration
              description: OK
