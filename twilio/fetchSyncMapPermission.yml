apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Maps Mapsid Permissions Identity
  description: Fetch a specific Sync Map Permission.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Maps Mapsid Permissions Identity
      description: Fetch a specific Sync Map Permission.
    paths:
      /v1/Services/{ServiceSid}/Maps/{MapSid}/Permissions/{Identity}:
        get:
          summary: Get Services Servicesid Maps Mapsid Permissions Identity
          description: Fetch a specific Sync Map Permission.
          operationId: fetchSyncMapPermission
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/sync.v1.service.sync_map.sync_map_permission
              description: OK
