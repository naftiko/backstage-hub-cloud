apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Credentials Aws Sid
  description: Fetch the AWS credentials specified by the provided Credential Sid
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Credentials Aws Sid
      description: Fetch the AWS credentials specified by the provided Credential Sid
    paths:
      /v1/Credentials/AWS/{Sid}:
        get:
          summary: Get Credentials Aws Sid
          description: Fetch the AWS credentials specified by the provided Credential Sid
          operationId: fetchCredentialAws
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/accounts.v1.credential.credential_aws'
              description: OK
