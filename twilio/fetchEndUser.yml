apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Endusers Sid
  description: Fetch specific End User Instance.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Endusers Sid
      description: Fetch specific End User Instance.
    paths:
      /v1/EndUsers/{Sid}:
        get:
          summary: Get Endusers Sid
          description: Fetch specific End User Instance.
          operationId: fetchEndUser
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trusthub.v1.end_user'
              description: OK
