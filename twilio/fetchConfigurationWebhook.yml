apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Configuration Webhooks
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Configuration Webhooks
      description: Needs a more full description created.
    paths:
      /v1/Configuration/Webhooks:
        get:
          summary: Get Configuration Webhooks
          description: Needs a more full description created.
          operationId: fetchConfigurationWebhook
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.configuration.configuration_webhook
              description: OK
