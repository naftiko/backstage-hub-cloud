apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Sourceipmappings
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Sourceipmappings
      description: Needs a more full description created.
    paths:
      /v1/SourceIpMappings:
        post:
          summary: Post Sourceipmappings
          description: Needs a more full description created.
          operationId: createSourceIpMapping
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateSourceIpMappingRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/voice.v1.source_ip_mapping'
              description: Created
