apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Conversations Conversationsid Webhooks Sid
  description: Fetch the configuration of a conversation-scoped webhook
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Conversations Conversationsid Webhooks Sid
      description: Fetch the configuration of a conversation-scoped webhook
    paths:
      /v1/Conversations/{ConversationSid}/Webhooks/{Sid}:
        get:
          summary: Get Conversations Conversationsid Webhooks Sid
          description: Fetch the configuration of a conversation-scoped webhook
          operationId: fetchConversationScopedWebhook
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.conversation.conversation_scoped_webhook
              description: OK
