apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Lists Listsid Permissions Identity
  description: Fetch a specific Sync List Permission.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Lists Listsid Permissions Identity
      description: Fetch a specific Sync List Permission.
    paths:
      /v1/Services/{ServiceSid}/Lists/{ListSid}/Permissions/{Identity}:
        get:
          summary: Get Services Servicesid Lists Listsid Permissions Identity
          description: Fetch a specific Sync List Permission.
          operationId: fetchSyncListPermission
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/sync.v1.service.sync_list.sync_list_permission
              description: OK
