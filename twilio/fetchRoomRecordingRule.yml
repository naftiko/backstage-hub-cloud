apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Rooms Roomsid Recordingrules
  description: Returns a list of Recording Rules for the Room.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Rooms Roomsid Recordingrules
      description: Returns a list of Recording Rules for the Room.
    paths:
      /v1/Rooms/{RoomSid}/RecordingRules:
        get:
          summary: Get Rooms Roomsid Recordingrules
          description: Returns a list of Recording Rules for the Room.
          operationId: fetchRoomRecordingRule
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/video.v1.room.room_recording_rule'
              description: OK
