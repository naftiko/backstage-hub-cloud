apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Entities Identity Factors Sid
  description: >-
    Update a specific Factor. This endpoint can be used to Verify a Factor if
    passed an `AuthPayload` param.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Entities Identity Factors Sid
      description: >-
        Update a specific Factor. This endpoint can be used to Verify a Factor if
        passed an `AuthPayload` param.
    paths:
      /v2/Services/{ServiceSid}/Entities/{Identity}/Factors/{Sid}:
        post:
          summary: Post Services Servicesid Entities Identity Factors Sid
          description: >-
            Update a specific Factor. This endpoint can be used to Verify a Factor
            if passed an `AuthPayload` param.
          operationId: updateFactor
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateFactorRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.entity.factor'
              description: OK
