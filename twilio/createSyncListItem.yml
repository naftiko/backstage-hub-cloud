apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Lists Listsid Items
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Lists Listsid Items
      description: Needs a more full description created.
    paths:
      /v1/Services/{ServiceSid}/Lists/{ListSid}/Items:
        post:
          summary: Post Services Servicesid Lists Listsid Items
          description: Needs a more full description created.
          operationId: createSyncListItem
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateSyncListItemRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/sync.v1.service.sync_list.sync_list_item'
              description: Created
