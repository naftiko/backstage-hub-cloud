apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Customerprofiles
  description: Create a new Customer-Profile.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Customerprofiles
      description: Create a new Customer-Profile.
    paths:
      /v1/CustomerProfiles:
        post:
          summary: Post Customerprofiles
          description: Create a new Customer-Profile.
          operationId: createCustomerProfile
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateCustomerProfileRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trusthub.v1.customer_profile'
              description: Created
