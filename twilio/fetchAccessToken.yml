apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Accesstokens Sid
  description: Fetch an Access Token for the Entity
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Accesstokens Sid
      description: Fetch an Access Token for the Entity
    paths:
      /v2/Services/{ServiceSid}/AccessTokens/{Sid}:
        get:
          summary: Get Services Servicesid Accesstokens Sid
          description: Fetch an Access Token for the Entity
          operationId: fetchAccessToken
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.access_token'
              description: OK
