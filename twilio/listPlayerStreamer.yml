apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Playerstreamers
  description: Returns a list of PlayerStreamers.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Playerstreamers
      description: Returns a list of PlayerStreamers.
    paths:
      /v1/PlayerStreamers:
        get:
          summary: Get Playerstreamers
          description: Returns a list of PlayerStreamers.
          operationId: listPlayerStreamer
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListPlayerStreamerResponse'
              description: OK
