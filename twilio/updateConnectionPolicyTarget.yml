apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Connectionpolicies Connectionpolicysid Targets Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Connectionpolicies Connectionpolicysid Targets Sid
      description: Needs a more full description created.
    paths:
      /v1/ConnectionPolicies/{ConnectionPolicySid}/Targets/{Sid}:
        post:
          summary: Post Connectionpolicies Connectionpolicysid Targets Sid
          description: Needs a more full description created.
          operationId: updateConnectionPolicyTarget
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateConnectionPolicyTargetRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/voice.v1.connection_policy.connection_policy_target
              description: OK
