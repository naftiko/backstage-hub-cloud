apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Mediaprocessors Sid
  description: Updates a MediaProcessor resource identified by a SID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Mediaprocessors Sid
      description: Updates a MediaProcessor resource identified by a SID.
    paths:
      /v1/MediaProcessors/{Sid}:
        post:
          summary: Post Mediaprocessors Sid
          description: Updates a MediaProcessor resource identified by a SID.
          operationId: updateMediaProcessor
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateMediaProcessorRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/media.v1.media_processor'
              description: OK
