apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Conversations Conversationsid Participants
  description: Add a new participant to the conversation
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Conversations Conversationsid Participants
      description: Add a new participant to the conversation
    paths:
      /v1/Conversations/{ConversationSid}/Participants:
        post:
          summary: Post Conversations Conversationsid Participants
          description: Add a new participant to the conversation
          operationId: createConversationParticipant
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateConversationParticipantRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.conversation.conversation_participant
              description: Created
