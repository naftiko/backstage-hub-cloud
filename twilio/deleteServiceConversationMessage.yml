apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Services Chatservicesid Conversations Conversationsid Messages Sid
  description: Remove a message from the conversation
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Services Chatservicesid Conversations Conversationsid Messages Sid
      description: Remove a message from the conversation
    paths:
      /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages/{Sid}:
        delete:
          summary: >-
            Delete Services Chatservicesid Conversations Conversationsid Messages
            Sid
          description: Remove a message from the conversation
          operationId: deleteServiceConversationMessage
          responses:
            '204':
              description: The resource was deleted successfully.
