apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Devices Devicesid Configs
  description: Create a config for a Microvisor Device.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Devices Devicesid Configs
      description: Create a config for a Microvisor Device.
    paths:
      /v1/Devices/{DeviceSid}/Configs:
        post:
          summary: Post Devices Devicesid Configs
          description: Create a config for a Microvisor Device.
          operationId: createDeviceConfig
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateDeviceConfigRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/microvisor.v1.device.device_config'
              description: Created
