apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Voice Callsid Annotation
  description: Get the Annotation for a specific Call.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Voice Callsid Annotation
      description: Get the Annotation for a specific Call.
    paths:
      /v1/Voice/{CallSid}/Annotation:
        get:
          summary: Get Voice Callsid Annotation
          description: Get the Annotation for a specific Call.
          operationId: fetchAnnotation
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/insights.v1.call.annotation'
              description: OK
