apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Assistants Assistantsid Stylesheet
  description: Updates the style sheet for an Assistant identified by `assistant_sid`.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Assistants Assistantsid Stylesheet
      description: Updates the style sheet for an Assistant identified by `assistant_sid`.
    paths:
      /v1/Assistants/{AssistantSid}/StyleSheet:
        post:
          summary: Post Assistants Assistantsid Stylesheet
          description: Updates the style sheet for an Assistant identified by `assistant_sid`.
          operationId: updateStyleSheet
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateStyleSheetRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/autopilot.v1.assistant.style_sheet'
              description: OK
