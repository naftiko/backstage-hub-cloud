apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Verificationcheck
  description: challenge a specific Verification Check.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Verificationcheck
      description: challenge a specific Verification Check.
    paths:
      /v2/Services/{ServiceSid}/VerificationCheck:
        post:
          summary: Post Services Servicesid Verificationcheck
          description: challenge a specific Verification Check.
          operationId: createVerificationCheck
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateVerificationCheckRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.verification_check'
              description: Created
