apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Ratelimits Ratelimitsid Buckets Sid
  description: Fetch a specific Bucket.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Ratelimits Ratelimitsid Buckets Sid
      description: Fetch a specific Bucket.
    paths:
      /v2/Services/{ServiceSid}/RateLimits/{RateLimitSid}/Buckets/{Sid}:
        get:
          summary: Get Services Servicesid Ratelimits Ratelimitsid Buckets Sid
          description: Fetch a specific Bucket.
          operationId: fetchBucket
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.rate_limit.bucket'
              description: OK
