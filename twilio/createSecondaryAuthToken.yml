apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Authtokens Secondary
  description: Create a new secondary Auth Token
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Authtokens Secondary
      description: Create a new secondary Auth Token
    paths:
      /v1/AuthTokens/Secondary:
        post:
          summary: Post Authtokens Secondary
          description: Create a new secondary Auth Token
          operationId: createSecondaryAuthToken
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/accounts.v1.secondary_auth_token'
              description: Created
