apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Attempts Sid
  description: Fetch a specific verification attempt.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Attempts Sid
      description: Fetch a specific verification attempt.
    paths:
      /v2/Attempts/{Sid}:
        get:
          summary: Get Attempts Sid
          description: Fetch a specific verification attempt.
          operationId: fetchVerificationAttempt
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.verification_attempt'
              description: OK
