apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Streams Sid
  description: Update a specific Stream.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Streams Sid
      description: Update a specific Stream.
    paths:
      /v1/Services/{ServiceSid}/Streams/{Sid}:
        post:
          summary: Post Services Servicesid Streams Sid
          description: Update a specific Stream.
          operationId: updateSyncStream
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateSyncStreamRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/sync.v1.service.sync_stream'
              description: OK
