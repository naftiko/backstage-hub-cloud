apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Safelist Numbers
  description: Add a new phone number to SafeList.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Safelist Numbers
      description: Add a new phone number to SafeList.
    paths:
      /v2/SafeList/Numbers:
        post:
          summary: Post Safelist Numbers
          description: Add a new phone number to SafeList.
          operationId: createSafelist
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateSafelistRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.safelist'
              description: Created
