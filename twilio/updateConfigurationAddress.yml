apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Configuration Addresses Sid
  description: Update an existing address configuration
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Configuration Addresses Sid
      description: Update an existing address configuration
    paths:
      /v1/Configuration/Addresses/{Sid}:
        post:
          summary: Post Configuration Addresses Sid
          description: Update an existing address configuration
          operationId: updateConfigurationAddress
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateConfigurationAddressRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/conversations.v1.configuration_address'
              description: OK
