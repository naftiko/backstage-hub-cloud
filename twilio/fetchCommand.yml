apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Commands Sid
  description: Fetch a Command instance from your account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Commands Sid
      description: Fetch a Command instance from your account.
    paths:
      /v1/Commands/{Sid}:
        get:
          summary: Get Commands Sid
          description: Fetch a Command instance from your account.
          operationId: fetchCommand
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/wireless.v1.command'
              description: OK
