apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Services Chatservicesid Bindings Sid
  description: Remove a push notification binding from the conversation service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Services Chatservicesid Bindings Sid
      description: Remove a push notification binding from the conversation service
    paths:
      /v1/Services/{ChatServiceSid}/Bindings/{Sid}:
        delete:
          summary: Delete Services Chatservicesid Bindings Sid
          description: Remove a push notification binding from the conversation service
          operationId: deleteServiceBinding
          responses:
            '204':
              description: The resource was deleted successfully.
