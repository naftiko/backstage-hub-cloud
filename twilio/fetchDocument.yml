apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Documents Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Documents Sid
      description: Needs a more full description created.
    paths:
      /v1/Services/{ServiceSid}/Documents/{Sid}:
        get:
          summary: Get Services Servicesid Documents Sid
          description: Needs a more full description created.
          operationId: fetchDocument
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/sync.v1.service.document'
              description: OK
