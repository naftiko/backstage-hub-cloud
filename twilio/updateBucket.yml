apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Ratelimits Ratelimitsid Buckets Sid
  description: Update a specific Bucket.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Ratelimits Ratelimitsid Buckets Sid
      description: Update a specific Bucket.
    paths:
      /v2/Services/{ServiceSid}/RateLimits/{RateLimitSid}/Buckets/{Sid}:
        post:
          summary: Post Services Servicesid Ratelimits Ratelimitsid Buckets Sid
          description: Update a specific Bucket.
          operationId: updateBucket
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateBucketRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.rate_limit.bucket'
              description: OK
