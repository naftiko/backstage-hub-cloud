apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Voice Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Voice Sid
      description: Needs a more full description created.
    paths:
      /v1/Voice/{Sid}:
        get:
          summary: Get Voice Sid
          description: Needs a more full description created.
          operationId: fetchCall
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/insights.v1.call'
              description: OK
