apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Playerstreamers Sid
  description: Returns a single PlayerStreamer resource identified by a SID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Playerstreamers Sid
      description: Returns a single PlayerStreamer resource identified by a SID.
    paths:
      /v1/PlayerStreamers/{Sid}:
        get:
          summary: Get Playerstreamers Sid
          description: Returns a single PlayerStreamer resource identified by a SID.
          operationId: fetchPlayerStreamer
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/media.v1.player_streamer'
              description: OK
