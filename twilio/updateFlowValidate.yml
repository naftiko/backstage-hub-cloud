apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Flows Validate
  description: Validate flow JSON definition
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Flows Validate
      description: Validate flow JSON definition
    paths:
      /v2/Flows/Validate:
        post:
          summary: Post Flows Validate
          description: Validate flow JSON definition
          operationId: updateFlowValidate
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateFlowValidateRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/studio.v2.flow_validate'
              description: OK
