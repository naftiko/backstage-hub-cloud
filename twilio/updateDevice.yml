apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Devices Sid
  description: Update a specific Device.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Devices Sid
      description: Update a specific Device.
    paths:
      /v1/Devices/{Sid}:
        post:
          summary: Post Devices Sid
          description: Update a specific Device.
          operationId: updateDevice
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateDeviceRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/microvisor.v1.device'
              description: OK
