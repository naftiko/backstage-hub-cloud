apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Sinks Sid
  description: Update a specific Sink
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Sinks Sid
      description: Update a specific Sink
    paths:
      /v1/Sinks/{Sid}:
        post:
          summary: Post Sinks Sid
          description: Update a specific Sink
          operationId: updateSink
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateSinkRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/events.v1.sink'
              description: OK
