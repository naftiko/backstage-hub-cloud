apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Messagingconfigurations Country
  description: Update a specific MessagingConfiguration
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Messagingconfigurations Country
      description: Update a specific MessagingConfiguration
    paths:
      /v2/Services/{ServiceSid}/MessagingConfigurations/{Country}:
        post:
          summary: Post Services Servicesid Messagingconfigurations Country
          description: Update a specific MessagingConfiguration
          operationId: updateMessagingConfiguration
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateMessagingConfigurationRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/verify.v2.service.messaging_configuration'
              description: OK
