apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Builds Sid Status
  description: Retrieve a specific Build resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Builds Sid Status
      description: Retrieve a specific Build resource.
    paths:
      /v1/Services/{ServiceSid}/Builds/{Sid}/Status:
        get:
          summary: Get Services Servicesid Builds Sid Status
          description: Retrieve a specific Build resource.
          operationId: fetchBuildStatus
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/serverless.v1.service.build.build_status'
              description: OK
