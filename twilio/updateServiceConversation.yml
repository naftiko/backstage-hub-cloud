apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Chatservicesid Conversations Sid
  description: Update an existing conversation in your service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Chatservicesid Conversations Sid
      description: Update an existing conversation in your service
    paths:
      /v1/Services/{ChatServiceSid}/Conversations/{Sid}:
        post:
          summary: Post Services Chatservicesid Conversations Sid
          description: Update an existing conversation in your service
          operationId: updateServiceConversation
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateServiceConversationRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.service.service_conversation
              description: OK
