apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Trunks Trunksid Credentiallists
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Trunks Trunksid Credentiallists
      description: Needs a more full description created.
    paths:
      /v1/Trunks/{TrunkSid}/CredentialLists:
        post:
          summary: Post Trunks Trunksid Credentiallists
          description: Needs a more full description created.
          operationId: createCredentialList
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateCredentialListRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trunking.v1.trunk.credential_list'
              description: Created
