apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Hostednumber Orders
  description: Host a phone number's capability on Twilio's platform.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Hostednumber Orders
      description: Host a phone number's capability on Twilio's platform.
    paths:
      /v2/HostedNumber/Orders:
        post:
          summary: Post Hostednumber Orders
          description: Host a phone number's capability on Twilio's platform.
          operationId: createHostedNumberOrder
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateHostedNumberOrderRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/numbers.v2.hosted_number_order'
              description: Created
