apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Entities Identity Factors
  description: Retrieve a list of all Factors for an Entity.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Entities Identity Factors
      description: Retrieve a list of all Factors for an Entity.
    paths:
      /v2/Services/{ServiceSid}/Entities/{Identity}/Factors:
        get:
          summary: Get Services Servicesid Entities Identity Factors
          description: Retrieve a list of all Factors for an Entity.
          operationId: listFactor
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListFactorResponse'
              description: OK
