apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Connectionpolicies
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Connectionpolicies
      description: Needs a more full description created.
    paths:
      /v1/ConnectionPolicies:
        post:
          summary: Post Connectionpolicies
          description: Needs a more full description created.
          operationId: createConnectionPolicy
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateConnectionPolicyRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/voice.v1.connection_policy'
              description: Created
