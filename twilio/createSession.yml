apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Sessions
  description: Create a new Session
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Sessions
      description: Create a new Session
    paths:
      /v1/Services/{ServiceSid}/Sessions:
        post:
          summary: Post Services Servicesid Sessions
          description: Create a new Session
          operationId: createSession
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateSessionRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/proxy.v1.service.session'
              description: Created
