apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Conversations Conversationsid Messages Sid
  description: Update an existing message in the conversation
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Conversations Conversationsid Messages Sid
      description: Update an existing message in the conversation
    paths:
      /v1/Conversations/{ConversationSid}/Messages/{Sid}:
        post:
          summary: Post Conversations Conversationsid Messages Sid
          description: Update an existing message in the conversation
          operationId: updateConversationMessage
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateConversationMessageRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/conversations.v1.conversation.conversation_message
              description: OK
