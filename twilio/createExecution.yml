apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Flows Flowsid Executions
  description: Triggers a new Execution for the Flow
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Flows Flowsid Executions
      description: Triggers a new Execution for the Flow
    paths:
      /v2/Flows/{FlowSid}/Executions:
        post:
          summary: Post Flows Flowsid Executions
          description: Triggers a new Execution for the Flow
          operationId: createExecution
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateExecutionRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/studio.v2.flow.execution'
              description: Created
