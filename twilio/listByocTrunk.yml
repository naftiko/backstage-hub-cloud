apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Byoctrunks
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Byoctrunks
      description: Needs a more full description created.
    paths:
      /v1/ByocTrunks:
        get:
          summary: Get Byoctrunks
          description: Needs a more full description created.
          operationId: listByocTrunk
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListByocTrunkResponse'
              description: OK
