apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Trunks Trunksid Originationurls
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Trunks Trunksid Originationurls
      description: Needs a more full description created.
    paths:
      /v1/Trunks/{TrunkSid}/OriginationUrls:
        post:
          summary: Post Trunks Trunksid Originationurls
          description: Needs a more full description created.
          operationId: createOriginationUrl
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateOriginationUrlRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/trunking.v1.trunk.origination_url'
              description: Created
