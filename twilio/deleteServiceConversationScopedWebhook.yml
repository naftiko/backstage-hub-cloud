apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Services Chatservicesid Conversations Conversationsid Webhooks Sid
  description: Remove an existing webhook scoped to the conversation
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Services Chatservicesid Conversations Conversationsid Webhooks Sid
      description: Remove an existing webhook scoped to the conversation
    paths:
      /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks/{Sid}:
        delete:
          summary: >-
            Delete Services Chatservicesid Conversations Conversationsid Webhooks
            Sid
          description: Remove an existing webhook scoped to the conversation
          operationId: deleteServiceConversationScopedWebhook
          responses:
            '204':
              description: The resource was deleted successfully.
