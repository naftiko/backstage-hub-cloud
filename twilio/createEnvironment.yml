apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Environments
  description: Create a new environment.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Environments
      description: Create a new environment.
    paths:
      /v1/Services/{ServiceSid}/Environments:
        post:
          summary: Post Services Servicesid Environments
          description: Create a new environment.
          operationId: createEnvironment
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateEnvironmentRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/serverless.v1.service.environment'
              description: Created
