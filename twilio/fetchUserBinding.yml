apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Users Usersid Bindings Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Users Usersid Bindings Sid
      description: Needs a more full description created.
    paths:
      /v2/Services/{ServiceSid}/Users/{UserSid}/Bindings/{Sid}:
        get:
          summary: Get Services Servicesid Users Usersid Bindings Sid
          description: Needs a more full description created.
          operationId: fetchUserBinding
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ip_messaging.v2.service.user.user_binding'
              description: OK
