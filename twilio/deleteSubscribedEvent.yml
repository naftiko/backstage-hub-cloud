apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Subscriptions Subscriptionsid Subscribedevents Type
  description: Remove an event type from a Subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Subscriptions Subscriptionsid Subscribedevents Type
      description: Remove an event type from a Subscription.
    paths:
      /v1/Subscriptions/{SubscriptionSid}/SubscribedEvents/{Type}:
        delete:
          summary: Delete Subscriptions Subscriptionsid Subscribedevents Type
          description: Remove an event type from a Subscription.
          operationId: deleteSubscribedEvent
          responses:
            '204':
              description: The resource was deleted successfully.
