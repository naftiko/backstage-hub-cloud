apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Services Servicesid Alphasenders
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Services Servicesid Alphasenders
      description: Needs a more full description created.
    paths:
      /v1/Services/{ServiceSid}/AlphaSenders:
        post:
          summary: Post Services Servicesid Alphasenders
          description: Needs a more full description created.
          operationId: createAlphaSender
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateAlphaSenderRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/messaging.v1.service.alpha_sender'
              description: Created
