apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Recordingsettings Default
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Recordingsettings Default
      description: Needs a more full description created.
    paths:
      /v1/RecordingSettings/Default:
        get:
          summary: Get Recordingsettings Default
          description: Needs a more full description created.
          operationId: fetchRecordingSettings
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/video.v1.recording_settings'
              description: OK
