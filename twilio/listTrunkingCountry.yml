apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Trunking Countries
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Trunking Countries
      description: Needs a more full description created.
    paths:
      /v2/Trunking/Countries:
        get:
          summary: Get Trunking Countries
          description: Needs a more full description created.
          operationId: listTrunkingCountry
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListTrunkingCountryResponse'
              description: OK
