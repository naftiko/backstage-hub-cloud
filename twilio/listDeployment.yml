apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Environments Environmentsid Deployments
  description: Retrieve a list of all Deployments.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Environments Environmentsid Deployments
      description: Retrieve a list of all Deployments.
    paths:
      /v1/Services/{ServiceSid}/Environments/{EnvironmentSid}/Deployments:
        get:
          summary: Get Services Servicesid Environments Environmentsid Deployments
          description: Retrieve a list of all Deployments.
          operationId: listDeployment
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListDeploymentResponse'
              description: OK
