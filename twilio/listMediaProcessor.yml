apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Mediaprocessors
  description: Returns a list of MediaProcessors.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Mediaprocessors
      description: Returns a list of MediaProcessors.
    paths:
      /v1/MediaProcessors:
        get:
          summary: Get Mediaprocessors
          description: Returns a list of MediaProcessors.
          operationId: listMediaProcessor
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListMediaProcessorResponse'
              description: OK
