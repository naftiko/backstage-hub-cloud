apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Secrets Key
  description: Delete a secret for an Account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Secrets Key
      description: Delete a secret for an Account.
    paths:
      /v1/Secrets/{Key}:
        delete:
          summary: Delete Secrets Key
          description: Delete a secret for an Account.
          operationId: deleteAccountSecret
          responses:
            '204':
              description: The resource was deleted successfully.
