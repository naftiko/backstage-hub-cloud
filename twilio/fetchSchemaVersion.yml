apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Schemas Id Versions Schemaversion
  description: Fetch a specific schema and version.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Schemas Id Versions Schemaversion
      description: Fetch a specific schema and version.
    paths:
      /v1/Schemas/{Id}/Versions/{SchemaVersion}:
        get:
          summary: Get Schemas Id Versions Schemaversion
          description: Fetch a specific schema and version.
          operationId: fetchSchemaVersion
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/events.v1.schema.schema_version'
              description: OK
