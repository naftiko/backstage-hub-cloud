apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Voice Callsid Annotation
  description: Update an Annotation for a specific Call.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Voice Callsid Annotation
      description: Update an Annotation for a specific Call.
    paths:
      /v1/Voice/{CallSid}/Annotation:
        post:
          summary: Post Voice Callsid Annotation
          description: Update an Annotation for a specific Call.
          operationId: updateAnnotation
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateAnnotationRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/insights.v1.call.annotation'
              description: OK
