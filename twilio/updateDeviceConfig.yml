apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Devices Devicesid Configs Key
  description: Update a config for a Microvisor Device.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Devices Devicesid Configs Key
      description: Update a config for a Microvisor Device.
    paths:
      /v1/Devices/{DeviceSid}/Configs/{Key}:
        post:
          summary: Post Devices Devicesid Configs Key
          description: Update a config for a Microvisor Device.
          operationId: updateDeviceConfig
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateDeviceConfigRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/microvisor.v1.device.device_config'
              description: OK
