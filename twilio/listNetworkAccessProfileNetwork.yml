apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Networkaccessprofiles Networkaccessprofilesid Networks
  description: Retrieve a list of Network Access Profile resource's Network resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Networkaccessprofiles Networkaccessprofilesid Networks
      description: Retrieve a list of Network Access Profile resource's Network resource.
    paths:
      /v1/NetworkAccessProfiles/{NetworkAccessProfileSid}/Networks:
        get:
          summary: Get Networkaccessprofiles Networkaccessprofilesid Networks
          description: Retrieve a list of Network Access Profile resource's Network resource.
          operationId: listNetworkAccessProfileNetwork
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListNetworkAccessProfileNetworkResponse'
              description: OK
