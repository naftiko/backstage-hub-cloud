apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Services Servicesid Lists Listsid Permissions Identity
  description: Delete a specific Sync List Permission.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Services Servicesid Lists Listsid Permissions Identity
      description: Delete a specific Sync List Permission.
    paths:
      /v1/Services/{ServiceSid}/Lists/{ListSid}/Permissions/{Identity}:
        delete:
          summary: Delete Services Servicesid Lists Listsid Permissions Identity
          description: Delete a specific Sync List Permission.
          operationId: deleteSyncListPermission
          responses:
            '204':
              description: The resource was deleted successfully.
