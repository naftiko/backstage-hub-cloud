apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Services Servicesid Sessions Sid
  description: Fetch a specific Session.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Services Servicesid Sessions Sid
      description: Fetch a specific Session.
    paths:
      /v1/Services/{ServiceSid}/Sessions/{Sid}:
        get:
          summary: Get Services Servicesid Sessions Sid
          description: Fetch a specific Session.
          operationId: fetchSession
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/proxy.v1.service.session'
              description: OK
