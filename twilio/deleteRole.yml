apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Services Servicesid Roles Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Services Servicesid Roles Sid
      description: Needs a more full description created.
    paths:
      /v2/Services/{ServiceSid}/Roles/{Sid}:
        delete:
          summary: Delete Services Servicesid Roles Sid
          description: Needs a more full description created.
          operationId: deleteRole
          responses:
            '204':
              description: The resource was deleted successfully.
