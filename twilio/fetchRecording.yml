apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Recordings Sid
  description: Returns a single Recording resource identified by a Recording SID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Recordings Sid
      description: Returns a single Recording resource identified by a Recording SID.
    paths:
      /v1/Recordings/{Sid}:
        get:
          summary: Get Recordings Sid
          description: Returns a single Recording resource identified by a Recording SID.
          operationId: fetchRecording
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/video.v1.recording'
              description: OK
