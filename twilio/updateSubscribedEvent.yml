apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Subscriptions Subscriptionsid Subscribedevents Type
  description: Update an Event for a Subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Subscriptions Subscriptionsid Subscribedevents Type
      description: Update an Event for a Subscription.
    paths:
      /v1/Subscriptions/{SubscriptionSid}/SubscribedEvents/{Type}:
        post:
          summary: Post Subscriptions Subscriptionsid Subscribedevents Type
          description: Update an Event for a Subscription.
          operationId: updateSubscribedEvent
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateSubscribedEventRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/events.v1.subscription.subscribed_event'
              description: OK
