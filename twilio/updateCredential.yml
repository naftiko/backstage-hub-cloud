apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Credentials Sid
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Credentials Sid
      description: Needs a more full description created.
    paths:
      /v1/Credentials/{Sid}:
        post:
          summary: Post Credentials Sid
          description: Needs a more full description created.
          operationId: updateCredential
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/UpdateCredentialRequest'
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/notify.v1.credential'
              description: OK
