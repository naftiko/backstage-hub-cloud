apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Ipcommands
  description: Retrieve a list of IP Commands from your account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Ipcommands
      description: Retrieve a list of IP Commands from your account.
    paths:
      /v1/IpCommands:
        get:
          summary: Get Ipcommands
          description: Retrieve a list of IP Commands from your account.
          operationId: listIpCommand
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListIpCommandResponse'
              description: OK
