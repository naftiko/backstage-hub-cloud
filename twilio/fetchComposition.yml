apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Compositions Sid
  description: Returns a single Composition resource identified by a Composition SID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Compositions Sid
      description: Returns a single Composition resource identified by a Composition SID.
    paths:
      /v1/Compositions/{Sid}:
        get:
          summary: Get Compositions Sid
          description: Returns a single Composition resource identified by a Composition SID.
          operationId: fetchComposition
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/video.v1.composition'
              description: OK
