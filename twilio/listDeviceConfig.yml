apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Devices Devicesid Configs
  description: Retrieve a list of all Configs for a Device.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Devices Devicesid Configs
      description: Retrieve a list of all Configs for a Device.
    paths:
      /v1/Devices/{DeviceSid}/Configs:
        get:
          summary: Get Devices Devicesid Configs
          description: Retrieve a list of all Configs for a Device.
          operationId: listDeviceConfig
          responses:
            '200':
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ListDeviceConfigResponse'
              description: OK
