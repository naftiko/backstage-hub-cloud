apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Credentials Publickeys
  description: Create a new Public Key Credential
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Credentials Publickeys
      description: Create a new Public Key Credential
    paths:
      /v1/Credentials/PublicKeys:
        post:
          summary: Post Credentials Publickeys
          description: Create a new Public Key Credential
          operationId: createCredentialPublicKey
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  $ref: '#/components/schemas/CreateCredentialPublicKeyRequest'
          responses:
            '201':
              content:
                application/json:
                  schema:
                    $ref: >-
                      #/components/schemas/accounts.v1.credential.credential_public_key
              description: Created
