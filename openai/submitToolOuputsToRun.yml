apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >
    When A Run Has The `status: "requires_action"` And `required_action.type` Is
    `submit_tool_outputs`, This Endpoint Can Be Used To Submit The Outputs From
    The Tool Calls Once They're All Completed. All Outputs Must Be Submitted In
    A Single Request.
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >
        When A Run Has The `status: "requires_action"` And `required_action.type` Is
        `submit_tool_outputs`, This Endpoint Can Be Used To Submit The Outputs From
        The Tool Calls Once They're All Completed. All Outputs Must Be Submitted In
        A Single Request.
      description: Needs a more full description created.
    paths:
      /threads/{thread_id}/runs/{run_id}/submit_tool_outputs:
        post:
          summary: >
            When A Run Has The `status: "requires_action"` And
            `required_action.type` Is `submit_tool_outputs`, This Endpoint Can Be
            Used To Submit The Outputs From The Tool Calls Once They're All
            Completed. All Outputs Must Be Submitted In A Single Request.
          description: Needs a more full description created.
          operationId: submitToolOuputsToRun
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/SubmitToolOutputsRunRequest'
          responses:
            '200':
              description: OK
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/RunObject'
