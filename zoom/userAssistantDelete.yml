apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post User Assistant Delete
  description: Delete a user's assistant.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post User Assistant Delete
      description: Delete a user's assistant.
    paths:
      /user/assistant/delete:
        post:
          summary: Post User Assistant Delete
          description: Delete a user's assistant.
          operationId: userAssistantDelete
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  type: object
                  properties:
                    id:
                      type: string
                      description: The host’s user id.
                    host_email:
                      type: string
                      description: The host’s email address.
          responses:
            '200':
              description: Success
              content:
                application/json:
                  example:
                    id: unique_id
                    host_email: email_address
                    updated_at: '2013-09-02T12:00:00.000Z'
