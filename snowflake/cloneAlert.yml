apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A New Alert By Cloning From The Specified Resource
  description: Create a new alert by cloning from the specified resource
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A New Alert By Cloning From The Specified Resource
      description: Create a new alert by cloning from the specified resource
    paths:
      /api/v2/databases/{database}/schemas/{schema}/alerts/{name}:clone:
        post:
          summary: Create A New Alert By Cloning From The Specified Resource
          description: Create a new alert by cloning from the specified resource
          operationId: cloneAlert
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/AlertClone'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
