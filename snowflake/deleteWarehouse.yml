apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Drop Warehouse
  description: >-
    Removes the specified virtual warehouse from the system. Equivalent to DROP
    WAREHOUSE in SQL.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Drop Warehouse
      description: >-
        Removes the specified virtual warehouse from the system. Equivalent to DROP
        WAREHOUSE in SQL.
    paths:
      /api/v2/warehouses/{name}:
        delete:
          summary: Drop Warehouse
          description: >-
            Removes the specified virtual warehouse from the system. Equivalent to
            DROP WAREHOUSE in SQL.
          operationId: deleteWarehouse
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
