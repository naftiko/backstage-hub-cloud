apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Api Databases Database Schemas Schema Services Name Containers
  description: List all the containers of the service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Api Databases Database Schemas Schema Services Name Containers
      description: List all the containers of the service
    paths:
      /api/v2/databases/{database}/schemas/{schema}/services/{name}/containers:
        get:
          summary: Get Api Databases Database Schemas Schema Services Name Containers
          description: List all the containers of the service
          operationId: listServiceContainers
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
