apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    Grant The Specified Privilege(s) On The Named Securable To The Named
    Grantee.
  description: >-
    Endpoint to indicate that the privileges listed in the request body should
    be granted.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Grant The Specified Privilege(s) On The Named Securable To The Named
        Grantee.
      description: >-
        Endpoint to indicate that the privileges listed in the request body should
        be granted.
    paths:
      /api/v2/grants/{granteeType}/{granteeName}/{securableType}/{securableName}/privileges:
        post:
          summary: >-
            Grant The Specified Privilege(s) On The Named Securable To The Named
            Grantee.
          description: >-
            Endpoint to indicate that the privileges listed in the request body
            should be granted.
          operationId: grantPrivilege
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/Grant'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
