apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    Get Api Databases Database Schemas Schema Services Service Roles Name
    Grants-of
  description: List all the grants of the service role
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Get Api Databases Database Schemas Schema Services Service Roles Name
        Grants-of
      description: List all the grants of the service role
    paths:
      /api/v2/databases/{database}/schemas/{schema}/services/{service}/roles/{name}/grants-of:
        get:
          summary: >-
            Get Api Databases Database Schemas Schema Services Service Roles Name
            Grants-of
          description: List all the grants of the service role
          operationId: listServiceRoleGrantsOf
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
