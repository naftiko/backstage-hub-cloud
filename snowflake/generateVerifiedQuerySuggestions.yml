apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Generate Vq Suggestions For A Semantic Model
  description: Generate VQ suggestions for a semantic model
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Generate Vq Suggestions For A Semantic Model
      description: Generate VQ suggestions for a semantic model
    paths:
      /api/v2/cortex/analyst/verified-query-suggestions:
        post:
          summary: Generate Vq Suggestions For A Semantic Model
          description: Generate VQ suggestions for a semantic model
          operationId: generateVerifiedQuerySuggestions
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/GenerateVerifiedQuerySuggestionsRequest'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
