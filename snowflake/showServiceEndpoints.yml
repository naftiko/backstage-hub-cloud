apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List The Endpoints In A Service.
  description: >-
    Lists the endpoints in a Snowpark Container Services service (or a job
    service).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List The Endpoints In A Service.
      description: >-
        Lists the endpoints in a Snowpark Container Services service (or a job
        service).
    paths:
      /api/v2/databases/{database}/schemas/{schema}/services/{name}/endpoints:
        get:
          summary: List The Endpoints In A Service.
          description: >-
            Lists the endpoints in a Snowpark Container Services service (or a job
            service).
          operationId: showServiceEndpoints
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
