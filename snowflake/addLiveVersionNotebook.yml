apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Adds A Live Version To The Notebook
  description: Adds a LIVE version to the notebook
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Adds A Live Version To The Notebook
      description: Adds a LIVE version to the notebook
    paths:
      /api/v2/databases/{database}/schemas/{schema}/notebooks/{name}:add-live-version:
        post:
          summary: Adds A Live Version To The Notebook
          description: Adds a LIVE version to the notebook
          operationId: addLiveVersionNotebook
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
