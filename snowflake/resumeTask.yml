apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Resume A Suspended Task.
  description: >-
    Resumes a suspended task object. This is equivalento an ALTER TASK ...
    RESUME.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Resume A Suspended Task.
      description: >-
        Resumes a suspended task object. This is equivalento an ALTER TASK ...
        RESUME.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/tasks/{name}:resume:
        post:
          summary: Resume A Suspended Task.
          description: >-
            Resumes a suspended task object. This is equivalento an ALTER TASK ...
            RESUME.
          operationId: resumeTask
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
