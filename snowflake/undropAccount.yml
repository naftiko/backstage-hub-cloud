apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Restore An Account.
  description: >-
    Restores a dropped account that has not yet been permanently deleted (a
    dropped account that is within its grace period).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Restore An Account.
      description: >-
        Restores a dropped account that has not yet been permanently deleted (a
        dropped account that is within its grace period).
    paths:
      /api/v2/accounts/{name}:undrop:
        post:
          summary: Restore An Account.
          description: >-
            Restores a dropped account that has not yet been permanently deleted (a
            dropped account that is within its grace period).
          operationId: undropAccount
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
