apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Suspend Refreshes On The Dynamic Table
  description: Suspend refreshes on the dynamic table
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Suspend Refreshes On The Dynamic Table
      description: Suspend refreshes on the dynamic table
    paths:
      /api/v2/databases/{database}/schemas/{schema}/dynamic-tables/{name}:suspend:
        post:
          summary: Suspend Refreshes On The Dynamic Table
          description: Suspend refreshes on the dynamic table
          operationId: suspendDynamicTable
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
