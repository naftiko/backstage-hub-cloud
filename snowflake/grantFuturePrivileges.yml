apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Grant Future Privileges To The Role
  description: Grant future privileges to the role
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Grant Future Privileges To The Role
      description: Grant future privileges to the role
    paths:
      /api/v2/roles/{name}/future-grants:
        post:
          summary: Grant Future Privileges To The Role
          description: Grant future privileges to the role
          operationId: grantFuturePrivileges
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/Grant'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
