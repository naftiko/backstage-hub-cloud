apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A New Database Role By Cloning From The Specified Resource
  description: Create a new database role by cloning from the specified resource
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A New Database Role By Cloning From The Specified Resource
      description: Create a new database role by cloning from the specified resource
    paths:
      /api/v2/databases/{database}/database-roles/{name}:clone:
        post:
          summary: Create A New Database Role By Cloning From The Specified Resource
          description: Create a new database role by cloning from the specified resource
          operationId: cloneDatabaseRole
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/DatabaseRoleClone'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
