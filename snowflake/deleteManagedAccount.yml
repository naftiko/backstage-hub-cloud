apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Managed Account.
  description: >-
    Removes a managed account, including all objects created in the account, and
    immediately restricts access to the account. Currently used by data
    providers to create reader accounts for their consumers. For more details,
    see Manage reader accounts.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Managed Account.
      description: >-
        Removes a managed account, including all objects created in the account, and
        immediately restricts access to the account. Currently used by data
        providers to create reader accounts for their consumers. For more details,
        see Manage reader accounts.
    paths:
      /api/v2/managed-accounts/{name}:
        delete:
          summary: Delete A Managed Account.
          description: >-
            Removes a managed account, including all objects created in the account,
            and immediately restricts access to the account. Currently used by data
            providers to create reader accounts for their consumers. For more
            details, see Manage reader accounts.
          operationId: deleteManagedAccount
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
