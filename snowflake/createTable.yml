apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Table (clone And Undrop Are Separate Subresources)
  description: Create a table.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Table (clone And Undrop Are Separate Subresources)
      description: Create a table.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/tables:
        post:
          summary: Create A Table (clone And Undrop Are Separate Subresources)
          description: Create a table.
          operationId: createTable
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/Table'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
