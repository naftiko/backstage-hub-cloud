apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Suspends An Active Compute Pool.
  description: >-
    Suspend a compute pool, if active. If the specified compute pool is already
    suspended, no action is taken.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Suspends An Active Compute Pool.
      description: >-
        Suspend a compute pool, if active. If the specified compute pool is already
        suspended, no action is taken.
    paths:
      /api/v2/compute-pools/{name}:suspend:
        post:
          summary: Suspends An Active Compute Pool.
          description: >-
            Suspend a compute pool, if active. If the specified compute pool is
            already suspended, no action is taken.
          operationId: suspendComputePool
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
