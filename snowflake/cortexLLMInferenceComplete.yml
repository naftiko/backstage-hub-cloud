apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Perform Llm Text Completion Inference.
  description: Perform LLM text completion inference, similar to snowflake.cortex.Complete.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Perform Llm Text Completion Inference.
      description: Perform LLM text completion inference, similar to snowflake.cortex.Complete.
    paths:
      /api/v2/cortex/inference:complete:
        post:
          summary: Perform Llm Text Completion Inference.
          description: >-
            Perform LLM text completion inference, similar to
            snowflake.cortex.Complete.
          operationId: cortexLLMInferenceComplete
          requestBody:
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/CompleteRequest'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
