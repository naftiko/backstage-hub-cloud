apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Disable Adaptive Warehouse
  description: >-
    Disable an adaptive warehouse and put the warehouse into a ‘disabled’ state,
    if the warehouse is not disabled.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Disable Adaptive Warehouse
      description: >-
        Disable an adaptive warehouse and put the warehouse into a ‘disabled’ state,
        if the warehouse is not disabled.
    paths:
      /api/v2/warehouses/{name}:disable:
        post:
          summary: Disable Adaptive Warehouse
          description: >-
            Disable an adaptive warehouse and put the warehouse into a ‘disabled’
            state, if the warehouse is not disabled.
          operationId: disableWarehouse
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
