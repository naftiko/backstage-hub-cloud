apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Suspend A Cortex Search Service.
  description: >-
    Suspends one or both of the indexing or serving targets of a cortex search
    service.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Suspend A Cortex Search Service.
      description: >-
        Suspends one or both of the indexing or serving targets of a cortex search
        service.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/cortex-search-services/{name}:suspend:
        post:
          summary: Suspend A Cortex Search Service.
          description: >-
            Suspends one or both of the indexing or serving targets of a cortex
            search service.
          operationId: suspendCortexSearchService
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
