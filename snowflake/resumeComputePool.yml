apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Resumes A Suspended Compute Pool.
  description: >-
    Resume a compute pool, if suspended. If the specified compute pool is
    already running, this will cause a 400 Bad Request error.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Resumes A Suspended Compute Pool.
      description: >-
        Resume a compute pool, if suspended. If the specified compute pool is
        already running, this will cause a 400 Bad Request error.
    paths:
      /api/v2/compute-pools/{name}:resume:
        post:
          summary: Resumes A Suspended Compute Pool.
          description: >-
            Resume a compute pool, if suspended. If the specified compute pool is
            already running, this will cause a 400 Bad Request error.
          operationId: resumeComputePool
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
