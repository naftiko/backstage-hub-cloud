apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Fetches A Compute Pool.
  description: >-
    Fetches a named compute pool. You can get the name of the compute pool from
    the `/api/v2/compute-pools` GET request.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Fetches A Compute Pool.
      description: >-
        Fetches a named compute pool. You can get the name of the compute pool from
        the `/api/v2/compute-pools` GET request.
    paths:
      /api/v2/compute-pools/{name}:
        get:
          summary: Fetches A Compute Pool.
          description: >-
            Fetches a named compute pool. You can get the name of the compute pool
            from the `/api/v2/compute-pools` GET request.
          operationId: fetchComputePool
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
