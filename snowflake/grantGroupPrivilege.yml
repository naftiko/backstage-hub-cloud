apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    Grant The Specified Privilege(s) On All/future (as Specified By
    Bulkgranttype) Securables Of This Type In A Given Scope To The Named
    Grantee.
  description: >-
    Endpoint to indicate that the privileges listed in the request body should
    be granted to all securables of this type in the given scope.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Grant The Specified Privilege(s) On All/future (as Specified By
        Bulkgranttype) Securables Of This Type In A Given Scope To The Named
        Grantee.
      description: >-
        Endpoint to indicate that the privileges listed in the request body should
        be granted to all securables of this type in the given scope.
    paths:
      /api/v2/grants/{granteeType}/{granteeName}/{bulkGrantType}/{securableTypePlural}/{scopeType}/{scopeName}/privileges:
        post:
          summary: >-
            Grant The Specified Privilege(s) On All/future (as Specified By
            Bulkgranttype) Securables Of This Type In A Given Scope To The Named
            Grantee.
          description: >-
            Endpoint to indicate that the privileges listed in the request body
            should be granted to all securables of this type in the given scope.
          operationId: grantGroupPrivilege
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/Grant'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
