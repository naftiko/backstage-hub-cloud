apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A New, Or Alters An Existing, Database.
  description: >-
    Creates a new, or alters an existing, database. You must provide the full
    database definition even when altering an existing database.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A New, Or Alters An Existing, Database.
      description: >-
        Creates a new, or alters an existing, database. You must provide the full
        database definition even when altering an existing database.
    paths:
      /api/v2/databases/{name}:
        put:
          summary: Create A New, Or Alters An Existing, Database.
          description: >-
            Creates a new, or alters an existing, database. You must provide the
            full database definition even when altering an existing database.
          operationId: createOrAlterDatabase
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/Database'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
