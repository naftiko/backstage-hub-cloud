apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Notification Integration
  description: Delete a notification integration
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Notification Integration
      description: Delete a notification integration
    paths:
      /api/v2/notification-integrations/{name}:
        delete:
          summary: Delete A Notification Integration
          description: Delete a notification integration
          operationId: deleteNotificationIntegration
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
