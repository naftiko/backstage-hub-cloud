apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get The Graph Runs That Are Completed For The Task.
  description: This function returns details for graph runs that are completed.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get The Graph Runs That Are Completed For The Task.
      description: This function returns details for graph runs that are completed.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/tasks/{name}/complete-graphs:
        get:
          summary: Get The Graph Runs That Are Completed For The Task.
          description: This function returns details for graph runs that are completed.
          operationId: getCompleteGraphs
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
