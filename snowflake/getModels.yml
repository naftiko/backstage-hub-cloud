apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Returns The Llms Available For The Current Session
  description: Returns the LLMs available for the current session
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Returns The Llms Available For The Current Session
      description: Returns the LLMs available for the current session
    paths:
      /api/v2/cortex/models:
        get:
          summary: Returns The Llms Available For The Current Session
          description: Returns the LLMs available for the current session
          operationId: getModels
          requestBody:
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/GetModelsRequest'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
