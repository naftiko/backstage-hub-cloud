apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Check Result Status Or Fetch Request
  description: Get result status or the result when it is ready.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Check Result Status Or Fetch Request
      description: Get result status or the result when it is ready.
    paths:
      /api/v2/results/{result_handler}:
        get:
          summary: Check Result Status Or Fetch Request
          description: Get result status or the result when it is ready.
          operationId: fetchResult
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
