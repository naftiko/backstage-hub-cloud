apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Use Current Warehouse For Session
  description: '[Deprecated] Specifies the active/current warehouse for the session.'
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Use Current Warehouse For Session
      description: '[Deprecated] Specifies the active/current warehouse for the session.'
    paths:
      /api/v2/warehouses/{name}:use:
        post:
          summary: Use Current Warehouse For Session
          description: '[Deprecated] Specifies the active/current warehouse for the session.'
          operationId: useWarehouse
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
