apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Api Databases Database Schemas Schema Services Name:resume
  description: Resume a service.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Api Databases Database Schemas Schema Services Name:resume
      description: Resume a service.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/services/{name}:resume:
        post:
          summary: Post Api Databases Database Schemas Schema Services Name:resume
          description: Resume a service.
          operationId: resumeService
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
