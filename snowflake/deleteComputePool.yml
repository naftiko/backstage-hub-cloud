apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Deletes A Compute Pool.
  description: >-
    Deletes a compute pool with the given name. If you enable the `ifExists`
    parameter, the operation succeeds even if the object does not exist.
    Otherwise, a 404 failure is returned if the object does not exist.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deletes A Compute Pool.
      description: >-
        Deletes a compute pool with the given name. If you enable the `ifExists`
        parameter, the operation succeeds even if the object does not exist.
        Otherwise, a 404 failure is returned if the object does not exist.
    paths:
      /api/v2/compute-pools/{name}:
        delete:
          summary: Deletes A Compute Pool.
          description: >-
            Deletes a compute pool with the given name. If you enable the `ifExists`
            parameter, the operation succeeds even if the object does not exist.
            Otherwise, a 404 failure is returned if the object does not exist.
          operationId: deleteComputePool
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
