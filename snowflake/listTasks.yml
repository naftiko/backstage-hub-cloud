apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Tasks
  description: >-
    Lists tasks under the database and schema, with show options as query
    parameters.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Tasks
      description: >-
        Lists tasks under the database and schema, with show options as query
        parameters.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/tasks:
        get:
          summary: List Tasks
          description: >-
            Lists tasks under the database and schema, with show options as query
            parameters.
          operationId: listTasks
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
