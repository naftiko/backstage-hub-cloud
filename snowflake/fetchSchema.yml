apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Api Databases Database Schemas Name
  description: Fetches a schema.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Api Databases Database Schemas Name
      description: Fetches a schema.
    paths:
      /api/v2/databases/{database}/schemas/{name}:
        get:
          summary: Get Api Databases Database Schemas Name
          description: Fetches a schema.
          operationId: fetchSchema
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
