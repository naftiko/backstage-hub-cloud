apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Execute A Job Service
  description: >-
    Create and execute a job service. See the JobService component definition
    for what is required to be provided in the request body.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Execute A Job Service
      description: >-
        Create and execute a job service. See the JobService component definition
        for what is required to be provided in the request body.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/services:execute-job:
        post:
          summary: Execute A Job Service
          description: >-
            Create and execute a job service. See the JobService component
            definition for what is required to be provided in the request body.
          operationId: executeJobService
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/JobService'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
