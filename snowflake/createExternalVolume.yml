apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create An External Volume
  description: Create an external volume
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create An External Volume
      description: Create an external volume
    paths:
      /api/v2/external-volumes:
        post:
          summary: Create An External Volume
          description: Create an external volume
          operationId: createExternalVolume
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ExternalVolume'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
