apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Table Using The Result Of The Specified Select Query
  description: Create a table as select.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Table Using The Result Of The Specified Select Query
      description: Create a table as select.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/tables/{name}:as_select:
        post:
          summary: Create A Table Using The Result Of The Specified Select Query
          description: Create a table as select.
          operationId: createTableAsSelectDeprecated
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/Table'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
