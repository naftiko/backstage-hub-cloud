apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Warehouse
  description: >-
    Show a list of warehouse filtered by pattern. Equivalent to SHOW WAREHOUSE
    in SQL.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Warehouse
      description: >-
        Show a list of warehouse filtered by pattern. Equivalent to SHOW WAREHOUSE
        in SQL.
    paths:
      /api/v2/warehouses:
        get:
          summary: List Warehouse
          description: >-
            Show a list of warehouse filtered by pattern. Equivalent to SHOW
            WAREHOUSE in SQL.
          operationId: listWarehouses
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
