apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Files In The Stage.
  description: List files in the stage -- this is equivalent to LIST @stage.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Files In The Stage.
      description: List files in the stage -- this is equivalent to LIST @stage.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/stages/{name}/files:
        get:
          summary: List Files In The Stage.
          description: List files in the stage -- this is equivalent to LIST @stage.
          operationId: listFiles
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
