apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Api Cortex Analyst Feedback
  description: Send a user feedback of Cortex Analyst response
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Api Cortex Analyst Feedback
      description: Send a user feedback of Cortex Analyst response
    paths:
      /api/v2/cortex/analyst/feedback:
        post:
          summary: Post Api Cortex Analyst Feedback
          description: Send a user feedback of Cortex Analyst response
          operationId: sendFeedback
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/SendFeedbackRequest'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
