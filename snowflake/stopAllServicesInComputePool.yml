apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Stops All Services On The Compute Pool.
  description: Stops all services in the compute pool.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Stops All Services On The Compute Pool.
      description: Stops all services in the compute pool.
    paths:
      /api/v2/compute-pools/{name}:stop-all-services:
        post:
          summary: Stops All Services On The Compute Pool.
          description: Stops all services in the compute pool.
          operationId: stopAllServicesInComputePool
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
