apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Swap With Another Dynamic Table
  description: Swap with another dynamic table
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Swap With Another Dynamic Table
      description: Swap with another dynamic table
    paths:
      /api/v2/databases/{database}/schemas/{schema}/dynamic-tables/{name}:swap-with:
        post:
          summary: Swap With Another Dynamic Table
          description: Swap with another dynamic table
          operationId: swapWithDynamicTable
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
