apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Resume Recluster Of An Iceberg Table
  description: >-
    Resume recluster of an iceberg table (iceberg tables managed by an external
    catalog do not allow clustering)
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Resume Recluster Of An Iceberg Table
      description: >-
        Resume recluster of an iceberg table (iceberg tables managed by an external
        catalog do not allow clustering)
    paths:
      /api/v2/databases/{database}/schemas/{schema}/iceberg-tables/{name}:resume-recluster:
        post:
          summary: Resume Recluster Of An Iceberg Table
          description: >-
            Resume recluster of an iceberg table (iceberg tables managed by an
            external catalog do not allow clustering)
          operationId: resumeReclusterIcebergTable
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
