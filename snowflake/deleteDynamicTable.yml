apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Dynamic Table
  description: >-
    Delete a dynamic table with the given name. If ifExists is used, the
    operation will succeed even if the object does not exist. Otherwise, there
    will be a failure if the drop is unsuccessful.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Dynamic Table
      description: >-
        Delete a dynamic table with the given name. If ifExists is used, the
        operation will succeed even if the object does not exist. Otherwise, there
        will be a failure if the drop is unsuccessful.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/dynamic-tables/{name}:
        delete:
          summary: Delete A Dynamic Table
          description: >-
            Delete a dynamic table with the given name. If ifExists is used, the
            operation will succeed even if the object does not exist. Otherwise,
            there will be a failure if the drop is unsuccessful.
          operationId: deleteDynamicTable
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
