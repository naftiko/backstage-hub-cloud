apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Cortex Search Service
  description: >-
    Delete a cortex search service with the given name. If ifExists is used, the
    operation will succeed even if the object does not exist. Otherwise, there
    will be a failure if the drop is unsuccessful.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Cortex Search Service
      description: >-
        Delete a cortex search service with the given name. If ifExists is used, the
        operation will succeed even if the object does not exist. Otherwise, there
        will be a failure if the drop is unsuccessful.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/cortex-search-services/{name}:
        delete:
          summary: Delete A Cortex Search Service
          description: >-
            Delete a cortex search service with the given name. If ifExists is used,
            the operation will succeed even if the object does not exist. Otherwise,
            there will be a failure if the drop is unsuccessful.
          operationId: deleteCortexSearchService
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
