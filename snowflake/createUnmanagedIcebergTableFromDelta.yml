apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create An Unmanaged Iceberg Table From Delta
  description: Create an unmanaged iceberg table from Delta
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create An Unmanaged Iceberg Table From Delta
      description: Create an unmanaged iceberg table from Delta
    paths:
      /api/v2/databases/{database}/schemas/{schema}/iceberg-tables:from-delta:
        post:
          summary: Create An Unmanaged Iceberg Table From Delta
          description: Create an unmanaged iceberg table from Delta
          operationId: createUnmanagedIcebergTableFromDelta
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/IcebergTableFromDelta'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
