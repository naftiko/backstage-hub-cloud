apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Fetch The Dependent Tasks Of A Task
  description: >-
    This operation returns a list of the dependent tasks of the task with
    identifier {name}.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Fetch The Dependent Tasks Of A Task
      description: >-
        This operation returns a list of the dependent tasks of the task with
        identifier {name}.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/tasks/{name}/dependents:
        get:
          summary: Fetch The Dependent Tasks Of A Task
          description: >-
            This operation returns a list of the dependent tasks of the task with
            identifier {name}.
          operationId: fetchTaskDependents
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
