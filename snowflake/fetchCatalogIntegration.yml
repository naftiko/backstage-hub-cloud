apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Fetch A Catalog Integration.
  description: Fetch a catalog integration.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Fetch A Catalog Integration.
      description: Fetch a catalog integration.
    paths:
      /api/v2/catalog-integrations/{name}:
        get:
          summary: Fetch A Catalog Integration.
          description: Fetch a catalog integration.
          operationId: fetchCatalogIntegration
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
