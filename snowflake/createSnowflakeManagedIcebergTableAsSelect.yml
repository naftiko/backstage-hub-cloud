apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Snowflake Managed Iceberg Table As Select
  description: Create a snowflake managed iceberg table as select
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Snowflake Managed Iceberg Table As Select
      description: Create a snowflake managed iceberg table as select
    paths:
      /api/v2/databases/{database}/schemas/{schema}/iceberg-tables:as-select:
        post:
          summary: Create A Snowflake Managed Iceberg Table As Select
          description: Create a snowflake managed iceberg table as select
          operationId: createSnowflakeManagedIcebergTableAsSelect
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/IcebergTableAsSelect'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
