apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Suspend Warehouse
  description: >-
    Remove all compute nodes from a warehouse and put the warehouse into a
    ‘Suspended’ state if current warehouse is not suspended.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Suspend Warehouse
      description: >-
        Remove all compute nodes from a warehouse and put the warehouse into a
        ‘Suspended’ state if current warehouse is not suspended.
    paths:
      /api/v2/warehouses/{name}:suspend:
        post:
          summary: Suspend Warehouse
          description: >-
            Remove all compute nodes from a warehouse and put the warehouse into a
            ‘Suspended’ state if current warehouse is not suspended.
          operationId: suspendWarehouse
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
