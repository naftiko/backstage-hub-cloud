apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Of Privileges Associated With This Grantee Type And Name
  description: >-
    List the roles and privileges granted to the specified grantee using the
    output of SHOW GRANTS TO
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Of Privileges Associated With This Grantee Type And Name
      description: >-
        List the roles and privileges granted to the specified grantee using the
        output of SHOW GRANTS TO
    paths:
      /api/v2/grants/{granteeType}/{granteeName}:
        get:
          summary: List Of Privileges Associated With This Grantee Type And Name
          description: >-
            List the roles and privileges granted to the specified grantee using the
            output of SHOW GRANTS TO
          operationId: listGrantsTo
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
