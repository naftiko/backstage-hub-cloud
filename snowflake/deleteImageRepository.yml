apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Deletes An Image Repository.
  description: >-
    Deletes an image repository with the given name. If you enable the
    `ifExists` query parameter, the operation succeeds even if the object does
    not exist. Otherwise, a 404 failure is returned if the object does not
    exist.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deletes An Image Repository.
      description: >-
        Deletes an image repository with the given name. If you enable the
        `ifExists` query parameter, the operation succeeds even if the object does
        not exist. Otherwise, a 404 failure is returned if the object does not
        exist.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/image-repositories/{name}:
        delete:
          summary: Deletes An Image Repository.
          description: >-
            Deletes an image repository with the given name. If you enable the
            `ifExists` query parameter, the operation succeeds even if the object
            does not exist. Otherwise, a 404 failure is returned if the object does
            not exist.
          operationId: deleteImageRepository
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
