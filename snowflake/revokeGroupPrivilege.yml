apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    Revoke The Specified Privilege On The Specified All/future Securable In The
    Given Scope From The Named Grantee.
  description: >-
    Endpoint to indicate that the privilege listed on the group securable in the
    given scope should be revoked.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Revoke The Specified Privilege On The Specified All/future Securable In The
        Given Scope From The Named Grantee.
      description: >-
        Endpoint to indicate that the privilege listed on the group securable in the
        given scope should be revoked.
    paths:
      /api/v2/grants/{granteeType}/{granteeName}/{bulkGrantType}/{securableTypePlural}/{scopeType}/{scopeName}/privileges/{privilege}:
        delete:
          summary: >-
            Revoke The Specified Privilege On The Specified All/future Securable In
            The Given Scope From The Named Grantee.
          description: >-
            Endpoint to indicate that the privilege listed on the group securable in
            the given scope should be revoked.
          operationId: revokeGroupPrivilege
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
