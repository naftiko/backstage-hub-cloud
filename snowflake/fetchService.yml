apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Api Databases Database Schemas Schema Services Name
  description: Fetch a service.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Api Databases Database Schemas Schema Services Name
      description: Fetch a service.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/services/{name}:
        get:
          summary: Get Api Databases Database Schemas Schema Services Name
          description: Fetch a service.
          operationId: fetchService
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
