apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List All Future Grants To The Role
  description: List all future grants to the role
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List All Future Grants To The Role
      description: List all future grants to the role
    paths:
      /api/v2/roles/{name}/future-grants:
        get:
          summary: List All Future Grants To The Role
          description: List all future grants to the role
          operationId: listFutureGrants
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
