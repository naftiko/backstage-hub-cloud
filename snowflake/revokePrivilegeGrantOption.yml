apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    Revoke The Grant Option For The Specified Privilege On The Named Securable
    From The Named Grantee.
  description: >-
    Endpoint to indicate that the grant option for the privilege listed in the
    path should be revoked.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Revoke The Grant Option For The Specified Privilege On The Named Securable
        From The Named Grantee.
      description: >-
        Endpoint to indicate that the grant option for the privilege listed in the
        path should be revoked.
    paths:
      /api/v2/grants/{granteeType}/{granteeName}/{securableType}/{securableName}/privileges/{privilege}/grant-option:
        delete:
          summary: >-
            Revoke The Grant Option For The Specified Privilege On The Named
            Securable From The Named Grantee.
          description: >-
            Endpoint to indicate that the grant option for the privilege listed in
            the path should be revoked.
          operationId: revokePrivilegeGrantOption
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
