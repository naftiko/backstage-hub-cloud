apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Api Databases Database Schemas Schema Services Name Logs
  description: Fetch the logs for a given service.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Api Databases Database Schemas Schema Services Name Logs
      description: Fetch the logs for a given service.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/services/{name}/logs:
        get:
          summary: Get Api Databases Database Schemas Schema Services Name Logs
          description: Fetch the logs for a given service.
          operationId: fetchServiceLogs
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
