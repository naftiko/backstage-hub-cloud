apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Clone A Snowflake Managed Iceberg Table
  description: Clone a snowflake managed iceberg table
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Clone A Snowflake Managed Iceberg Table
      description: Clone a snowflake managed iceberg table
    paths:
      /api/v2/databases/{database}/schemas/{schema}/iceberg-tables/{name}:clone:
        post:
          summary: Clone A Snowflake Managed Iceberg Table
          description: Clone a snowflake managed iceberg table
          operationId: cloneSnowflakeManagedIcebergTable
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/IcebergTableClone'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
