apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Changes The Name Of The Notebook To New Name.
  description: >-
    Changes the name of the notebook to new name. The new identifier must be
    unique for the schema.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Changes The Name Of The Notebook To New Name.
      description: >-
        Changes the name of the notebook to new name. The new identifier must be
        unique for the schema.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/notebooks/{name}:rename:
        post:
          summary: Changes The Name Of The Notebook To New Name.
          description: >-
            Changes the name of the notebook to new name. The new identifier must be
            unique for the schema.
          operationId: renameNotebook
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
