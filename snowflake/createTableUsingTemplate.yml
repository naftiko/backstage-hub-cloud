apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Table Using The Templates Specified In Staged Files
  description: Create a table using template.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Table Using The Templates Specified In Staged Files
      description: Create a table using template.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/tables:using-template:
        post:
          summary: Create A Table Using The Templates Specified In Staged Files
          description: Create a table using template.
          operationId: createTableUsingTemplate
          requestBody:
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/TableUsingTemplate'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
