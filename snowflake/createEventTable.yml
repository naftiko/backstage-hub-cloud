apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create An Event Table
  description: Create an event table
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create An Event Table
      description: Create an event table
    paths:
      /api/v2/databases/{database}/schemas/{schema}/event-tables:
        post:
          summary: Create An Event Table
          description: Create an event table
          operationId: createEventTable
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/EventTable'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
