apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Revoke Future Grants From The Role
  description: Revoke future grants from the role
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Revoke Future Grants From The Role
      description: Revoke future grants from the role
    paths:
      /api/v2/roles/{name}/future-grants:revoke:
        post:
          summary: Revoke Future Grants From The Role
          description: Revoke future grants from the role
          operationId: revokeFutureGrants
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/Grant'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
