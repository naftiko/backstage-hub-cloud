apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Iceberg Table Like
  description: >-
    Creates a new table with the same column definitions as an existing table,
    but without copying data from the existing table.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Iceberg Table Like
      description: >-
        Creates a new table with the same column definitions as an existing table,
        but without copying data from the existing table.
    paths:
      /api/v2/databases/{database}/schemas/{schema}/iceberg-tables/{name}:create-like:
        post:
          summary: Create Iceberg Table Like
          description: >-
            Creates a new table with the same column definitions as an existing
            table, but without copying data from the existing table.
          operationId: createSnowflakeManagedIcebergTableLike
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/IcebergTableLike'
          responses:
            '504':
              $ref: common.yaml#/components/responses/504GatewayTimeout
