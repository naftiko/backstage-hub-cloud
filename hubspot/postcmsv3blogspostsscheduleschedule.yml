apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Schedule A Blog Post To Be Published
  description: Schedule a Blog Post to be Published.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Schedule A Blog Post To Be Published
      description: Schedule a Blog Post to be Published.
    paths:
      /cms/v3/blogs/posts/schedule:
        post:
          summary: Schedule A Blog Post To Be Published
          description: Schedule a Blog Post to be Published.
          operationId: postcmsv3blogspostsscheduleschedule
          requestBody:
            description: The JSON representation of the ContentScheduleRequestVNext object.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ContentScheduleRequestVNext'
          responses:
            default:
              $ref: '#/components/responses/Error'
