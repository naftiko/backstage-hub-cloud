apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Validate The Contents Of A File
  description: >-
    Validates the file contents passed to the endpoint given a specified path
    and environment. Accepts multipart/form-data content type.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Validate The Contents Of A File
      description: >-
        Validates the file contents passed to the endpoint given a specified path
        and environment. Accepts multipart/form-data content type.
    paths:
      /cms/v3/source-code/{environment}/validate/{path}:
        post:
          summary: Validate The Contents Of A File
          description: >-
            Validates the file contents passed to the endpoint given a specified
            path and environment. Accepts multipart/form-data content type.
          operationId: postcmsv3sourcecode{Environment}Validate{Path}DoValidate
          requestBody:
            content:
              multipart/form-data:
                schema:
                  type: object
                  properties:
                    file:
                      type: string
                      format: binary
          responses:
            default:
              $ref: '#/components/responses/Error'
