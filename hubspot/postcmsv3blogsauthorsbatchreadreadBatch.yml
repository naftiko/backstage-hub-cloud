apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Retrieve A Batch Of Blog Authors
  description: Retrieve the Blog Author objects identified in the request body.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieve A Batch Of Blog Authors
      description: Retrieve the Blog Author objects identified in the request body.
    paths:
      /cms/v3/blogs/authors/batch/read:
        post:
          summary: Retrieve A Batch Of Blog Authors
          description: Retrieve the Blog Author objects identified in the request body.
          operationId: postcmsv3blogsauthorsbatchreadreadBatch
          requestBody:
            description: The JSON array of Blog Author ids.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/BatchInputString'
          responses:
            default:
              $ref: '#/components/responses/Error'
