apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Extract A Zip File
  description: >-
    Extract a zip file in the developer file system. Extraction status can be
    checked with the `/extract/async/tasks/taskId/status` endpoint below.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Extract A Zip File
      description: >-
        Extract a zip file in the developer file system. Extraction status can be
        checked with the `/extract/async/tasks/taskId/status` endpoint below.
    paths:
      /cms/v3/source-code/extract/async:
        post:
          summary: Extract A Zip File
          description: >-
            Extract a zip file in the developer file system. Extraction status can
            be checked with the `/extract/async/tasks/taskId/status` endpoint below.
          operationId: postcmsv3sourcecodeextractasyncdoAsync
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/FileExtractRequest'
          responses:
            default:
              $ref: '#/components/responses/Error'
