apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Extraction Status
  description: >-
    Get the status of an extraction by the `taskId` returned from the initial
    `extract/async` request.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Extraction Status
      description: >-
        Get the status of an extraction by the `taskId` returned from the initial
        `extract/async` request.
    paths:
      /cms/v3/source-code/extract/async/tasks/{taskId}/status:
        get:
          summary: Get Extraction Status
          description: >-
            Get the status of an extraction by the `taskId` returned from the
            initial `extract/async` request.
          operationId: getcmsv3sourcecodeextractasynctasks{TaskId}StatusgetAsyncStatus
          responses:
            default:
              $ref: '#/components/responses/Error'
