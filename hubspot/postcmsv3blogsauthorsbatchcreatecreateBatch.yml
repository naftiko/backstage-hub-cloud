apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Batch Of Blog Authors
  description: Create the Blog Author objects detailed in the request body.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Batch Of Blog Authors
      description: Create the Blog Author objects detailed in the request body.
    paths:
      /cms/v3/blogs/authors/batch/create:
        post:
          summary: Create A Batch Of Blog Authors
          description: Create the Blog Author objects detailed in the request body.
          operationId: postcmsv3blogsauthorsbatchcreatecreateBatch
          requestBody:
            description: The JSON array of new Blog Authors to create.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/BatchInputBlogAuthor'
          responses:
            default:
              $ref: '#/components/responses/Error'
