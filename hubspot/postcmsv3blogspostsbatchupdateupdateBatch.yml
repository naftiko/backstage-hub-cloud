apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update A Batch Of Blog Posts
  description: Update the Blog Post objects identified in the request body.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update A Batch Of Blog Posts
      description: Update the Blog Post objects identified in the request body.
    paths:
      /cms/v3/blogs/posts/batch/update:
        post:
          summary: Update A Batch Of Blog Posts
          description: Update the Blog Post objects identified in the request body.
          operationId: postcmsv3blogspostsbatchupdateupdateBatch
          requestBody:
            description: A JSON array of the JSON representations of the updated Blog Posts.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/BatchInputJsonNode'
          responses:
            default:
              $ref: '#/components/responses/Error'
