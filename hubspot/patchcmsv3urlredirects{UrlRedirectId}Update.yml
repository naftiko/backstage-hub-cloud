apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update A Redirect
  description: Updates the settings for an existing URL redirect.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update A Redirect
      description: Updates the settings for an existing URL redirect.
    paths:
      /cms/v3/url-redirects/{urlRedirectId}:
        patch:
          summary: Update A Redirect
          description: Updates the settings for an existing URL redirect.
          operationId: patchcmsv3urlredirects{UrlRedirectId}Update
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/UrlMapping'
          responses:
            default:
              $ref: '#/components/responses/Error'
