apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Clone A Blog Post
  description: Clone a Blog Post.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Clone A Blog Post
      description: Clone a Blog Post.
    paths:
      /cms/v3/blogs/posts/clone:
        post:
          summary: Clone A Blog Post
          description: Clone a Blog Post.
          operationId: postcmsv3blogspostscloneclone
          requestBody:
            description: The JSON representation of the ContentCloneRequest object.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ContentCloneRequestVNext'
          responses:
            default:
              $ref: '#/components/responses/Error'
