apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Redirect
  description: Creates and configures a new URL redirect.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Redirect
      description: Creates and configures a new URL redirect.
    paths:
      /cms/v3/url-redirects/:
        post:
          summary: Create A Redirect
          description: Creates and configures a new URL redirect.
          operationId: postcmsv3urlredirectscreate
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/UrlMappingCreateRequestBody'
          responses:
            default:
              $ref: '#/components/responses/Error'
