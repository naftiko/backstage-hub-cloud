apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Retrieve A Batch Of Blog Posts
  description: Retrieve the Blog Post objects identified in the request body.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieve A Batch Of Blog Posts
      description: Retrieve the Blog Post objects identified in the request body.
    paths:
      /cms/v3/blogs/posts/batch/read:
        post:
          summary: Retrieve A Batch Of Blog Posts
          description: Retrieve the Blog Post objects identified in the request body.
          operationId: postcmsv3blogspostsbatchreadreadBatch
          requestBody:
            description: The JSON array of Blog Post ids.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/BatchInputString'
          responses:
            default:
              $ref: '#/components/responses/Error'
