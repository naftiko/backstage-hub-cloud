apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A New Blog Author
  description: Create a new Blog Author.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A New Blog Author
      description: Create a new Blog Author.
    paths:
      /cms/v3/blogs/authors:
        post:
          summary: Create A New Blog Author
          description: Create a new Blog Author.
          operationId: postcmsv3blogsauthorscreate
          requestBody:
            description: The JSON representation of a new Blog Author.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/BlogAuthor'
          responses:
            default:
              $ref: '#/components/responses/Error'
