apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Set A New Primary Language
  description: Set a Blog Author as the primary language of a multi-language group.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Set A New Primary Language
      description: Set a Blog Author as the primary language of a multi-language group.
    paths:
      /cms/v3/blogs/authors/multi-language/set-new-lang-primary:
        put:
          summary: Set A New Primary Language
          description: Set a Blog Author as the primary language of a multi-language group.
          operationId: putcmsv3blogsauthorsmultilanguagesetnewlangprimarysetLangPrimary
          requestBody:
            description: The JSON representation of the SetNewLanguagePrimaryRequest object.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/SetNewLanguagePrimaryRequestVNext'
          responses:
            default:
              $ref: '#/components/responses/Error'
