apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Set A New Primary Language
  description: >-
    Set the primary language of a multi-language group to the language of the
    provided post (specified as an id in the request body)
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Set A New Primary Language
      description: >-
        Set the primary language of a multi-language group to the language of the
        provided post (specified as an id in the request body)
    paths:
      /cms/v3/blogs/posts/multi-language/set-new-lang-primary:
        put:
          summary: Set A New Primary Language
          description: >-
            Set the primary language of a multi-language group to the language of
            the provided post (specified as an id in the request body)
          operationId: putcmsv3blogspostsmultilanguagesetnewlangprimarysetLangPrimary
          requestBody:
            description: The JSON representation of the SetNewLanguagePrimaryRequest object.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/SetNewLanguagePrimaryRequestVNext'
          responses:
            default:
              $ref: '#/components/responses/Error'
