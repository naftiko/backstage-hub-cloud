apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Current Redirects
  description: >-
    Returns all existing URL redirects. Results can be limited and filtered by
    creation or updated date.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Current Redirects
      description: >-
        Returns all existing URL redirects. Results can be limited and filtered by
        creation or updated date.
    paths:
      /cms/v3/url-redirects/:
        get:
          summary: Get Current Redirects
          description: >-
            Returns all existing URL redirects. Results can be limited and filtered
            by creation or updated date.
          operationId: getcmsv3urlredirectsgetPage
          responses:
            default:
              $ref: '#/components/responses/Error'
