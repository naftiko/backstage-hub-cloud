apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Redirect
  description: Delete one existing redirect, so it is no longer mapped.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Redirect
      description: Delete one existing redirect, so it is no longer mapped.
    paths:
      /cms/v3/url-redirects/{urlRedirectId}:
        delete:
          summary: Delete A Redirect
          description: Delete one existing redirect, so it is no longer mapped.
          operationId: deletecmsv3urlredirects{UrlRedirectId}Archive
          responses:
            default:
              $ref: '#/components/responses/Error'
