apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Batch Of Blog Authors
  description: Delete the Blog Author objects identified in the request body.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Batch Of Blog Authors
      description: Delete the Blog Author objects identified in the request body.
    paths:
      /cms/v3/blogs/authors/batch/archive:
        post:
          summary: Delete A Batch Of Blog Authors
          description: Delete the Blog Author objects identified in the request body.
          operationId: postcmsv3blogsauthorsbatcharchivearchiveBatch
          requestBody:
            description: The JSON array of Blog Author ids.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/BatchInputString'
          responses:
            default:
              $ref: '#/components/responses/Error'
