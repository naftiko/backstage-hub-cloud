apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get The Customization Template For An Oidc Subject Claim For A Repository
  description: >-
    Gets the customization template for an OpenID Connect (OIDC) subject
    claim.OAuth tokens and personal access tokens (classic) need the `repo`
    scope to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get The Customization Template For An Oidc Subject Claim For A Repository
      description: >-
        Gets the customization template for an OpenID Connect (OIDC) subject
        claim.OAuth tokens and personal access tokens (classic) need the `repo`
        scope to use this endpoint.
    paths:
      /repos/{owner}/{repo}/actions/oidc/customization/sub:
        get:
          summary: >-
            Get The Customization Template For An Oidc Subject Claim For A
            Repository
          description: >-
            Gets the customization template for an OpenID Connect (OIDC) subject
            claim.OAuth tokens and personal access tokens (classic) need the `repo`
            scope to use this endpoint.
          operationId: actionsgetcustomoidcsubclaimforrepo
          responses:
            '404':
              $ref: '#/components/responses/not_found'
