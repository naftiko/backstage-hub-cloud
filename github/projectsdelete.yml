apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Project
  description: >-
    Deletes a project board. Returns a `404 Not Found` status if projects are
    disabled.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Project
      description: >-
        Deletes a project board. Returns a `404 Not Found` status if projects are
        disabled.
    paths:
      /projects/{project_id}:
        delete:
          summary: Delete A Project
          description: >-
            Deletes a project board. Returns a `404 Not Found` status if projects
            are disabled.
          operationId: projectsdelete
          responses:
            '410':
              $ref: '#/components/responses/gone'
