apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get The Global Announcement Banner
  description: >-
    Gets the current message and expiration date of the global announcement
    banner in your enterprise.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get The Global Announcement Banner
      description: >-
        Gets the current message and expiration date of the global announcement
        banner in your enterprise.
    paths:
      /enterprise/announcement:
        get:
          summary: Get The Global Announcement Banner
          description: >-
            Gets the current message and expiration date of the global announcement
            banner in your enterprise.
          operationId: enterpriseadmingetannouncement
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/announcement'
                  examples:
                    default:
                      $ref: '#/components/examples/announcement'
