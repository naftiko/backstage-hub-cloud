apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Labels For A Self-hosted Runner For A Repository
  description: >-
    Lists all labels for a self-hosted runner configured in a
    repository.Authenticated users must have admin access to the repository to
    use this endpoint.OAuth app tokens and personal access tokens (classic) need
    the `repo` scope to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Labels For A Self-hosted Runner For A Repository
      description: >-
        Lists all labels for a self-hosted runner configured in a
        repository.Authenticated users must have admin access to the repository to
        use this endpoint.OAuth app tokens and personal access tokens (classic) need
        the `repo` scope to use this endpoint.
    paths:
      /repos/{owner}/{repo}/actions/runners/{runner_id}/labels:
        get:
          summary: List Labels For A Self-hosted Runner For A Repository
          description: >-
            Lists all labels for a self-hosted runner configured in a
            repository.Authenticated users must have admin access to the repository
            to use this endpoint.OAuth app tokens and personal access tokens
            (classic) need the `repo` scope to use this endpoint.
          operationId: actionslistlabelsforselfhostedrunnerforrepo
          responses:
            '404':
              $ref: '#/components/responses/not_found'
