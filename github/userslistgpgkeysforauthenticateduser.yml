apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Gpg Keys For The Authenticated User
  description: >-
    Lists the current user's GPG keys.OAuth app tokens and personal access
    tokens (classic) need the `read:gpg_key` scope to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Gpg Keys For The Authenticated User
      description: >-
        Lists the current user's GPG keys.OAuth app tokens and personal access
        tokens (classic) need the `read:gpg_key` scope to use this endpoint.
    paths:
      /user/gpg_keys:
        get:
          summary: List Gpg Keys For The Authenticated User
          description: >-
            Lists the current user's GPG keys.OAuth app tokens and personal access
            tokens (classic) need the `read:gpg_key` scope to use this endpoint.
          operationId: userslistgpgkeysforauthenticateduser
          responses:
            '404':
              $ref: '#/components/responses/not_found'
