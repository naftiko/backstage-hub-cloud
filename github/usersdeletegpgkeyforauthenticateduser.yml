apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Gpg Key For The Authenticated User
  description: >-
    Removes a GPG key from the authenticated user's GitHub account.OAuth app
    tokens and personal access tokens (classic) need the `admin:gpg_key` scope
    to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Gpg Key For The Authenticated User
      description: >-
        Removes a GPG key from the authenticated user's GitHub account.OAuth app
        tokens and personal access tokens (classic) need the `admin:gpg_key` scope
        to use this endpoint.
    paths:
      /user/gpg_keys/{gpg_key_id}:
        delete:
          summary: Delete A Gpg Key For The Authenticated User
          description: >-
            Removes a GPG key from the authenticated user's GitHub account.OAuth app
            tokens and personal access tokens (classic) need the `admin:gpg_key`
            scope to use this endpoint.
          operationId: usersdeletegpgkeyforauthenticateduser
          responses:
            '422':
              $ref: '#/components/responses/validation_failed'
