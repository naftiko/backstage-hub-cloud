apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Remove Pre-receive Hook Enforcement For An Organization
  description: Removes any overrides for this hook at the org level for this org.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Remove Pre-receive Hook Enforcement For An Organization
      description: Removes any overrides for this hook at the org level for this org.
    paths:
      /orgs/{org}/pre-receive-hooks/{pre_receive_hook_id}:
        delete:
          summary: Remove Pre-receive Hook Enforcement For An Organization
          description: Removes any overrides for this hook at the org level for this org.
          operationId: enterpriseadminremoveprereceivehookenforcementfororg
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/org-pre-receive-hook'
                  examples:
                    default:
                      $ref: '#/components/examples/org-pre-receive-hook'
