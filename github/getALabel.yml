apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Label
  description: Gets a label using the given name.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Label
      description: Gets a label using the given name.
    paths:
      /repos/{owner}/{repo}/labels/{name}:
        get:
          summary: Get A Label
          description: Gets a label using the given name.
          operationId: getALabel
          responses:
            '404':
              $ref: '#/components/responses/not_found'
