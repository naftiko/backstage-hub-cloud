apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Email Addresses For The Authenticated User
  description: >-
    Lists all of your email addresses, and specifies which one is visibleto the
    public.OAuth app tokens and personal access tokens (classic) need the
    `user:email` scope to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Email Addresses For The Authenticated User
      description: >-
        Lists all of your email addresses, and specifies which one is visibleto the
        public.OAuth app tokens and personal access tokens (classic) need the
        `user:email` scope to use this endpoint.
    paths:
      /user/emails:
        get:
          summary: List Email Addresses For The Authenticated User
          description: >-
            Lists all of your email addresses, and specifies which one is visibleto
            the public.OAuth app tokens and personal access tokens (classic) need
            the `user:email` scope to use this endpoint.
          operationId: userslistemailsforauthenticateduser
          responses:
            '404':
              $ref: '#/components/responses/not_found'
