apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Self-hosted Runner Group For An Enterprise
  description: >-
    Gets a specific self-hosted runner group for an enterprise.OAuth app tokens
    and personal access tokens (classic) need the `manage_runners:enterprise`
    scope to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Self-hosted Runner Group For An Enterprise
      description: >-
        Gets a specific self-hosted runner group for an enterprise.OAuth app tokens
        and personal access tokens (classic) need the `manage_runners:enterprise`
        scope to use this endpoint.
    paths:
      /enterprises/{enterprise}/actions/runner-groups/{runner_group_id}:
        get:
          summary: Get A Self-hosted Runner Group For An Enterprise
          description: >-
            Gets a specific self-hosted runner group for an enterprise.OAuth app
            tokens and personal access tokens (classic) need the
            `manage_runners:enterprise` scope to use this endpoint.
          operationId: enterpriseadmingetselfhostedrunnergroupforenterprise
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/runner-groups-enterprise'
                  examples:
                    default:
                      $ref: '#/components/examples/runner-group-enterprise'
