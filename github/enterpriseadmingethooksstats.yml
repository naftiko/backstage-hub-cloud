apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Hooks Statistics
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Hooks Statistics
      description: Needs a more full description created.
    paths:
      /enterprise/stats/hooks:
        get:
          summary: Get Hooks Statistics
          description: Needs a more full description created.
          operationId: enterpriseadmingethooksstats
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/enterprise-hook-overview'
                  examples:
                    default:
                      value:
                        total_hooks: 50
                        active_hooks: 47
                        inactive_hooks: 3
