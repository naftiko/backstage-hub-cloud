apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Repository Invitations For The Authenticated User
  description: >-
    When authenticating as a user, this endpoint will list all currently open
    repository invitations for that user.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Repository Invitations For The Authenticated User
      description: >-
        When authenticating as a user, this endpoint will list all currently open
        repository invitations for that user.
    paths:
      /user/repository_invitations:
        get:
          summary: List Repository Invitations For The Authenticated User
          description: >-
            When authenticating as a user, this endpoint will list all currently
            open repository invitations for that user.
          operationId: listRepositoryInvitationsForTheAuthenticatedUser
          responses:
            '404':
              $ref: '#/components/responses/not_found'
