apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Personal Access Tokens
  description: Lists personal access tokens for all users, including admin users.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Personal Access Tokens
      description: Lists personal access tokens for all users, including admin users.
    paths:
      /admin/tokens:
        get:
          summary: List Personal Access Tokens
          description: Lists personal access tokens for all users, including admin users.
          operationId: enterpriseadminlistpersonalaccesstokens
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    type: array
                    items:
                      $ref: '#/components/schemas/authorization'
                  examples:
                    default:
                      $ref: '#/components/examples/authorization-items'
              headers:
                Link:
                  $ref: '#/components/headers/link'
