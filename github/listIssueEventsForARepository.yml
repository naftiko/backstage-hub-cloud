apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Issue Events For A Repository
  description: Lists events for a repository.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Issue Events For A Repository
      description: Lists events for a repository.
    paths:
      /repos/{owner}/{repo}/issues/events:
        get:
          summary: List Issue Events For A Repository
          description: Lists events for a repository.
          operationId: listIssueEventsForARepository
          responses:
            '422':
              $ref: '#/components/responses/validation_failed'
