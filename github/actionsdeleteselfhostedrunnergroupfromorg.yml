apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Self-hosted Runner Group From An Organization
  description: >-
    Deletes a self-hosted runner group for an organization.OAuth tokens and
    personal access tokens (classic) need the `admin:org` scope to use this
    endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Self-hosted Runner Group From An Organization
      description: >-
        Deletes a self-hosted runner group for an organization.OAuth tokens and
        personal access tokens (classic) need the `admin:org` scope to use this
        endpoint.
    paths:
      /orgs/{org}/actions/runner-groups/{runner_group_id}:
        delete:
          summary: Delete A Self-hosted Runner Group From An Organization
          description: >-
            Deletes a self-hosted runner group for an organization.OAuth tokens and
            personal access tokens (classic) need the `admin:org` scope to use this
            endpoint.
          operationId: actionsdeleteselfhostedrunnergroupfromorg
          responses:
            '204':
              description: Response
