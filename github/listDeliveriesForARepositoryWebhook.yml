apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Deliveries For A Repository Webhook
  description: >-
    Returns a list of webhook deliveries for a webhook configured in a
    repository.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Deliveries For A Repository Webhook
      description: >-
        Returns a list of webhook deliveries for a webhook configured in a
        repository.
    paths:
      /repos/{owner}/{repo}/hooks/{hook_id}/deliveries:
        get:
          summary: List Deliveries For A Repository Webhook
          description: >-
            Returns a list of webhook deliveries for a webhook configured in a
            repository.
          operationId: listDeliveriesForARepositoryWebhook
          responses:
            '422':
              $ref: '#/components/responses/validation_failed'
