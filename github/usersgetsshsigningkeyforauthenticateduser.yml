apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get An Ssh Signing Key For The Authenticated User
  description: >-
    Gets extended details for an SSH signing key.OAuth app tokens and personal
    access tokens (classic) need the `read:ssh_signing_key` scope to use this
    endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get An Ssh Signing Key For The Authenticated User
      description: >-
        Gets extended details for an SSH signing key.OAuth app tokens and personal
        access tokens (classic) need the `read:ssh_signing_key` scope to use this
        endpoint.
    paths:
      /user/ssh_signing_keys/{ssh_signing_key_id}:
        get:
          summary: Get An Ssh Signing Key For The Authenticated User
          description: >-
            Gets extended details for an SSH signing key.OAuth app tokens and
            personal access tokens (classic) need the `read:ssh_signing_key` scope
            to use this endpoint.
          operationId: usersgetsshsigningkeyforauthenticateduser
          responses:
            '404':
              $ref: '#/components/responses/not_found'
