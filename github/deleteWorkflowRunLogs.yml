apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Workflow Run Logs
  description: >-
    Deletes all logs for a workflow run.OAuth tokens and personal access tokens
    (classic) need the `repo` scope to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Workflow Run Logs
      description: >-
        Deletes all logs for a workflow run.OAuth tokens and personal access tokens
        (classic) need the `repo` scope to use this endpoint.
    paths:
      /repos/{owner}/{repo}/actions/runs/{run_id}/logs:
        delete:
          summary: Delete Workflow Run Logs
          description: >-
            Deletes all logs for a workflow run.OAuth tokens and personal access
            tokens (classic) need the `repo` scope to use this endpoint.
          operationId: deleteWorkflowRunLogs
          responses:
            '500':
              $ref: '#/components/responses/internal_error'
