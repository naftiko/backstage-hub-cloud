apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update A Code Scanning Default Setup Configuration
  description: >-
    Updates a code scanning default setup configuration.OAuth app tokens and
    personal access tokens (classic) need the `repo` scope to use this endpoint
    with private or public repositories, or the `public_repo` scope to use this
    endpoint with only public repositories.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update A Code Scanning Default Setup Configuration
      description: >-
        Updates a code scanning default setup configuration.OAuth app tokens and
        personal access tokens (classic) need the `repo` scope to use this endpoint
        with private or public repositories, or the `public_repo` scope to use this
        endpoint with only public repositories.
    paths:
      /repos/{owner}/{repo}/code-scanning/default-setup:
        patch:
          summary: Update A Code Scanning Default Setup Configuration
          description: >-
            Updates a code scanning default setup configuration.OAuth app tokens and
            personal access tokens (classic) need the `repo` scope to use this
            endpoint with private or public repositories, or the `public_repo` scope
            to use this endpoint with only public repositories.
          operationId: updateACodeScanningDefaultSetupConfiguration
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/code-scanning-default-setup-update'
                examples:
                  default:
                    $ref: '#/components/examples/code-scanning-default-setup-update'
          responses:
            '503':
              $ref: '#/components/responses/service_unavailable'
