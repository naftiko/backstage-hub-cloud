apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Repository Events
  description: >-
    **Note**: This API is not built to serve real-time use cases. Depending on
    the time of day, event latency can be anywhere from 30s to 6h.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Repository Events
      description: >-
        **Note**: This API is not built to serve real-time use cases. Depending on
        the time of day, event latency can be anywhere from 30s to 6h.
    paths:
      /repos/{owner}/{repo}/events:
        get:
          summary: List Repository Events
          description: >-
            **Note**: This API is not built to serve real-time use cases. Depending
            on the time of day, event latency can be anywhere from 30s to 6h.
          operationId: activitylistrepoevents
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    type: array
                    items:
                      $ref: '#/components/schemas/event'
                  examples:
                    200-response:
                      $ref: '#/components/examples/repo-events-items'
