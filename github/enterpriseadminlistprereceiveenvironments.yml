apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Pre-receive Environments
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Pre-receive Environments
      description: Needs a more full description created.
    paths:
      /admin/pre-receive-environments:
        get:
          summary: List Pre-receive Environments
          description: Needs a more full description created.
          operationId: enterpriseadminlistprereceiveenvironments
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    type: array
                    items:
                      $ref: '#/components/schemas/pre-receive-environment'
                  examples:
                    default:
                      $ref: '#/components/examples/pre-receive-environment-items'
