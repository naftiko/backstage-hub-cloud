apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Scim Provisioning Information For An Enterprise User
  description: >-
    **Note:** The SCIM API endpoints for enterprise accounts are currently in
    *private* beta and are subject to change.Gets information about a SCIM user.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Scim Provisioning Information For An Enterprise User
      description: >-
        **Note:** The SCIM API endpoints for enterprise accounts are currently in
        *private* beta and are subject to change.Gets information about a SCIM user.
    paths:
      /scim/v2/Users/{scim_user_id}:
        get:
          summary: Get Scim Provisioning Information For An Enterprise User
          description: >-
            **Note:** The SCIM API endpoints for enterprise accounts are currently
            in *private* beta and are subject to change.Gets information about a
            SCIM user.
          operationId: enterpriseadmingetprovisioninginformationforenterpriseuser
          responses:
            '500':
              $ref: '#/components/responses/scim_internal_error'
