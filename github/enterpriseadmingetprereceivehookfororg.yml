apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Pre-receive Hook For An Organization
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Pre-receive Hook For An Organization
      description: Needs a more full description created.
    paths:
      /orgs/{org}/pre-receive-hooks/{pre_receive_hook_id}:
        get:
          summary: Get A Pre-receive Hook For An Organization
          description: Needs a more full description created.
          operationId: enterpriseadmingetprereceivehookfororg
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/org-pre-receive-hook'
                  examples:
                    default:
                      $ref: '#/components/examples/org-pre-receive-hook'
