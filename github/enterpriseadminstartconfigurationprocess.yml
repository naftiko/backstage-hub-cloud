apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Start A Configuration Process
  description: >-
    This endpoint allows you to start a configuration process at any time for
    your updated settings to take effect:
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Start A Configuration Process
      description: >-
        This endpoint allows you to start a configuration process at any time for
        your updated settings to take effect:
    paths:
      /setup/api/configure:
        post:
          summary: Start A Configuration Process
          description: >-
            This endpoint allows you to start a configuration process at any time
            for your updated settings to take effect:
          operationId: enterpriseadminstartconfigurationprocess
          responses:
            '401':
              description: Unauthorized
