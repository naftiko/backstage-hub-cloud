apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Labels For A Self-hosted Runner For An Enterprise
  description: >-
    Lists all labels for a self-hosted runner configured in an enterprise.OAuth
    app tokens and personal access tokens (classic) need the
    `manage_runners:enterprise` scope to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Labels For A Self-hosted Runner For An Enterprise
      description: >-
        Lists all labels for a self-hosted runner configured in an enterprise.OAuth
        app tokens and personal access tokens (classic) need the
        `manage_runners:enterprise` scope to use this endpoint.
    paths:
      /enterprises/{enterprise}/actions/runners/{runner_id}/labels:
        get:
          summary: List Labels For A Self-hosted Runner For An Enterprise
          description: >-
            Lists all labels for a self-hosted runner configured in an
            enterprise.OAuth app tokens and personal access tokens (classic) need
            the `manage_runners:enterprise` scope to use this endpoint.
          operationId: enterpriseadminlistlabelsforselfhostedrunnerforenterprise
          responses:
            '404':
              $ref: '#/components/responses/not_found'
