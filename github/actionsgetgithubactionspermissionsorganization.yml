apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Github Actions Permissions For An Organization
  description: >-
    Gets the GitHub Actions permissions policy for repositories and allowed
    actions in an organization.You must authenticate using an access token with
    the `admin:org` scope to use this endpoint. GitHub Apps must have the
    `administration` organization permission to use this API.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Github Actions Permissions For An Organization
      description: >-
        Gets the GitHub Actions permissions policy for repositories and allowed
        actions in an organization.You must authenticate using an access token with
        the `admin:org` scope to use this endpoint. GitHub Apps must have the
        `administration` organization permission to use this API.
    paths:
      /orgs/{org}/actions/permissions:
        get:
          summary: Get Github Actions Permissions For An Organization
          description: >-
            Gets the GitHub Actions permissions policy for repositories and allowed
            actions in an organization.You must authenticate using an access token
            with the `admin:org` scope to use this endpoint. GitHub Apps must have
            the `administration` organization permission to use this API.
          operationId: actionsgetgithubactionspermissionsorganization
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/actions-organization-permissions'
                  examples:
                    default:
                      $ref: '#/components/examples/actions-organization-permissions'
