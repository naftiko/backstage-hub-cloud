apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Tag Protection State For A Repository
  description: >-
    This deletes a tag protection state for a repository.This endpoint is only
    available to repository administrators.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Tag Protection State For A Repository
      description: >-
        This deletes a tag protection state for a repository.This endpoint is only
        available to repository administrators.
    paths:
      /repos/{owner}/{repo}/tags/protection/{tag_protection_id}:
        delete:
          summary: Delete A Tag Protection State For A Repository
          description: >-
            This deletes a tag protection state for a repository.This endpoint is
            only available to repository administrators.
          operationId: deleteATagProtectionStateForARepository
          responses:
            '404':
              $ref: '#/components/responses/not_found'
