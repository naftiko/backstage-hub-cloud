apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Github Actions Cache Usage Policy For An Enterprise
  description: >-
    Gets the GitHub Actions cache usage policy for an enterprise.OAuth tokens
    and personal access tokens (classic) need the `admin:enterprise` scope to
    use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Github Actions Cache Usage Policy For An Enterprise
      description: >-
        Gets the GitHub Actions cache usage policy for an enterprise.OAuth tokens
        and personal access tokens (classic) need the `admin:enterprise` scope to
        use this endpoint.
    paths:
      /enterprises/{enterprise}/actions/cache/usage-policy:
        get:
          summary: Get Github Actions Cache Usage Policy For An Enterprise
          description: >-
            Gets the GitHub Actions cache usage policy for an enterprise.OAuth
            tokens and personal access tokens (classic) need the `admin:enterprise`
            scope to use this endpoint.
          operationId: actionsgetactionscacheusagepolicyforenterprise
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/actions-cache-usage-policy-enterprise'
                  examples:
                    default:
                      $ref: '#/components/examples/actions-cache-usage-policy-enterprise'
              headers:
                Link:
                  $ref: '#/components/headers/link'
