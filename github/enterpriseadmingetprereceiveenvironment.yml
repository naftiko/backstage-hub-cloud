apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Pre-receive Environment
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Pre-receive Environment
      description: Needs a more full description created.
    paths:
      /admin/pre-receive-environments/{pre_receive_environment_id}:
        get:
          summary: Get A Pre-receive Environment
          description: Needs a more full description created.
          operationId: enterpriseadmingetprereceiveenvironment
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/pre-receive-environment'
                  examples:
                    default:
                      $ref: '#/components/examples/pre-receive-environment'
