apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Ssh Signing Keys For The Authenticated User
  description: >-
    Lists the SSH signing keys for the authenticated user's GitHub account.OAuth
    app tokens and personal access tokens (classic) need the
    `read:ssh_signing_key` scope to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Ssh Signing Keys For The Authenticated User
      description: >-
        Lists the SSH signing keys for the authenticated user's GitHub account.OAuth
        app tokens and personal access tokens (classic) need the
        `read:ssh_signing_key` scope to use this endpoint.
    paths:
      /user/ssh_signing_keys:
        get:
          summary: List Ssh Signing Keys For The Authenticated User
          description: >-
            Lists the SSH signing keys for the authenticated user's GitHub
            account.OAuth app tokens and personal access tokens (classic) need the
            `read:ssh_signing_key` scope to use this endpoint.
          operationId: userslistsshsigningkeysforauthenticateduser
          responses:
            '404':
              $ref: '#/components/responses/not_found'
