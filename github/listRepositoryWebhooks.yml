apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Repository Webhooks
  description: >-
    Lists webhooks for a repository. `last response` may return null if there
    have not been any deliveries within 30 days.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Repository Webhooks
      description: >-
        Lists webhooks for a repository. `last response` may return null if there
        have not been any deliveries within 30 days.
    paths:
      /repos/{owner}/{repo}/hooks:
        get:
          summary: List Repository Webhooks
          description: >-
            Lists webhooks for a repository. `last response` may return null if
            there have not been any deliveries within 30 days.
          operationId: listRepositoryWebhooks
          responses:
            '404':
              $ref: '#/components/responses/not_found'
