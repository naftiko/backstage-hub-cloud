apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Pre-receive Hook
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Pre-receive Hook
      description: Needs a more full description created.
    paths:
      /admin/pre-receive-hooks/{pre_receive_hook_id}:
        get:
          summary: Get A Pre-receive Hook
          description: Needs a more full description created.
          operationId: enterpriseadmingetprereceivehook
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/pre-receive-hook'
                  examples:
                    default:
                      $ref: '#/components/examples/pre-receive-hook'
