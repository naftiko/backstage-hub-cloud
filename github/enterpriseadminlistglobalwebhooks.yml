apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Global Webhooks
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Global Webhooks
      description: Needs a more full description created.
    paths:
      /admin/hooks:
        get:
          summary: List Global Webhooks
          description: Needs a more full description created.
          operationId: enterpriseadminlistglobalwebhooks
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    type: array
                    items:
                      $ref: '#/components/schemas/global-hook'
                  examples:
                    default:
                      $ref: '#/components/examples/global-hook-items'
              headers:
                Link:
                  $ref: '#/components/headers/link'
