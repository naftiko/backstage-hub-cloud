apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Set The Customization Template For An Oidc Subject Claim For An Organization
  description: >-
    Creates or updates the customization template for an OpenID Connect (OIDC)
    subject claim.OAuth app tokens and personal access tokens (classic) need the
    `write:org` scope to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Set The Customization Template For An Oidc Subject Claim For An Organization
      description: >-
        Creates or updates the customization template for an OpenID Connect (OIDC)
        subject claim.OAuth app tokens and personal access tokens (classic) need the
        `write:org` scope to use this endpoint.
    paths:
      /orgs/{org}/actions/oidc/customization/sub:
        put:
          summary: >-
            Set The Customization Template For An Oidc Subject Claim For An
            Organization
          description: >-
            Creates or updates the customization template for an OpenID Connect
            (OIDC) subject claim.OAuth app tokens and personal access tokens
            (classic) need the `write:org` scope to use this endpoint.
          operationId: oidcupdateoidccustomsubtemplatefororg
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/oidc-custom-sub'
                examples:
                  default:
                    $ref: '#/components/examples/oidc-custom-sub'
          responses:
            '404':
              $ref: '#/components/responses/not_found'
