apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Github Enterprise Server Pages Site
  description: >-
    Gets information about a GitHub Enterprise Server Pages site.OAuth app
    tokens and personal access tokens (classic) need the `repo` scope to use
    this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Github Enterprise Server Pages Site
      description: >-
        Gets information about a GitHub Enterprise Server Pages site.OAuth app
        tokens and personal access tokens (classic) need the `repo` scope to use
        this endpoint.
    paths:
      /repos/{owner}/{repo}/pages:
        get:
          summary: Get A Github Enterprise Server Pages Site
          description: >-
            Gets information about a GitHub Enterprise Server Pages site.OAuth app
            tokens and personal access tokens (classic) need the `repo` scope to use
            this endpoint.
          operationId: getAGitHubEnterpriseServerPagesSite
          responses:
            '404':
              $ref: '#/components/responses/not_found'
