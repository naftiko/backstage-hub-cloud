apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Set Announcement Banner For Organization
  description: Sets the announcement banner to display for the organization.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Set Announcement Banner For Organization
      description: Sets the announcement banner to display for the organization.
    paths:
      /orgs/{org}/announcement:
        patch:
          summary: Set Announcement Banner For Organization
          description: Sets the announcement banner to display for the organization.
          operationId: announcementbannerssetannouncementbannerfororg
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/announcement'
                examples:
                  default:
                    $ref: '#/components/examples/announcement'
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/announcement-banner'
                  examples:
                    default:
                      $ref: '#/components/examples/announcement'
