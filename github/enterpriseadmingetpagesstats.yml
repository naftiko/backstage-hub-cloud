apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Pages Statistics
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Pages Statistics
      description: Needs a more full description created.
    paths:
      /enterprise/stats/pages:
        get:
          summary: Get Pages Statistics
          description: Needs a more full description created.
          operationId: enterpriseadmingetpagesstats
          responses:
            '200':
              description: Response
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/enterprise-page-overview'
                  examples:
                    default:
                      value:
                        total_pages: 20
