apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Remove Pre-receive Hook Enforcement For A Repository
  description: >-
    Deletes any overridden enforcement on this repository for the specified
    hook.Responds with effective values inherited from owner and/or global
    level.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Remove Pre-receive Hook Enforcement For A Repository
      description: >-
        Deletes any overridden enforcement on this repository for the specified
        hook.Responds with effective values inherited from owner and/or global
        level.
    paths:
      /repos/{owner}/{repo}/pre-receive-hooks/{pre_receive_hook_id}:
        delete:
          summary: Remove Pre-receive Hook Enforcement For A Repository
          description: >-
            Deletes any overridden enforcement on this repository for the specified
            hook.Responds with effective values inherited from owner and/or global
            level.
          operationId: removePreReceiveHookEnforcementForARepository
          responses:
            '200':
              description: >-
                Responds with effective values inherited from owner and/or global
                level.
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/repository-pre-receive-hook'
                  examples:
                    default:
                      $ref: '#/components/examples/repository-pre-receive-hook'
