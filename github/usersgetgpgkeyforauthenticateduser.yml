apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Gpg Key For The Authenticated User
  description: >-
    View extended details for a single GPG key.OAuth app tokens and personal
    access tokens (classic) need the `read:gpg_key` scope to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Gpg Key For The Authenticated User
      description: >-
        View extended details for a single GPG key.OAuth app tokens and personal
        access tokens (classic) need the `read:gpg_key` scope to use this endpoint.
    paths:
      /user/gpg_keys/{gpg_key_id}:
        get:
          summary: Get A Gpg Key For The Authenticated User
          description: >-
            View extended details for a single GPG key.OAuth app tokens and personal
            access tokens (classic) need the `read:gpg_key` scope to use this
            endpoint.
          operationId: usersgetgpgkeyforauthenticateduser
          responses:
            '404':
              $ref: '#/components/responses/not_found'
