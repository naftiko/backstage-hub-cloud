apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Deployment Branch Policy
  description: >-
    Deletes a deployment branch policy for an environment.OAuth app tokens and
    personal access tokens (classic) need the `repo` scope to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Deployment Branch Policy
      description: >-
        Deletes a deployment branch policy for an environment.OAuth app tokens and
        personal access tokens (classic) need the `repo` scope to use this endpoint.
    paths:
      /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}:
        delete:
          summary: Delete A Deployment Branch Policy
          description: >-
            Deletes a deployment branch policy for an environment.OAuth app tokens
            and personal access tokens (classic) need the `repo` scope to use this
            endpoint.
          operationId: reposdeletedeploymentbranchpolicy
          responses:
            '204':
              description: Response
