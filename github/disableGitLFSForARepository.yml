apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Disable Git Lfs For A Repository
  description: >-
    Disables Git LFS for a repository.OAuth app tokens and personal access
    tokens (classic) need the `admin:enterprise` scope to use this endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Disable Git Lfs For A Repository
      description: >-
        Disables Git LFS for a repository.OAuth app tokens and personal access
        tokens (classic) need the `admin:enterprise` scope to use this endpoint.
    paths:
      /repos/{owner}/{repo}/lfs:
        delete:
          summary: Disable Git Lfs For A Repository
          description: >-
            Disables Git LFS for a repository.OAuth app tokens and personal access
            tokens (classic) need the `admin:enterprise` scope to use this endpoint.
          operationId: disableGitLFSForARepository
          responses:
            '204':
              description: Response
