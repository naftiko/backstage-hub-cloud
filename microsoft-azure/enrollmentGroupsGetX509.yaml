apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-the-primary-or-secondary-x509-certificate-of-an-enrollment-group
  description: >-
    Get details about the primary or secondary x509 certificate of an enrollment
    group, if it exists.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get The Primary Or Secondary X509 Certificate Of An Enrollment Group
      description: >-
        Get details about the primary or secondary x509 certificate of an enrollment
        group, if it exists.
    paths:
      /enrollmentGroups/{enrollmentGroupId}/certificates/{entry}:
        get:
          summary: Get The Primary Or Secondary X509 Certificate Of An Enrollment Group
          description: >-
            Get details about the primary or secondary x509 certificate of an
            enrollment group, if it exists.
          operationId: enrollmentGroupsGetX509
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
