apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-weather-delete-data-jobid
  description: Get weather data delete job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Weather Delete-data Jobid
      description: Get weather data delete job.
    paths:
      /weather/delete-data/{jobId}:
        get:
          summary: Get Weather Delete-data Jobid
          description: Get weather data delete job.
          operationId: weatherGetDataDeleteJobDetails
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
