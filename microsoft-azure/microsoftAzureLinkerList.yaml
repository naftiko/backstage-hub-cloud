apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureLinkerList
  description: >-
    Returns list of Linkers which connects to the resource. which supports to
    config both application and target service during the resource provision.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Resourceuri Providers Microsoft Servicelinker Linkers
      description: >-
        Returns list of Linkers which connects to the resource. which supports to
        config both application and target service during the resource provision.
    paths:
      /{resourceUri}/providers/Microsoft.ServiceLinker/linkers:
        get:
          summary: Get Resourceuri Providers Microsoft Servicelinker Linkers
          description: >-
            Returns list of Linkers which connects to the resource. which supports
            to config both application and target service during the resource
            provision.
          operationId: microsoftAzureLinkerList
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: >-
                  ../../../../../common-types/resource-management/v3/types.json#/definitions/ErrorResponse
