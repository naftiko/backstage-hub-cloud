apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureJobListsparksessionjob
  description: List all spark sessions.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Livy Sessions
      description: List all spark sessions.
    paths:
      /livy/sessions:
        get:
          summary: Get Livy Sessions
          description: List all spark sessions.
          operationId: microsoftAzureJobListsparksessionjob
          responses:
            '200':
              description: Success
              schema:
                $ref: '#/definitions/SparkSessionCollection'
