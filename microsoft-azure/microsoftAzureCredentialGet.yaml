apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureCredentialGet
  description: Gets credential information
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Credentials Credentialname
      description: Gets credential information
    paths:
      /credentials/{credentialName}:
        get:
          summary: Get Credentials Credentialname
          description: Gets credential information
          operationId: microsoftAzureCredentialGet
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
