apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: update-the-loads-of-provided-partitions-for-specific-metrics
  description: >-
    Updates the load value and predicted load value for all the partitions
    provided for specified metrics.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update The Loads Of Provided Partitions For Specific Metrics.
      description: >-
        Updates the load value and predicted load value for all the partitions
        provided for specified metrics.
    paths:
      /$/UpdatePartitionLoad:
        post:
          summary: Update The Loads Of Provided Partitions For Specific Metrics.
          description: >-
            Updates the load value and predicted load value for all the partitions
            provided for specified metrics.
          operationId: updatePartitionLoad
          responses:
            default:
              description: The error code response per specified partition.
              schema:
                $ref: '#/definitions/FabricError'
