apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: deletes-a-worker-and-all-of-its-traces
  description: Deletes a worker and all of its traces.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deletes A Worker And All Of Its Traces.
      description: Deletes a worker and all of its traces.
    paths:
      /routing/workers/{workerId}:
        delete:
          summary: Deletes A Worker And All Of Its Traces.
          description: Deletes a worker and all of its traces.
          operationId: jobRouterOperationsDeleteWorker
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
