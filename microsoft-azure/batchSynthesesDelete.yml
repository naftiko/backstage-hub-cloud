apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Batchsyntheses Id
  description: Deletes the batch synthesis identified by the given ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Batchsyntheses Id
      description: Deletes the batch synthesis identified by the given ID.
    paths:
      /batchsyntheses/{id}:
        delete:
          summary: Delete Batchsyntheses Id
          description: Deletes the batch synthesis identified by the given ID.
          operationId: batchSynthesesDelete
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code
