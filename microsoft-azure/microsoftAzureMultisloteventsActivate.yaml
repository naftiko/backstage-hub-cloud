apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureMultisloteventsActivate
  description: >-
    Report that the specified event was actually used or displayed to the user
    and a rewards should be expected for it.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Activate Multi Slot Event '
      description: >-
        Report that the specified event was actually used or displayed to the user
        and a rewards should be expected for it.
    paths:
      /multislot/events/{eventId}/activate:
        post:
          summary: 'Activate Multi Slot Event '
          description: >-
            Report that the specified event was actually used or displayed to the
            user and a rewards should be expected for it.
          operationId: microsoftAzureMultisloteventsActivate
          responses:
            default:
              description: Invalid activate event request.
              schema:
                $ref: '#/definitions/ErrorResponse'
