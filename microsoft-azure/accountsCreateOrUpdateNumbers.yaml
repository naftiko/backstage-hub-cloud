apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: create-or-update-numbers
  description: Create or update up to 100 Numbers on the specified account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Or Update Numbers.
      description: Create or update up to 100 Numbers on the specified account.
    paths:
      /accounts/{accountName}:createOrUpdateNumbers:
        post:
          summary: Create Or Update Numbers.
          description: Create or update up to 100 Numbers on the specified account.
          operationId: accountsCreateOrUpdateNumbers
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
