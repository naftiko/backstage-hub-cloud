apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: put-dataconnections-dataconnectionname
  description: Create or replace a data connection with a given dataConnectionName.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Dataconnections Dataconnectionname
      description: Create or replace a data connection with a given dataConnectionName.
    paths:
      /dataConnections/{dataConnectionName}:
        put:
          summary: Put Dataconnections Dataconnectionname
          description: Create or replace a data connection with a given dataConnectionName.
          operationId: dataConnectionsCreateOrReplaceDataConnection
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
