apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Retrieves A Queue's Statistics.
  description: Retrieves a queue's statistics.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieves A Queue's Statistics.
      description: Retrieves a queue's statistics.
    paths:
      /routing/queues/{queueId}/statistics:
        get:
          summary: Retrieves A Queue's Statistics.
          description: Retrieves a queue's statistics.
          operationId: jobRouterOperationsGetQueueStatistics
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
