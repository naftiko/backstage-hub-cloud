apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-specific-schema-versions
  description: Gets one specific version of one schema.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Specific Schema Versions.
      description: Gets one specific version of one schema.
    paths:
      /$schemaGroups/{groupName}/schemas/{schemaName}/versions/{schemaVersion}:
        get:
          summary: Get Specific Schema Versions.
          description: Gets one specific version of one schema.
          operationId: getSchemaByVersion
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
