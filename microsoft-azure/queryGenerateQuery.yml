apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Providers Microsoft.resourcegraph Generatequery
  description: Generates ARG query for a given user prompt.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Providers Microsoft.resourcegraph Generatequery
      description: Generates ARG query for a given user prompt.
    paths:
      /providers/Microsoft.ResourceGraph/generateQuery:
        post:
          summary: Post Providers Microsoft.resourcegraph Generatequery
          description: Generates ARG query for a given user prompt.
          operationId: queryGenerateQuery
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/ErrorResponse'
