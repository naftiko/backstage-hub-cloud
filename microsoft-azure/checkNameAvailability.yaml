apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-providers-microsoftmanagement-checknameavailability
  description: Checks if the specified management group name is valid and unique
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Providers Microsoft.management Checknameavailability
      description: Checks if the specified management group name is valid and unique
    paths:
      /providers/Microsoft.Management/checkNameAvailability:
        post:
          summary: Post Providers Microsoft.management Checknameavailability
          description: Checks if the specified management group name is valid and unique
          operationId: checkNameAvailability
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
