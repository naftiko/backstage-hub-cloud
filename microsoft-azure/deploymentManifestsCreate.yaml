apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: create-a-new-deployment-manifest
  description: Create a new deployment manifest.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A New Deployment Manifest.
      description: Create a new deployment manifest.
    paths:
      /deploymentManifests/{deploymentManifestId}:
        put:
          summary: Create A New Deployment Manifest.
          description: Create a new deployment manifest.
          operationId: deploymentManifestsCreate
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
