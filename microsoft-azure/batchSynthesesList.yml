apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Batchsyntheses
  description: Gets the list of batch syntheses.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Batchsyntheses
      description: Gets the list of batch syntheses.
    paths:
      /batchsyntheses:
        get:
          summary: Get Batchsyntheses
          description: Gets the list of batch syntheses.
          operationId: batchSynthesesList
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code
