apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Detect Change Point For The Entire Series
  description: Evaluate the change point score of every series point.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Detect Change Point For The Entire Series
      description: Evaluate the change point score of every series point.
    paths:
      /timeseries/changepoint/detect:
        post:
          summary: Detect Change Point For The Entire Series
          description: Evaluate the change point score of every series point.
          operationId: univariateDetectUnivariateChangePoint
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Univariate.AnomalyDetectorError'
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code.
