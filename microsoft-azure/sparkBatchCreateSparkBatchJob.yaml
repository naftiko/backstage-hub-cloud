apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-batches
  description: Create new spark batch job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Batches
      description: Create new spark batch job.
    paths:
      /batches:
        post:
          summary: Post Batches
          description: Create new spark batch job.
          operationId: sparkBatchCreateSparkBatchJob
          responses:
            '200':
              description: Success
              schema:
                $ref: '#/definitions/SparkBatchJob'
