apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-personalvoices-id
  description: Creates a new personal voice with audio files in the client.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Personalvoices Id
      description: Creates a new personal voice with audio files in the client.
    paths:
      /personalvoices/{id}:
        post:
          summary: Post Personalvoices Id
          description: Creates a new personal voice with audio files in the client.
          operationId: personalVoicesPost
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
