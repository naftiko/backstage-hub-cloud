apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: associate-app-component-a-azure-resource-model-to-a-test-model-or-test-run
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Associate App Component (a Azure Resource Model) To A Test Model Or Test Run
      description: Needs a more full description created.
    paths:
      /appcomponents/{name}:
        patch:
          summary: >-
            Associate App Component (a Azure Resource Model) To A Test Model Or Test
            Run
          description: Needs a more full description created.
          operationId: appComponentCreateOrUpdateAppComponents
          responses:
            default:
              description: >-
                ***Error Responses: ***

                 * "400", "InvalidHostOrResourceName", "Host name or domain is not valid" .

                 * "400", "RequiredAppComponentName", "name field is required"

                 * "400", "RequiredTestOrTestRun", "Either test name or test run name is required"

                 * "400", "InvalidAppComponentName", "name field should be valid"

                 * "400", "InvalidValue",  "value field is either null or not a valid map".

                 * "404", "TestRunNotFound", "test run not found with given name"

                 * "404", "TestNotFound", "Test not found with given name"

                * "500", "InternalServerError", "Unable to process the request,
                please retry"
              schema:
                $ref: '#/definitions/DefaultErrorResponseBody'
              headers:
                x-ms-error-code:
                  description: The error code for specific error that occurred.
                  type: string
