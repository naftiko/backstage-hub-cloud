apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureScanresultCancelscan
  description: Cancels a scan
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Datasources Datasourcename Scans Scanname Runs Runid :cancel
      description: Cancels a scan
    paths:
      /datasources/{dataSourceName}/scans/{scanName}/runs/{runId}/:cancel:
        post:
          summary: Post Datasources Datasourcename Scans Scanname Runs Runid :cancel
          description: Cancels a scan
          operationId: microsoftAzureScanresultCancelscan
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
