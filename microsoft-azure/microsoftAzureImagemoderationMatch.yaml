apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureImagemoderationMatch
  description: >-
    Fuzzily match an image against one of your custom image lists. You can
    create and manage your custom image lists by using this API. <br><br>Returns
    the ID and tags of a matching image.<br><br>Note: You must refresh the index
    on the corresponding image list to ensure that additions and removals are
    reflected in the response.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Contentmoderator Moderate 0 Processimage Match
      description: >-
        Fuzzily match an image against one of your custom image lists. You can
        create and manage your custom image lists by using this API. <br><br>Returns
        the ID and tags of a matching image.<br><br>Note: You must refresh the index
        on the corresponding image list to ensure that additions and removals are
        reflected in the response.
    paths:
      /contentmoderator/moderate/v1.0/ProcessImage/Match:
        post:
          summary: Post Contentmoderator Moderate 0 Processimage Match
          description: >-
            Fuzzily match an image against one of your custom image lists. You can
            create and manage your custom image lists by using this API.
            <br><br>Returns the ID and tags of a matching image.<br><br>Note: You
            must refresh the index on the corresponding image list to ensure that
            additions and removals are reflected in the response.
          operationId: microsoftAzureImagemoderationMatch
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/APIError'
