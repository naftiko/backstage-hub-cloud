apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-datasets-datasetid-records
  description: Returns a paginated list of records.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Datasets Datasetid Records
      description: Returns a paginated list of records.
    paths:
      /datasets/{datasetId}/records:
        get:
          summary: Get Datasets Datasetid Records
          description: Returns a paginated list of records.
          operationId: datasetRecordsList
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
