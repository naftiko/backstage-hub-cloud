apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Workflowtasks Taskid Change-task-status
  description: Update the status of a workflow task request.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Workflowtasks Taskid Change-task-status
      description: Update the status of a workflow task request.
    paths:
      /workflowtasks/{taskId}/change-task-status:
        post:
          summary: Post Workflowtasks Taskid Change-task-status
          description: Update the status of a workflow task request.
          operationId: updateTaskStatus
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
