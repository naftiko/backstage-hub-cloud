apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Sites On The Hostingenvironment (app Service Environment).
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Sites On The Hostingenvironment (app Service Environment).
      description: Needs a more full description created.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Web/hostingEnvironments/{name}/sites:
        get:
          summary: Get All Sites On The Hostingenvironment (app Service Environment).
          description: Needs a more full description created.
          operationId: hostingEnvironmentsGetHostingEnvironmentSites
          responses:
            '200':
              description: OK
              schema:
                $ref: '#/definitions/SiteCollection'
