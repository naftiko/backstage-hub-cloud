apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Livy Sessions
  description: Create a new spark session.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Livy Sessions
      description: Create a new spark session.
    paths:
      /livy/sessions:
        post:
          summary: Post Livy Sessions
          description: Create a new spark session.
          operationId: jobSubmitSparkSessionJob
          responses:
            '201':
              description: Created
              schema:
                $ref: '#/definitions/SparkSessionJob'
