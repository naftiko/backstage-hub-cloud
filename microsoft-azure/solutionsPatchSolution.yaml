apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: update-solution
  description: >-
    Update a solution with specified name. Supports partial updates, for example
    only tags can be provided.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update Solution.
      description: >-
        Update a solution with specified name. Supports partial updates, for example
        only tags can be provided.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/migrateProjects/{migrateProjectName}/solutions/{solutionName}:
        patch:
          summary: Update Solution.
          description: >-
            Update a solution with specified name. Supports partial updates, for
            example only tags can be provided.
          operationId: solutionsPatchSolution
          responses:
            '200':
              description: OK
              schema:
                $ref: '#/definitions/Solution'
