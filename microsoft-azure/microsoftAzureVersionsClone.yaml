apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureVersionsClone
  description: Creates a new version from the selected version.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Apps Appid Versions Versionid Clone
      description: Creates a new version from the selected version.
    paths:
      /apps/{appId}/versions/{versionId}/clone:
        post:
          summary: Post Apps Appid Versions Versionid Clone
          description: Creates a new version from the selected version.
          operationId: microsoftAzureVersionsClone
          responses:
            default:
              description: Error Response.
              schema:
                $ref: '#/definitions/ErrorResponse'
