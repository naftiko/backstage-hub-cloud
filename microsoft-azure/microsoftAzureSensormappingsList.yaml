apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureSensormappingsList
  description: Returns a paginated list of sensor mapping resources.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Sensor Mappings
      description: Returns a paginated list of sensor mapping resources.
    paths:
      /sensor-mappings:
        get:
          summary: Get Sensor Mappings
          description: Returns a paginated list of sensor mapping resources.
          operationId: microsoftAzureSensormappingsList
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
