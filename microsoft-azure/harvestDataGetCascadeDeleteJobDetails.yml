apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Harvest-data Cascade-delete Jobid
  description: Get cascade delete job for harvest data resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Harvest-data Cascade-delete Jobid
      description: Get cascade delete job for harvest data resource.
    paths:
      /harvest-data/cascade-delete/{jobId}:
        get:
          summary: Get Harvest-data Cascade-delete Jobid
          description: Get cascade delete job for harvest data resource.
          operationId: harvestDataGetCascadeDeleteJobDetails
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
