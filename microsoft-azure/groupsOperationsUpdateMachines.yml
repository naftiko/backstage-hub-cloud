apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update Machines In Group.
  description: Update machines in group by adding or removing machines.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update Machines In Group.
      description: Update machines in group by adding or removing machines.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/assessmentProjects/{projectName}/groups/{groupName}/updateMachines:
        post:
          summary: Update Machines In Group.
          description: Update machines in group by adding or removing machines.
          operationId: groupsOperationsUpdateMachines
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: >-
                  ../../../../../../common-types/resource-management/v5/types.json#/definitions/ErrorResponse
