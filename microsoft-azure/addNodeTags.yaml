apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: adds-the-list-of-tags-on-the-specified-node
  description: This api allows adding tags to the specified node.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Adds The List Of Tags On The Specified Node.
      description: This api allows adding tags to the specified node.
    paths:
      /Nodes/{nodeName}/$/AddNodeTags:
        post:
          summary: Adds The List Of Tags On The Specified Node.
          description: This api allows adding tags to the specified node.
          operationId: addNodeTags
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
