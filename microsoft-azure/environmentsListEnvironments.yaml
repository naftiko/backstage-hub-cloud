apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-projects-projectname-environments
  description: Lists the environments for a project.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Projects Projectname Environments
      description: Lists the environments for a project.
    paths:
      /projects/{projectName}/environments:
        get:
          summary: Get Projects Projectname Environments
          description: Lists the environments for a project.
          operationId: environmentsListEnvironments
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: devcenter.json#/definitions/CloudError
              headers:
                x-ms-error-code:
                  description: The error code for specific error that occurred.
                  type: string
