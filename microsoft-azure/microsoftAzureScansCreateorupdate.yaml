apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureScansCreateorupdate
  description: Creates an instance of a scan
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Datasources Datasourcename Scans Scanname
      description: Creates an instance of a scan
    paths:
      /datasources/{dataSourceName}/scans/{scanName}:
        put:
          summary: Put Datasources Datasourcename Scans Scanname
          description: Creates an instance of a scan
          operationId: microsoftAzureScansCreateorupdate
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
