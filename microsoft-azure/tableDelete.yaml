apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-tablestable
  description: Operation permanently deletes the specified table.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Tables('table')
      description: Operation permanently deletes the specified table.
    paths:
      /Tables('{table}'):
        delete:
          summary: Delete Tables('table')
          description: Operation permanently deletes the specified table.
          operationId: tableDelete
          responses:
            default:
              description: Failure
              headers:
                x-ms-error-code:
                  x-ms-client-name: ErrorCode
                  type: string
              schema:
                $ref: '#/definitions/TableServiceError'
