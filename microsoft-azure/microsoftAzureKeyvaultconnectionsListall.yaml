apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureKeyvaultconnectionsListall
  description: List azureKeyVaults in account
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Azurekeyvaults
      description: List azureKeyVaults in account
    paths:
      /azureKeyVaults:
        get:
          summary: Get Azurekeyvaults
          description: List azureKeyVaults in account
          operationId: microsoftAzureKeyvaultconnectionsListall
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
