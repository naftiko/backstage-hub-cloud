apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: verify-profile
  description: >-
    Verifies existing profiles against input audio.Limitations:* Minimum audio
    input length is **1 second*** Maximum audio input length is **120 seconds***
    Minimum effective speech length (excluding silence and other "non-speech"
    frames) is **4 seconds**  This limitation can be disabled by setting
    "ignoreMinLength" to **true**.* Minimum audio Signal-to-noise ratio (SNR) is
    **0dB**
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Verify Profile
      description: >-
        Verifies existing profiles against input audio.Limitations:* Minimum audio
        input length is **1 second*** Maximum audio input length is **120 seconds***
        Minimum effective speech length (excluding silence and other "non-speech"
        frames) is **4 seconds**  This limitation can be disabled by setting
        "ignoreMinLength" to **true**.* Minimum audio Signal-to-noise ratio (SNR) is
        **0dB**
    paths:
      /text-independent/profiles/{profileId}/verify:
        post:
          summary: Verify Profile
          description: >-
            Verifies existing profiles against input audio.Limitations:* Minimum
            audio input length is **1 second*** Maximum audio input length is **120
            seconds*** Minimum effective speech length (excluding silence and other
            "non-speech" frames) is **4 seconds**  This limitation can be disabled
            by setting "ignoreMinLength" to **true**.* Minimum audio Signal-to-noise
            ratio (SNR) is **0dB**
          operationId: textIndependentVerifyProfile
          responses:
            '500':
              $ref: '#/responses/ServerError'
