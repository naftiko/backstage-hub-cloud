apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Datasources Datasourcename Scans Scanname Triggers Default
  description: Deletes the trigger associated with the scan
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Datasources Datasourcename Scans Scanname Triggers Default
      description: Deletes the trigger associated with the scan
    paths:
      /datasources/{dataSourceName}/scans/{scanName}/triggers/default:
        delete:
          summary: Delete Datasources Datasourcename Scans Scanname Triggers Default
          description: Deletes the trigger associated with the scan
          operationId: triggersDeleteTrigger
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
