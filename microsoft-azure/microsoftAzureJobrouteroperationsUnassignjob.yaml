apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureJobrouteroperationsUnassignjob
  description: Unassign a job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Unassign A Job '
      description: Unassign a job.
    paths:
      /routing/jobs/{jobId}/assignments/{assignmentId}:unassign:
        post:
          summary: 'Unassign A Job '
          description: Unassign a job.
          operationId: microsoftAzureJobrouteroperationsUnassignjob
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
