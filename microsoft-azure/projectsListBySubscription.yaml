apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-all-migrate-projects
  description: Get all the migrate projects in the subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Migrate Projects.
      description: Get all the migrate projects in the subscription.
    paths:
      /subscriptions/{subscriptionId}/providers/Microsoft.Migrate/migrateProjects:
        get:
          summary: Get All Migrate Projects.
          description: Get all the migrate projects in the subscription.
          operationId: projectsListBySubscription
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
