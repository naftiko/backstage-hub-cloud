apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureSystemscanrulesetsGet
  description: Get a system scan ruleset for a data source
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Systemscanrulesets Datasources Datasourcetype
      description: Get a system scan ruleset for a data source
    paths:
      /systemScanRulesets/datasources/{dataSourceType}:
        get:
          summary: Get Systemscanrulesets Datasources Datasourcetype
          description: Get a system scan ruleset for a data source
          operationId: microsoftAzureSystemscanrulesetsGet
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
