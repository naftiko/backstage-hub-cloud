apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: verify-profile
  description: >-
    Verifies existing profiles against input audio.Limitations:* Minimum audio
    input length per request is **1 second*** Maximum audio input length per
    request is **10 seconds*** Minimum audio Signal-to-noise ratio (SNR) is **10
    dB**
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Verify Profile
      description: >-
        Verifies existing profiles against input audio.Limitations:* Minimum audio
        input length per request is **1 second*** Maximum audio input length per
        request is **10 seconds*** Minimum audio Signal-to-noise ratio (SNR) is **10
        dB**
    paths:
      /text-dependent/profiles/{profileId}/verify:
        post:
          summary: Verify Profile
          description: >-
            Verifies existing profiles against input audio.Limitations:* Minimum
            audio input length per request is **1 second*** Maximum audio input
            length per request is **10 seconds*** Minimum audio Signal-to-noise
            ratio (SNR) is **10 dB**
          operationId: textDependentVerifyProfile
          responses:
            '500':
              $ref: '#/responses/ServerError'
