apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: create-or-update-the-cluster-manager
  description: >-
    Create a new cluster manager or update properties of the cluster manager if
    it exists.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Or Update The Cluster Manager.
      description: >-
        Create a new cluster manager or update properties of the cluster manager if
        it exists.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.NetworkCloud/clusterManagers/{clusterManagerName}:
        put:
          summary: Create Or Update The Cluster Manager.
          description: >-
            Create a new cluster manager or update properties of the cluster manager
            if it exists.
          operationId: clusterManagersCreateOrUpdate
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: >-
                  ../../../../../common-types/resource-management/v5/types.json#/definitions/ErrorResponse
