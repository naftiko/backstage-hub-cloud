apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Implements List Virtual Machine Within Rg Method
  description: Returns list of virtual machine within resource group
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Implements List Virtual Machine Within Rg Method
      description: Returns list of virtual machine within resource group
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.VMwareCloudSimple/virtualMachines:
        get:
          summary: Implements List Virtual Machine Within Rg Method
          description: Returns list of virtual machine within resource group
          operationId: virtualMachinesListByResourceGroup
          responses:
            default:
              description: General Error
              schema:
                $ref: '#/definitions/CSRPError'
