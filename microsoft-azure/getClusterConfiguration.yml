apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get The Service Fabric Standalone Cluster Configuration.
  description: >-
    The cluster configuration contains properties of the cluster that include
    different node types on the cluster,security configurations, fault, and
    upgrade domain topologies, etc.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get The Service Fabric Standalone Cluster Configuration.
      description: >-
        The cluster configuration contains properties of the cluster that include
        different node types on the cluster,security configurations, fault, and
        upgrade domain topologies, etc.
    paths:
      /$/GetClusterConfiguration:
        get:
          summary: Get The Service Fabric Standalone Cluster Configuration.
          description: >-
            The cluster configuration contains properties of the cluster that
            include different node types on the cluster,security configurations,
            fault, and upgrade domain topologies, etc.
          operationId: getClusterConfiguration
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
