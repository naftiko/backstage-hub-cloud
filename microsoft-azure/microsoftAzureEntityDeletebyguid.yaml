apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureEntityDeletebyguid
  description: Delete an entity identified by its GUID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Atlas Entity Guid Guid
      description: Delete an entity identified by its GUID.
    paths:
      /atlas/v2/entity/guid/{guid}:
        delete:
          summary: Delete Atlas Entity Guid Guid
          description: Delete an entity identified by its GUID.
          operationId: microsoftAzureEntityDeletebyguid
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
