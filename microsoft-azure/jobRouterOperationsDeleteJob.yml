apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Deletes A Job And All Of Its Traces.
  description: Deletes a job and all of its traces.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deletes A Job And All Of Its Traces.
      description: Deletes a job and all of its traces.
    paths:
      /routing/jobs/{jobId}:
        delete:
          summary: Deletes A Job And All Of Its Traces.
          description: Deletes a job and all of its traces.
          operationId: jobRouterOperationsDeleteJob
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
