apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-docs-searchindex
  description: Sends a batch of document write actions to the Azure Search index.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Docs Search.index
      description: Sends a batch of document write actions to the Azure Search index.
    paths:
      /docs/search.index:
        post:
          summary: Post Docs Search.index
          description: Sends a batch of document write actions to the Azure Search index.
          operationId: documentsIndex
          responses:
            '207':
              description: >-
                Response containing the status of operations for all actions in the
                batch.
              schema:
                $ref: '#/definitions/DocumentIndexResult'
