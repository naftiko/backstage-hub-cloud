apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Authoring Analyze-conversations Projects Projectname :export
  description: Triggers a job to export a project's data.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Authoring Analyze-conversations Projects Projectname :export
      description: Triggers a job to export a project's data.
    paths:
      /authoring/analyze-conversations/projects/{projectName}/:export:
        post:
          summary: Post Authoring Analyze-conversations Projects Projectname :export
          description: Triggers a job to export a project's data.
          operationId: conversationalAnalysisAuthoringExport
          responses:
            default:
              description: Error response.
              schema:
                $ref: common.json#/definitions/ErrorResponse
