apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-atlas-types-typedef-name-name
  description: Delete API for type identified by its name.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Atlas Types Typedef Name Name
      description: Delete API for type identified by its name.
    paths:
      /atlas/v2/types/typedef/name/{name}:
        delete:
          summary: Delete Atlas Types Typedef Name Name
          description: Delete API for type identified by its name.
          operationId: typesDeleteTypeByName
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
