apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-documentmodelscompose
  description: >-
    Creates a new document model from document types of existing document
    models.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Documentmodels:compose
      description: >-
        Creates a new document model from document types of existing document
        models.
    paths:
      /documentModels:compose:
        post:
          summary: Post Documentmodels:compose
          description: >-
            Creates a new document model from document types of existing document
            models.
          operationId: documentModelsComposeModel
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/ErrorResponse'
