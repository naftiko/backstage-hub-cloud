apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureAccesstokensCreateaccesstoken
  description: Creates an access-token with given access-token id in guid format.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Accounts Accountid Access Tokens Accesstokenid
      description: Creates an access-token with given access-token id in guid format.
    paths:
      /accounts/{accountId}/access-tokens/{accessTokenId}:
        put:
          summary: Put Accounts Accountid Access Tokens Accesstokenid
          description: Creates an access-token with given access-token id in guid format.
          operationId: microsoftAzureAccesstokensCreateaccesstoken
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
