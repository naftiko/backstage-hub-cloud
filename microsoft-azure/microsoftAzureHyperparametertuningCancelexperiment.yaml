apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureHyperparametertuningCancelexperiment
  description: Cancel a HyperDrive Experiment.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Cancel An Experiment '
      description: Cancel a HyperDrive Experiment.
    paths:
      /hyperdrive/v1.0/{armScope}/runs/{runId}/cancel:
        post:
          summary: 'Cancel An Experiment '
          description: Cancel a HyperDrive Experiment.
          operationId: microsoftAzureHyperparametertuningCancelexperiment
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/HyperDriveErrorResponse'
