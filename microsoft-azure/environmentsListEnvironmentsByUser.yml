apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Projects Projectname Users Userid Environments
  description: Lists the environments for a project and user.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Projects Projectname Users Userid Environments
      description: Lists the environments for a project and user.
    paths:
      /projects/{projectName}/users/{userId}/environments:
        get:
          summary: Get Projects Projectname Users Userid Environments
          description: Lists the environments for a project and user.
          operationId: environmentsListEnvironmentsByUser
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: devcenter.json#/definitions/CloudError
              headers:
                x-ms-error-code:
                  description: The error code for specific error that occurred.
                  type: string
