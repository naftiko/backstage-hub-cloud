apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: deletes-the-migrateagent
  description: Deletes the modernizeProject agent.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deletes The Migrateagent.
      description: Deletes the modernizeProject agent.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/migrateAgents/{agentName}:
        delete:
          summary: Deletes The Migrateagent.
          description: Deletes the modernizeProject agent.
          operationId: migrateAgentDelete
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
