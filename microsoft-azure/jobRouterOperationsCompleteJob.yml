apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Completes An Assigned Job.
  description: Completes an assigned job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Completes An Assigned Job.
      description: Completes an assigned job.
    paths:
      /routing/jobs/{jobId}/assignments/{assignmentId}:complete:
        post:
          summary: Completes An Assigned Job.
          description: Completes an assigned job.
          operationId: jobRouterOperationsCompleteJob
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
