apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: use-to-check-the-status-of-the-style-creation-process
  description: >-
    The `Get Operation` API is an HTTP `GET` request used to check the status of
    the style creation process and retrieve the `styletId`.### Submit Operations
    RequestTo view the current progress of a style operation, you will use a
    `GET` request where the `operationId` given the path is the ID that
    represents the operation.### Operation ResponseWhile in progress, a `200-OK`
    http status code is returned with no extra headers. If the operation
    succeeds, a `200-OK` http status code is returned with the
    `Resource-Location` in the header.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Use To Check The Status Of The Style Creation Process.
      description: >-
        The `Get Operation` API is an HTTP `GET` request used to check the status of
        the style creation process and retrieve the `styletId`.### Submit Operations
        RequestTo view the current progress of a style operation, you will use a
        `GET` request where the `operationId` given the path is the ID that
        represents the operation.### Operation ResponseWhile in progress, a `200-OK`
        http status code is returned with no extra headers. If the operation
        succeeds, a `200-OK` http status code is returned with the
        `Resource-Location` in the header.
    paths:
      /styles/operations/{operationId}:
        get:
          summary: Use To Check The Status Of The Style Creation Process.
          description: >-
            The `Get Operation` API is an HTTP `GET` request used to check the
            status of the style creation process and retrieve the `styletId`.###
            Submit Operations RequestTo view the current progress of a style
            operation, you will use a `GET` request where the `operationId` given
            the path is the ID that represents the operation.### Operation
            ResponseWhile in progress, a `200-OK` http status code is returned with
            no extra headers. If the operation succeeds, a `200-OK` http status code
            is returned with the `Resource-Location` in the header.
          operationId: styleGetOperation
          responses:
            default:
              $ref: ../../../Common/preview/1.0/common.json#/responses/default
