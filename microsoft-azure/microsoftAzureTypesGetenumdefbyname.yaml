apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureTypesGetenumdefbyname
  description: Get the enum definition by its name (unique).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Atlas Types Enumdef Name Name
      description: Get the enum definition by its name (unique).
    paths:
      /atlas/v2/types/enumdef/name/{name}:
        get:
          summary: Get Atlas Types Enumdef Name Name
          description: Get the enum definition by its name (unique).
          operationId: microsoftAzureTypesGetenumdefbyname
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
