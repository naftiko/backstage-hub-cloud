apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureHarvestdataListbypartyid
  description: Returns a paginated list of harvest data resources under a particular farm.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Parties Partyid Harvest Data
      description: Returns a paginated list of harvest data resources under a particular farm.
    paths:
      /parties/{partyId}/harvest-data:
        get:
          summary: Get Parties Partyid Harvest Data
          description: >-
            Returns a paginated list of harvest data resources under a particular
            farm.
          operationId: microsoftAzureHarvestdataListbypartyid
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
