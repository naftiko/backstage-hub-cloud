apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: gets-the-workload-deployment-operation-status
  description: Tracks the results of an asynchronous operation on the workload deployment.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Gets The Workload Deployment Operation Status.
      description: Tracks the results of an asynchronous operation on the workload deployment.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments/{workloadDeploymentName}/operations/{operationId}:
        get:
          summary: Gets The Workload Deployment Operation Status.
          description: >-
            Tracks the results of an asynchronous operation on the workload
            deployment.
          operationId: workloadDeploymentOperationStatusGet
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
