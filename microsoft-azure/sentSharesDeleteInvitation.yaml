apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-invitation-in-a-share
  description: Delete a sent share invitation
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Invitation In A Share.
      description: Delete a sent share invitation
    paths:
      /sentShares/{sentShareId}/sentShareInvitations/{sentShareInvitationId}:
        delete:
          summary: Delete Invitation In A Share.
          description: Delete a sent share invitation
          operationId: sentSharesDeleteInvitation
          responses:
            default:
              description: Default error response.
              schema:
                $ref: '#/definitions/PurviewShareError'
              headers:
                x-ms-error-code:
                  description: The error code
                  type: string
