apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: implements-get-virtualmachinetemplates-in-a-subscription
  description: List of VirtualMachineTemplates in a subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Implements Get Virtualmachinetemplates In A Subscription.
      description: List of VirtualMachineTemplates in a subscription.
    paths:
      /subscriptions/{subscriptionId}/providers/Microsoft.ScVmm/virtualMachineTemplates:
        get:
          summary: Implements Get Virtualmachinetemplates In A Subscription.
          description: List of VirtualMachineTemplates in a subscription.
          operationId: virtualMachineTemplatesListBySubscription
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
