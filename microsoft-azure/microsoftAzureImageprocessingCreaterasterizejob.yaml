apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureImageprocessingCreaterasterizejob
  description: Create a ImageProcessing Rasterize job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Image Processing Rasterize Jobid
      description: Create a ImageProcessing Rasterize job.
    paths:
      /image-processing/rasterize/{jobId}:
        put:
          summary: Put Image Processing Rasterize Jobid
          description: Create a ImageProcessing Rasterize job.
          operationId: microsoftAzureImageprocessingCreaterasterizejob
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
