apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureEnvironmentsCreateorreplaceenvironment
  description: Creates or updates an environment.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Projects Projectname Users Userid Environments Environmentname
      description: Creates or updates an environment.
    paths:
      /projects/{projectName}/users/{userId}/environments/{environmentName}:
        put:
          summary: Put Projects Projectname Users Userid Environments Environmentname
          description: Creates or updates an environment.
          operationId: microsoftAzureEnvironmentsCreateorreplaceenvironment
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: devcenter.json#/definitions/CloudError
              headers:
                x-ms-error-code:
                  description: The error code for specific error that occurred.
                  type: string
