apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-gov-members-proposals-proposalid-actions
  description: Returns actions contained in a proposal.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Gov Members Proposals Proposalid Actions
      description: Returns actions contained in a proposal.
    paths:
      /gov/members/proposals/{proposalId}/actions:
        get:
          summary: Get Gov Members Proposals Proposalid Actions
          description: Returns actions contained in a proposal.
          operationId: proposalsGetActions
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
