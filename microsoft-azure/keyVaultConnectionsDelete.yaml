apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-azurekeyvaults-azurekeyvaultname
  description: Deletes the azureKeyVault associated with the account
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Azurekeyvaults Azurekeyvaultname
      description: Deletes the azureKeyVault associated with the account
    paths:
      /azureKeyVaults/{azureKeyVaultName}:
        delete:
          summary: Delete Azurekeyvaults Azurekeyvaultname
          description: Deletes the azureKeyVault associated with the account
          operationId: keyVaultConnectionsDelete
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
