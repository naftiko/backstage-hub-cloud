apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Lists The Azure Dev Spaces Controllers In A Subscription.
  description: >-
    Lists all the Azure Dev Spaces Controllers with their properties in the
    subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Lists The Azure Dev Spaces Controllers In A Subscription.
      description: >-
        Lists all the Azure Dev Spaces Controllers with their properties in the
        subscription.
    paths:
      /subscriptions/{subscriptionId}/providers/Microsoft.DevSpaces/controllers:
        get:
          summary: Lists The Azure Dev Spaces Controllers In A Subscription.
          description: >-
            Lists all the Azure Dev Spaces Controllers with their properties in the
            subscription.
          operationId: controllersList
          responses:
            default:
              description: Error response describing the reason for operation failure.
              schema:
                $ref: '#/definitions/DevSpacesErrorResponse'
