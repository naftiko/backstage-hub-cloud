apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: list-all-certificates-of-given-nginx-deployment
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List All Certificates Of Given Nginx Deployment
      description: Needs a more full description created.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Nginx.NginxPlus/nginxDeployments/{deploymentName}/certificates:
        get:
          summary: List All Certificates Of Given Nginx Deployment
          description: Needs a more full description created.
          operationId: certificatesList
          responses:
            default:
              description: Default error response.
              schema:
                $ref: '#/definitions/ResourceProviderDefaultErrorResponse'
