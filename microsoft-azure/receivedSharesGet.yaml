apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-a-received-share-by-unique-id
  description: Get a received share
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Received Share By Unique Id.
      description: Get a received share
    paths:
      /receivedShares/{receivedShareId}:
        get:
          summary: Get A Received Share By Unique Id.
          description: Get a received share
          operationId: receivedSharesGet
          responses:
            default:
              description: Default error response.
              schema:
                $ref: '#/definitions/PurviewShareError'
              headers:
                x-ms-error-code:
                  description: The error code
                  type: string
