apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Tillage-data
  description: Returns a paginated list of tillage data resources across all parties.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Tillage-data
      description: Returns a paginated list of tillage data resources across all parties.
    paths:
      /tillage-data:
        get:
          summary: Get Tillage-data
          description: Returns a paginated list of tillage data resources across all parties.
          operationId: tillageDataList
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
