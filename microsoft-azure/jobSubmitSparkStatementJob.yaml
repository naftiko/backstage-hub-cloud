apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-livy-sessions-sessionid-statements
  description: Create a statement within a spark session.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Livy Sessions Sessionid Statements
      description: Create a statement within a spark session.
    paths:
      /livy/sessions/{sessionId}/statements:
        post:
          summary: Post Livy Sessions Sessionid Statements
          description: Create a statement within a spark session.
          operationId: jobSubmitSparkStatementJob
          responses:
            '201':
              description: Created
              schema:
                $ref: '#/definitions/SparkStatement'
