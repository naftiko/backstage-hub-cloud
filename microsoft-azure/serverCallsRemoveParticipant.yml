apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Remove Participant From The Call.
  description: Remove participant from the call.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Remove Participant From The Call.
      description: Remove participant from the call.
    paths:
      /calling/serverCalls/{serverCallId}/participants/{participantId}:
        delete:
          summary: Remove Participant From The Call.
          description: Remove participant from the call.
          operationId: serverCallsRemoveParticipant
          responses:
            '500':
              $ref: '#/responses/500'
