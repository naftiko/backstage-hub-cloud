apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Returns List Of Versions Within Sku
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Returns List Of Versions Within Sku
      description: Needs a more full description created.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DataBoxEdge/dataBoxEdgeDevices/{deviceName}/publishers/{publisherName}/offers/{offerName}/skus/{skuName}/versions:
        get:
          summary: Returns List Of Versions Within Sku
          description: Needs a more full description created.
          operationId: marketplaceImageVersionListVersionswithinSKU
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
