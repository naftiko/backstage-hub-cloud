apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-atlas-entity-bulk
  description: >-
    Delete a list of entities in bulk identified by their GUIDs or unique
    attributes.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Atlas Entity Bulk
      description: >-
        Delete a list of entities in bulk identified by their GUIDs or unique
        attributes.
    paths:
      /atlas/v2/entity/bulk:
        delete:
          summary: Delete Atlas Entity Bulk
          description: >-
            Delete a list of entities in bulk identified by their GUIDs or unique
            attributes.
          operationId: entityDeleteByGuids
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
