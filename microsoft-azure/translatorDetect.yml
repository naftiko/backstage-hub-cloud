apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Detect
  description: Identifies the language of a string of text.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Detect
      description: Identifies the language of a string of text.
    paths:
      /Detect:
        post:
          summary: Post Detect
          description: Identifies the language of a string of text.
          operationId: translatorDetect
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/ErrorMessage'
