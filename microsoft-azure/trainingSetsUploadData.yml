apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Trainingsets Id:upload
  description: Uploads data to the specified training set.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Trainingsets Id:upload
      description: Uploads data to the specified training set.
    paths:
      /trainingsets/{id}:upload:
        post:
          summary: Post Trainingsets Id:upload
          description: Uploads data to the specified training set.
          operationId: trainingSetsUploadData
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
