apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-a-domain-topic
  description: Get properties of a domain topic.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Domain Topic.
      description: Get properties of a domain topic.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.EventGrid/domains/{domainName}/topics/{domainTopicName}:
        get:
          summary: Get A Domain Topic.
          description: Get properties of a domain topic.
          operationId: domainTopicsGet
          responses:
            default:
              description: |-
                *** Error Responses: ***

                 * 400 Bad Request.

                 * 500 Internal Server Error.
