apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create And Start A New Test Run With The Given Test Run Id.
  description: Create and start a new test run with the given test run Id.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create And Start A New Test Run With The Given Test Run Id.
      description: Create and start a new test run with the given test run Id.
    paths:
      /test-runs/{testRunId}:
        patch:
          summary: Create And Start A New Test Run With The Given Test Run Id.
          description: Create and start a new test run with the given test run Id.
          operationId: loadTestRunCreateOrUpdateTestRun
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
