apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Sessions
  description: Create new spark session.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Sessions
      description: Create new spark session.
    paths:
      /sessions:
        post:
          summary: Post Sessions
          description: Create new spark session.
          operationId: sparkSessionCreateSparkSession
          responses:
            '200':
              description: Success
              schema:
                $ref: '#/definitions/SparkSession'
