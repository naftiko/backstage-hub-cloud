apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureIothubresourceCreateeventhubconsumergroup
  description: Add a consumer group to an Event Hub-compatible endpoint in an IoT hub.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Add A Consumer Group To An Event Hub Compatible Endpoint In An Iot Hub
      description: Add a consumer group to an Event Hub-compatible endpoint in an IoT hub.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Devices/IotHubs/{resourceName}/eventHubEndpoints/{eventHubEndpointName}/ConsumerGroups/{name}:
        put:
          summary: Add A Consumer Group To An Event Hub Compatible Endpoint In An Iot Hub
          description: Add a consumer group to an Event Hub-compatible endpoint in an IoT hub.
          operationId: microsoftAzureIothubresourceCreateeventhubconsumergroup
          responses:
            default:
              description: DefaultErrorResponse
              schema:
                $ref: '#/definitions/ErrorDetails'
