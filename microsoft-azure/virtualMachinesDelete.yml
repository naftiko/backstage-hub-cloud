apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Implements Virtual Machine Delete Method
  description: Delete virtual machine
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Implements Virtual Machine Delete Method
      description: Delete virtual machine
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.VMwareCloudSimple/virtualMachines/{virtualMachineName}:
        delete:
          summary: Implements Virtual Machine Delete Method
          description: Delete virtual machine
          operationId: virtualMachinesDelete
          responses:
            default:
              description: General Error
              schema:
                $ref: '#/definitions/CSRPError'
              headers:
                Content-Type:
                  type: string
                  default: application/json
