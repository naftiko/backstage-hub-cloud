apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-atlas-entity-bulk
  description: >-
    Create or update entities in Atlas in bulk.Existing entity is matched using
    its unique guid if supplied or by its unique attributes eg:
    qualifiedName.Map and array of collections are not well supported. E.g.,
    array>, array>.For each contact type, the maximum number of contacts is 20.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Atlas Entity Bulk
      description: >-
        Create or update entities in Atlas in bulk.Existing entity is matched using
        its unique guid if supplied or by its unique attributes eg:
        qualifiedName.Map and array of collections are not well supported. E.g.,
        array>, array>.For each contact type, the maximum number of contacts is 20.
    paths:
      /atlas/v2/entity/bulk:
        post:
          summary: Post Atlas Entity Bulk
          description: >-
            Create or update entities in Atlas in bulk.Existing entity is matched
            using its unique guid if supplied or by its unique attributes eg:
            qualifiedName.Map and array of collections are not well supported. E.g.,
            array>, array>.For each contact type, the maximum number of contacts is
            20.
          operationId: entityCreateOrUpdateEntities
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
