apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureImportjobsDelete
  description: >-
    Deletes an import job.<br>Status codes:<br>* 204 No Content<br>* 400 Bad
    Request<br>  * ValidationFailed - The import job request is not valid.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Jobs Imports Id
      description: >-
        Deletes an import job.<br>Status codes:<br>* 204 No Content<br>* 400 Bad
        Request<br>  * ValidationFailed - The import job request is not valid.
    paths:
      /jobs/imports/{id}:
        delete:
          summary: Delete Jobs Imports Id
          description: >-
            Deletes an import job.<br>Status codes:<br>* 204 No Content<br>* 400 Bad
            Request<br>  * ValidationFailed - The import job request is not valid.
          operationId: microsoftAzureImportjobsDelete
          responses:
            default:
              description: Default response.
              schema:
                $ref: '#/definitions/ErrorResponse'
