apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-sessions-sessionid-statements-statementid-cancel
  description: Kill a statement within a session.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Sessions Sessionid Statements Statementid Cancel
      description: Kill a statement within a session.
    paths:
      /sessions/{sessionId}/statements/{statementId}/cancel:
        post:
          summary: Post Sessions Sessionid Statements Statementid Cancel
          description: Kill a statement within a session.
          operationId: sparkSessionCancelSparkStatement
          responses:
            '200':
              description: Success
              schema:
                $ref: '#/definitions/SparkStatementCancellationResult'
