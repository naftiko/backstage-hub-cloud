apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Patient Timeline Job
  description: Creates a Patient Timeline job with the given request body.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Patient Timeline Job
      description: Creates a Patient Timeline job with the given request body.
    paths:
      /patient-timeline/jobs:
        post:
          summary: Create Patient Timeline Job
          description: Creates a Patient Timeline job with the given request body.
          operationId: patientTimelineCreateJob
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
