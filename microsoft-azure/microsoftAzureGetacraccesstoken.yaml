apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureGetacraccesstoken
  description: Exchange ACR Refresh token for an ACR Access Token
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Oauth2 Token
      description: Exchange ACR Refresh token for an ACR Access Token
    paths:
      /oauth2/token:
        post:
          summary: Post Oauth2 Token
          description: Exchange ACR Refresh token for an ACR Access Token
          operationId: microsoftAzureGetacraccesstoken
          responses:
            default:
              description: ACR error response describing why the operation failed.
              schema:
                $ref: '#/definitions/AcrErrors'
