apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: lists-the-modernizeprojects
  description: Gets the list of modernizeProjects in the given subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Lists The Modernizeprojects.
      description: Gets the list of modernizeProjects in the given subscription.
    paths:
      /subscriptions/{subscriptionId}/providers/Microsoft.Migrate/modernizeProjects:
        get:
          summary: Lists The Modernizeprojects.
          description: Gets the list of modernizeProjects in the given subscription.
          operationId: modernizeProjectListBySubscription
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
