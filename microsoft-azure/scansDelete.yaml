apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-datasources-datasourcename-scans-scanname
  description: Deletes the scan associated with the data source
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Datasources Datasourcename Scans Scanname
      description: Deletes the scan associated with the data source
    paths:
      /datasources/{dataSourceName}/scans/{scanName}:
        delete:
          summary: Delete Datasources Datasourcename Scans Scanname
          description: Deletes the scan associated with the data source
          operationId: scansDelete
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
