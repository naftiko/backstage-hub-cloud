apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Indexes('indexname') Search.stats
  description: >-
    Returns statistics for the given index, including a document count and
    storage usage.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Indexes('indexname') Search.stats
      description: >-
        Returns statistics for the given index, including a document count and
        storage usage.
    paths:
      /indexes('{indexName}')/search.stats:
        get:
          summary: Get Indexes('indexname') Search.stats
          description: >-
            Returns statistics for the given index, including a document count and
            storage usage.
          operationId: indexesGetStatistics
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
