apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Scenes Satellite Ingest-data Jobid
  description: Get a satellite data ingestion job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Scenes Satellite Ingest-data Jobid
      description: Get a satellite data ingestion job.
    paths:
      /scenes/satellite/ingest-data/{jobId}:
        get:
          summary: Get Scenes Satellite Ingest-data Jobid
          description: Get a satellite data ingestion job.
          operationId: scenesGetSatelliteDataIngestionJobDetails
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
