apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzurePublish
  description: >-
    Publish live metrics to the Live Metrics service when there is an active
    subscription to the metrics.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Quickpulseservice Svc Post
      description: >-
        Publish live metrics to the Live Metrics service when there is an active
        subscription to the metrics.
    paths:
      /QuickPulseService.svc/post:
        post:
          summary: Post Quickpulseservice Svc Post
          description: >-
            Publish live metrics to the Live Metrics service when there is an active
            subscription to the metrics.
          operationId: microsoftAzurePublish
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/ServiceError'
