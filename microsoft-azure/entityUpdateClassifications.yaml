apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: put-atlas-entity-guid-guid-classifications
  description: Update classifications to an existing entity represented by a guid.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Atlas Entity Guid Guid Classifications
      description: Update classifications to an existing entity represented by a guid.
    paths:
      /atlas/v2/entity/guid/{guid}/classifications:
        put:
          summary: Put Atlas Entity Guid Guid Classifications
          description: Update classifications to an existing entity represented by a guid.
          operationId: entityUpdateClassifications
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
