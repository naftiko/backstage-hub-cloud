apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureWebappsRestartslot
  description: Description for Restarts an app (or deployment slot, if specified).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Restarts An App Or Deployment Slot, If Specified '
      description: Description for Restarts an app (or deployment slot, if specified).
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Web/sites/{name}/slots/{slot}/restart:
        post:
          summary: 'Restarts An App Or Deployment Slot, If Specified '
          description: Description for Restarts an app (or deployment slot, if specified).
          operationId: microsoftAzureWebappsRestartslot
          responses:
            default:
              description: App Service error response.
              schema:
                $ref: ./CommonDefinitions.json#/definitions/DefaultErrorResponse
