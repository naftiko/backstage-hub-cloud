apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Gets Ledger Entries From A Collection Corresponding To A Range.
  description: >-
    A collection id may optionally be specified. Only entries in the specified
    (or default) collection will be returned.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Gets Ledger Entries From A Collection Corresponding To A Range.
      description: >-
        A collection id may optionally be specified. Only entries in the specified
        (or default) collection will be returned.
    paths:
      /app/transactions:
        get:
          summary: Gets Ledger Entries From A Collection Corresponding To A Range.
          description: >-
            A collection id may optionally be specified. Only entries in the
            specified (or default) collection will be returned.
          operationId: listLedgerEntries
          responses:
            default:
              description: Error response describing why the operation failed
              schema:
                $ref: common.json#/definitions/ConfidentialLedgerError
