apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Closes A Completed Job.
  description: Closes a completed job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Closes A Completed Job.
      description: Closes a completed job.
    paths:
      /routing/jobs/{jobId}/assignments/{assignmentId}:close:
        post:
          summary: Closes A Completed Job.
          description: Closes a completed job.
          operationId: jobRouterOperationsCloseJob
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
