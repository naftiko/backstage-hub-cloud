apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Teams Civic Address.
  description: Get details of a specific Teams Civic Address using its ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Teams Civic Address.
      description: Get details of a specific Teams Civic Address using its ID.
    paths:
      /accounts/{accountName}/teamsCivicAddresses/{civicAddressId}:
        get:
          summary: Get Teams Civic Address.
          description: Get details of a specific Teams Civic Address using its ID.
          operationId: accountsGetTeamsCivicAddress
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
