apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Adds A User Or Updates A User's Fields.
  description: A JSON merge patch is applied for existing users
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Adds A User Or Updates A User's Fields.
      description: A JSON merge patch is applied for existing users
    paths:
      /app/users/{userId}:
        patch:
          summary: Adds A User Or Updates A User's Fields.
          description: A JSON merge patch is applied for existing users
          operationId: createOrUpdateUser
          responses:
            default:
              description: Error response describing why the operation failed
              schema:
                $ref: common.json#/definitions/ConfidentialLedgerError
