apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-feature-importance
  description: Delete the Feature Importance associated with the Id.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Feature Importance.
      description: Delete the Feature Importance associated with the Id.
    paths:
      /featureImportances/{featureImportanceId}:
        delete:
          summary: Delete Feature Importance.
          description: Delete the Feature Importance associated with the Id.
          operationId: featureImportancesDelete
          responses:
            default:
              description: A generic error has occurred on the server.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
