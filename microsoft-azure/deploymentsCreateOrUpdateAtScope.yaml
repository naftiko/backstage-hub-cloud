apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: deploys-resources-at-a-given-scope
  description: >-
    You can provide the template and parameters directly in the request or link
    to JSON files.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deploys Resources At A Given Scope.
      description: >-
        You can provide the template and parameters directly in the request or link
        to JSON files.
    paths:
      /{scope}/providers/Microsoft.Resources/deployments/{deploymentName}:
        put:
          summary: Deploys Resources At A Given Scope.
          description: >-
            You can provide the template and parameters directly in the request or
            link to JSON files.
          operationId: deploymentsCreateOrUpdateAtScope
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
