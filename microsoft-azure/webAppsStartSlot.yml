apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Starts An App (or Deployment Slot, If Specified).
  description: Description for Starts an app (or deployment slot, if specified).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Starts An App (or Deployment Slot, If Specified).
      description: Description for Starts an app (or deployment slot, if specified).
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Web/sites/{name}/slots/{slot}/start:
        post:
          summary: Starts An App (or Deployment Slot, If Specified).
          description: Description for Starts an app (or deployment slot, if specified).
          operationId: webAppsStartSlot
          responses:
            default:
              description: App Service error response.
              schema:
                $ref: ./CommonDefinitions.json#/definitions/DefaultErrorResponse
