apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    delete-persongroups-persongroupid-persons-personid-persistedfaces-persistedfaceid
  description: >-
    Delete a face from a person in a person group by specified personGroupId,
    personId and persistedFaceId. Adding/deleting faces to/from a same person
    will be processed sequentially. Adding/deleting faces to/from different
    persons are processed in parallel.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Delete Persongroups Persongroupid Persons Personid Persistedfaces
        Persistedfaceid
      description: >-
        Delete a face from a person in a person group by specified personGroupId,
        personId and persistedFaceId. Adding/deleting faces to/from a same person
        will be processed sequentially. Adding/deleting faces to/from different
        persons are processed in parallel.
    paths:
      /persongroups/{personGroupId}/persons/{personId}/persistedfaces/{persistedFaceId}:
        delete:
          summary: >-
            Delete Persongroups Persongroupid Persons Personid Persistedfaces
            Persistedfaceid
          description: >-
            Delete a face from a person in a person group by specified
            personGroupId, personId and persistedFaceId. Adding/deleting faces
            to/from a same person will be processed sequentially. Adding/deleting
            faces to/from different persons are processed in parallel.
          operationId: personGroupPersonDeleteFace
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/APIError'
