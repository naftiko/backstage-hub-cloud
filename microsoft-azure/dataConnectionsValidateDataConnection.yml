apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Dataconnections:validate
  description: Validate a data connection with a given dataConnectionName.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Dataconnections:validate
      description: Validate a data connection with a given dataConnectionName.
    paths:
      /dataConnections:validate:
        post:
          summary: Post Dataconnections:validate
          description: Validate a data connection with a given dataConnectionName.
          operationId: dataConnectionsValidateDataConnection
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
