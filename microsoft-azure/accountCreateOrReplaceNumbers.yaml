apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-account-accountname-numbersbatch
  description: >-
    Adds or replaces between 1 and 100 numbers (inclusive) in the specified
    account. In the body, specify a list of phone numbers and and the
    corresponding details (for example, the services to enable) for each number.
    This operation is atomic; if any item fails, then all items fail.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Account Accountname Numbers:batch
      description: >-
        Adds or replaces between 1 and 100 numbers (inclusive) in the specified
        account. In the body, specify a list of phone numbers and and the
        corresponding details (for example, the services to enable) for each number.
        This operation is atomic; if any item fails, then all items fail.
    paths:
      /account/{accountName}/numbers:batch:
        post:
          summary: Post Account Accountname Numbers:batch
          description: >-
            Adds or replaces between 1 and 100 numbers (inclusive) in the specified
            account. In the body, specify a list of phone numbers and and the
            corresponding details (for example, the services to enable) for each
            number. This operation is atomic; if any item fails, then all items
            fail.
          operationId: accountCreateOrReplaceNumbers
          responses:
            default:
              description: An error response object.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
