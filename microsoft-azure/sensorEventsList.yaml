apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-sensor-events
  description: >-
    Returns a list of sensor events data. Time span for query is limited to 90
    days at a time.Returns last 90 days events when startDateTime and
    endDateTime are not provided.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Sensor-events
      description: >-
        Returns a list of sensor events data. Time span for query is limited to 90
        days at a time.Returns last 90 days events when startDateTime and
        endDateTime are not provided.
    paths:
      /sensor-events:
        get:
          summary: Get Sensor-events
          description: >-
            Returns a list of sensor events data. Time span for query is limited to
            90 days at a time.Returns last 90 days events when startDateTime and
            endDateTime are not provided.
          operationId: sensorEventsList
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
