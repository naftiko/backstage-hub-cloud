apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureDeletemanifest
  description: >-
    Delete the manifest identified by `name` and `reference`. Note that a
    manifest can _only_ be deleted by `digest`.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete  Name Manifests Reference
      description: >-
        Delete the manifest identified by `name` and `reference`. Note that a
        manifest can _only_ be deleted by `digest`.
    paths:
      /v2/{name}/manifests/{reference}:
        delete:
          summary: Delete  Name Manifests Reference
          description: >-
            Delete the manifest identified by `name` and `reference`. Note that a
            manifest can _only_ be deleted by `digest`.
          operationId: microsoftAzureDeletemanifest
          responses:
            default:
              description: ACR error response describing why the operation failed.
              schema:
                $ref: '#/definitions/AcrErrors'
