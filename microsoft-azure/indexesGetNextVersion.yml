apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Indexes Name:getnextversion
  description: Get next Index version.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Indexes Name:getnextversion
      description: Get next Index version.
    paths:
      /indexes/{name}:getNextVersion:
        post:
          summary: Post Indexes Name:getnextversion
          description: Get next Index version.
          operationId: indexesGetNextVersion
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
