apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: lists-openshift-clusters-in-the-specified-subscription
  description: The operation returns properties of each OpenShift cluster.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Lists Openshift Clusters In The Specified Subscription.
      description: The operation returns properties of each OpenShift cluster.
    paths:
      /subscriptions/{subscriptionId}/providers/Microsoft.RedHatOpenShift/openShiftClusters:
        get:
          summary: Lists Openshift Clusters In The Specified Subscription.
          description: The operation returns properties of each OpenShift cluster.
          operationId: openShiftClustersList
          responses:
            default:
              description: >-
                Error response describing why the operation failed.  If the resource
                doesn't exist, 404 (Not Found) is returned.  If any of the input
                parameters is wrong, 400 (Bad Request) is returned.
              schema:
                $ref: '#/definitions/CloudError'
