apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureStartquorumloss
  description: >-
    This API is useful for a temporary quorum loss situation on your
    service.<br><br>Call the GetQuorumLossProgress API with the same OperationId
    to return information on the operation started with this API.<br><br>This
    can only be called on stateful persisted (HasPersistedState==true)
    services.  Do not use this API on stateless services or stateful in-memory
    only services.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Induces Quorum Loss For A Given Stateful Service Partition '
      description: >-
        This API is useful for a temporary quorum loss situation on your
        service.<br><br>Call the GetQuorumLossProgress API with the same OperationId
        to return information on the operation started with this API.<br><br>This
        can only be called on stateful persisted (HasPersistedState==true)
        services.  Do not use this API on stateless services or stateful in-memory
        only services.
    paths:
      /Faults/Services/{serviceId}/$/GetPartitions/{partitionId}/$/StartQuorumLoss:
        post:
          summary: 'Induces Quorum Loss For A Given Stateful Service Partition '
          description: >-
            This API is useful for a temporary quorum loss situation on your
            service.<br><br>Call the GetQuorumLossProgress API with the same
            OperationId to return information on the operation started with this
            API.<br><br>This can only be called on stateful persisted
            (HasPersistedState==true) services.  Do not use this API on stateless
            services or stateful in-memory only services.
          operationId: microsoftAzureStartquorumloss
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
