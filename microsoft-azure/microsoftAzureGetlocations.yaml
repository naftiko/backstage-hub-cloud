apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureGetlocations
  description: Returns location for user tenant.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Providers Microsoft Intune Locations
      description: Returns location for user tenant.
    paths:
      /providers/Microsoft.Intune/locations:
        get:
          summary: Get Providers Microsoft Intune Locations
          description: Returns location for user tenant.
          operationId: microsoftAzureGetlocations
          responses:
            default:
              description: unexpected error
              schema:
                $ref: '#/definitions/Error'
