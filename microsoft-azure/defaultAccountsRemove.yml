apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Removes The Default Account From The Scope.
  description: Removes the default account from the scope.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Removes The Default Account From The Scope.
      description: Removes the default account from the scope.
    paths:
      /providers/Microsoft.Purview/removeDefaultAccount:
        post:
          summary: Removes The Default Account From The Scope.
          description: Removes the default account from the scope.
          operationId: defaultAccountsRemove
          responses:
            default:
              description: An error response received from the provider.
              schema:
                $ref: '#/definitions/ErrorResponseModel'
