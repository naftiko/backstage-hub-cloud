apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Sets The Default Account For The Scope.
  description: Sets the default account for the scope.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Sets The Default Account For The Scope.
      description: Sets the default account for the scope.
    paths:
      /providers/Microsoft.Purview/setDefaultAccount:
        post:
          summary: Sets The Default Account For The Scope.
          description: Sets the default account for the scope.
          operationId: defaultAccountsSet
          responses:
            default:
              description: An error response received from the provider.
              schema:
                $ref: '#/definitions/ErrorResponseModel'
