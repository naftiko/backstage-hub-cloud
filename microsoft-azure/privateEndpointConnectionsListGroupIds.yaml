apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    returns-all-group-ids-supported-by-the-notification-hubs-rpthis-is-a-public-api-required-by-the-networking-rp-contract-it-can-be-used-directly-by-notification-hubs-users
  description: >-
    Even though this namespace requires subscription id, resource group and
    namespace name, it returns a constant payload (for a given namespacE) every
    time it's called.That's why we don't send it to the sibling RP, but process
    it directly in the scale unit that received the request.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: "Returns All Group Ids Supported By The Notification Hubs Rp.\r\nthis Is A Public Api Required By The Networking Rp Contract. It Can Be Used Directly By Notification Hubs Users."
      description: >-
        Even though this namespace requires subscription id, resource group and
        namespace name, it returns a constant payload (for a given namespacE) every
        time it's called.That's why we don't send it to the sibling RP, but process
        it directly in the scale unit that received the request.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.NotificationHubs/namespaces/{namespaceName}/privateLinkResources:
        get:
          summary: "Returns All Group Ids Supported By The Notification Hubs Rp.\r\nthis Is A Public Api Required By The Networking Rp Contract. It Can Be Used Directly By Notification Hubs Users."
          description: >-
            Even though this namespace requires subscription id, resource group and
            namespace name, it returns a constant payload (for a given namespacE)
            every time it's called.That's why we don't send it to the sibling RP,
            but process it directly in the scale unit that received the request.
          operationId: privateEndpointConnectionsListGroupIds
          responses:
            default:
              description: Error code with reason.
              schema:
                $ref: >-
                  ../../../../../common-types/resource-management/v5/types.json#/definitions/ErrorResponse
