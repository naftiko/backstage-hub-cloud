apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: use-to-get-a-list-of-all-map-configurations
  description: >-
    ### Submit List Request The Map Configuration `List` API is an HTTP `GET`
    request used to get a list of all map configurations.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Use To Get A List Of All Map Configurations.
      description: >-
        ### Submit List Request The Map Configuration `List` API is an HTTP `GET`
        request used to get a list of all map configurations.
    paths:
      /styles/mapconfigurations:
        get:
          summary: Use To Get A List Of All Map Configurations.
          description: >-
            ### Submit List Request The Map Configuration `List` API is an HTTP
            `GET` request used to get a list of all map configurations.
          operationId: mapConfigurationList
          responses:
            default:
              $ref: ../../../Common/preview/1.0/common.json#/responses/default
