apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureJobrouteroperationsAcceptjoboffer
  description: >-
    Accepts an offer to work on a job and returns a 409/Conflict if another
    agent accepted the job already.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Accepts An Offer To Work On A Job And Returns A 409 Conflict If Another
        Agent Accepted The Job Already 
      description: >-
        Accepts an offer to work on a job and returns a 409/Conflict if another
        agent accepted the job already.
    paths:
      /routing/workers/{workerId}/offers/{offerId}:accept:
        post:
          summary: >-
            Accepts An Offer To Work On A Job And Returns A 409 Conflict If Another
            Agent Accepted The Job Already 
          description: >-
            Accepts an offer to work on a job and returns a 409/Conflict if another
            agent accepted the job already.
          operationId: microsoftAzureJobrouteroperationsAcceptjoboffer
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
