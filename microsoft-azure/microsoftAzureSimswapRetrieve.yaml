apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureSimswapRetrieve
  description: Provides timestamp of latest SIM swap
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Sim Swap Sim Swap:retrieve
      description: Provides timestamp of latest SIM swap
    paths:
      /sim-swap/sim-swap:retrieve:
        post:
          summary: Post Sim Swap Sim Swap:retrieve
          description: Provides timestamp of latest SIM swap
          operationId: microsoftAzureSimswapRetrieve
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
