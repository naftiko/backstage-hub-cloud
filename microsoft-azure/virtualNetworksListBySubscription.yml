apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Implements Get Virtualnetworks In A Subscription.
  description: List of VirtualNetworks in a subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Implements Get Virtualnetworks In A Subscription.
      description: List of VirtualNetworks in a subscription.
    paths:
      /subscriptions/{subscriptionId}/providers/Microsoft.ScVmm/virtualNetworks:
        get:
          summary: Implements Get Virtualnetworks In A Subscription.
          description: List of VirtualNetworks in a subscription.
          operationId: virtualNetworksListBySubscription
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
