apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-providers-microsoftintune-locations-hostname
  description: Returns location for given tenant.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Providers Microsoft.intune Locations Hostname
      description: Returns location for given tenant.
    paths:
      /providers/Microsoft.Intune/locations/hostName:
        get:
          summary: Get Providers Microsoft.intune Locations Hostname
          description: Returns location for given tenant.
          operationId: getLocationByHostName
          responses:
            default:
              description: unexpected error
              schema:
                $ref: '#/definitions/Error'
