apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-documentclassifiers-classifieridanalyze
  description: Classifies document with document classifier.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Documentclassifiers Classifierid:analyze
      description: Classifies document with document classifier.
    paths:
      /documentClassifiers/{classifierId}:analyze:
        post:
          summary: Post Documentclassifiers Classifierid:analyze
          description: Classifies document with document classifier.
          operationId: documentClassifiersClassifyDocumentFromStream
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/ErrorResponse'
