apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Fields Cascade-delete Jobid
  description: Get a cascade delete job for specified field.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Fields Cascade-delete Jobid
      description: Get a cascade delete job for specified field.
    paths:
      /fields/cascade-delete/{jobId}:
        get:
          summary: Get Fields Cascade-delete Jobid
          description: Get a cascade delete job for specified field.
          operationId: fieldsGetCascadeDeleteJobDetails
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
