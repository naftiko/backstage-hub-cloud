apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: deletes-an-guestagent
  description: Implements GuestAgent DELETE method.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deletes An Guestagent.
      description: Implements GuestAgent DELETE method.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ScVmm/virtualMachines/{virtualMachineName}/guestAgents/{guestAgentName}:
        delete:
          summary: Deletes An Guestagent.
          description: Implements GuestAgent DELETE method.
          operationId: guestAgentsDelete
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
