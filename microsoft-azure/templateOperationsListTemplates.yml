apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Messages Channels Channelid Templates
  description: List all templates for given Azure Communication Services channel
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Messages Channels Channelid Templates
      description: List all templates for given Azure Communication Services channel
    paths:
      /messages/channels/{channelId}/templates:
        get:
          summary: Get Messages Channels Channelid Templates
          description: List all templates for given Azure Communication Services channel
          operationId: templateOperationsListTemplates
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
