apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List The Modules Present In A Device
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List The Modules Present In A Device
      description: Needs a more full description created.
    paths:
      /devices/{deviceId}/modules:
        get:
          summary: List The Modules Present In A Device
          description: Needs a more full description created.
          operationId: devicesListModules
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
