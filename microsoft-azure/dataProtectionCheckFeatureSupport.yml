apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Validates If A Feature Is Supported
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Validates If A Feature Is Supported
      description: Needs a more full description created.
    paths:
      /subscriptions/{subscriptionId}/providers/Microsoft.DataProtection/locations/{location}/checkFeatureSupport:
        post:
          summary: Validates If A Feature Is Supported
          description: Needs a more full description created.
          operationId: dataProtectionCheckFeatureSupport
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
