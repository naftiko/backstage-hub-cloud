apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureJobrouteroperationsGetinqueueposition
  description: Gets a job's position details.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Gets A Job S Position Details '
      description: Gets a job's position details.
    paths:
      /routing/jobs/{jobId}/position:
        get:
          summary: 'Gets A Job S Position Details '
          description: Gets a job's position details.
          operationId: microsoftAzureJobrouteroperationsGetinqueueposition
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
