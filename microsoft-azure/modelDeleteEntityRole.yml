apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete An Entity Role In A Version Of The Application.
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete An Entity Role In A Version Of The Application.
      description: Needs a more full description created.
    paths:
      /apps/{appId}/versions/{versionId}/entities/{entityId}/roles/{roleId}:
        delete:
          summary: Delete An Entity Role In A Version Of The Application.
          description: Needs a more full description created.
          operationId: modelDeleteEntityRole
          responses:
            default:
              description: Error Response.
              schema:
                $ref: '#/definitions/ErrorResponse'
