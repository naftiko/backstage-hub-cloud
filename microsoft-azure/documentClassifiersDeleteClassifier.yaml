apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-documentclassifiers-classifierid
  description: Deletes document classifier.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Documentclassifiers Classifierid
      description: Deletes document classifier.
    paths:
      /documentClassifiers/{classifierId}:
        delete:
          summary: Delete Documentclassifiers Classifierid
          description: Deletes document classifier.
          operationId: documentClassifiersDeleteClassifier
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/ErrorResponse'
