apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Return Vector From An Image.
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Return Vector From An Image.
      description: Needs a more full description created.
    paths:
      /retrieval:vectorizeImage:
        post:
          summary: Return Vector From An Image.
          description: Needs a more full description created.
          operationId: imageRetrievalVectorizeImage
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
