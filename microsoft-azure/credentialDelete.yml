apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Credentials Credentialname
  description: Deletes the credentials associated with the account
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Credentials Credentialname
      description: Deletes the credentials associated with the account
    paths:
      /credentials/{credentialName}:
        delete:
          summary: Delete Credentials Credentialname
          description: Deletes the credentials associated with the account
          operationId: credentialDelete
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
