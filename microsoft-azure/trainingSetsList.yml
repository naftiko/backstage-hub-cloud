apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Trainingsets
  description: Gets a list of training sets for the authenticated Speech service resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Trainingsets
      description: Gets a list of training sets for the authenticated Speech service resource.
    paths:
      /trainingsets:
        get:
          summary: Get Trainingsets
          description: >-
            Gets a list of training sets for the authenticated Speech service
            resource.
          operationId: trainingSetsList
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
