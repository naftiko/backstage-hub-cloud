apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get The Devices Of A Device Group
  description: Get the list of devices by device group ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get The Devices Of A Device Group
      description: Get the list of devices by device group ID.
    paths:
      /deviceGroups/{deviceGroupId}/devices:
        get:
          summary: Get The Devices Of A Device Group
          description: Get the list of devices by device group ID.
          operationId: deviceGroupsGetDevices
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
