apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: removes-the-list-of-tags-from-the-specified-node
  description: This api allows removing set of tags from the specified node.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Removes The List Of Tags From The Specified Node.
      description: This api allows removing set of tags from the specified node.
    paths:
      /Nodes/{nodeName}/$/RemoveNodeTags:
        post:
          summary: Removes The List Of Tags From The Specified Node.
          description: This api allows removing set of tags from the specified node.
          operationId: removeNodeTags
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
