apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Timezone Windowstoiana Format
  description: >-
    __Windows to IANA Time Zone__**Applies to**: S0 and S1 pricing tiers.This
    API returns a corresponding IANA ID, given a valid Windows Time Zone ID.
    Multiple IANA IDs may be returned for a single Windows ID. It is possible to
    narrow these results by adding an optional territory parameter.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Timezone Windowstoiana Format
      description: >-
        __Windows to IANA Time Zone__**Applies to**: S0 and S1 pricing tiers.This
        API returns a corresponding IANA ID, given a valid Windows Time Zone ID.
        Multiple IANA IDs may be returned for a single Windows ID. It is possible to
        narrow these results by adding an optional territory parameter.
    paths:
      /timezone/windowsToIana/{format}:
        get:
          summary: Get Timezone Windowstoiana Format
          description: >-
            __Windows to IANA Time Zone__**Applies to**: S0 and S1 pricing
            tiers.This API returns a corresponding IANA ID, given a valid Windows
            Time Zone ID. Multiple IANA IDs may be returned for a single Windows ID.
            It is possible to narrow these results by adding an optional territory
            parameter.
          operationId: timezoneGetTimezoneWindowsToIANA
          responses:
            '500':
              $ref: '#/responses/500'
