apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureMetadataGettopicpartitions
  description: >-
    Get metadata about all partitions for a specific topic with partition ID,
    earliest offset, latest offset, leader, replicas, and in-sync replicas
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Metadata About All Partitions For A Specific Topic
      description: >-
        Get metadata about all partitions for a specific topic with partition ID,
        earliest offset, latest offset, leader, replicas, and in-sync replicas
    paths:
      /v1/metadata/topics/{topic}/partitions:
        get:
          summary: Get Metadata About All Partitions For A Specific Topic
          description: >-
            Get metadata about all partitions for a specific topic with partition
            ID, earliest offset, latest offset, leader, replicas, and in-sync
            replicas
          operationId: microsoftAzureMetadataGettopicpartitions
          responses:
            '500':
              description: Internal Server Error
