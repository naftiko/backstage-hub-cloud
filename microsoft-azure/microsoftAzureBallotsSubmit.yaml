apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureBallotsSubmit
  description: >-
    Submit an executable ballot for a specific proposal. This may be as simple
    as `return true` to vote in favour, or contain reads from the KV and
    conditions on the proposal contents.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Gov Members Proposals Proposalid Ballots Memberid:submit
      description: >-
        Submit an executable ballot for a specific proposal. This may be as simple
        as `return true` to vote in favour, or contain reads from the KV and
        conditions on the proposal contents.
    paths:
      /gov/members/proposals/{proposalId}/ballots/{memberId}:submit:
        post:
          summary: Post Gov Members Proposals Proposalid Ballots Memberid:submit
          description: >-
            Submit an executable ballot for a specific proposal. This may be as
            simple as `return true` to vote in favour, or contain reads from the KV
            and conditions on the proposal contents.
          operationId: microsoftAzureBallotsSubmit
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
