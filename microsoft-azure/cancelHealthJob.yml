apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Cancel Healthcare Prediction Job
  description: Cancel healthcare prediction job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Cancel Healthcare Prediction Job
      description: Cancel healthcare prediction job.
    paths:
      /entities/health/jobs/{jobId}:
        delete:
          summary: Cancel Healthcare Prediction Job
          description: Cancel healthcare prediction job.
          operationId: cancelHealthJob
          responses:
            '500':
              description: Internal error response
              schema:
                $ref: '#/definitions/ErrorResponse'
              x-ms-error-response: true
