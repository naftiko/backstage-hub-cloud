apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-crop-products
  description: Returns a paginated list of crop product resources.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Crop-products
      description: Returns a paginated list of crop product resources.
    paths:
      /crop-products:
        get:
          summary: Get Crop-products
          description: Returns a paginated list of crop product resources.
          operationId: cropProductsList
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
