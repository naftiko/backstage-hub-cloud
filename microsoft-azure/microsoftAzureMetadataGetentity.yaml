apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureMetadataGetentity
  description: Gets the list of metadata entities.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Providers Microsoft Resourcehealth Metadata Name
      description: Gets the list of metadata entities.
    paths:
      /providers/Microsoft.ResourceHealth/metadata/{name}:
        get:
          summary: Get Providers Microsoft Resourcehealth Metadata Name
          description: Gets the list of metadata entities.
          operationId: microsoftAzureMetadataGetentity
          responses:
            default:
              description: DefaultErrorResponse
              schema:
                $ref: '#/definitions/ErrorResponse'
