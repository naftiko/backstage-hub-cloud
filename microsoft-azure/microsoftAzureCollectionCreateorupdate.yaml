apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureCollectionCreateorupdate
  description: >-
    Creates or updates an entity to a collection.<br>Existing entity is matched
    using its unique guid if supplied or by its unique attributes eg:
    qualifiedName.<br>Map and array of collections are not well supported. E.g.,
    array>, array>.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Collections Collection Entity
      description: >-
        Creates or updates an entity to a collection.<br>Existing entity is matched
        using its unique guid if supplied or by its unique attributes eg:
        qualifiedName.<br>Map and array of collections are not well supported. E.g.,
        array>, array>.
    paths:
      /collections/{collection}/entity:
        post:
          summary: Post Collections Collection Entity
          description: >-
            Creates or updates an entity to a collection.<br>Existing entity is
            matched using its unique guid if supplied or by its unique attributes
            eg: qualifiedName.<br>Map and array of collections are not well
            supported. E.g., array>, array>.
          operationId: microsoftAzureCollectionCreateorupdate
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
