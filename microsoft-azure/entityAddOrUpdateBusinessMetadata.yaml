apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-atlas-entity-guid-guid-businessmetadata
  description: Add business metadata to an entity.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Atlas Entity Guid Guid Businessmetadata
      description: Add business metadata to an entity.
    paths:
      /atlas/v2/entity/guid/{guid}/businessmetadata:
        post:
          summary: Post Atlas Entity Guid Guid Businessmetadata
          description: Add business metadata to an entity.
          operationId: entityAddOrUpdateBusinessMetadata
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
