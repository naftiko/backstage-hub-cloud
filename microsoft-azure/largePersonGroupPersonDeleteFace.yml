apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    Delete Largepersongroups Largepersongroupid Persons Personid Persistedfaces
    Persistedfaceid
  description: >-
    Delete a face from a person in a large person group by specified
    largePersonGroupId, personId and persistedFaceId. Adding/deleting faces
    to/from a same person will be processed sequentially. Adding/deleting faces
    to/from different persons are processed in parallel.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Delete Largepersongroups Largepersongroupid Persons Personid Persistedfaces
        Persistedfaceid
      description: >-
        Delete a face from a person in a large person group by specified
        largePersonGroupId, personId and persistedFaceId. Adding/deleting faces
        to/from a same person will be processed sequentially. Adding/deleting faces
        to/from different persons are processed in parallel.
    paths:
      /largepersongroups/{largePersonGroupId}/persons/{personId}/persistedfaces/{persistedFaceId}:
        delete:
          summary: >-
            Delete Largepersongroups Largepersongroupid Persons Personid
            Persistedfaces Persistedfaceid
          description: >-
            Delete a face from a person in a large person group by specified
            largePersonGroupId, personId and persistedFaceId. Adding/deleting faces
            to/from a same person will be processed sequentially. Adding/deleting
            faces to/from different persons are processed in parallel.
          operationId: largePersonGroupPersonDeleteFace
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/APIError'
