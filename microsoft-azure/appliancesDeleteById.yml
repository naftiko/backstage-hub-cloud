apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Applianceid
  description: Deletes the appliance.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Applianceid
      description: Deletes the appliance.
    paths:
      /{applianceId}:
        delete:
          summary: Delete Applianceid
          description: Deletes the appliance.
          operationId: appliancesDeleteById
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
