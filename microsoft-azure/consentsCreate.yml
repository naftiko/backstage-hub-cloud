apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Put Consents Id
  description: Creates a new voice talent consent with the provided audio URL.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Consents Id
      description: Creates a new voice talent consent with the provided audio URL.
    paths:
      /consents/{id}:
        put:
          summary: Put Consents Id
          description: Creates a new voice talent consent with the provided audio URL.
          operationId: consentsCreate
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
