apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Metadata About A Specific Kafka Topic-partition
  description: >-
    Get metadata about a specific Kafka topic-partition with partition ID,
    earliest offset, latest offset, leader, replicas, and in-sync replicas
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Metadata About A Specific Kafka Topic-partition
      description: >-
        Get metadata about a specific Kafka topic-partition with partition ID,
        earliest offset, latest offset, leader, replicas, and in-sync replicas
    paths:
      /v1/metadata/topics/{topic}/partitions/{partition}:
        get:
          summary: Get Metadata About A Specific Kafka Topic-partition
          description: >-
            Get metadata about a specific Kafka topic-partition with partition ID,
            earliest offset, latest offset, leader, replicas, and in-sync replicas
          operationId: metadataGetMetadataForPartition
          responses:
            '500':
              description: Internal Server Error
