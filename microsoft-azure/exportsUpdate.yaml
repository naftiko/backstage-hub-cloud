apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: patch-an-export
  description: Perform an incremental update to an export.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Patch An Export.
      description: Perform an incremental update to an export.
    paths:
      /dataExport/exports/{exportId}:
        patch:
          summary: Patch An Export.
          description: Perform an incremental update to an export.
          operationId: exportsUpdate
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
