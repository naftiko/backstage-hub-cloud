apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Move Collections.
  description: Get all the Move Collections in the subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Move Collections.
      description: Get all the Move Collections in the subscription.
    paths:
      /subscriptions/{subscriptionId}/providers/Microsoft.Migrate/moveCollections:
        get:
          summary: Get All Move Collections.
          description: Get all the Move Collections in the subscription.
          operationId: moveCollectionsListMoveCollectionsBySubscription
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
