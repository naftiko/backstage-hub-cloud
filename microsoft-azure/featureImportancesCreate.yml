apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Feature Importance.
  description: Submit a new Feature Importance job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Feature Importance.
      description: Submit a new Feature Importance job.
    paths:
      /featureImportances/{featureImportanceId}:
        put:
          summary: Create Feature Importance.
          description: Submit a new Feature Importance job.
          operationId: featureImportancesCreate
          responses:
            default:
              description: Invalid contract.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
