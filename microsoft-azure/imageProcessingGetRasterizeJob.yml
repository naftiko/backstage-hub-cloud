apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Image-processing Rasterize Jobid
  description: Get ImageProcessing Rasterize job's details.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Image-processing Rasterize Jobid
      description: Get ImageProcessing Rasterize job's details.
    paths:
      /image-processing/rasterize/{jobId}:
        get:
          summary: Get Image-processing Rasterize Jobid
          description: Get ImageProcessing Rasterize job's details.
          operationId: imageProcessingGetRasterizeJob
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
