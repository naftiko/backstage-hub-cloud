apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-the-private-endpoint-connections
  description: Get all the private endpoint connections under a migrate project.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get The Private Endpoint Connections.
      description: Get all the private endpoint connections under a migrate project.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/migrateProjects/{migrateProjectName}/privateEndpointConnections:
        get:
          summary: Get The Private Endpoint Connections.
          description: Get all the private endpoint connections under a migrate project.
          operationId: privateEndpointConnectionsControllerGetPrivateEndpointConnections
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
