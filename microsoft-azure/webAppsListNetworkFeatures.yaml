apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: gets-all-network-features-used-by-the-app-or-deployment-slot-if-specified
  description: >-
    Description for Gets all network features used by the app (or deployment
    slot, if specified).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Gets All Network Features Used By The App (or Deployment Slot, If
        Specified).
      description: >-
        Description for Gets all network features used by the app (or deployment
        slot, if specified).
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Web/sites/{name}/networkFeatures/{view}:
        get:
          summary: >-
            Gets All Network Features Used By The App (or Deployment Slot, If
            Specified).
          description: >-
            Description for Gets all network features used by the app (or deployment
            slot, if specified).
          operationId: webAppsListNetworkFeatures
          responses:
            default:
              description: App Service error response.
              schema:
                $ref: ./CommonDefinitions.json#/definitions/DefaultErrorResponse
