apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-datasourcesdatasourcename
  description: Retrieves a datasource definition.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Datasources('datasourcename')
      description: Retrieves a datasource definition.
    paths:
      /datasources('{dataSourceName}'):
        get:
          summary: Get Datasources('datasourcename')
          description: Retrieves a datasource definition.
          operationId: dataSourcesGet
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
