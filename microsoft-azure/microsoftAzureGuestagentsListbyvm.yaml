apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureGuestagentsListbyvm
  description: Returns the list of GuestAgent of the given vm.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Implements Get Guestagent In A Vm '
      description: Returns the list of GuestAgent of the given vm.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ScVmm/virtualMachines/{virtualMachineName}/guestAgents:
        get:
          summary: 'Implements Get Guestagent In A Vm '
          description: Returns the list of GuestAgent of the given vm.
          operationId: microsoftAzureGuestagentsListbyvm
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
