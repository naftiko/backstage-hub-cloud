apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-indexes
  description: Creates a new search index.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Indexes
      description: Creates a new search index.
    paths:
      /indexes:
        post:
          summary: Post Indexes
          description: Creates a new search index.
          operationId: indexesCreate
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
