apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: retrieves-an-existing-room-by-id
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieves An Existing Room By Id.
      description: Needs a more full description created.
    paths:
      /rooms/{roomId}:
        get:
          summary: Retrieves An Existing Room By Id.
          description: Needs a more full description created.
          operationId: roomsGet
          responses:
            default:
              description: Error response
              schema:
                $ref: '#/definitions/CommunicationErrorResponse'
              headers:
                x-ms-error-code:
                  x-ms-client-name: ErrorCode
                  type: string
