apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: creates-or-updates-a-volume-resource
  description: >-
    Creates a Volume resource with the specified name, description and
    properties. If Volume resource with the same name exists, then it is updated
    with the specified description and properties.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Creates Or Updates A Volume Resource.
      description: >-
        Creates a Volume resource with the specified name, description and
        properties. If Volume resource with the same name exists, then it is updated
        with the specified description and properties.
    paths:
      /Resources/Volumes/{volumeResourceName}:
        put:
          summary: Creates Or Updates A Volume Resource.
          description: >-
            Creates a Volume resource with the specified name, description and
            properties. If Volume resource with the same name exists, then it is
            updated with the specified description and properties.
          operationId: meshVolumeCreateOrUpdate
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/FabricError'
