apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-test-run-details-by-test-run-id
  description: Get test run details by test run Id.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Test Run Details By Test Run Id.
      description: Get test run details by test run Id.
    paths:
      /test-runs/{testRunId}:
        get:
          summary: Get Test Run Details By Test Run Id.
          description: Get test run details by test run Id.
          operationId: loadTestRunGetTestRun
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
