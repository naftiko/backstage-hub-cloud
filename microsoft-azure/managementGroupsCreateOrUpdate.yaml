apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: put-providers-microsoftmanagement-managementgroups-groupid
  description: >-
    Create or update a management group.If a management group is already created
    and a subsequent create request is issued with different properties, the
    management group properties will be updated.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Providers Microsoft.management Managementgroups Groupid
      description: >-
        Create or update a management group.If a management group is already created
        and a subsequent create request is issued with different properties, the
        management group properties will be updated.
    paths:
      /providers/Microsoft.Management/managementGroups/{groupId}:
        put:
          summary: Put Providers Microsoft.management Managementgroups Groupid
          description: >-
            Create or update a management group.If a management group is already
            created and a subsequent create request is issued with different
            properties, the management group properties will be updated.
          operationId: managementGroupsCreateOrUpdate
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
