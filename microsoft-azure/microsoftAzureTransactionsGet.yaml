apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureTransactionsGet
  description: >-
    Get status of transaction by transaction ID, to determine whether it has
    committed.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Gov Service Transactions Transactionid
      description: >-
        Get status of transaction by transaction ID, to determine whether it has
        committed.
    paths:
      /gov/service/transactions/{transactionId}:
        get:
          summary: Get Gov Service Transactions Transactionid
          description: >-
            Get status of transaction by transaction ID, to determine whether it has
            committed.
          operationId: microsoftAzureTransactionsGet
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
