apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureFacelistList
  description: >-
    List face lists’ faceListId, name, userData and recognitionModel.  <br>To
    get face information inside faceList use [FaceList -
    Get](https://docs.microsoft.com/rest/api/faceapi/facelist/get)<br>
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Facelists
      description: >-
        List face lists’ faceListId, name, userData and recognitionModel.  <br>To
        get face information inside faceList use [FaceList -
        Get](https://docs.microsoft.com/rest/api/faceapi/facelist/get)<br>
    paths:
      /facelists:
        get:
          summary: Get Facelists
          description: >-
            List face lists’ faceListId, name, userData and recognitionModel. 
            <br>To get face information inside faceList use [FaceList -
            Get](https://docs.microsoft.com/rest/api/faceapi/facelist/get)<br>
          operationId: microsoftAzureFacelistList
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/APIError'
