apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureEnableapplicationbackup
  description: >-
    Enables periodic backup of stateful partitions which are part of this
    Service Fabric application. Each partition is backed up individually as per
    the specified backup policy description. <br>Note only C# based Reliable
    Actor and Reliable Stateful services are currently supported for periodic
    backup.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Enables Periodic Backup Of Stateful Partitions Under This Service Fabric
        Application 
      description: >-
        Enables periodic backup of stateful partitions which are part of this
        Service Fabric application. Each partition is backed up individually as per
        the specified backup policy description. <br>Note only C# based Reliable
        Actor and Reliable Stateful services are currently supported for periodic
        backup.
    paths:
      /Applications/{applicationId}/$/EnableBackup:
        post:
          summary: >-
            Enables Periodic Backup Of Stateful Partitions Under This Service Fabric
            Application 
          description: >-
            Enables periodic backup of stateful partitions which are part of this
            Service Fabric application. Each partition is backed up individually as
            per the specified backup policy description. <br>Note only C# based
            Reliable Actor and Reliable Stateful services are currently supported
            for periodic backup.
          operationId: microsoftAzureEnableapplicationbackup
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
