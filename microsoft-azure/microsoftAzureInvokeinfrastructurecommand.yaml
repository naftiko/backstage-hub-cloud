apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureInvokeinfrastructurecommand
  description: >-
    For clusters that have one or more instances of the Infrastructure Service
    configured,<br>this API provides a way to send infrastructure-specific
    commands to a particular<br>instance of the Infrastructure
    Service.<br><br>Available commands and their corresponding response formats
    vary depending upon<br>the infrastructure on which the cluster is
    running.<br><br>This API supports the Service Fabric platform; it is not
    meant to be used directly from your code.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Invokes An Administrative Command On The Given Infrastructure Service
        Instance 
      description: >-
        For clusters that have one or more instances of the Infrastructure Service
        configured,<br>this API provides a way to send infrastructure-specific
        commands to a particular<br>instance of the Infrastructure
        Service.<br><br>Available commands and their corresponding response formats
        vary depending upon<br>the infrastructure on which the cluster is
        running.<br><br>This API supports the Service Fabric platform; it is not
        meant to be used directly from your code.
    paths:
      /$/InvokeInfrastructureCommand:
        post:
          summary: >-
            Invokes An Administrative Command On The Given Infrastructure Service
            Instance 
          description: >-
            For clusters that have one or more instances of the Infrastructure
            Service configured,<br>this API provides a way to send
            infrastructure-specific commands to a particular<br>instance of the
            Infrastructure Service.<br><br>Available commands and their
            corresponding response formats vary depending upon<br>the infrastructure
            on which the cluster is running.<br><br>This API supports the Service
            Fabric platform; it is not meant to be used directly from your code.
          operationId: microsoftAzureInvokeinfrastructurecommand
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
