apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureStatedigestsAcknowledge
  description: >-
    Submit a signed acknowledgement of a recent digest of the service status, to
    transition the member to Active.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Gov Members State Digests Memberid:ack
      description: >-
        Submit a signed acknowledgement of a recent digest of the service status, to
        transition the member to Active.
    paths:
      /gov/members/state-digests/{memberId}:ack:
        post:
          summary: Post Gov Members State Digests Memberid:ack
          description: >-
            Submit a signed acknowledgement of a recent digest of the service
            status, to transition the member to Active.
          operationId: microsoftAzureStatedigestsAcknowledge
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
