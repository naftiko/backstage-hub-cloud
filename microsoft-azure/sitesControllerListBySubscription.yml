apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Vmware Sites.
  description: Get all the vmware sites in the subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Vmware Sites.
      description: Get all the vmware sites in the subscription.
    paths:
      /subscriptions/{subscriptionId}/providers/Microsoft.OffAzure/vmwareSites:
        get:
          summary: Get All Vmware Sites.
          description: Get all the vmware sites in the subscription.
          operationId: sitesControllerListBySubscription
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: >-
                  ../../../../../common-types/resource-management/v3/types.json#/definitions/ErrorResponse
