apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureStyleList
  description: >-
    <br><br>The `List` API is an HTTP `GET` request that allows the caller to
    fetch a list of all styles in the current Azure Maps Creator account. The
    default indoor style `microsoft-maps:indoor_2022-01-01` and
    `microsoft-maps:indoor_dark_2022-01-01` will not show in the list response.
    But you can still use it as your own style by the predefined
    alias.<br><br>### Submit List Request
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Use To Get A List Of Styles '
      description: >-
        <br><br>The `List` API is an HTTP `GET` request that allows the caller to
        fetch a list of all styles in the current Azure Maps Creator account. The
        default indoor style `microsoft-maps:indoor_2022-01-01` and
        `microsoft-maps:indoor_dark_2022-01-01` will not show in the list response.
        But you can still use it as your own style by the predefined
        alias.<br><br>### Submit List Request
    paths:
      /styles:
        get:
          summary: 'Use To Get A List Of Styles '
          description: >-
            <br><br>The `List` API is an HTTP `GET` request that allows the caller
            to fetch a list of all styles in the current Azure Maps Creator account.
            The default indoor style `microsoft-maps:indoor_2022-01-01` and
            `microsoft-maps:indoor_dark_2022-01-01` will not show in the list
            response. But you can still use it as your own style by the predefined
            alias.<br><br>### Submit List Request
          operationId: microsoftAzureStyleList
          responses:
            default:
              $ref: ../../../Common/preview/1.0/common.json#/responses/default
