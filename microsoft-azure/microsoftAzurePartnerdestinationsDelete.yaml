apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzurePartnerdestinationsDelete
  description: Delete existing partner destination.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Delete A Partner Destination '
      description: Delete existing partner destination.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.EventGrid/partnerDestinations/{partnerDestinationName}:
        delete:
          summary: 'Delete A Partner Destination '
          description: Delete existing partner destination.
          operationId: microsoftAzurePartnerdestinationsDelete
          responses:
            default:
              description: |-
                *** Error Responses: ***

                 * 400 Bad Request.

                 * 404 Not Found.

                 * 500 Internal Server Error.
