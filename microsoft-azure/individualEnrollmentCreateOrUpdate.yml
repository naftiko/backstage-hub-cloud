apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Or Update A Device Enrollment Record.
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Or Update A Device Enrollment Record.
      description: Needs a more full description created.
    paths:
      /enrollments/{id}:
        put:
          summary: Create Or Update A Device Enrollment Record.
          description: Needs a more full description created.
          operationId: individualEnrollmentCreateOrUpdate
          responses:
            default:
              description: Error response
              schema:
                $ref: '#/definitions/ProvisioningServiceErrorDetails'
              headers:
                x-ms-error-code:
                  description: The error code for specific error that occurred.
                  type: string
