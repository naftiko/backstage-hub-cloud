apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Workflowtasks Taskid Reassign
  description: Reassign a workflow task.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Workflowtasks Taskid Reassign
      description: Reassign a workflow task.
    paths:
      /workflowtasks/{taskId}/reassign:
        post:
          summary: Post Workflowtasks Taskid Reassign
          description: Reassign a workflow task.
          operationId: reassignWorkflowTask
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
