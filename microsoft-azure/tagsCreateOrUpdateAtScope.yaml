apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: creates-or-updates-the-entire-set-of-tags-on-a-resource-or-subscription
  description: >-
    This operation allows adding or replacing the entire set of tags on the
    specified resource or subscription. The specified entity can have a maximum
    of 50 tags.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Creates Or Updates The Entire Set Of Tags On A Resource Or Subscription.
      description: >-
        This operation allows adding or replacing the entire set of tags on the
        specified resource or subscription. The specified entity can have a maximum
        of 50 tags.
    paths:
      /{scope}/providers/Microsoft.Resources/tags/default:
        put:
          summary: Creates Or Updates The Entire Set Of Tags On A Resource Or Subscription.
          description: >-
            This operation allows adding or replacing the entire set of tags on the
            specified resource or subscription. The specified entity can have a
            maximum of 50 tags.
          operationId: tagsCreateOrUpdateAtScope
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
