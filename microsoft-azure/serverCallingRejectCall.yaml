apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: reject-the-call
  description: Reject the call.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Reject The Call.
      description: Reject the call.
    paths:
      /calling/callConnections:reject:
        post:
          summary: Reject The Call.
          description: Reject the call.
          operationId: serverCallingRejectCall
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/CommunicationErrorResponse'
