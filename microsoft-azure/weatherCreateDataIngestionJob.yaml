apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: put-weather-ingest-data-jobid
  description: Create a weather data ingestion job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Weather Ingest-data Jobid
      description: Create a weather data ingestion job.
    paths:
      /weather/ingest-data/{jobId}:
        put:
          summary: Put Weather Ingest-data Jobid
          description: Create a weather data ingestion job.
          operationId: weatherCreateDataIngestionJob
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
