apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Documentmodels:build
  description: Builds a custom document analysis model.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Documentmodels:build
      description: Builds a custom document analysis model.
    paths:
      /documentModels:build:
        post:
          summary: Post Documentmodels:build
          description: Builds a custom document analysis model.
          operationId: documentModelsBuildModel
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/ErrorResponse'
