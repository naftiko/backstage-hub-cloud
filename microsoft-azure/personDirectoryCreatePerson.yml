apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Creates A New Person In Person Directory.
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Creates A New Person In Person Directory.
      description: Needs a more full description created.
    paths:
      /persons:
        post:
          summary: Creates A New Person In Person Directory.
          description: Needs a more full description created.
          operationId: personDirectoryCreatePerson
          responses:
            default:
              description: >-
                Response entity accompanying non-successful responses containing
                additional details about the error.
              schema:
                $ref: '#/definitions/Error'
