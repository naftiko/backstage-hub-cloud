apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: update-an-existing-deployment-manifest
  description: Update an existing deployment manifest.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update An Existing Deployment Manifest.
      description: Update an existing deployment manifest.
    paths:
      /deploymentManifests/{deploymentManifestId}:
        patch:
          summary: Update An Existing Deployment Manifest.
          description: Update an existing deployment manifest.
          operationId: deploymentManifestsUpdate
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
