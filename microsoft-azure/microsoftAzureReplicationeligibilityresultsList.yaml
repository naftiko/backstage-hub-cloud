apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureReplicationeligibilityresultsList
  description: >-
    Validates whether a given VM can be protected or not in which case returns
    list of errors.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Gets The Validation Errors In Case The Vm Is Unsuitable For Protection '
      description: >-
        Validates whether a given VM can be protected or not in which case returns
        list of errors.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Compute/virtualMachines/{virtualMachineName}/providers/Microsoft.RecoveryServices/replicationEligibilityResults:
        get:
          summary: 'Gets The Validation Errors In Case The Vm Is Unsuitable For Protection '
          description: >-
            Validates whether a given VM can be protected or not in which case
            returns list of errors.
          operationId: microsoftAzureReplicationeligibilityresultsList
          responses:
            '200':
              description: OK
              schema:
                $ref: '#/definitions/ReplicationEligibilityResultsCollection'
