apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-subscriptions-subscriptionid-providers-microsoftimportexport-jobs
  description: Returns all active and completed jobs in a subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Subscriptions Subscriptionid Providers Microsoft.importexport Jobs
      description: Returns all active and completed jobs in a subscription.
    paths:
      /subscriptions/{subscriptionId}/providers/Microsoft.ImportExport/jobs:
        get:
          summary: Get Subscriptions Subscriptionid Providers Microsoft.importexport Jobs
          description: Returns all active and completed jobs in a subscription.
          operationId: jobsListBySubscription
          responses:
            default:
              description: An error occurs.
              schema:
                $ref: '#/definitions/ErrorResponse'
