apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Seasonal-fields Cascade-delete Jobid
  description: Get cascade delete job for specified seasonal field.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Seasonal-fields Cascade-delete Jobid
      description: Get cascade delete job for specified seasonal field.
    paths:
      /seasonal-fields/cascade-delete/{jobId}:
        get:
          summary: Get Seasonal-fields Cascade-delete Jobid
          description: Get cascade delete job for specified seasonal field.
          operationId: seasonalFieldsGetCascadeDeleteJobDetails
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
