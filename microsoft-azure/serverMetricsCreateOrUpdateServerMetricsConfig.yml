apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Associate Server Metrics Config To A Test Model Or Test Run
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Associate Server Metrics Config To A Test Model Or Test Run
      description: Needs a more full description created.
    paths:
      /serverMetricsConfig/{name}:
        patch:
          summary: Associate Server Metrics Config To A Test Model Or Test Run
          description: Needs a more full description created.
          operationId: serverMetricsCreateOrUpdateServerMetricsConfig
          responses:
            default:
              description: |-
                ***Error Responses: ***

                 * "400", "InvalidHostOrResourceName", "Host name or domain is not valid" .

                 * "400", "RequiredServerMetricsName", "name is required"

                 * "400", "RequiredTestOrTestRun", "Either test name or test run name is required"

                 * "500", "InternalServerError", "Unable to process the request, please retry"
              schema:
                $ref: '#/definitions/DefaultErrorResponseBody'
              headers:
                x-ms-error-code:
                  description: The error code for specific error that occurred.
                  type: string
