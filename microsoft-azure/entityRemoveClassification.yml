apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Atlas Entity Guid Guid Classification Classificationname
  description: Delete a given classification from an existing entity represented by a GUID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Atlas Entity Guid Guid Classification Classificationname
      description: Delete a given classification from an existing entity represented by a GUID.
    paths:
      /atlas/v2/entity/guid/{guid}/classification/{classificationName}:
        delete:
          summary: Delete Atlas Entity Guid Guid Classification Classificationname
          description: >-
            Delete a given classification from an existing entity represented by a
            GUID.
          operationId: entityRemoveClassification
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
