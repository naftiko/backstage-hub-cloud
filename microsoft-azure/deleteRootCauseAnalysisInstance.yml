apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete An Instance For Root Cause Analysis
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete An Instance For Root Cause Analysis
      description: Needs a more full description created.
    paths:
      /rootCauseAnalysis/instances/{instanceId}:
        delete:
          summary: Delete An Instance For Root Cause Analysis
          description: Needs a more full description created.
          operationId: deleteRootCauseAnalysisInstance
          responses:
            default:
              description: Client error or server error (4xx or 5xx)
              schema:
                $ref: '#/definitions/ErrorDetail'
              headers:
                x-ms-error-code:
                  description: error code
                  type: string
