apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureListmanagementimageDeleteimage
  description: Deletes an image from the list with the passed list ID and image ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Contentmoderator Lists 0 Imagelists Listid Images Imageid
      description: Deletes an image from the list with the passed list ID and image ID.
    paths:
      /contentmoderator/lists/v1.0/imagelists/{listId}/images/{ImageId}:
        delete:
          summary: Delete Contentmoderator Lists 0 Imagelists Listid Images Imageid
          description: Deletes an image from the list with the passed list ID and image ID.
          operationId: microsoftAzureListmanagementimageDeleteimage
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/APIError'
