apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Table(partitionkey='partitionkey',rowkey='rowkey')
  description: Queries a single entity in a table.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Table(partitionkey='partitionkey',rowkey='rowkey')
      description: Queries a single entity in a table.
    paths:
      /{table}(PartitionKey='{partitionKey}',RowKey='{rowKey}'):
        get:
          summary: Get Table(partitionkey='partitionkey',rowkey='rowkey')
          description: Queries a single entity in a table.
          operationId: tableQueryEntityWithPartitionAndRowKey
          responses:
            default:
              description: Failure
              headers:
                x-ms-error-code:
                  x-ms-client-name: ErrorCode
                  type: string
              schema:
                $ref: '#/definitions/TableServiceError'
