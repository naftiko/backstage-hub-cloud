apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-indexersindexername-searchrun
  description: Runs an indexer on-demand.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Indexers('indexername') Search.run
      description: Runs an indexer on-demand.
    paths:
      /indexers('{indexerName}')/search.run:
        post:
          summary: Post Indexers('indexername') Search.run
          description: Runs an indexer on-demand.
          operationId: indexersRun
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
