apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Clusters In The Resource Group.
  description: Get a list of clusters in the provided resource group.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Clusters In The Resource Group.
      description: Get a list of clusters in the provided resource group.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.NetworkCloud/clusters:
        get:
          summary: List Clusters In The Resource Group.
          description: Get a list of clusters in the provided resource group.
          operationId: clustersListByResourceGroup
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: >-
                  ../../../../../common-types/resource-management/v5/types.json#/definitions/ErrorResponse
