apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: removes-configuration-overrides-on-the-specified-node
  description: >-
    This api allows removing all existing configuration overrides on specified
    node.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Removes Configuration Overrides On The Specified Node.
      description: >-
        This api allows removing all existing configuration overrides on specified
        node.
    paths:
      /Nodes/{nodeName}/$/RemoveConfigurationOverrides:
        delete:
          summary: Removes Configuration Overrides On The Specified Node.
          description: >-
            This api allows removing all existing configuration overrides on
            specified node.
          operationId: removeConfigurationOverrides
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
