apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Submit A Sign Operation.
  description: Submit a sign operation under the created account and profile name provided.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Submit A Sign Operation.
      description: Submit a sign operation under the created account and profile name provided.
    paths:
      /codesigningaccounts/{accountName}/certificateprofiles/{certificateProfile}:sign:
        post:
          summary: Submit A Sign Operation.
          description: >-
            Submit a sign operation under the created account and profile name
            provided.
          operationId: certificateProfileOperationsSign
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
