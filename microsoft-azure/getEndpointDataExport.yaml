apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: gets-the-specified-endpoint-data-export-task-for-the-authenticated-user
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Gets The Specified Endpoint Data Export Task For The Authenticated User.
      description: Needs a more full description created.
    paths:
      /endpoints/{endpointId}/data/{id}:
        get:
          summary: Gets The Specified Endpoint Data Export Task For The Authenticated User.
          description: Needs a more full description created.
          operationId: getEndpointDataExport
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/Error'
