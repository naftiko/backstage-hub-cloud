apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get The List Of Private Link Resources.
  description: Get the list of private link resources.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get The List Of Private Link Resources.
      description: Get the list of private link resources.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/migrateProjects/{migrateProjectName}/privateLinkResources:
        get:
          summary: Get The List Of Private Link Resources.
          description: Get the list of private link resources.
          operationId: privateLinkResourceControllerGetPrivateLinkResources
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
