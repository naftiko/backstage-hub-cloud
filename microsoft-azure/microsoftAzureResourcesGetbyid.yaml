apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureResourcesGetbyid
  description: Gets a resource by ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Resourceid
      description: Gets a resource by ID.
    paths:
      /{resourceId}:
        get:
          summary: Get Resourceid
          description: Gets a resource by ID.
          operationId: microsoftAzureResourcesGetbyid
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
