apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureStartpartitionrestart
  description: >-
    This API is useful for testing failover.<br><br>If used to target a
    stateless service partition, RestartPartitionMode must be
    AllReplicasOrInstances.<br><br>Call the GetPartitionRestartProgress API
    using the same OperationId to get the progress.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        This Api Will Restart Some Or All Replicas Or Instances Of The Specified
        Partition 
      description: >-
        This API is useful for testing failover.<br><br>If used to target a
        stateless service partition, RestartPartitionMode must be
        AllReplicasOrInstances.<br><br>Call the GetPartitionRestartProgress API
        using the same OperationId to get the progress.
    paths:
      /Faults/Services/{serviceId}/$/GetPartitions/{partitionId}/$/StartRestart:
        post:
          summary: >-
            This Api Will Restart Some Or All Replicas Or Instances Of The Specified
            Partition 
          description: >-
            This API is useful for testing failover.<br><br>If used to target a
            stateless service partition, RestartPartitionMode must be
            AllReplicasOrInstances.<br><br>Call the GetPartitionRestartProgress API
            using the same OperationId to get the progress.
          operationId: microsoftAzureStartpartitionrestart
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
