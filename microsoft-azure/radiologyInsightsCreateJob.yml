apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Radiology Insights Job
  description: Creates a Radiology Insights job with the given request body.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Radiology Insights Job
      description: Creates a Radiology Insights job with the given request body.
    paths:
      /radiology-insights/jobs:
        post:
          summary: Create Radiology Insights Job
          description: Creates a Radiology Insights job with the given request body.
          operationId: radiologyInsightsCreateJob
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
