apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Associated Server Metrics Configuration For The Given Test Run.
  description: Get associated server metrics configuration for the given test run.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Associated Server Metrics Configuration For The Given Test Run.
      description: Get associated server metrics configuration for the given test run.
    paths:
      /test-runs/{testRunId}/server-metrics-config:
        get:
          summary: Get Associated Server Metrics Configuration For The Given Test Run.
          description: Get associated server metrics configuration for the given test run.
          operationId: loadTestRunGetServerMetricsConfig
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
