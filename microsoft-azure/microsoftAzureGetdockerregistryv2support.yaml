apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureGetdockerregistryv2support
  description: >-
    Tells whether this Docker Registry instance supports Docker Registry HTTP
    API v2
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Get  '
      description: >-
        Tells whether this Docker Registry instance supports Docker Registry HTTP
        API v2
    paths:
      /v2/:
        get:
          summary: 'Get  '
          description: >-
            Tells whether this Docker Registry instance supports Docker Registry
            HTTP API v2
          operationId: microsoftAzureGetdockerregistryv2support
          responses:
            default:
              description: ACR error response describing why the operation failed.
              schema:
                $ref: '#/definitions/AcrErrors'
