apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureGetpartitionhealth
  description: >-
    Use EventsHealthStateFilter to filter the collection of health events
    reported on the service based on the health state.<br>Use
    ReplicasHealthStateFilter to filter the collection of ReplicaHealthState
    objects on the partition.<br>If you specify a partition that does not exist
    in the health store, this request returns an error.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Gets The Health Of The Specified Service Fabric Partition '
      description: >-
        Use EventsHealthStateFilter to filter the collection of health events
        reported on the service based on the health state.<br>Use
        ReplicasHealthStateFilter to filter the collection of ReplicaHealthState
        objects on the partition.<br>If you specify a partition that does not exist
        in the health store, this request returns an error.
    paths:
      /Partitions/{partitionId}/$/GetHealth:
        get:
          summary: 'Gets The Health Of The Specified Service Fabric Partition '
          description: >-
            Use EventsHealthStateFilter to filter the collection of health events
            reported on the service based on the health state.<br>Use
            ReplicasHealthStateFilter to filter the collection of ReplicaHealthState
            objects on the partition.<br>If you specify a partition that does not
            exist in the health store, this request returns an error.
          operationId: microsoftAzureGetpartitionhealth
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
