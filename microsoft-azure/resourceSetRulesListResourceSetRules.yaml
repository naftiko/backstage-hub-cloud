apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-resourcesetruleconfigs
  description: Get a resource set config service model.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Resourcesetruleconfigs
      description: Get a resource set config service model.
    paths:
      /resourceSetRuleConfigs:
        get:
          summary: Get Resourcesetruleconfigs
          description: Get a resource set config service model.
          operationId: resourceSetRulesListResourceSetRules
          responses:
            default:
              description: An error response received from the provider.
              schema:
                $ref: '#/definitions/ErrorResponseModel'
