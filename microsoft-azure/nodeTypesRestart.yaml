apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: restarts-one-or-more-nodes-on-the-node-type
  description: >-
    Restarts one or more nodes on the node type. It will disable the fabric
    nodes, trigger a restart on the VMs and activate the nodes back again.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Restarts One Or More Nodes On The Node Type.
      description: >-
        Restarts one or more nodes on the node type. It will disable the fabric
        nodes, trigger a restart on the VMs and activate the nodes back again.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ServiceFabric/managedClusters/{clusterName}/nodeTypes/{nodeTypeName}/restart:
        post:
          summary: Restarts One Or More Nodes On The Node Type.
          description: >-
            Restarts one or more nodes on the node type. It will disable the fabric
            nodes, trigger a restart on the VMs and activate the nodes back again.
          operationId: nodeTypesRestart
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/ErrorModel'
