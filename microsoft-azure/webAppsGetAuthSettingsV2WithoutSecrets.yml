apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Gets Site's Authentication / Authorization Settings For Apps Via The Format
  description: >-
    Description for Gets site's Authentication / Authorization settings for apps
    via the V2 format
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Gets Site's Authentication / Authorization Settings For Apps Via The Format
      description: >-
        Description for Gets site's Authentication / Authorization settings for apps
        via the V2 format
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Web/sites/{name}/config/authsettingsV2:
        get:
          summary: >-
            Gets Site's Authentication / Authorization Settings For Apps Via The
            Format
          description: >-
            Description for Gets site's Authentication / Authorization settings for
            apps via the V2 format
          operationId: webAppsGetAuthSettingsV2WithoutSecrets
          responses:
            default:
              description: App Service error response.
              schema:
                $ref: ./CommonDefinitions.json#/definitions/DefaultErrorResponse
