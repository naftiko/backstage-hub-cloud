apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Acr Name _manifests Reference
  description: Get manifest attributes
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Acr Name _manifests Reference
      description: Get manifest attributes
    paths:
      /acr/v1/{name}/_manifests/{reference}:
        get:
          summary: Get Acr Name _manifests Reference
          description: Get manifest attributes
          operationId: getAcrManifestAttributes
          responses:
            default:
              description: ACR error response describing why the operation failed.
              schema:
                $ref: '#/definitions/AcrErrors'
