apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureLoadtestrunListmetricdimensionvalues
  description: List the dimension values for the given metric dimension name.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'List The Dimension Values For The Given Metric Dimension Name '
      description: List the dimension values for the given metric dimension name.
    paths:
      /test-runs/{testRunId}/metric-dimensions/{name}/values:
        get:
          summary: 'List The Dimension Values For The Given Metric Dimension Name '
          description: List the dimension values for the given metric dimension name.
          operationId: microsoftAzureLoadtestrunListmetricdimensionvalues
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
