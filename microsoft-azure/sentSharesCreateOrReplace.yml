apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Or Replace A Sent Share.
  description: Create or replace a sent share
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Or Replace A Sent Share.
      description: Create or replace a sent share
    paths:
      /sentShares/{sentShareId}:
        put:
          summary: Create Or Replace A Sent Share.
          description: Create or replace a sent share
          operationId: sentSharesCreateOrReplace
          responses:
            default:
              description: Default error response.
              schema:
                $ref: '#/definitions/PurviewShareError'
              headers:
                x-ms-error-code:
                  description: The error code
                  type: string
