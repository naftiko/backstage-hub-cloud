apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Removes The Primary Or Secondary X509 Certificate Of An Enrollment Group
  description: Removes the primary or secondary x509 certificate of an enrollment group.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Removes The Primary Or Secondary X509 Certificate Of An Enrollment Group
      description: Removes the primary or secondary x509 certificate of an enrollment group.
    paths:
      /enrollmentGroups/{enrollmentGroupId}/certificates/{entry}:
        delete:
          summary: Removes The Primary Or Secondary X509 Certificate Of An Enrollment Group
          description: >-
            Removes the primary or secondary x509 certificate of an enrollment
            group.
          operationId: enrollmentGroupsRemoveX509
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
