apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Retrieves Existing Distribution Policies.
  description: Retrieves existing distribution policies.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieves Existing Distribution Policies.
      description: Retrieves existing distribution policies.
    paths:
      /routing/distributionPolicies:
        get:
          summary: Retrieves Existing Distribution Policies.
          description: Retrieves existing distribution policies.
          operationId: jobRouterAdministrationOperationsListDistributionPolicies
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
