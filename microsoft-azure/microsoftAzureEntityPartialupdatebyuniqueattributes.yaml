apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureEntityPartialupdatebyuniqueattributes
  description: >-
    Update entity partially - Allow a subset of attributes to be updated
    on<br>an entity which is identified by its type and unique attribute  eg:
    Referenceable.qualifiedName.<br>Null updates are not possible.<br>In
    addition to the typeName path parameter, attribute key-value pair(s) can be
    provided in the following format:<br>attr:=.<br>NOTE: The attrName and
    attrValue should be unique across entities, eg. qualifiedName.<br>The REST
    request would look something like this:<br>PUT
    /v2/entity/uniqueAttribute/type/aType?attr:aTypeAttribute=someValue.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Atlas Entity Uniqueattribute Type Typename
      description: >-
        Update entity partially - Allow a subset of attributes to be updated
        on<br>an entity which is identified by its type and unique attribute  eg:
        Referenceable.qualifiedName.<br>Null updates are not possible.<br>In
        addition to the typeName path parameter, attribute key-value pair(s) can be
        provided in the following format:<br>attr:=.<br>NOTE: The attrName and
        attrValue should be unique across entities, eg. qualifiedName.<br>The REST
        request would look something like this:<br>PUT
        /v2/entity/uniqueAttribute/type/aType?attr:aTypeAttribute=someValue.
    paths:
      /atlas/v2/entity/uniqueAttribute/type/{typeName}:
        put:
          summary: Put Atlas Entity Uniqueattribute Type Typename
          description: >-
            Update entity partially - Allow a subset of attributes to be updated
            on<br>an entity which is identified by its type and unique attribute 
            eg: Referenceable.qualifiedName.<br>Null updates are not possible.<br>In
            addition to the typeName path parameter, attribute key-value pair(s) can
            be provided in the following format:<br>attr:=.<br>NOTE: The attrName
            and attrValue should be unique across entities, eg.
            qualifiedName.<br>The REST request would look something like
            this:<br>PUT
            /v2/entity/uniqueAttribute/type/aType?attr:aTypeAttribute=someValue.
          operationId: microsoftAzureEntityPartialupdatebyuniqueattributes
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
