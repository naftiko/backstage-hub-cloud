apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: remove-a-participant-from-the-call
  description: Remove a participant from the call.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Remove A Participant From The Call.
      description: Remove a participant from the call.
    paths:
      /calling/callConnections/{callConnectionId}/participants/{participantId}:
        delete:
          summary: Remove A Participant From The Call.
          description: Remove a participant from the call.
          operationId: callConnectionsRemoveParticipant
          responses:
            '500':
              $ref: '#/responses/500'
