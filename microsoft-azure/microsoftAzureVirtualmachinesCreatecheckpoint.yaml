apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureVirtualmachinesCreatecheckpoint
  description: Creates a checkpoint in virtual machine.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Implements The Operation To Creates A Checkpoint In A Virtual Machine '
      description: Creates a checkpoint in virtual machine.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ScVmm/virtualMachines/{virtualMachineName}/createCheckpoint:
        post:
          summary: 'Implements The Operation To Creates A Checkpoint In A Virtual Machine '
          description: Creates a checkpoint in virtual machine.
          operationId: microsoftAzureVirtualmachinesCreatecheckpoint
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
