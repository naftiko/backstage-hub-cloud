apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-projects-projectname-users-userid-environments-environmentname
  description: Deletes an environment and all its associated resources
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Projects Projectname Users Userid Environments Environmentname
      description: Deletes an environment and all its associated resources
    paths:
      /projects/{projectName}/users/{userId}/environments/{environmentName}:
        delete:
          summary: Delete Projects Projectname Users Userid Environments Environmentname
          description: Deletes an environment and all its associated resources
          operationId: environmentsDeleteEnvironment
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: devcenter.json#/definitions/CloudError
              headers:
                x-ms-error-code:
                  description: The error code for specific error that occurred.
                  type: string
