apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: creates-or-updates-a-worker
  description: Creates or updates a worker.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Creates Or Updates A Worker.
      description: Creates or updates a worker.
    paths:
      /routing/workers/{workerId}:
        patch:
          summary: Creates Or Updates A Worker.
          description: Creates or updates a worker.
          operationId: jobRouterOperationsUpsertWorker
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
