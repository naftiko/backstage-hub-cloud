apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Redirect A Call.
  description: Redirect a call.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Redirect A Call.
      description: Redirect a call.
    paths:
      /calling/callConnections:redirect:
        post:
          summary: Redirect A Call.
          description: Redirect a call.
          operationId: serverCallingRedirectCall
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/CommunicationErrorResponse'
