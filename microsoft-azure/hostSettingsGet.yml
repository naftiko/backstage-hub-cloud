apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Subscriptions Subscriptionid Providers Microsoft.botservice Hostsettings
  description: >-
    Get per subscription settings needed to host bot in compute resource such as
    Azure App Service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Subscriptions Subscriptionid Providers Microsoft.botservice Hostsettings
      description: >-
        Get per subscription settings needed to host bot in compute resource such as
        Azure App Service
    paths:
      /subscriptions/{subscriptionId}/providers/Microsoft.BotService/hostSettings:
        get:
          summary: >-
            Get Subscriptions Subscriptionid Providers Microsoft.botservice
            Hostsettings
          description: >-
            Get per subscription settings needed to host bot in compute resource
            such as Azure App Service
          operationId: hostSettingsGet
          responses:
            default:
              description: Error response describing why the operation failed
              schema:
                $ref: '#/definitions/Error'
