apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-healthcare-analysis-job-status-and-results
  description: Get details of the healthcare prediction job specified by the jobId.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Healthcare Analysis Job Status And Results
      description: Get details of the healthcare prediction job specified by the jobId.
    paths:
      /entities/health/jobs/{jobId}:
        get:
          summary: Get Healthcare Analysis Job Status And Results
          description: Get details of the healthcare prediction job specified by the jobId.
          operationId: healthStatus
          responses:
            '500':
              description: Internal error response
              schema:
                $ref: '#/definitions/ErrorResponse'
              x-ms-error-response: true
