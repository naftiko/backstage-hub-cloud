apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Skillsets
  description: List all skillsets in a search service.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Skillsets
      description: List all skillsets in a search service.
    paths:
      /skillsets:
        get:
          summary: Get Skillsets
          description: List all skillsets in a search service.
          operationId: skillsetsList
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
