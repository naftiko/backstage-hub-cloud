apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Put Storage Storage-account-name
  description: >-
    Creates or updates a new storage account. This operation requires the
    storage/set permission.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Storage Storage-account-name
      description: >-
        Creates or updates a new storage account. This operation requires the
        storage/set permission.
    paths:
      /storage/{storage-account-name}:
        put:
          summary: Put Storage Storage-account-name
          description: >-
            Creates or updates a new storage account. This operation requires the
            storage/set permission.
          operationId: setStorageAccount
          responses:
            default:
              description: Key Vault error response describing why the operation failed.
              schema:
                $ref: common.json#/definitions/KeyVaultError
