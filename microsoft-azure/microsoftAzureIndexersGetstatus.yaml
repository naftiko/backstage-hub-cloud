apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureIndexersGetstatus
  description: Returns the current status and execution history of an indexer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Indexers Indexername  Search Status
      description: Returns the current status and execution history of an indexer.
    paths:
      /indexers('{indexerName}')/search.status:
        get:
          summary: Get Indexers Indexername  Search Status
          description: Returns the current status and execution history of an indexer.
          operationId: microsoftAzureIndexersGetstatus
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
