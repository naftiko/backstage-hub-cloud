apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-authoring-analyze-conversations-projects-projectname-deployments-swap
  description: Swaps two existing deployments with each other.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Authoring Analyze-conversations Projects Projectname Deployments :swap
      description: Swaps two existing deployments with each other.
    paths:
      /authoring/analyze-conversations/projects/{projectName}/deployments/:swap:
        post:
          summary: >-
            Post Authoring Analyze-conversations Projects Projectname Deployments
            :swap
          description: Swaps two existing deployments with each other.
          operationId: conversationalAnalysisAuthoringSwapDeployments
          responses:
            default:
              description: Error response.
              schema:
                $ref: common.json#/definitions/ErrorResponse
