apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Cancel A Task By Taskid.
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Cancel A Task By Taskid.
      description: Needs a more full description created.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/workspaces/{workspaceName}/tasks/{taskId}:cancel:
        post:
          summary: Cancel A Task By Taskid.
          description: Needs a more full description created.
          operationId: tasksCancel
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
