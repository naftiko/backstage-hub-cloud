apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Gets A Vmmserver.
  description: Implements VMMServer GET method.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Gets A Vmmserver.
      description: Implements VMMServer GET method.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ScVmm/vmmServers/{vmmServerName}:
        get:
          summary: Gets A Vmmserver.
          description: Implements VMMServer GET method.
          operationId: vmmServersGet
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
