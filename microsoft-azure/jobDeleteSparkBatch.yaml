apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-livy-batches-batchid
  description: Cancels a running spark batch job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Livy Batches Batchid
      description: Cancels a running spark batch job.
    paths:
      /livy/batches/{batchId}:
        delete:
          summary: Delete Livy Batches Batchid
          description: Cancels a running spark batch job.
          operationId: jobDeleteSparkBatch
          responses:
            '200':
              description: Success
              schema:
                $ref: '#/definitions/SparkJobDeletedResult'
