apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureEncryptedsharesGet
  description: >-
    Retrieve a member's recovery share, encrypted with that member's
    share-encryption key.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Gov Recovery Encrypted Shares Memberid
      description: >-
        Retrieve a member's recovery share, encrypted with that member's
        share-encryption key.
    paths:
      /gov/recovery/encrypted-shares/{memberId}:
        get:
          summary: Get Gov Recovery Encrypted Shares Memberid
          description: >-
            Retrieve a member's recovery share, encrypted with that member's
            share-encryption key.
          operationId: microsoftAzureEncryptedsharesGet
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
