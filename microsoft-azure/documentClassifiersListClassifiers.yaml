apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-documentclassifiers
  description: List all document classifiers.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Documentclassifiers
      description: List all document classifiers.
    paths:
      /documentClassifiers:
        get:
          summary: Get Documentclassifiers
          description: List all document classifiers.
          operationId: documentClassifiersListClassifiers
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/ErrorResponse'
