apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: update-module-properties-via-patch
  description: Update property values of a module.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update Module Properties Via Patch
      description: Update property values of a module.
    paths:
      /devices/{deviceId}/modules/{moduleName}/properties:
        patch:
          summary: Update Module Properties Via Patch
          description: Update property values of a module.
          operationId: devicesUpdateModuleProperties
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
