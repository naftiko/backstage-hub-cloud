apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-collections-collectionname-scans
  description: List scans of the collection
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Collections Collectionname Scans
      description: List scans of the collection
    paths:
      /collections/{collectionName}/scans:
        get:
          summary: Get Collections Collectionname Scans
          description: List scans of the collection
          operationId: scansListByCollection
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
