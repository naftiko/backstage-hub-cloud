apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-workflowtasks-taskid-approve-approval
  description: Approve an approval task.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Workflowtasks Taskid Approve-approval
      description: Approve an approval task.
    paths:
      /workflowtasks/{taskId}/approve-approval:
        post:
          summary: Post Workflowtasks Taskid Approve-approval
          description: Approve an approval task.
          operationId: approveApprovalTask
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
