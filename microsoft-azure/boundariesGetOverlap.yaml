apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-parties-partyid-boundaries-boundaryid-overlap
  description: Returns overlapping area between two boundary Ids.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Parties Partyid Boundaries Boundaryid Overlap
      description: Returns overlapping area between two boundary Ids.
    paths:
      /parties/{partyId}/boundaries/{boundaryId}/overlap:
        get:
          summary: Get Parties Partyid Boundaries Boundaryid Overlap
          description: Returns overlapping area between two boundary Ids.
          operationId: boundariesGetOverlap
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
