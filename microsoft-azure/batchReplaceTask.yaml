apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: put-jobs-jobid-tasks-taskid
  description: Updates the properties of the specified Task.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Jobs Jobid Tasks Taskid
      description: Updates the properties of the specified Task.
    paths:
      /jobs/{jobId}/tasks/{taskId}:
        put:
          summary: Put Jobs Jobid Tasks Taskid
          description: Updates the properties of the specified Task.
          operationId: batchReplaceTask
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/BatchError'
