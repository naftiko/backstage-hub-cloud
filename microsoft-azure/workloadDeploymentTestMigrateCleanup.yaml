apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: performs-test-migrate-cleanup
  description: Performs the test migrate cleanup on the workload deployment.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Performs Test Migrate Cleanup.
      description: Performs the test migrate cleanup on the workload deployment.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments/{workloadDeploymentName}/testMigrateCleanup:
        post:
          summary: Performs Test Migrate Cleanup.
          description: Performs the test migrate cleanup on the workload deployment.
          operationId: workloadDeploymentTestMigrateCleanup
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
