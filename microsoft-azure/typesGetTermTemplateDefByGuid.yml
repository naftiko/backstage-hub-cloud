apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Types Termtemplatedef Guid Guid
  description: Get the term template definition for the given GUID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Types Termtemplatedef Guid Guid
      description: Get the term template definition for the given GUID.
    paths:
      /types/termtemplatedef/guid/{guid}:
        get:
          summary: Get Types Termtemplatedef Guid Guid
          description: Get the term template definition for the given GUID.
          operationId: typesGetTermTemplateDefByGuid
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
