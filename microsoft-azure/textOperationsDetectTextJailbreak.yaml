apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: analyze-text-jailbreak
  description: A synchronous API for the analysis of text jailbreak.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Analyze Text Jailbreak
      description: A synchronous API for the analysis of text jailbreak.
    paths:
      /text:detectJailbreak:
        post:
          summary: Analyze Text Jailbreak
          description: A synchronous API for the analysis of text jailbreak.
          operationId: textOperationsDetectTextJailbreak
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
