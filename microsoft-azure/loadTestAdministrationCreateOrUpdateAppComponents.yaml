apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: add-an-app-component-to-a-test
  description: Add an app component to a test by providing the resource Id, name and type.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Add An App Component To A Test.
      description: Add an app component to a test by providing the resource Id, name and type.
    paths:
      /tests/{testId}/app-components:
        patch:
          summary: Add An App Component To A Test.
          description: >-
            Add an app component to a test by providing the resource Id, name and
            type.
          operationId: loadTestAdministrationCreateOrUpdateAppComponents
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
