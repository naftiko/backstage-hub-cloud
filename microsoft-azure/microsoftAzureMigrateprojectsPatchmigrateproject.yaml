apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureMigrateprojectsPatchmigrateproject
  description: >-
    Update a migrate project with specified name. Supports partial updates, for
    example only tags can be provided.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Update Migrate Project '
      description: >-
        Update a migrate project with specified name. Supports partial updates, for
        example only tags can be provided.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/migrateProjects/{migrateProjectName}:
        patch:
          summary: 'Update Migrate Project '
          description: >-
            Update a migrate project with specified name. Supports partial updates,
            for example only tags can be provided.
          operationId: microsoftAzureMigrateprojectsPatchmigrateproject
          responses:
            '200':
              description: OK
              schema:
                $ref: '#/definitions/MigrateProject'
