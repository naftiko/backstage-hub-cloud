apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: gets-services-of-a-given-application
  description: >-
    Gets the information about all services of a given service of an
    application. The information includes the runtime properties of the service
    instance.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Gets Services Of A Given Application.
      description: >-
        Gets the information about all services of a given service of an
        application. The information includes the runtime properties of the service
        instance.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ServiceFabricMesh/applications/{applicationName}/services:
        get:
          summary: Gets Services Of A Given Application.
          description: >-
            Gets the information about all services of a given service of an
            application. The information includes the runtime properties of the
            service instance.
          operationId: serviceListByApplicationName
          responses:
            '200':
              description: OK
              schema:
                $ref: '#/definitions/ServiceList'
