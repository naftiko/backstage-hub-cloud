apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Livy Batches
  description: List all spark batch jobs
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Livy Batches
      description: List all spark batch jobs
    paths:
      /livy/batches:
        get:
          summary: Get Livy Batches
          description: List all spark batch jobs
          operationId: jobListSparkBatchJob
          responses:
            '200':
              description: Success
              schema:
                $ref: '#/definitions/SparkBatchJobCollection'
