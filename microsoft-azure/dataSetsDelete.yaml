apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: deletes-the-specified-dataset
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deletes The Specified Dataset.
      description: Needs a more full description created.
    paths:
      /datasets/{id}:
        delete:
          summary: Deletes The Specified Dataset.
          description: Needs a more full description created.
          operationId: datasetsDelete
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/Error'
