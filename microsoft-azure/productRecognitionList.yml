apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List All Product Recognition Run Of A Model.
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List All Product Recognition Run Of A Model.
      description: Needs a more full description created.
    paths:
      /productrecognition/{modelName}/runs:
        get:
          summary: List All Product Recognition Run Of A Model.
          description: Needs a more full description created.
          operationId: productRecognitionList
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
