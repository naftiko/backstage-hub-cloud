apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: patch-datasets-datasetid-records-recordid
  description: Creates or updates a dataset record.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Patch Datasets Datasetid Records Recordid
      description: Creates or updates a dataset record.
    paths:
      /datasets/{datasetId}/records/{recordId}:
        patch:
          summary: Patch Datasets Datasetid Records Recordid
          description: Creates or updates a dataset record.
          operationId: datasetRecordsCreateOrUpdate
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
