apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Creates Or Updates A Application Resource.
  description: >-
    Creates a Application resource with the specified name, description and
    properties. If Application resource with the same name exists, then it is
    updated with the specified description and properties.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Creates Or Updates A Application Resource.
      description: >-
        Creates a Application resource with the specified name, description and
        properties. If Application resource with the same name exists, then it is
        updated with the specified description and properties.
    paths:
      /Resources/Applications/{applicationResourceName}:
        put:
          summary: Creates Or Updates A Application Resource.
          description: >-
            Creates a Application resource with the specified name, description and
            properties. If Application resource with the same name exists, then it
            is updated with the specified description and properties.
          operationId: meshApplicationCreateOrUpdate
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/FabricError'
