apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureDatasetGetoperation
  description: >-
    This API allows the caller to view the current progress of a dataset
    operation and the path is obtained from a call to the Create API.<br><br>###
    Submit Operations Request<br><br>To view the current progress of a dataset
    operation, you will use a `GET` request where the `operationId` given the
    path is the ID that represents the operation.<br><br>### Operation
    Response<br><br>While in progress, a `200-OK` http status code will be
    returned with no extra headers. If the operation succeeds, a `200-OK` http
    status code with Resource-Location header will be returned.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Datasets Operations Operationid
      description: >-
        This API allows the caller to view the current progress of a dataset
        operation and the path is obtained from a call to the Create API.<br><br>###
        Submit Operations Request<br><br>To view the current progress of a dataset
        operation, you will use a `GET` request where the `operationId` given the
        path is the ID that represents the operation.<br><br>### Operation
        Response<br><br>While in progress, a `200-OK` http status code will be
        returned with no extra headers. If the operation succeeds, a `200-OK` http
        status code with Resource-Location header will be returned.
    paths:
      /datasets/operations/{operationId}:
        get:
          summary: Get Datasets Operations Operationid
          description: >-
            This API allows the caller to view the current progress of a dataset
            operation and the path is obtained from a call to the Create
            API.<br><br>### Submit Operations Request<br><br>To view the current
            progress of a dataset operation, you will use a `GET` request where the
            `operationId` given the path is the ID that represents the
            operation.<br><br>### Operation Response<br><br>While in progress, a
            `200-OK` http status code will be returned with no extra headers. If the
            operation succeeds, a `200-OK` http status code with Resource-Location
            header will be returned.
          operationId: microsoftAzureDatasetGetoperation
          responses:
            '400':
              $ref: '#/responses/400'
