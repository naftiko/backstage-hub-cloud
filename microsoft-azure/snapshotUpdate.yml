apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Patch Snapshots Snapshotid
  description: >-
    Update the information of a snapshot. Only the source subscription who took
    the snapshot can update the snapshot.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Patch Snapshots Snapshotid
      description: >-
        Update the information of a snapshot. Only the source subscription who took
        the snapshot can update the snapshot.
    paths:
      /snapshots/{snapshotId}:
        patch:
          summary: Patch Snapshots Snapshotid
          description: >-
            Update the information of a snapshot. Only the source subscription who
            took the snapshot can update the snapshot.
          operationId: snapshotUpdate
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/APIError'
