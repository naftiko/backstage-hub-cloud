apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Performs Migrate.
  description: Performs migrate on the workload deployment.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Performs Migrate.
      description: Performs migrate on the workload deployment.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments/{workloadDeploymentName}/migrate:
        post:
          summary: Performs Migrate.
          description: Performs migrate on the workload deployment.
          operationId: workloadDeploymentMigrate
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
