apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureOauthprovidersCreatecascadedeletejob
  description: Create cascade delete job for oauthProvider resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Oauth Providers Cascade Delete Jobid
      description: Create cascade delete job for oauthProvider resource.
    paths:
      /oauth/providers/cascade-delete/{jobId}:
        put:
          summary: Put Oauth Providers Cascade Delete Jobid
          description: Create cascade delete job for oauthProvider resource.
          operationId: microsoftAzureOauthprovidersCreatecascadedeletejob
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
