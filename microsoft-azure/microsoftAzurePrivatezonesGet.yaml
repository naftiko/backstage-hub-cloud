apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzurePrivatezonesGet
  description: >-
    Gets a Private DNS zone. Retrieves the zone properties, but not the virtual
    networks links or the record sets within the zone.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Get Subscriptions Subscriptionid Resourcegroups Resourcegroupname Providers
        Microsoft Network Privatednszones Privatezonename
      description: >-
        Gets a Private DNS zone. Retrieves the zone properties, but not the virtual
        networks links or the record sets within the zone.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Network/privateDnsZones/{privateZoneName}:
        get:
          summary: >-
            Get Subscriptions Subscriptionid Resourcegroups Resourcegroupname
            Providers Microsoft Network Privatednszones Privatezonename
          description: >-
            Gets a Private DNS zone. Retrieves the zone properties, but not the
            virtual networks links or the record sets within the zone.
          operationId: microsoftAzurePrivatezonesGet
          responses:
            default:
              description: >-
                Default response. It will be deserialized as per the Error
                definition.
              schema:
                $ref: '#/definitions/CloudError'
