apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-systemscanrulesets-versions-version
  description: Get a scan ruleset by version
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Systemscanrulesets Versions Version
      description: Get a scan ruleset by version
    paths:
      /systemScanRulesets/versions/{version}:
        get:
          summary: Get Systemscanrulesets Versions Version
          description: Get a scan ruleset by version
          operationId: systemScanRulesetsGetByVersion
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
