apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Synchronizations In A Share
  description: List synchronizations of a share
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Synchronizations In A Share
      description: List synchronizations of a share
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DataShare/accounts/{accountName}/shares/{shareName}/listSynchronizations:
        post:
          summary: List Synchronizations In A Share
          description: List synchronizations of a share
          operationId: sharesListSynchronizations
          responses:
            default:
              description: >-
                An error response received from the Microsoft.DataShare resource
                provider.
              schema:
                $ref: '#/definitions/DataShareError'
