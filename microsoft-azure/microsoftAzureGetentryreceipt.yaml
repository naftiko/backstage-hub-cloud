apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureGetentryreceipt
  description: Get the receipt of an entry given a specific entry id.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Entries Entryid Receipt
      description: Get the receipt of an entry given a specific entry id.
    paths:
      /entries/{entryId}/receipt:
        get:
          summary: Get Entries Entryid Receipt
          description: Get the receipt of an entry given a specific entry id.
          operationId: microsoftAzureGetentryreceipt
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
