apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-tenantid-users-objectid-getmembergroups
  description: >-
    Gets a collection that contains the object IDs of the groups of which the
    user is a member.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Tenantid Users Objectid Getmembergroups
      description: >-
        Gets a collection that contains the object IDs of the groups of which the
        user is a member.
    paths:
      /{tenantID}/users/{objectId}/getMemberGroups:
        post:
          summary: Post Tenantid Users Objectid Getmembergroups
          description: >-
            Gets a collection that contains the object IDs of the groups of which
            the user is a member.
          operationId: usersGetMemberGroups
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/GraphError'
