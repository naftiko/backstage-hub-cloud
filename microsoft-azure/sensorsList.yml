apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Sensor-partners Sensorpartnerid Sensors
  description: Returns a paginated list of sensor resources.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Sensor-partners Sensorpartnerid Sensors
      description: Returns a paginated list of sensor resources.
    paths:
      /sensor-partners/{sensorPartnerId}/sensors:
        get:
          summary: Get Sensor-partners Sensorpartnerid Sensors
          description: Returns a paginated list of sensor resources.
          operationId: sensorsList
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
