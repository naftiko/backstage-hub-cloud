apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-documentmodels-modelid
  description: Deletes document model.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Documentmodels Modelid
      description: Deletes document model.
    paths:
      /documentModels/{modelId}:
        delete:
          summary: Delete Documentmodels Modelid
          description: Deletes document model.
          operationId: documentModelsDeleteModel
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/ErrorResponse'
