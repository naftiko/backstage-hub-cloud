apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-gov-members-proposals-proposalid
  description: Returns a summary of a single proposed change to the service.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Gov Members Proposals Proposalid
      description: Returns a summary of a single proposed change to the service.
    paths:
      /gov/members/proposals/{proposalId}:
        get:
          summary: Get Gov Members Proposals Proposalid
          description: Returns a summary of a single proposed change to the service.
          operationId: proposalsGet
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
