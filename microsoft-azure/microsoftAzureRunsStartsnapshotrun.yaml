apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureRunsStartsnapshotrun
  description: >-
    Starts an experiment run on the remote compute target using the provided
    definition.json file to define the run.<br>            The code for the run
    is retrieved using the snapshotId in definition.json.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Start A Run From A Snapshot On A Remote Compute Target '
      description: >-
        Starts an experiment run on the remote compute target using the provided
        definition.json file to define the run.<br>            The code for the run
        is retrieved using the snapshotId in definition.json.
    paths:
      /execution/v1.0/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/experiments/{experimentName}/snapshotrun:
        post:
          summary: 'Start A Run From A Snapshot On A Remote Compute Target '
          description: >-
            Starts an experiment run on the remote compute target using the provided
            definition.json file to define the run.<br>            The code for the
            run is retrieved using the snapshotId in definition.json.
          operationId: microsoftAzureRunsStartsnapshotrun
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
