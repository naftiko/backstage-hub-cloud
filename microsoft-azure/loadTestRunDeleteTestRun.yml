apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete An Existing Load Test Run.
  description: Delete an existing load test run by providing the testRunId.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete An Existing Load Test Run.
      description: Delete an existing load test run by providing the testRunId.
    paths:
      /test-runs/{testRunId}:
        delete:
          summary: Delete An Existing Load Test Run.
          description: Delete an existing load test run by providing the testRunId.
          operationId: loadTestRunDeleteTestRun
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
