apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    gets-the-ledger-entry-at-the-specified-transaction-id-a-collection-id-may-optionally-be-specified-to-indicate-the-collection-from-which-to-fetch-the-value
  description: >-
    To return older ledger entries, the relevant sections of the ledger must be
    read from disk and validated. To prevent blocking within the enclave, the
    response will indicate whether the entry is ready and part of the response,
    or if the loading is still ongoing.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Gets The Ledger Entry At The Specified Transaction Id. A Collection Id May
        Optionally Be Specified To Indicate The Collection From Which To Fetch The
        Value.
      description: >-
        To return older ledger entries, the relevant sections of the ledger must be
        read from disk and validated. To prevent blocking within the enclave, the
        response will indicate whether the entry is ready and part of the response,
        or if the loading is still ongoing.
    paths:
      /app/transactions/{transactionId}:
        get:
          summary: >-
            Gets The Ledger Entry At The Specified Transaction Id. A Collection Id
            May Optionally Be Specified To Indicate The Collection From Which To
            Fetch The Value.
          description: >-
            To return older ledger entries, the relevant sections of the ledger must
            be read from disk and validated. To prevent blocking within the enclave,
            the response will indicate whether the entry is ready and part of the
            response, or if the loading is still ongoing.
          operationId: getLedgerEntry
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: common.json#/definitions/ConfidentialLedgerError
