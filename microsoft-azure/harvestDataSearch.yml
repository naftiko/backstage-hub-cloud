apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Harvest-data:search
  description: Search for HarvestData across all parties by intersecting geometry.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Harvest-data:search
      description: Search for HarvestData across all parties by intersecting geometry.
    paths:
      /harvest-data:search:
        post:
          summary: Post Harvest-data:search
          description: Search for HarvestData across all parties by intersecting geometry.
          operationId: harvestDataSearch
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
