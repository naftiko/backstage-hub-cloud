apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: creates-or-updates-a-distribution-policy
  description: Creates or updates a distribution policy.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Creates Or Updates A Distribution Policy.
      description: Creates or updates a distribution policy.
    paths:
      /routing/distributionPolicies/{distributionPolicyId}:
        patch:
          summary: Creates Or Updates A Distribution Policy.
          description: Creates or updates a distribution policy.
          operationId: jobRouterAdministrationOperationsUpsertDistributionPolicy
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
