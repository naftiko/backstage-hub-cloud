apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Datasources Datasourcename Scans
  description: List scans in data source
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Datasources Datasourcename Scans
      description: List scans in data source
    paths:
      /datasources/{dataSourceName}/scans:
        get:
          summary: Get Datasources Datasourcename Scans
          description: List scans in data source
          operationId: scansListByDataSource
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
