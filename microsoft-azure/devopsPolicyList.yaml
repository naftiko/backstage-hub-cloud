apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-devops-policies
  description: List all devops policies.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Devops-policies
      description: List all devops policies.
    paths:
      /devops-policies:
        get:
          summary: Get Devops-policies
          description: List all devops policies.
          operationId: devopsPolicyList
          responses:
            default:
              description: An error response received from the Policy Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
              headers:
                x-ms-error-code:
                  type: string
                  description: The error code
