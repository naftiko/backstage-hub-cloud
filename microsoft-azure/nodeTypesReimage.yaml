apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: reimages-one-or-more-nodes-on-the-node-type
  description: >-
    Reimages one or more nodes on the node type. It will disable the fabric
    nodes, trigger a reimage on the VMs and activate the nodes back again.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Reimages One Or More Nodes On The Node Type.
      description: >-
        Reimages one or more nodes on the node type. It will disable the fabric
        nodes, trigger a reimage on the VMs and activate the nodes back again.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ServiceFabric/managedClusters/{clusterName}/nodeTypes/{nodeTypeName}/reimage:
        post:
          summary: Reimages One Or More Nodes On The Node Type.
          description: >-
            Reimages one or more nodes on the node type. It will disable the fabric
            nodes, trigger a reimage on the VMs and activate the nodes back again.
          operationId: nodeTypesReimage
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/ErrorModel'
