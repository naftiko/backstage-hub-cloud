apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-livy-batches-batchid-log
  description: Gets a single spark batch job logs.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Livy Batches Batchid Log
      description: Gets a single spark batch job logs.
    paths:
      /livy/batches/{batchId}/log:
        get:
          summary: Get Livy Batches Batchid Log
          description: Gets a single spark batch job logs.
          operationId: jobGetSparkBatchLog
          responses:
            '200':
              description: Success
              schema:
                $ref: '#/definitions/SparkJobLog'
