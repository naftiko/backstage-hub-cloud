apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-farm-equipments-farmequipmentid
  description: Deletes dataset for given farm equipment id.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Farm-equipments Farmequipmentid
      description: Deletes dataset for given farm equipment id.
    paths:
      /farm-equipments/{farmEquipmentId}:
        delete:
          summary: Delete Farm-equipments Farmequipmentid
          description: Deletes dataset for given farm equipment id.
          operationId: farmEquipmentsDelete
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
