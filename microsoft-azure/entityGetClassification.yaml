apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-atlas-entity-guid-guid-classification-classificationname
  description: Get classification for a given entity represented by a GUID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Atlas Entity Guid Guid Classification Classificationname
      description: Get classification for a given entity represented by a GUID.
    paths:
      /atlas/v2/entity/guid/{guid}/classification/{classificationName}:
        get:
          summary: Get Atlas Entity Guid Guid Classification Classificationname
          description: Get classification for a given entity represented by a GUID.
          operationId: entityGetClassification
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
