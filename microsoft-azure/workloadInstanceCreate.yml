apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Puts The Workload Instance.
  description: Creates the workload instance in the given modernizeProject.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Puts The Workload Instance.
      description: Creates the workload instance in the given modernizeProject.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadInstances/{workloadInstanceName}:
        put:
          summary: Puts The Workload Instance.
          description: Creates the workload instance in the given modernizeProject.
          operationId: workloadInstanceCreate
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
