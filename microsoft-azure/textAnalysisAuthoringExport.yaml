apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-authoring-analyze-text-projects-projectname-export
  description: Triggers a job to export a project's data.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Authoring Analyze-text Projects Projectname :export
      description: Triggers a job to export a project's data.
    paths:
      /authoring/analyze-text/projects/{projectName}/:export:
        post:
          summary: Post Authoring Analyze-text Projects Projectname :export
          description: Triggers a job to export a project's data.
          operationId: textAnalysisAuthoringExport
          responses:
            default:
              description: Error response.
              schema:
                $ref: common.json#/definitions/ErrorResponse
