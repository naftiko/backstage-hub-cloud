apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureEnrollmentgroupsCreate
  description: Create an enrollment group.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create An Enrollment Group
      description: Create an enrollment group.
    paths:
      /enrollmentGroups/{enrollmentGroupId}:
        put:
          summary: Create An Enrollment Group
          description: Create an enrollment group.
          operationId: microsoftAzureEnrollmentgroupsCreate
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
