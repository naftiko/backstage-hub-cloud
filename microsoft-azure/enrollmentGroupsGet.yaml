apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-an-enrollment-group-by-id
  description: Get details about an enrollment group by ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get An Enrollment Group By Id
      description: Get details about an enrollment group by ID.
    paths:
      /enrollmentGroups/{enrollmentGroupId}:
        get:
          summary: Get An Enrollment Group By Id
          description: Get details about an enrollment group by ID.
          operationId: enrollmentGroupsGet
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
