apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureDeviceregistrationstateGet
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Gets The Device Registration State '
      description: Needs a more full description created.
    paths:
      /registrations/{id}:
        get:
          summary: 'Gets The Device Registration State '
          description: Needs a more full description created.
          operationId: microsoftAzureDeviceregistrationstateGet
          responses:
            default:
              description: Error response
              schema:
                $ref: '#/definitions/ProvisioningServiceErrorDetails'
              headers:
                x-ms-error-code:
                  description: The error code for specific error that occurred.
                  type: string
