apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Test Runs With Given Filters
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Test Runs With Given Filters
      description: Needs a more full description created.
    paths:
      /testruns/sortAndFilter:
        get:
          summary: Get All Test Runs With Given Filters
          description: Needs a more full description created.
          operationId: testRunGetAppTestRunsSearch
          responses:
            default:
              description: >-
                *** Error Responses: ***

                 * "400", "InvalidHostOrResourceName", "Host name or domain is not valid"

                 * "404", "TestRunNotFound", "test run not found with given identifier"

                * "500", "InternalServerError", "Unable to process the request,
                please retry"
              schema:
                $ref: '#/definitions/DefaultErrorResponseBody'
              headers:
                x-ms-error-code:
                  description: The error code for specific error that occurred.
                  type: string
