apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Patch Table(partitionkey='partitionkey',rowkey='rowkey')
  description: Merge entity in a table.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Patch Table(partitionkey='partitionkey',rowkey='rowkey')
      description: Merge entity in a table.
    paths:
      /{table}(PartitionKey='{partitionKey}',RowKey='{rowKey}'):
        patch:
          summary: Patch Table(partitionkey='partitionkey',rowkey='rowkey')
          description: Merge entity in a table.
          operationId: tableMergeEntity
          responses:
            default:
              description: Failure
              headers:
                x-ms-error-code:
                  x-ms-client-name: ErrorCode
                  type: string
              schema:
                $ref: '#/definitions/TableServiceError'
