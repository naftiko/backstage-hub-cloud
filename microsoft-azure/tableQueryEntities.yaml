apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-table
  description: Queries entities in a table.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Table()
      description: Queries entities in a table.
    paths:
      /{table}():
        get:
          summary: Get Table()
          description: Queries entities in a table.
          operationId: tableQueryEntities
          responses:
            default:
              description: Failure
              headers:
                x-ms-error-code:
                  x-ms-client-name: ErrorCode
                  type: string
              schema:
                $ref: '#/definitions/TableServiceError'
