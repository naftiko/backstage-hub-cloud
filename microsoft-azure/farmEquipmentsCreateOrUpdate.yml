apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Patch Farm-equipments Farmequipmentid
  description: Creates or updates a farm equipment resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Patch Farm-equipments Farmequipmentid
      description: Creates or updates a farm equipment resource.
    paths:
      /farm-equipments/{farmEquipmentId}:
        patch:
          summary: Patch Farm-equipments Farmequipmentid
          description: Creates or updates a farm equipment resource.
          operationId: farmEquipmentsCreateOrUpdate
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
