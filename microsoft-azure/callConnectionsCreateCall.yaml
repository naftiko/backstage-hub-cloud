apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: create-a-new-call
  description: Create a new call.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A New Call.
      description: Create a new call.
    paths:
      /calling/callConnections:
        post:
          summary: Create A New Call.
          description: Create a new call.
          operationId: callConnectionsCreateCall
          responses:
            '500':
              $ref: '#/responses/500'
