apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Keys For A Topic.
  description: List the two keys used to publish to a topic.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Keys For A Topic.
      description: List the two keys used to publish to a topic.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.EventGrid/topics/{topicName}/listKeys:
        post:
          summary: List Keys For A Topic.
          description: List the two keys used to publish to a topic.
          operationId: topicsListSharedAccessKeys
          responses:
            default:
              description: |-
                *** Error Responses: ***

                 * 400 Bad Request.

                 * 500 Internal Server Error.
