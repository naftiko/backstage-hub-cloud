apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: list-the-metric-definitions-for-a-load-test-run
  description: List the metric definitions for a load test run.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List The Metric Definitions For A Load Test Run.
      description: List the metric definitions for a load test run.
    paths:
      /test-runs/{testRunId}/metric-definitions:
        get:
          summary: List The Metric Definitions For A Load Test Run.
          description: List the metric definitions for a load test run.
          operationId: loadTestRunListMetricDefinitions
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
