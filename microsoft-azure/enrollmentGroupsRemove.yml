apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete An Enrollment Group
  description: Delete an enrollment group by ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete An Enrollment Group
      description: Delete an enrollment group by ID.
    paths:
      /enrollmentGroups/{enrollmentGroupId}:
        delete:
          summary: Delete An Enrollment Group
          description: Delete an enrollment group by ID.
          operationId: enrollmentGroupsRemove
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
