apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update An Individual Device Attestation Via Patch
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update An Individual Device Attestation Via Patch
      description: Needs a more full description created.
    paths:
      /devices/{deviceId}/attestation:
        patch:
          summary: Update An Individual Device Attestation Via Patch
          description: Needs a more full description created.
          operationId: devicesUpdateAttestation
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
