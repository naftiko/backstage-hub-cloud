apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Datasources('datasourcename')
  description: Deletes a datasource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Datasources('datasourcename')
      description: Deletes a datasource.
    paths:
      /datasources('{dataSourceName}'):
        delete:
          summary: Delete Datasources('datasourcename')
          description: Deletes a datasource.
          operationId: dataSourcesDelete
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
