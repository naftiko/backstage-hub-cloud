apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureMapconfigurationList
  description: >-
    <br><br>### Submit List Request <br><br>The Map Configuration `List` API is
    an HTTP `GET` request used to get a list of all map configurations.<br><br>
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Use To Get A List Of All Map Configurations '
      description: >-
        <br><br>### Submit List Request <br><br>The Map Configuration `List` API is
        an HTTP `GET` request used to get a list of all map configurations.<br><br>
    paths:
      /styles/mapconfigurations:
        get:
          summary: 'Use To Get A List Of All Map Configurations '
          description: >-
            <br><br>### Submit List Request <br><br>The Map Configuration `List` API
            is an HTTP `GET` request used to get a list of all map
            configurations.<br><br>
          operationId: microsoftAzureMapconfigurationList
          responses:
            default:
              $ref: ../../../Common/preview/1.0/common.json#/responses/default
