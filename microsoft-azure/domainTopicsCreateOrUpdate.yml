apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Or Update A Domain Topic.
  description: >-
    Asynchronously creates or updates a new domain topic with the specified
    parameters.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Or Update A Domain Topic.
      description: >-
        Asynchronously creates or updates a new domain topic with the specified
        parameters.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.EventGrid/domains/{domainName}/topics/{domainTopicName}:
        put:
          summary: Create Or Update A Domain Topic.
          description: >-
            Asynchronously creates or updates a new domain topic with the specified
            parameters.
          operationId: domainTopicsCreateOrUpdate
          responses:
            default:
              description: |-
                *** Error Responses: ***

                 * 400 Bad Request.

                 * 500 Internal Server Error.
