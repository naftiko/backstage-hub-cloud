apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-a-domain-topic
  description: Delete existing domain topic.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Domain Topic.
      description: Delete existing domain topic.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.EventGrid/domains/{domainName}/topics/{domainTopicName}:
        delete:
          summary: Delete A Domain Topic.
          description: Delete existing domain topic.
          operationId: domainTopicsDelete
          responses:
            default:
              description: |-
                *** Error Responses: ***

                 * 400 Bad Request.

                 * 404 Not Found.

                 * 500 Internal Server Error.
