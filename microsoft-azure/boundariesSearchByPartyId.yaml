apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-parties-partyid-boundaries
  description: Search for boundaries by fields and intersecting geometry.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Parties Partyid Boundaries
      description: Search for boundaries by fields and intersecting geometry.
    paths:
      /parties/{partyId}/boundaries:
        post:
          summary: Post Parties Partyid Boundaries
          description: Search for boundaries by fields and intersecting geometry.
          operationId: boundariesSearchByPartyId
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
