apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: list-keys-for-a-partner-namespace
  description: List the two keys used to publish to a partner namespace.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Keys For A Partner Namespace.
      description: List the two keys used to publish to a partner namespace.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.EventGrid/partnerNamespaces/{partnerNamespaceName}/listKeys:
        post:
          summary: List Keys For A Partner Namespace.
          description: List the two keys used to publish to a partner namespace.
          operationId: partnerNamespacesListSharedAccessKeys
          responses:
            default:
              description: |-
                *** Error Responses: ***

                 * 400 Bad Request.

                 * 500 Internal Server Error.
