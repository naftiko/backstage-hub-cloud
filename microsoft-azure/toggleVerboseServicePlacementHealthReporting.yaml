apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: changes-the-verbosity-of-service-placement-health-reporting
  description: >-
    If verbosity is set to true, then detailed health reports will be generated
    when replicas cannot be placed or dropped.If verbosity is set to false, then
    no health reports will be generated when replicas cannot be placed or
    dropped.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Changes The Verbosity Of Service Placement Health Reporting.
      description: >-
        If verbosity is set to true, then detailed health reports will be generated
        when replicas cannot be placed or dropped.If verbosity is set to false, then
        no health reports will be generated when replicas cannot be placed or
        dropped.
    paths:
      /$/ToggleVerboseServicePlacementHealthReporting:
        post:
          summary: Changes The Verbosity Of Service Placement Health Reporting.
          description: >-
            If verbosity is set to true, then detailed health reports will be
            generated when replicas cannot be placed or dropped.If verbosity is set
            to false, then no health reports will be generated when replicas cannot
            be placed or dropped.
          operationId: toggleVerboseServicePlacementHealthReporting
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
