apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: update-device-properties-via-patch
  description: Update property values of a device by device ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update Device Properties Via Patch
      description: Update property values of a device by device ID.
    paths:
      /devices/{deviceId}/properties:
        patch:
          summary: Update Device Properties Via Patch
          description: Update property values of a device by device ID.
          operationId: devicesUpdateProperties
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
