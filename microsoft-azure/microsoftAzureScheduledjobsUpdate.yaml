apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureScheduledjobsUpdate
  description: Update an existing scheduled job by ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update A Scheduled Job Via Patch
      description: Update an existing scheduled job by ID.
    paths:
      /scheduledJobs/{scheduledJobId}:
        patch:
          summary: Update A Scheduled Job Via Patch
          description: Update an existing scheduled job by ID.
          operationId: microsoftAzureScheduledjobsUpdate
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
