apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Implements The Operation To Restart A Virtual Machine.
  description: Restart virtual machine.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Implements The Operation To Restart A Virtual Machine.
      description: Restart virtual machine.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ScVmm/virtualMachines/{virtualMachineName}/restart:
        post:
          summary: Implements The Operation To Restart A Virtual Machine.
          description: Restart virtual machine.
          operationId: virtualMachinesRestart
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
