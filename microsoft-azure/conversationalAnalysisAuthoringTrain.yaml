apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-authoring-analyze-conversations-projects-projectname-train
  description: Triggers a training job for a project.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Authoring Analyze-conversations Projects Projectname :train
      description: Triggers a training job for a project.
    paths:
      /authoring/analyze-conversations/projects/{projectName}/:train:
        post:
          summary: Post Authoring Analyze-conversations Projects Projectname :train
          description: Triggers a training job for a project.
          operationId: conversationalAnalysisAuthoringTrain
          responses:
            default:
              description: Error response.
              schema:
                $ref: common.json#/definitions/ErrorResponse
