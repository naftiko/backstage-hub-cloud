apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-atlas-entity-guid-guid-labels
  description: Set labels to a given entity
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Atlas Entity Guid Guid Labels
      description: Set labels to a given entity
    paths:
      /atlas/v2/entity/guid/{guid}/labels:
        post:
          summary: Post Atlas Entity Guid Guid Labels
          description: Set labels to a given entity
          operationId: entitySetLabels
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
