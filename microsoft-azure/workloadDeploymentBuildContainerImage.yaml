apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: build-container-image
  description: Performs the build container image action on the workload deployment.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Build Container Image.
      description: Performs the build container image action on the workload deployment.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments/{workloadDeploymentName}/buildContainerImage:
        post:
          summary: Build Container Image.
          description: Performs the build container image action on the workload deployment.
          operationId: workloadDeploymentBuildContainerImage
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
