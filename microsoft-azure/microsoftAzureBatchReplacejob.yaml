apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureBatchReplacejob
  description: >-
    This fully replaces all the updatable properties of the Job. For example,
    if<br>the Job has constraints associated with it and if constraints is not
    specified<br>with this request, then the Batch service will remove the
    existing constraints.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Updates The Properties Of The Specified Job '
      description: >-
        This fully replaces all the updatable properties of the Job. For example,
        if<br>the Job has constraints associated with it and if constraints is not
        specified<br>with this request, then the Batch service will remove the
        existing constraints.
    paths:
      /jobs/{jobId}:
        put:
          summary: 'Updates The Properties Of The Specified Job '
          description: >-
            This fully replaces all the updatable properties of the Job. For
            example, if<br>the Job has constraints associated with it and if
            constraints is not specified<br>with this request, then the Batch
            service will remove the existing constraints.
          operationId: microsoftAzureBatchReplacejob
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/BatchError'
