apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: gets-the-modernizeproject-operation-status
  description: Tracks the results of an asynchronous operation on the modernizeProject.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Gets The Modernizeproject Operation Status.
      description: Tracks the results of an asynchronous operation on the modernizeProject.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/operations/{operationId}:
        get:
          summary: Gets The Modernizeproject Operation Status.
          description: Tracks the results of an asynchronous operation on the modernizeProject.
          operationId: modernizeProjectOperationStatusGet
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
