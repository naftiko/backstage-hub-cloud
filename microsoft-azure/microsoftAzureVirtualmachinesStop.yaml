apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureVirtualmachinesStop
  description: 'Power off virtual machine, options: shutdown, poweroff, and suspend'
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Implements Shutdown, Poweroff, And Suspend Method For A Virtual Machine
      description: 'Power off virtual machine, options: shutdown, poweroff, and suspend'
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.VMwareCloudSimple/virtualMachines/{virtualMachineName}/stop:
        post:
          summary: Implements Shutdown, Poweroff, And Suspend Method For A Virtual Machine
          description: 'Power off virtual machine, options: shutdown, poweroff, and suspend'
          operationId: microsoftAzureVirtualmachinesStop
          responses:
            default:
              description: General Error
              schema:
                $ref: '#/definitions/CSRPError'
              headers:
                Content-Type:
                  type: string
                  default: application/json
