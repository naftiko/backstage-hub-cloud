apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: refreshes-the-migrateagent
  description: Refreshes the modernizeProject agent.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Refreshes The Migrateagent.
      description: Refreshes the modernizeProject agent.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/migrateAgents/{agentName}/refresh:
        post:
          summary: Refreshes The Migrateagent.
          description: Refreshes the modernizeProject agent.
          operationId: migrateAgentRefresh
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
