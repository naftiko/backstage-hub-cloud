apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureSignedinuserGet
  description: Gets the details for the currently logged-in user.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Tenantid Me
      description: Gets the details for the currently logged-in user.
    paths:
      /{tenantID}/me:
        get:
          summary: Get Tenantid Me
          description: Gets the details for the currently logged-in user.
          operationId: microsoftAzureSignedinuserGet
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/GraphError'
