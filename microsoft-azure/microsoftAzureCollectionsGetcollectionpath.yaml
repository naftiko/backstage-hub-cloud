apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureCollectionsGetcollectionpath
  description: >-
    Gets the parent name and parent friendly name chains that represent the
    collection path.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Collections Collectionname Getcollectionpath
      description: >-
        Gets the parent name and parent friendly name chains that represent the
        collection path.
    paths:
      /collections/{collectionName}/getCollectionPath:
        get:
          summary: Get Collections Collectionname Getcollectionpath
          description: >-
            Gets the parent name and parent friendly name chains that represent the
            collection path.
          operationId: microsoftAzureCollectionsGetcollectionpath
          responses:
            default:
              description: An error response received from the provider.
              schema:
                $ref: '#/definitions/ErrorResponseModel'
