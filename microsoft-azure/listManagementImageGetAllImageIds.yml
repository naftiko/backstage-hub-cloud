apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Contentmoderator Lists .0 Imagelists Listid Images
  description: Gets all image IDs from the list with listId equal to the passed list ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Contentmoderator Lists .0 Imagelists Listid Images
      description: Gets all image IDs from the list with listId equal to the passed list ID.
    paths:
      /contentmoderator/lists/v1.0/imagelists/{listId}/images:
        get:
          summary: Get Contentmoderator Lists .0 Imagelists Listid Images
          description: >-
            Gets all image IDs from the list with listId equal to the passed list
            ID.
          operationId: listManagementImageGetAllImageIds
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/APIError'
