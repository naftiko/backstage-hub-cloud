apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: remove-blocklistitems-from-text-blocklist
  description: >-
    Remove blocklistItems from a text blocklist. You can remove at most 100
    BlocklistItems in one request.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Remove Blocklistitems From Text Blocklist
      description: >-
        Remove blocklistItems from a text blocklist. You can remove at most 100
        BlocklistItems in one request.
    paths:
      /text/blocklists/{blocklistName}:removeBlocklistItems:
        post:
          summary: Remove Blocklistitems From Text Blocklist
          description: >-
            Remove blocklistItems from a text blocklist. You can remove at most 100
            BlocklistItems in one request.
          operationId: textBlocklistsRemoveBlocklistItems
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
