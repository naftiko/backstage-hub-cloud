apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: create-a-share-in-the-given-account
  description: 'Create a share '
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Share In The Given Account.
      description: 'Create a share '
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DataShare/accounts/{accountName}/shares/{shareName}:
        put:
          summary: Create A Share In The Given Account.
          description: 'Create a share '
          operationId: sharesCreate
          responses:
            default:
              description: >-
                An error response received from the Microsoft.DataShare resource
                provider.
              schema:
                $ref: '#/definitions/DataShareError'
