apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: implements-the-operation-to-stop-a-virtual-machine
  description: The operation to power off (stop) a virtual machine instance.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Implements The Operation To Stop A Virtual Machine.
      description: The operation to power off (stop) a virtual machine instance.
    paths:
      /{resourceUri}/providers/Microsoft.ConnectedVMwarevSphere/virtualMachineInstances/default/stop:
        post:
          summary: Implements The Operation To Stop A Virtual Machine.
          description: The operation to power off (stop) a virtual machine instance.
          operationId: virtualMachineInstancesStop
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: >-
                  ../../../../../common-types/resource-management/v3/types.json#/definitions/ErrorResponse
