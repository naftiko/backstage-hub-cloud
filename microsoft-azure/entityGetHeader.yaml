apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-atlas-entity-guid-guid-header
  description: Get entity header given its GUID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Atlas Entity Guid Guid Header
      description: Get entity header given its GUID.
    paths:
      /atlas/v2/entity/guid/{guid}/header:
        get:
          summary: Get Atlas Entity Guid Guid Header
          description: Get entity header given its GUID.
          operationId: entityGetHeader
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
