apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureDevicesApplymanifest
  description: Apply a deployment manifest to an edge device.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Apply A Deployment Manifest To An Edge Device '
      description: Apply a deployment manifest to an edge device.
    paths:
      /devices/{deviceId}/applyDeploymentManifest:
        post:
          summary: 'Apply A Deployment Manifest To An Edge Device '
          description: Apply a deployment manifest to an edge device.
          operationId: microsoftAzureDevicesApplymanifest
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
