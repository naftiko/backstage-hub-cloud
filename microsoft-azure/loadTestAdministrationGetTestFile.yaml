apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-all-the-files-that-are-associated-with-a-test
  description: Get all the files that are associated with a test.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All The Files That Are Associated With A Test.
      description: Get all the files that are associated with a test.
    paths:
      /tests/{testId}/files/{fileName}:
        get:
          summary: Get All The Files That Are Associated With A Test.
          description: Get all the files that are associated with a test.
          operationId: loadTestAdministrationGetTestFile
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
