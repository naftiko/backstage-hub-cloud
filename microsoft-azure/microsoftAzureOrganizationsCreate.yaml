apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureOrganizationsCreate
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create An Organization In The Application
      description: Needs a more full description created.
    paths:
      /organizations/{organizationId}:
        put:
          summary: Create An Organization In The Application
          description: Needs a more full description created.
          operationId: microsoftAzureOrganizationsCreate
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
