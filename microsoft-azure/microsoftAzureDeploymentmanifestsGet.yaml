apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureDeploymentmanifestsGet
  description: Get a deployment manifest by ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Get A Deployment Manifest By Id '
      description: Get a deployment manifest by ID.
    paths:
      /deploymentManifests/{deploymentManifestId}:
        get:
          summary: 'Get A Deployment Manifest By Id '
          description: Get a deployment manifest by ID.
          operationId: microsoftAzureDeploymentmanifestsGet
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
