apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Filesystem Properties.
  description: >-
    All system and user-defined filesystem properties are specified in the
    response headers.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Filesystem Properties.
      description: >-
        All system and user-defined filesystem properties are specified in the
        response headers.
    paths:
      /{filesystem}:
        head:
          summary: Get Filesystem Properties.
          description: >-
            All system and user-defined filesystem properties are specified in the
            response headers.
          operationId: filesystemGetProperties
          responses:
            default:
              $ref: '#/responses/ErrorResponse'
