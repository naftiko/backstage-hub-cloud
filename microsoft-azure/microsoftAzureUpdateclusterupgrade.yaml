apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureUpdateclusterupgrade
  description: Update the upgrade parameters used during a Service Fabric cluster upgrade.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Update The Upgrade Parameters Of A Service Fabric Cluster Upgrade '
      description: Update the upgrade parameters used during a Service Fabric cluster upgrade.
    paths:
      /$/UpdateUpgrade:
        post:
          summary: 'Update The Upgrade Parameters Of A Service Fabric Cluster Upgrade '
          description: >-
            Update the upgrade parameters used during a Service Fabric cluster
            upgrade.
          operationId: microsoftAzureUpdateclusterupgrade
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
