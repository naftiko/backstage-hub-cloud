apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: lists-the-workload-deployments
  description: Gets the list of workload deployments in the given modernizeProject.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Lists The Workload Deployments.
      description: Gets the list of workload deployments in the given modernizeProject.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments:
        get:
          summary: Lists The Workload Deployments.
          description: Gets the list of workload deployments in the given modernizeProject.
          operationId: workloadDeploymentList
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
