apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: put-synonymmapssynonymmapname
  description: Creates a new synonym map or updates a synonym map if it already exists.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Synonymmaps('synonymmapname')
      description: Creates a new synonym map or updates a synonym map if it already exists.
    paths:
      /synonymmaps('{synonymMapName}'):
        put:
          summary: Put Synonymmaps('synonymmapname')
          description: Creates a new synonym map or updates a synonym map if it already exists.
          operationId: synonymMapsCreateOrUpdate
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
