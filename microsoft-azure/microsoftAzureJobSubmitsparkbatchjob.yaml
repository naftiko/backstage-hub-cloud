apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureJobSubmitsparkbatchjob
  description: Create a new spark batch job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Livy Batches
      description: Create a new spark batch job.
    paths:
      /livy/batches:
        post:
          summary: Post Livy Batches
          description: Create a new spark batch job.
          operationId: microsoftAzureJobSubmitsparkbatchjob
          responses:
            '201':
              description: Created
              schema:
                $ref: '#/definitions/SparkBatchJob'
