apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureDeploymentsCalculatetemplatehash
  description: Calculate the hash of the given template.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Providers Microsoft Resources Calculatetemplatehash
      description: Calculate the hash of the given template.
    paths:
      /providers/Microsoft.Resources/calculateTemplateHash:
        post:
          summary: Post Providers Microsoft Resources Calculatetemplatehash
          description: Calculate the hash of the given template.
          operationId: microsoftAzureDeploymentsCalculatetemplatehash
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
