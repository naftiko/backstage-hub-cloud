apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Patch Applianceid
  description: >-
    Updates an existing appliance. The only value that can be updated via PATCH
    currently is the tags.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Patch Applianceid
      description: >-
        Updates an existing appliance. The only value that can be updated via PATCH
        currently is the tags.
    paths:
      /{applianceId}:
        patch:
          summary: Patch Applianceid
          description: >-
            Updates an existing appliance. The only value that can be updated via
            PATCH currently is the tags.
          operationId: appliancesUpdateById
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
