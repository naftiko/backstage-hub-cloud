apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureBatchGetnoderemotedesktopfile
  description: >-
    Before you can access a Compute Node by using the RDP file, you must create
    a<br>user Account on the Compute Node. This API can only be invoked on Pools
    created<br>with a cloud service configuration. For Pools created with a
    virtual machine<br>configuration, see the GetRemoteLoginSettings API.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Gets The Remote Desktop Protocol File For The Specified Compute Node '
      description: >-
        Before you can access a Compute Node by using the RDP file, you must create
        a<br>user Account on the Compute Node. This API can only be invoked on Pools
        created<br>with a cloud service configuration. For Pools created with a
        virtual machine<br>configuration, see the GetRemoteLoginSettings API.
    paths:
      /pools/{poolId}/nodes/{nodeId}/rdp:
        get:
          summary: 'Gets The Remote Desktop Protocol File For The Specified Compute Node '
          description: >-
            Before you can access a Compute Node by using the RDP file, you must
            create a<br>user Account on the Compute Node. This API can only be
            invoked on Pools created<br>with a cloud service configuration. For
            Pools created with a virtual machine<br>configuration, see the
            GetRemoteLoginSettings API.
          operationId: microsoftAzureBatchGetnoderemotedesktopfile
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/BatchError'
