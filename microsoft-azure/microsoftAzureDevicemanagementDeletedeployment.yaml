apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureDevicemanagementDeletedeployment
  description: Deletes a deployment.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Delete Deviceupdate Instanceid Management Groups Groupid Deployments
        Deploymentid
      description: Deletes a deployment.
    paths:
      /deviceUpdate/{instanceId}/management/groups/{groupId}/deployments/{deploymentId}:
        delete:
          summary: >-
            Delete Deviceupdate Instanceid Management Groups Groupid Deployments
            Deploymentid
          description: Deletes a deployment.
          operationId: microsoftAzureDevicemanagementDeletedeployment
          responses:
            default:
              description: Default response.
              schema:
                $ref: '#/definitions/ErrorResponse'
