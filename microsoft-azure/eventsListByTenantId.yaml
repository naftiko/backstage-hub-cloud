apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-providers-microsoftresourcehealth-events
  description: Lists current service health events in the tenant.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Providers Microsoft.resourcehealth Events
      description: Lists current service health events in the tenant.
    paths:
      /providers/Microsoft.ResourceHealth/events:
        get:
          summary: Get Providers Microsoft.resourcehealth Events
          description: Lists current service health events in the tenant.
          operationId: eventsListByTenantId
          responses:
            default:
              description: DefaultErrorResponse
              schema:
                $ref: '#/definitions/ErrorResponse'
