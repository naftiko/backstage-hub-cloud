apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureTextanalysisauthoringListtrainingjobs
  description: Lists the non-expired training jobs created for a project.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Authoring Analyze Text Projects Projectname Train Jobs
      description: Lists the non-expired training jobs created for a project.
    paths:
      /authoring/analyze-text/projects/{projectName}/train/jobs:
        get:
          summary: Get Authoring Analyze Text Projects Projectname Train Jobs
          description: Lists the non-expired training jobs created for a project.
          operationId: microsoftAzureTextanalysisauthoringListtrainingjobs
          responses:
            default:
              description: Error response.
              schema:
                $ref: common.json#/definitions/ErrorResponse
