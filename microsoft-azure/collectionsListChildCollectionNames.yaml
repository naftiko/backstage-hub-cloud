apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-collections-collectionname-getchildcollectionnames
  description: Lists the child collections names in the collection.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Collections Collectionname Getchildcollectionnames
      description: Lists the child collections names in the collection.
    paths:
      /collections/{collectionName}/getChildCollectionNames:
        get:
          summary: Get Collections Collectionname Getchildcollectionnames
          description: Lists the child collections names in the collection.
          operationId: collectionsListChildCollectionNames
          responses:
            default:
              description: An error response received from the provider.
              schema:
                $ref: '#/definitions/ErrorResponseModel'
