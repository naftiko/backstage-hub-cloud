apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-authoring-analyze-text-projects-projectname-train-jobs-jobid-cancel
  description: Triggers a cancellation for a running training job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Authoring Analyze-text Projects Projectname Train Jobs Jobid :cancel
      description: Triggers a cancellation for a running training job.
    paths:
      /authoring/analyze-text/projects/{projectName}/train/jobs/{jobId}/:cancel:
        post:
          summary: >-
            Post Authoring Analyze-text Projects Projectname Train Jobs Jobid
            :cancel
          description: Triggers a cancellation for a running training job.
          operationId: textAnalysisAuthoringCancelTrainingJob
          responses:
            default:
              description: Error response.
              schema:
                $ref: common.json#/definitions/ErrorResponse
