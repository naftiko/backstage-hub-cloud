apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Nested Event Subscription For A Domain Topic.
  description: Delete a nested existing event subscription for a domain topic.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Nested Event Subscription For A Domain Topic.
      description: Delete a nested existing event subscription for a domain topic.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.EventGrid/domains/{domainName}/topics/{topicName}/eventSubscriptions/{eventSubscriptionName}:
        delete:
          summary: Delete A Nested Event Subscription For A Domain Topic.
          description: Delete a nested existing event subscription for a domain topic.
          operationId: domainTopicEventSubscriptionsDelete
          responses:
            default:
              description: |-
                *** Error Responses: ***

                 * 400 Bad Request.

                 * 404 Not Found.

                 * 500 Internal Server Error.
