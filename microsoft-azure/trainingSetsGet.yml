apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Trainingsets Id
  description: Gets the training set identified by the given ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Trainingsets Id
      description: Gets the training set identified by the given ID.
    paths:
      /trainingsets/{id}:
        get:
          summary: Get Trainingsets Id
          description: Gets the training set identified by the given ID.
          operationId: trainingSetsGet
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
