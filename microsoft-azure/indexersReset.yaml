apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-indexersindexername-searchreset
  description: Resets the change tracking state associated with an indexer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Indexers('indexername') Search.reset
      description: Resets the change tracking state associated with an indexer.
    paths:
      /indexers('{indexerName}')/search.reset:
        post:
          summary: Post Indexers('indexername') Search.reset
          description: Resets the change tracking state associated with an indexer.
          operationId: indexersReset
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
