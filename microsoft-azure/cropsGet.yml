apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Crops Cropid
  description: Gets a specified crop resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Crops Cropid
      description: Gets a specified crop resource.
    paths:
      /crops/{cropId}:
        get:
          summary: Get Crops Cropid
          description: Gets a specified crop resource.
          operationId: cropsGet
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
