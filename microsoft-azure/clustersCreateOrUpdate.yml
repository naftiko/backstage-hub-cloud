apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Or Update The Cluster.
  description: Create a new cluster or update the properties of the cluster if it exists.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Or Update The Cluster.
      description: Create a new cluster or update the properties of the cluster if it exists.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.NetworkCloud/clusters/{clusterName}:
        put:
          summary: Create Or Update The Cluster.
          description: >-
            Create a new cluster or update the properties of the cluster if it
            exists.
          operationId: clustersCreateOrUpdate
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: >-
                  ../../../../../common-types/resource-management/v5/types.json#/definitions/ErrorResponse
