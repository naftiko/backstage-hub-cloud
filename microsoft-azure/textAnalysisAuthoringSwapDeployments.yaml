apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-authoring-analyze-text-projects-projectname-deployments-swap
  description: Swaps two existing deployments with each other.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Authoring Analyze-text Projects Projectname Deployments :swap
      description: Swaps two existing deployments with each other.
    paths:
      /authoring/analyze-text/projects/{projectName}/deployments/:swap:
        post:
          summary: Post Authoring Analyze-text Projects Projectname Deployments :swap
          description: Swaps two existing deployments with each other.
          operationId: textAnalysisAuthoringSwapDeployments
          responses:
            default:
              description: Error response.
              schema:
                $ref: common.json#/definitions/ErrorResponse
