apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureExperimentsGetbyquery
  description: >-
    Get all experiments in a specific workspace with the specified query
    filters.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Get All Experiments In A Specific Workspace '
      description: >-
        Get all experiments in a specific workspace with the specified query
        filters.
    paths:
      /history/v1.0/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/experiments:query:
        post:
          summary: 'Get All Experiments In A Specific Workspace '
          description: >-
            Get all experiments in a specific workspace with the specified query
            filters.
          operationId: microsoftAzureExperimentsGetbyquery
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
