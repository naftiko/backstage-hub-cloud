apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: gets-a-blob-upload-uri
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Gets A Blob Upload Uri.
      description: Needs a more full description created.
    paths:
      /accounts/{accountId}/blobUploadEndpoint:
        get:
          summary: Gets A Blob Upload Uri.
          description: Needs a more full description created.
          operationId: blobUploadEndpointGet
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
