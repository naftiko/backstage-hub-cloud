apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureTextanalysisauthoringGetexportstatus
  description: >-
    Gets the status of an export job. Once job completes, returns the project
    metadata, and assets.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Authoring Analyze Text Projects Projectname Export Jobs Jobid
      description: >-
        Gets the status of an export job. Once job completes, returns the project
        metadata, and assets.
    paths:
      /authoring/analyze-text/projects/{projectName}/export/jobs/{jobId}:
        get:
          summary: Get Authoring Analyze Text Projects Projectname Export Jobs Jobid
          description: >-
            Gets the status of an export job. Once job completes, returns the
            project metadata, and assets.
          operationId: microsoftAzureTextanalysisauthoringGetexportstatus
          responses:
            default:
              description: Error response.
              schema:
                $ref: common.json#/definitions/ErrorResponse
