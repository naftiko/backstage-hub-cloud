apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Personalvoices Id
  description: Deletes the personal voice identified by the given ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Personalvoices Id
      description: Deletes the personal voice identified by the given ID.
    paths:
      /personalvoices/{id}:
        delete:
          summary: Delete Personalvoices Id
          description: Deletes the personal voice identified by the given ID.
          operationId: personalVoicesDelete
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
