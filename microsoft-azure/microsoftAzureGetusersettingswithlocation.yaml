apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureGetusersettingswithlocation
  description: >-
    Get current user settings for current signed in user. This operation returns
    settings for the user's cloud shell preferences including preferred
    location, storage profile, shell type, font and size settings.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Get User Settings '
      description: >-
        Get current user settings for current signed in user. This operation returns
        settings for the user's cloud shell preferences including preferred
        location, storage profile, shell type, font and size settings.
    paths:
      /providers/Microsoft.Portal/locations/{location}/userSettings/{userSettingsName}:
        get:
          summary: 'Get User Settings '
          description: >-
            Get current user settings for current signed in user. This operation
            returns settings for the user's cloud shell preferences including
            preferred location, storage profile, shell type, font and size settings.
          operationId: microsoftAzureGetusersettingswithlocation
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
