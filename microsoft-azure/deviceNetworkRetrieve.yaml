apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-device-network-networkretrieve
  description: >-
    Retrieves the network a given device is on. Returns network in a networkCode
    format that can be used for other APIs.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Device-network Network:retrieve
      description: >-
        Retrieves the network a given device is on. Returns network in a networkCode
        format that can be used for other APIs.
    paths:
      /device-network/network:retrieve:
        post:
          summary: Post Device-network Network:retrieve
          description: >-
            Retrieves the network a given device is on. Returns network in a
            networkCode format that can be used for other APIs.
          operationId: deviceNetworkRetrieve
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
