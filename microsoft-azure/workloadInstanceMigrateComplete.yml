apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Performs Complete Migrate.
  description: Performs the complete migrate on the workload instance.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Performs Complete Migrate.
      description: Performs the complete migrate on the workload instance.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadInstances/{workloadInstanceName}/completeMigration:
        post:
          summary: Performs Complete Migrate.
          description: Performs the complete migrate on the workload instance.
          operationId: workloadInstanceMigrateComplete
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
