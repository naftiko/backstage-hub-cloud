apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-metadatapolicies-policyid
  description: Gets a metadata policy
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Metadatapolicies Policyid
      description: Gets a metadata policy
    paths:
      /metadataPolicies/{policyId}:
        get:
          summary: Get Metadatapolicies Policyid
          description: Gets a metadata policy
          operationId: metadataPolicyGet
          responses:
            default:
              description: An error response received from the Metadata Policy Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
              headers:
                x-ms-error-code:
                  type: string
                  description: The error code
