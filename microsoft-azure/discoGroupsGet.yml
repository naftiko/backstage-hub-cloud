apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Retrieve A Disco Group With A Given Discogroupname.
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieve A Disco Group With A Given Discogroupname.
      description: Needs a more full description created.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/workspaces/{workspaceName}/discoGroups/{discoGroupName}:
        get:
          summary: Retrieve A Disco Group With A Given Discogroupname.
          description: Needs a more full description created.
          operationId: discoGroupsGet
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
