apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Put Indexes('indexname')
  description: Creates a new search index or updates an index if it already exists.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Indexes('indexname')
      description: Creates a new search index or updates an index if it already exists.
    paths:
      /indexes('{indexName}'):
        put:
          summary: Put Indexes('indexname')
          description: Creates a new search index or updates an index if it already exists.
          operationId: indexesCreateOrUpdate
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
