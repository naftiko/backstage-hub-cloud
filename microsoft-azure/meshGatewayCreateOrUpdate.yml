apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Creates Or Updates A Gateway Resource.
  description: >-
    Creates a Gateway resource with the specified name, description and
    properties. If Gateway resource with the same name exists, then it is
    updated with the specified description and properties. Use Gateway resource
    to provide public connectivity to application services.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Creates Or Updates A Gateway Resource.
      description: >-
        Creates a Gateway resource with the specified name, description and
        properties. If Gateway resource with the same name exists, then it is
        updated with the specified description and properties. Use Gateway resource
        to provide public connectivity to application services.
    paths:
      /Resources/Gateways/{gatewayResourceName}:
        put:
          summary: Creates Or Updates A Gateway Resource.
          description: >-
            Creates a Gateway resource with the specified name, description and
            properties. If Gateway resource with the same name exists, then it is
            updated with the specified description and properties. Use Gateway
            resource to provide public connectivity to application services.
          operationId: meshGatewayCreateOrUpdate
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/FabricError'
