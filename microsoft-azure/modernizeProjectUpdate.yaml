apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: updates-the-modernizeproject
  description: Performs update on the modernizeProject.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Updates The Modernizeproject.
      description: Performs update on the modernizeProject.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}:
        patch:
          summary: Updates The Modernizeproject.
          description: Performs update on the modernizeProject.
          operationId: modernizeProjectUpdate
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
