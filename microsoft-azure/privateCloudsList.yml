apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Implements Private Cloud List Get Method
  description: Returns list of private clouds in particular region
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Implements Private Cloud List Get Method
      description: Returns list of private clouds in particular region
    paths:
      /subscriptions/{subscriptionId}/providers/Microsoft.VMwareCloudSimple/locations/{regionId}/privateClouds:
        get:
          summary: Implements Private Cloud List Get Method
          description: Returns list of private clouds in particular region
          operationId: privateCloudsList
          responses:
            default:
              description: General Error
              schema:
                $ref: '#/definitions/CSRPError'
