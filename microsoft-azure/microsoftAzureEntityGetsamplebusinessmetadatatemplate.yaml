apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureEntityGetsamplebusinessmetadatatemplate
  description: Get the sample Template for uploading/creating bulk BusinessMetaData
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Atlas Entity Businessmetadata Import Template
      description: Get the sample Template for uploading/creating bulk BusinessMetaData
    paths:
      /atlas/v2/entity/businessmetadata/import/template:
        get:
          summary: Get Atlas Entity Businessmetadata Import Template
          description: Get the sample Template for uploading/creating bulk BusinessMetaData
          operationId: microsoftAzureEntityGetsamplebusinessmetadatatemplate
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
