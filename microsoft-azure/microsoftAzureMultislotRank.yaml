apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureMultislotRank
  description: >-
    Submit a Personalizer multi-slot rank request. Receives a context, a list of
    actions, and a list of slots. Returns which of the provided actions should
    be used in each slot, in each rewardActionId.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Post Multi Slot Rank '
      description: >-
        Submit a Personalizer multi-slot rank request. Receives a context, a list of
        actions, and a list of slots. Returns which of the provided actions should
        be used in each slot, in each rewardActionId.
    paths:
      /multislot/rank:
        post:
          summary: 'Post Multi Slot Rank '
          description: >-
            Submit a Personalizer multi-slot rank request. Receives a context, a
            list of actions, and a list of slots. Returns which of the provided
            actions should be used in each slot, in each rewardActionId.
          operationId: microsoftAzureMultislotRank
          responses:
            default:
              description: Invalid request.
              schema:
                $ref: '#/definitions/ErrorResponse'
