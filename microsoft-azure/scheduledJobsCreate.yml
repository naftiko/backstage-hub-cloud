apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Or Update A Scheduled Job
  description: Create or update a scheduled job by ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Or Update A Scheduled Job
      description: Create or update a scheduled job by ID.
    paths:
      /scheduledJobs/{scheduledJobId}:
        put:
          summary: Create Or Update A Scheduled Job
          description: Create or update a scheduled job by ID.
          operationId: scheduledJobsCreate
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
