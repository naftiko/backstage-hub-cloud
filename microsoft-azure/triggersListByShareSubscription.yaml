apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: list-triggers-in-a-share-subscription
  description: List Triggers in a share subscription
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Triggers In A Share Subscription.
      description: List Triggers in a share subscription
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DataShare/accounts/{accountName}/shareSubscriptions/{shareSubscriptionName}/triggers:
        get:
          summary: List Triggers In A Share Subscription.
          description: List Triggers in a share subscription
          operationId: triggersListByShareSubscription
          responses:
            default:
              description: >-
                An error response received from the Microsoft.DataShare resource
                provider.
              schema:
                $ref: '#/definitions/DataShareError'
