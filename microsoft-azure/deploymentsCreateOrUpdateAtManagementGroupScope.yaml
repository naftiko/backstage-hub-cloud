apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: deploys-resources-at-management-group-scope
  description: >-
    You can provide the template and parameters directly in the request or link
    to JSON files.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deploys Resources At Management Group Scope.
      description: >-
        You can provide the template and parameters directly in the request or link
        to JSON files.
    paths:
      /providers/Microsoft.Management/managementGroups/{groupId}/providers/Microsoft.Resources/deployments/{deploymentName}:
        put:
          summary: Deploys Resources At Management Group Scope.
          description: >-
            You can provide the template and parameters directly in the request or
            link to JSON files.
          operationId: deploymentsCreateOrUpdateAtManagementGroupScope
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
