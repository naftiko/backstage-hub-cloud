apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-deviceupdate-instanceid-updates-operations
  description: >-
    Get a list of all import update operations. Completed operations are kept
    for 7 days before auto-deleted. Delete operations are not returned by this
    API version.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Deviceupdate Instanceid Updates Operations
      description: >-
        Get a list of all import update operations. Completed operations are kept
        for 7 days before auto-deleted. Delete operations are not returned by this
        API version.
    paths:
      /deviceUpdate/{instanceId}/updates/operations:
        get:
          summary: Get Deviceupdate Instanceid Updates Operations
          description: >-
            Get a list of all import update operations. Completed operations are
            kept for 7 days before auto-deleted. Delete operations are not returned
            by this API version.
          operationId: deviceUpdateListOperations
          responses:
            default:
              description: Default response.
              schema:
                $ref: '#/definitions/ErrorResponse'
