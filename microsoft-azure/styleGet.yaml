apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: use-to-get-details-of-a-style
  description: >-
    ### Submit Get RequestThe `Get` API is an HTTP `GET` request that allows the
    caller to fetch the details of an existing style by passing in the `styleId`
    or its [alias](). Get the two predefined default styles using the alias
    `microsoft-maps:indoor_2022-01-01` for indoor light and
    `microsoft-maps:indoor_dark_2022-01-01` for indoor dark.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Use To Get Details Of A Style.
      description: >-
        ### Submit Get RequestThe `Get` API is an HTTP `GET` request that allows the
        caller to fetch the details of an existing style by passing in the `styleId`
        or its [alias](). Get the two predefined default styles using the alias
        `microsoft-maps:indoor_2022-01-01` for indoor light and
        `microsoft-maps:indoor_dark_2022-01-01` for indoor dark.
    paths:
      /styles/{styleId}:
        get:
          summary: Use To Get Details Of A Style.
          description: >-
            ### Submit Get RequestThe `Get` API is an HTTP `GET` request that allows
            the caller to fetch the details of an existing style by passing in the
            `styleId` or its [alias](). Get the two predefined default styles using
            the alias `microsoft-maps:indoor_2022-01-01` for indoor light and
            `microsoft-maps:indoor_dark_2022-01-01` for indoor dark.
          operationId: styleGet
          responses:
            default:
              $ref: ../../../Common/preview/1.0/common.json#/responses/default
