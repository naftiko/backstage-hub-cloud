apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureCropsList
  description: Returns a paginated list of crop resources.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Crops
      description: Returns a paginated list of crop resources.
    paths:
      /crops:
        get:
          summary: Get Crops
          description: Returns a paginated list of crop resources.
          operationId: microsoftAzureCropsList
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
