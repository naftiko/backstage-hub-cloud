apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureEntityImportbusinessmetadata
  description: Upload the file for creating Business Metadata in BULK
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Atlas Entity Businessmetadata Import
      description: Upload the file for creating Business Metadata in BULK
    paths:
      /atlas/v2/entity/businessmetadata/import:
        post:
          summary: Post Atlas Entity Businessmetadata Import
          description: Upload the file for creating Business Metadata in BULK
          operationId: microsoftAzureEntityImportbusinessmetadata
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
