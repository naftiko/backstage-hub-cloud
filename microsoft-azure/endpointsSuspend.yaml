apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-endpoints-idsuspend
  description: Suspends the endpoint identified by the given ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Endpoints Id:suspend
      description: Suspends the endpoint identified by the given ID.
    paths:
      /endpoints/{id}:suspend:
        post:
          summary: Post Endpoints Id:suspend
          description: Suspends the endpoint identified by the given ID.
          operationId: endpointsSuspend
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
