apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-all-test-runs-for-the-given-filters
  description: Get all test runs for the given filters.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Test Runs For The Given Filters.
      description: Get all test runs for the given filters.
    paths:
      /test-runs:
        get:
          summary: Get All Test Runs For The Given Filters.
          description: Get all test runs for the given filters.
          operationId: loadTestRunListTestRuns
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
