apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Documentmodels
  description: List all document models
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Documentmodels
      description: List all document models
    paths:
      /documentModels:
        get:
          summary: Get Documentmodels
          description: List all document models
          operationId: documentModelsListModels
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/ErrorResponse'
