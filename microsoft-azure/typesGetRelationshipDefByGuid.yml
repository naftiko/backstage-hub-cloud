apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Atlas Types Relationshipdef Guid Guid
  description: Get the relationship definition for the given GUID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Atlas Types Relationshipdef Guid Guid
      description: Get the relationship definition for the given GUID.
    paths:
      /atlas/v2/types/relationshipdef/guid/{guid}:
        get:
          summary: Get Atlas Types Relationshipdef Guid Guid
          description: Get the relationship definition for the given GUID.
          operationId: typesGetRelationshipDefByGuid
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
