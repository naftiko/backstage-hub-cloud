apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureDevicemanagementGetdeployment
  description: Gets the deployment properties.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Get Deviceupdate Instanceid Management Groups Groupid Deployments
        Deploymentid
      description: Gets the deployment properties.
    paths:
      /deviceUpdate/{instanceId}/management/groups/{groupId}/deployments/{deploymentId}:
        get:
          summary: >-
            Get Deviceupdate Instanceid Management Groups Groupid Deployments
            Deploymentid
          description: Gets the deployment properties.
          operationId: microsoftAzureDevicemanagementGetdeployment
          responses:
            default:
              description: Default response.
              schema:
                $ref: '#/definitions/ErrorResponse'
