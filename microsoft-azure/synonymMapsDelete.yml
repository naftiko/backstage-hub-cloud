apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Synonymmaps('synonymmapname')
  description: Deletes a synonym map.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Synonymmaps('synonymmapname')
      description: Deletes a synonym map.
    paths:
      /synonymmaps('{synonymMapName}'):
        delete:
          summary: Delete Synonymmaps('synonymmapname')
          description: Deletes a synonym map.
          operationId: synonymMapsDelete
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
