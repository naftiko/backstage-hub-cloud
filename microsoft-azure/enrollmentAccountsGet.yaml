apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-providers-microsoftbilling-enrollmentaccounts-name
  description: Gets a enrollment account by name.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Providers Microsoft.billing Enrollmentaccounts Name
      description: Gets a enrollment account by name.
    paths:
      /providers/Microsoft.Billing/enrollmentAccounts/{name}:
        get:
          summary: Get Providers Microsoft.billing Enrollmentaccounts Name
          description: Gets a enrollment account by name.
          operationId: enrollmentAccountsGet
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
