apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Deviceupdate Instanceid Management Groups
  description: >-
    Gets a list of all device groups.  The $default group will always be
    returned first.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Deviceupdate Instanceid Management Groups
      description: >-
        Gets a list of all device groups.  The $default group will always be
        returned first.
    paths:
      /deviceUpdate/{instanceId}/management/groups:
        get:
          summary: Get Deviceupdate Instanceid Management Groups
          description: >-
            Gets a list of all device groups.  The $default group will always be
            returned first.
          operationId: deviceManagementListGroups
          responses:
            default:
              description: Default response.
              schema:
                $ref: '#/definitions/ErrorResponse'
