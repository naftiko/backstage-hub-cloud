apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Persongroups Persongroupid Persons Personid
  description: >-
    Delete an existing person from a person group. The persistedFaceId,
    userData, person name and face feature in the person entry will all be
    deleted.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Persongroups Persongroupid Persons Personid
      description: >-
        Delete an existing person from a person group. The persistedFaceId,
        userData, person name and face feature in the person entry will all be
        deleted.
    paths:
      /persongroups/{personGroupId}/persons/{personId}:
        delete:
          summary: Delete Persongroups Persongroupid Persons Personid
          description: >-
            Delete an existing person from a person group. The persistedFaceId,
            userData, person name and face feature in the person entry will all be
            deleted.
          operationId: personGroupPersonDelete
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/APIError'
