apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: list-keys-for-a-domain
  description: List the two keys used to publish to a domain.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Keys For A Domain.
      description: List the two keys used to publish to a domain.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.EventGrid/domains/{domainName}/listKeys:
        post:
          summary: List Keys For A Domain.
          description: List the two keys used to publish to a domain.
          operationId: domainsListSharedAccessKeys
          responses:
            default:
              description: |-
                *** Error Responses: ***

                 * 400 Bad Request.

                 * 500 Internal Server Error.
