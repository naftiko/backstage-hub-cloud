apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureGetapplicationmanifest
  description: The response contains the application manifest XML as a string.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Gets The Manifest Describing An Application Type '
      description: The response contains the application manifest XML as a string.
    paths:
      /ApplicationTypes/{applicationTypeName}/$/GetApplicationManifest:
        get:
          summary: 'Gets The Manifest Describing An Application Type '
          description: The response contains the application manifest XML as a string.
          operationId: microsoftAzureGetapplicationmanifest
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
