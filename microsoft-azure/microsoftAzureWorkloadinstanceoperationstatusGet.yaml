apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureWorkloadinstanceoperationstatusGet
  description: Tracks the results of an asynchronous operation on the workload instance.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Gets The Workload Instance Operation Status '
      description: Tracks the results of an asynchronous operation on the workload instance.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadInstances/{workloadInstanceName}/operations/{operationId}:
        get:
          summary: 'Gets The Workload Instance Operation Status '
          description: >-
            Tracks the results of an asynchronous operation on the workload
            instance.
          operationId: microsoftAzureWorkloadinstanceoperationstatusGet
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ArmError'
