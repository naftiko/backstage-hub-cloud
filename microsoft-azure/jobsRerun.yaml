apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: rerun-a-job-on-failed-devices
  description: Execute a rerun of an existing job on all failed devices.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Rerun A Job On Failed Devices
      description: Execute a rerun of an existing job on all failed devices.
    paths:
      /jobs/{jobId}/rerun/{rerunId}:
        put:
          summary: Rerun A Job On Failed Devices
          description: Execute a rerun of an existing job on all failed devices.
          operationId: jobsRerun
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
