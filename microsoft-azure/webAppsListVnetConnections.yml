apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Gets The Virtual Networks The App (or Deployment Slot) Is Connected To.
  description: >-
    Description for Gets the virtual networks the app (or deployment slot) is
    connected to.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Gets The Virtual Networks The App (or Deployment Slot) Is Connected To.
      description: >-
        Description for Gets the virtual networks the app (or deployment slot) is
        connected to.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Web/sites/{name}/virtualNetworkConnections:
        get:
          summary: Gets The Virtual Networks The App (or Deployment Slot) Is Connected To.
          description: >-
            Description for Gets the virtual networks the app (or deployment slot)
            is connected to.
          operationId: webAppsListVnetConnections
          responses:
            default:
              description: App Service error response.
              schema:
                $ref: ./CommonDefinitions.json#/definitions/DefaultErrorResponse
