apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Put Boundaries Cascade-delete Jobid
  description: Create a cascade delete job for specified boundary.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Boundaries Cascade-delete Jobid
      description: Create a cascade delete job for specified boundary.
    paths:
      /boundaries/cascade-delete/{jobId}:
        put:
          summary: Put Boundaries Cascade-delete Jobid
          description: Create a cascade delete job for specified boundary.
          operationId: boundariesCreateCascadeDeleteJob
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
