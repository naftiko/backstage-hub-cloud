apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: create-an-inference-task-for-root-cause-analysis
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create An Inference Task For Root Cause Analysis
      description: Needs a more full description created.
    paths:
      /rootCauseAnalysis/instances/{instanceId}/models/{modelId}/inferences/{inferenceId}:
        put:
          summary: Create An Inference Task For Root Cause Analysis
          description: Needs a more full description created.
          operationId: createRootCauseAnalysisInferenceTask
          responses:
            default:
              description: Client error or server error (4xx or 5xx)
              schema:
                $ref: '#/definitions/ErrorDetail'
              headers:
                x-ms-error-code:
                  description: error code
                  type: string
