apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: gets-all-partitions-related-events
  description: The response is list of PartitionEvent objects.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Gets All Partitions-related Events.
      description: The response is list of PartitionEvent objects.
    paths:
      /EventsStore/Partitions/Events:
        get:
          summary: Gets All Partitions-related Events.
          description: The response is list of PartitionEvent objects.
          operationId: getPartitionsEventList
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
