apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Livy Sessions Sessionid Statements
  description: Gets a list of statements within a spark session.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Livy Sessions Sessionid Statements
      description: Gets a list of statements within a spark session.
    paths:
      /livy/sessions/{sessionId}/statements:
        get:
          summary: Get Livy Sessions Sessionid Statements
          description: Gets a list of statements within a spark session.
          operationId: jobListSparkStatementJob
          responses:
            '200':
              description: Success
              schema:
                $ref: '#/definitions/SparkStatementCollection'
