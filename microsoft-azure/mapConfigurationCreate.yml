apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Use To Create A Map Configuration.
  description: >-
    For more information on what a map configuration is, see the [Creator for
    indoor maps](/azure/azure-maps/creator-indoor-maps#map-configuration)
    concepts article.### Submit Create Request The Map Configuration `Create`
    API is an HTTP `POST` request used to create a new map configuration.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Use To Create A Map Configuration.
      description: >-
        For more information on what a map configuration is, see the [Creator for
        indoor maps](/azure/azure-maps/creator-indoor-maps#map-configuration)
        concepts article.### Submit Create Request The Map Configuration `Create`
        API is an HTTP `POST` request used to create a new map configuration.
    paths:
      /styles/mapconfigurations:
        post:
          summary: Use To Create A Map Configuration.
          description: >-
            For more information on what a map configuration is, see the [Creator
            for indoor
            maps](/azure/azure-maps/creator-indoor-maps#map-configuration) concepts
            article.### Submit Create Request The Map Configuration `Create` API is
            an HTTP `POST` request used to create a new map configuration.
          operationId: mapConfigurationCreate
          responses:
            default:
              $ref: ../../../Common/preview/1.0/common.json#/responses/default
