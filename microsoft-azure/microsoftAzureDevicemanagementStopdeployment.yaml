apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureDevicemanagementStopdeployment
  description: Stops a deployment.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Post Deviceupdate Instanceid Management Groups Groupid Deviceclasssubgroups
        Deviceclassid Deployments Deploymentid:cancel
      description: Stops a deployment.
    paths:
      /deviceUpdate/{instanceId}/management/groups/{groupId}/deviceClassSubgroups/{deviceClassId}/deployments/{deploymentId}:cancel:
        post:
          summary: >-
            Post Deviceupdate Instanceid Management Groups Groupid
            Deviceclasssubgroups Deviceclassid Deployments Deploymentid:cancel
          description: Stops a deployment.
          operationId: microsoftAzureDevicemanagementStopdeployment
          responses:
            default:
              description: Default response.
              schema:
                $ref: '#/definitions/ErrorResponse'
