apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureSearchGetsearchaddressstructured
  description: >-
    **Structured Address Geocoding**<br><br>**Applies to**: S0 and S1 pricing
    tiers.<br><br><br>Azure Address Geocoding can also be accessed for 
    structured address look up exclusively. The geocoding search index will be
    queried for everything above the  street level data. No POIs will be
    returned. Note that the geocoder is very tolerant of typos and incomplete 
    addresses. It will also handle everything from exact  street addresses or
    street or intersections as well as higher level geographies such as city
    centers,  counties, states etc.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Search Address Structured Format
      description: >-
        **Structured Address Geocoding**<br><br>**Applies to**: S0 and S1 pricing
        tiers.<br><br><br>Azure Address Geocoding can also be accessed for 
        structured address look up exclusively. The geocoding search index will be
        queried for everything above the  street level data. No POIs will be
        returned. Note that the geocoder is very tolerant of typos and incomplete 
        addresses. It will also handle everything from exact  street addresses or
        street or intersections as well as higher level geographies such as city
        centers,  counties, states etc.
    paths:
      /search/address/structured/{format}:
        get:
          summary: Get Search Address Structured Format
          description: >-
            **Structured Address Geocoding**<br><br>**Applies to**: S0 and S1
            pricing tiers.<br><br><br>Azure Address Geocoding can also be accessed
            for  structured address look up exclusively. The geocoding search index
            will be queried for everything above the  street level data. No POIs
            will be returned. Note that the geocoder is very tolerant of typos and
            incomplete  addresses. It will also handle everything from exact  street
            addresses or street or intersections as well as higher level geographies
            such as city centers,  counties, states etc.
          operationId: microsoftAzureSearchGetsearchaddressstructured
          responses:
            '500':
              $ref: '#/responses/500'
