apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureTranslatorTranslate
  description: Translates text into one or more languages.<br>
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Translate
      description: Translates text into one or more languages.<br>
    paths:
      /translate:
        post:
          summary: Post Translate
          description: Translates text into one or more languages.<br>
          operationId: microsoftAzureTranslatorTranslate
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/ErrorMessage'
