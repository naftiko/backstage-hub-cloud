apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureLineageGetlineagegraph
  description: Get lineage info of the entity specified by GUID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Atlas Lineage Guid
      description: Get lineage info of the entity specified by GUID.
    paths:
      /atlas/v2/lineage/{guid}:
        get:
          summary: Get Atlas Lineage Guid
          description: Get lineage info of the entity specified by GUID.
          operationId: microsoftAzureLineageGetlineagegraph
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
