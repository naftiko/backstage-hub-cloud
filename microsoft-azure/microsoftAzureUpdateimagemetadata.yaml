apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureUpdateimagemetadata
  description: >-
    This API accepts a batch of image Ids, and metadata, to update images. There
    is a limit of 64 images.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Update Metadata Of Images '
      description: >-
        This API accepts a batch of image Ids, and metadata, to update images. There
        is a limit of 64 images.
    paths:
      /projects/{projectId}/images/metadata:
        post:
          summary: 'Update Metadata Of Images '
          description: >-
            This API accepts a batch of image Ids, and metadata, to update images.
            There is a limit of 64 images.
          operationId: microsoftAzureUpdateimagemetadata
          responses:
            default:
              description: Error response
              schema:
                $ref: '#/definitions/CustomVisionError'
