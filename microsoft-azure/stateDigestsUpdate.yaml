apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-gov-members-state-digests-memberidupdate
  description: >-
    Request that this member's stateDigest is updated to a fresher value. Only
    this member may update their own stateDigest. Returns the refreshed value.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Gov Members State-digests Memberid:update
      description: >-
        Request that this member's stateDigest is updated to a fresher value. Only
        this member may update their own stateDigest. Returns the refreshed value.
    paths:
      /gov/members/state-digests/{memberId}:update:
        post:
          summary: Post Gov Members State-digests Memberid:update
          description: >-
            Request that this member's stateDigest is updated to a fresher value.
            Only this member may update their own stateDigest. Returns the refreshed
            value.
          operationId: stateDigestsUpdate
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
