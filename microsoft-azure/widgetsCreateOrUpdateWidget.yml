apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Patch Widgets Widgetname
  description: Creates or updates a Widget asynchronously.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Patch Widgets Widgetname
      description: Creates or updates a Widget asynchronously.
    paths:
      /widgets/{widgetName}:
        patch:
          summary: Patch Widgets Widgetname
          description: Creates or updates a Widget asynchronously.
          operationId: widgetsCreateOrUpdateWidget
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
