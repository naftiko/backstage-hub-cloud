apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureTextanalysisauthoringTrain
  description: Triggers a training job for a project.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Authoring Analyze Text Projects Projectname :train
      description: Triggers a training job for a project.
    paths:
      /authoring/analyze-text/projects/{projectName}/:train:
        post:
          summary: Post Authoring Analyze Text Projects Projectname :train
          description: Triggers a training job for a project.
          operationId: microsoftAzureTextanalysisauthoringTrain
          responses:
            default:
              description: Error response.
              schema:
                $ref: common.json#/definitions/ErrorResponse
