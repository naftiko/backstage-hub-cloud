apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-tasks-taskidcancel
  description: Cancel a task by taskId.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Tasks Taskid:cancel
      description: Cancel a task by taskId.
    paths:
      /tasks/{taskId}:cancel:
        post:
          summary: Post Tasks Taskid:cancel
          description: Cancel a task by taskId.
          operationId: tasksCancelTask
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
