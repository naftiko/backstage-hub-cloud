apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Eventroutes
  description: Retrieves all event routes.Status codes:* 200 OK
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Eventroutes
      description: Retrieves all event routes.Status codes:* 200 OK
    paths:
      /eventroutes:
        get:
          summary: Get Eventroutes
          description: Retrieves all event routes.Status codes:* 200 OK
          operationId: eventRoutesList
          responses:
            default:
              description: Default response.
              schema:
                $ref: '#/definitions/ErrorResponse'
