apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-acr-name-manifests
  description: List manifests of a repository
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Acr Name _manifests
      description: List manifests of a repository
    paths:
      /acr/v1/{name}/_manifests:
        get:
          summary: Get Acr Name _manifests
          description: List manifests of a repository
          operationId: getAcrManifests
          responses:
            default:
              description: ACR error response describing why the operation failed.
              schema:
                $ref: '#/definitions/AcrErrors'
