apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    Gets The Azure Active Directory Metadata Used For Secured Connection To
    Cluster.
  description: >-
    Gets the Azure Active Directory metadata used for secured connection to
    cluster.This API is not supposed to be called separately. It provides
    information needed to set up an Azure Active Directory secured connection
    with a Service Fabric cluster.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Gets The Azure Active Directory Metadata Used For Secured Connection To
        Cluster.
      description: >-
        Gets the Azure Active Directory metadata used for secured connection to
        cluster.This API is not supposed to be called separately. It provides
        information needed to set up an Azure Active Directory secured connection
        with a Service Fabric cluster.
    paths:
      /$/GetAadMetadata:
        get:
          summary: >-
            Gets The Azure Active Directory Metadata Used For Secured Connection To
            Cluster.
          description: >-
            Gets the Azure Active Directory metadata used for secured connection to
            cluster.This API is not supposed to be called separately. It provides
            information needed to set up an Azure Active Directory secured
            connection with a Service Fabric cluster.
          operationId: getAadMetadata
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
