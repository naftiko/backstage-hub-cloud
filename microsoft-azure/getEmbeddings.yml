apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Deployments Deploymentid Embeddings
  description: Return the embeddings for a given prompt.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Deployments Deploymentid Embeddings
      description: Return the embeddings for a given prompt.
    paths:
      /deployments/{deploymentId}/embeddings:
        post:
          summary: Post Deployments Deploymentid Embeddings
          description: Return the embeddings for a given prompt.
          operationId: getEmbeddings
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
