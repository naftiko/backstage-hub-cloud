apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: removes-or-unregisters-a-service-fabric-application-type-from-the-cluster
  description: >-
    This operation can only be performed if all application instances of the
    application type have been deleted. Once the application type is
    unregistered, no new application instances can be created for this
    particular application type.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Removes Or Unregisters A Service Fabric Application Type From The Cluster.
      description: >-
        This operation can only be performed if all application instances of the
        application type have been deleted. Once the application type is
        unregistered, no new application instances can be created for this
        particular application type.
    paths:
      /ApplicationTypes/{applicationTypeName}/$/Unprovision:
        post:
          summary: >-
            Removes Or Unregisters A Service Fabric Application Type From The
            Cluster.
          description: >-
            This operation can only be performed if all application instances of the
            application type have been deleted. Once the application type is
            unregistered, no new application instances can be created for this
            particular application type.
          operationId: unprovisionApplicationType
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
