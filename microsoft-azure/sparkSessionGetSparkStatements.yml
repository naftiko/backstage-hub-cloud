apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Sessions Sessionid Statements
  description: Gets a list of statements within a spark session.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Sessions Sessionid Statements
      description: Gets a list of statements within a spark session.
    paths:
      /sessions/{sessionId}/statements:
        get:
          summary: Get Sessions Sessionid Statements
          description: Gets a list of statements within a spark session.
          operationId: sparkSessionGetSparkStatements
          responses:
            '200':
              description: Success
              schema:
                $ref: '#/definitions/SparkStatementCollection'
