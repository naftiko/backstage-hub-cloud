apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-documentmodels-modelid-analyzeresults-resultid
  description: Gets the result of document analysis.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Documentmodels Modelid Analyzeresults Resultid
      description: Gets the result of document analysis.
    paths:
      /documentModels/{modelId}/analyzeResults/{resultId}:
        get:
          summary: Get Documentmodels Modelid Analyzeresults Resultid
          description: Gets the result of document analysis.
          operationId: documentModelsGetAnalyzeResult
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/ErrorResponse'
