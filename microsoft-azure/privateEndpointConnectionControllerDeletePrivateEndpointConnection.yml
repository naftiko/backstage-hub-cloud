apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete The Endpoint
  description: >-
    Delete the private endpoint. Deleting non-existent private endpoint is a
    no-operation.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete The Endpoint
      description: >-
        Delete the private endpoint. Deleting non-existent private endpoint is a
        no-operation.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/migrateProjects/{migrateProjectName}/privateEndpointConnections/{peConnectionName}:
        delete:
          summary: Delete The Endpoint
          description: >-
            Delete the private endpoint. Deleting non-existent private endpoint is a
            no-operation.
          operationId: privateEndpointConnectionControllerDeletePrivateEndpointConnection
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
