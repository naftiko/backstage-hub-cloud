apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Put Datasources Datasourcename Scans Scanname Triggers Default
  description: Creates an instance of a trigger
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Datasources Datasourcename Scans Scanname Triggers Default
      description: Creates an instance of a trigger
    paths:
      /datasources/{dataSourceName}/scans/{scanName}/triggers/default:
        put:
          summary: Put Datasources Datasourcename Scans Scanname Triggers Default
          description: Creates an instance of a trigger
          operationId: triggersCreateTrigger
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
