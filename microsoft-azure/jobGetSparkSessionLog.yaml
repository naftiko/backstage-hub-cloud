apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-livy-sessions-sessionid-log
  description: Gets a single spark session job logs.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Livy Sessions Sessionid Log
      description: Gets a single spark session job logs.
    paths:
      /livy/sessions/{sessionId}/log:
        get:
          summary: Get Livy Sessions Sessionid Log
          description: Gets a single spark session job logs.
          operationId: jobGetSparkSessionLog
          responses:
            '200':
              description: Success
              schema:
                $ref: '#/definitions/SparkJobLog'
