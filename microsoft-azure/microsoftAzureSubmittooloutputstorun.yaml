apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureSubmitToolOutputsToRun
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >
        When A Run Has The `status: "requires Action"` And `required Action Type` Is
        `submit Tool Outputs`, This Endpoint Can Be Used To Submit The Outputs From
        The Tool Calls Once They Re All Completed All Outputs Must Be Submitted In A
        Single Request 
      description: Needs a more full description created.
    paths:
      /threads/{thread_id}/runs/{run_id}/submit_tool_outputs:
        post:
          summary: >
            When A Run Has The `status: "requires Action"` And `required Action
            Type` Is `submit Tool Outputs`, This Endpoint Can Be Used To Submit The
            Outputs From The Tool Calls Once They Re All Completed All Outputs Must
            Be Submitted In A Single Request 
          description: Needs a more full description created.
          operationId: microsoftAzureSubmitToolOutputsToRun
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/submitToolOutputsRunRequest'
          responses:
            '200':
              description: OK
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/runObject'
