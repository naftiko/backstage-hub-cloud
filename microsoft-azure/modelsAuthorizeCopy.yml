apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    Allows Another Speech Resource (source) To Copy A Model To This Speech
    Resource (target).
  description: >-
    This method can be used to allow copying a model from another speech
    resource.Only custom models can be copied from another speech resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Allows Another Speech Resource (source) To Copy A Model To This Speech
        Resource (target).
      description: >-
        This method can be used to allow copying a model from another speech
        resource.Only custom models can be copied from another speech resource.
    paths:
      /models:authorizecopy:
        post:
          summary: >-
            Allows Another Speech Resource (source) To Copy A Model To This Speech
            Resource (target).
          description: >-
            This method can be used to allow copying a model from another speech
            resource.Only custom models can be copied from another speech resource.
          operationId: modelsAuthorizeCopy
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/Error'
