apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureTypeBulkupdate
  description: >-
    Update all types in bulk, changes detected in the type definitions would be
    persisted.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Atlas Types Typedefs
      description: >-
        Update all types in bulk, changes detected in the type definitions would be
        persisted.
    paths:
      /atlas/v2/types/typedefs:
        put:
          summary: Put Atlas Types Typedefs
          description: >-
            Update all types in bulk, changes detected in the type definitions would
            be persisted.
          operationId: microsoftAzureTypeBulkupdate
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
