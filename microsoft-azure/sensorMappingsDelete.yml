apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Sensor-mappings Sensormappingid
  description: Deletes a sensor mapping entity.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Sensor-mappings Sensormappingid
      description: Deletes a sensor mapping entity.
    paths:
      /sensor-mappings/{sensorMappingId}:
        delete:
          summary: Delete Sensor-mappings Sensormappingid
          description: Deletes a sensor mapping entity.
          operationId: sensorMappingsDelete
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
