apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureCallconnectionsTransfertoparticipant
  description: Transfer the call to a participant.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Transfer The Call To A Participant '
      description: Transfer the call to a participant.
    paths:
      /calling/callConnections/{callConnectionId}:transferToParticipant:
        post:
          summary: 'Transfer The Call To A Participant '
          description: Transfer the call to a participant.
          operationId: microsoftAzureCallconnectionsTransfertoparticipant
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/CommunicationErrorResponse'
