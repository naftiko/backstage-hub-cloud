apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Atlas Types Typedefs
  description: Delete API for all types in bulk.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Atlas Types Typedefs
      description: Delete API for all types in bulk.
    paths:
      /atlas/v2/types/typedefs:
        delete:
          summary: Delete Atlas Types Typedefs
          description: Delete API for all types in bulk.
          operationId: typesDeleteTypeDefinitions
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
