apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-all-default-server-metrics-config-with-supported-resource-type
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Default Server Metrics Config With Supported Resource Type
      description: Needs a more full description created.
    paths:
      /serverMetricsConfig/default:
        get:
          summary: Get All Default Server Metrics Config With Supported Resource Type
          description: Needs a more full description created.
          operationId: serverMetricsGetServerDefaultMetrics
          responses:
            default:
              description: |-
                ***Error Responses: ***

                 * "400", "InvalidHostOrResourceName", "Host name or domain is not valid" .

                 * "500", "InternalServerError", "Unable to process the request, please retry"
              schema:
                $ref: '#/definitions/DefaultErrorResponseBody'
              headers:
                x-ms-error-code:
                  description: The error code for specific error that occurred.
                  type: string
