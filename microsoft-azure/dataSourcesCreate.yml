apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Datasources
  description: Creates a new datasource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Datasources
      description: Creates a new datasource.
    paths:
      /datasources:
        post:
          summary: Post Datasources
          description: Creates a new datasource.
          operationId: dataSourcesCreate
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
