apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureLoadtestadministrationGetservermetricsconfig
  description: List server metrics configuration for the given test.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'List Server Metrics Configuration For The Given Test '
      description: List server metrics configuration for the given test.
    paths:
      /tests/{testId}/server-metrics-config:
        get:
          summary: 'List Server Metrics Configuration For The Given Test '
          description: List server metrics configuration for the given test.
          operationId: microsoftAzureLoadtestadministrationGetservermetricsconfig
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
