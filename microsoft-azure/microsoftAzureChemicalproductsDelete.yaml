apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureChemicalproductsDelete
  description: Deletes dataset for given chemicalProduct id.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Chemical Products Chemicalproductid
      description: Deletes dataset for given chemicalProduct id.
    paths:
      /chemical-products/{chemicalProductId}:
        delete:
          summary: Delete Chemical Products Chemicalproductid
          description: Deletes dataset for given chemicalProduct id.
          operationId: microsoftAzureChemicalproductsDelete
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
