apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: add-participants-to-the-call
  description: Add participants to the call.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Add Participants To The Call.
      description: Add participants to the call.
    paths:
      /calling/callConnections/{callConnectionId}/participants:add:
        post:
          summary: Add Participants To The Call.
          description: Add participants to the call.
          operationId: callConnectionsAddParticipant
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/CommunicationErrorResponse'
