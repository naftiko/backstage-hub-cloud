apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: create-or-update-metrics-configuration-of-the-cluster
  description: >-
    Create new or update the existing metrics configuration of the provided
    cluster.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Or Update Metrics Configuration Of The Cluster.
      description: >-
        Create new or update the existing metrics configuration of the provided
        cluster.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.NetworkCloud/clusters/{clusterName}/metricsConfigurations/{metricsConfigurationName}:
        put:
          summary: Create Or Update Metrics Configuration Of The Cluster.
          description: >-
            Create new or update the existing metrics configuration of the provided
            cluster.
          operationId: metricsConfigurationsCreateOrUpdate
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: >-
                  ../../../../../common-types/resource-management/v5/types.json#/definitions/ErrorResponse
