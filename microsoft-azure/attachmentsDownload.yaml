apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-parties-partyid-attachments-attachmentid-file
  description: Downloads and returns attachment as response for the given input filePath.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Parties Partyid Attachments Attachmentid File
      description: Downloads and returns attachment as response for the given input filePath.
    paths:
      /parties/{partyId}/attachments/{attachmentId}/file:
        get:
          summary: Get Parties Partyid Attachments Attachmentid File
          description: >-
            Downloads and returns attachment as response for the given input
            filePath.
          operationId: attachmentsDownload
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
