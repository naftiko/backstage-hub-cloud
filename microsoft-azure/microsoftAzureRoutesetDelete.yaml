apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureRoutesetDelete
  description: >-
    <br><br>Creator makes it possible to develop applications based on your
    private indoor map data using Azure Maps API and SDKs. See the [Creator for
    indoor
    maps](https://docs.microsoft.com/azure/azure-maps/creator-indoor-maps)
    article for an introduction to the Azure Maps Creator concepts and
    tools.<br><br>### Submit Delete Request<br><br>The `Delete` API is an HTTP
    `DELETE` request sent with the `routesetId` of the routeset to delete.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Use To Delete A Routeset '
      description: >-
        <br><br>Creator makes it possible to develop applications based on your
        private indoor map data using Azure Maps API and SDKs. See the [Creator for
        indoor
        maps](https://docs.microsoft.com/azure/azure-maps/creator-indoor-maps)
        article for an introduction to the Azure Maps Creator concepts and
        tools.<br><br>### Submit Delete Request<br><br>The `Delete` API is an HTTP
        `DELETE` request sent with the `routesetId` of the routeset to delete.
    paths:
      /routesets/{routesetId}:
        delete:
          summary: 'Use To Delete A Routeset '
          description: >-
            <br><br>Creator makes it possible to develop applications based on your
            private indoor map data using Azure Maps API and SDKs. See the [Creator
            for indoor
            maps](https://docs.microsoft.com/azure/azure-maps/creator-indoor-maps)
            article for an introduction to the Azure Maps Creator concepts and
            tools.<br><br>### Submit Delete Request<br><br>The `Delete` API is an
            HTTP `DELETE` request sent with the `routesetId` of the routeset to
            delete.
          operationId: microsoftAzureRoutesetDelete
          responses:
            default:
              $ref: ../../../Common/preview/1.0/common.json#/responses/default
