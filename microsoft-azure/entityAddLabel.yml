apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Put Atlas Entity Guid Guid Labels
  description: add given labels to a given entity
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Atlas Entity Guid Guid Labels
      description: add given labels to a given entity
    paths:
      /atlas/v2/entity/guid/{guid}/labels:
        put:
          summary: Put Atlas Entity Guid Guid Labels
          description: add given labels to a given entity
          operationId: entityAddLabel
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
