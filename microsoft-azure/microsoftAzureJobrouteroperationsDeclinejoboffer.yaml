apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureJobrouteroperationsDeclinejoboffer
  description: Declines an offer to work on a job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Declines An Offer To Work On A Job '
      description: Declines an offer to work on a job.
    paths:
      /routing/workers/{workerId}/offers/{offerId}:decline:
        post:
          summary: 'Declines An Offer To Work On A Job '
          description: Declines an offer to work on a job.
          operationId: microsoftAzureJobrouteroperationsDeclinejoboffer
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
