apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: deletes-the-fabric
  description: Removes the fabric.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deletes The Fabric.
      description: Removes the fabric.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DataReplication/replicationFabrics/{fabricName}:
        delete:
          summary: Deletes The Fabric.
          description: Removes the fabric.
          operationId: fabricDelete
          responses:
            default:
              description: Error
              schema:
                $ref: >-
                  ../../../../../common-types/resource-management/v5/types.json#/definitions/ErrorResponse
