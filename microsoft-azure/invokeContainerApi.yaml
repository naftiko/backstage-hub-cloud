apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: invoke-container-api-on-a-container-deployed-on-a-service-fabric-node
  description: >-
    Invoke container API on a container deployed on a Service Fabric node for
    the given code package.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Invoke Container Api On A Container Deployed On A Service Fabric Node.
      description: >-
        Invoke container API on a container deployed on a Service Fabric node for
        the given code package.
    paths:
      /Nodes/{nodeName}/$/GetApplications/{applicationId}/$/GetCodePackages/$/ContainerApi:
        post:
          summary: Invoke Container Api On A Container Deployed On A Service Fabric Node.
          description: >-
            Invoke container API on a container deployed on a Service Fabric node
            for the given code package.
          operationId: invokeContainerApi
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
