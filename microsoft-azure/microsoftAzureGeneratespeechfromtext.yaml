apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureGeneratespeechfromtext
  description: Generates text-to-speech audio from the input text.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Deployments Deploymentid Audio Speech
      description: Generates text-to-speech audio from the input text.
    paths:
      /deployments/{deploymentId}/audio/speech:
        post:
          summary: Post Deployments Deploymentid Audio Speech
          description: Generates text-to-speech audio from the input text.
          operationId: microsoftAzureGeneratespeechfromtext
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
