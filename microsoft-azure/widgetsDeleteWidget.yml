apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Widgets Widgetname
  description: Delete a Widget asynchronously.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Widgets Widgetname
      description: Delete a Widget asynchronously.
    paths:
      /widgets/{widgetName}:
        delete:
          summary: Delete Widgets Widgetname
          description: Delete a Widget asynchronously.
          operationId: widgetsDeleteWidget
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
