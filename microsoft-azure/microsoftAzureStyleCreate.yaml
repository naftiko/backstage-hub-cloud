apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureStyleCreate
  description: >-
    <br><br>## Submit Create Request<br><br> The `Style Create` API is an HTTP
    `POST` request used to create an Azure Maps style by passing in either a
    JSON or ZIP file containing the style information. <br><br>The Create Style
    API is a [long-running operation](https://aka.ms/am-creator-lrt-v2).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Use To Create An Azure Maps Style '
      description: >-
        <br><br>## Submit Create Request<br><br> The `Style Create` API is an HTTP
        `POST` request used to create an Azure Maps style by passing in either a
        JSON or ZIP file containing the style information. <br><br>The Create Style
        API is a [long-running operation](https://aka.ms/am-creator-lrt-v2).
    paths:
      /styles:
        post:
          summary: 'Use To Create An Azure Maps Style '
          description: >-
            <br><br>## Submit Create Request<br><br> The `Style Create` API is an
            HTTP `POST` request used to create an Azure Maps style by passing in
            either a JSON or ZIP file containing the style information. <br><br>The
            Create Style API is a [long-running
            operation](https://aka.ms/am-creator-lrt-v2).
          operationId: microsoftAzureStyleCreate
          responses:
            default:
              $ref: ../../../Common/preview/1.0/common.json#/responses/default
