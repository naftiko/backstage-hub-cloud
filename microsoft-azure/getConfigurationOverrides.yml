apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Gets The List Of Configuration Overrides On The Specified Node.
  description: >-
    This api allows getting all existing configuration overrides on the
    specified node.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Gets The List Of Configuration Overrides On The Specified Node.
      description: >-
        This api allows getting all existing configuration overrides on the
        specified node.
    paths:
      /Nodes/{nodeName}/$/GetConfigurationOverrides:
        get:
          summary: Gets The List Of Configuration Overrides On The Specified Node.
          description: >-
            This api allows getting all existing configuration overrides on the
            specified node.
          operationId: getConfigurationOverrides
          responses:
            default:
              description: The detailed error response.
              schema:
                $ref: '#/definitions/FabricError'
