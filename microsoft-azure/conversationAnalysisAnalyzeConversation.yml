apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post :analyze-conversations
  description: Analyzes the input conversation utterance.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post :analyze-conversations
      description: Analyzes the input conversation utterance.
    paths:
      /:analyze-conversations:
        post:
          summary: Post :analyze-conversations
          description: Analyzes the input conversation utterance.
          operationId: conversationAnalysisAnalyzeConversation
          responses:
            default:
              description: Error response.
              schema:
                $ref: common.json#/definitions/ErrorResponse
              headers:
                x-ms-error-code:
                  description: The error code for specific error that occurred.
                  type: string
