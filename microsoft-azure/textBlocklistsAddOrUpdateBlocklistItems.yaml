apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: add-or-update-blocklistitems-to-text-blocklist
  description: >-
    Add or update blocklistItems to a text blocklist. You can add or update at
    most 100 blocklistItems in one request.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Add Or Update Blocklistitems To Text Blocklist
      description: >-
        Add or update blocklistItems to a text blocklist. You can add or update at
        most 100 blocklistItems in one request.
    paths:
      /text/blocklists/{blocklistName}:addOrUpdateBlocklistItems:
        post:
          summary: Add Or Update Blocklistitems To Text Blocklist
          description: >-
            Add or update blocklistItems to a text blocklist. You can add or update
            at most 100 blocklistItems in one request.
          operationId: textBlocklistsAddOrUpdateBlocklistItems
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
