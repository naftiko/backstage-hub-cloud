apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-widgets-widgetname
  description: Fetch a Widget by name.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Widgets Widgetname
      description: Fetch a Widget by name.
    paths:
      /widgets/{widgetName}:
        get:
          summary: Get Widgets Widgetname
          description: Fetch a Widget by name.
          operationId: widgetsGetWidget
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
