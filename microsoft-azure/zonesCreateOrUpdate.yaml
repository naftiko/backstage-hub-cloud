apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: patch-parties-partyid-zones-zoneid
  description: Creates or updates a Zone resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Patch Parties Partyid Zones Zoneid
      description: Creates or updates a Zone resource.
    paths:
      /parties/{partyId}/zones/{zoneId}:
        patch:
          summary: Patch Parties Partyid Zones Zoneid
          description: Creates or updates a Zone resource.
          operationId: zonesCreateOrUpdate
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  x-ms-client-name: ErrorCode
              x-ms-error-response: true
