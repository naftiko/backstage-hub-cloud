apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-subscriptions-subscriptionid-resources
  description: Get all the resources in a subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Subscriptions Subscriptionid Resources
      description: Get all the resources in a subscription.
    paths:
      /subscriptions/{subscriptionId}/resources:
        get:
          summary: Get Subscriptions Subscriptionid Resources
          description: Get all the resources in a subscription.
          operationId: resourcesList
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
