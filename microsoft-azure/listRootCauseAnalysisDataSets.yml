apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Root Cause Analysis Data Sets
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Root Cause Analysis Data Sets
      description: Needs a more full description created.
    paths:
      /rootCauseAnalysis/dataSets:
        get:
          summary: List Root Cause Analysis Data Sets
          description: Needs a more full description created.
          operationId: listRootCauseAnalysisDataSets
          responses:
            default:
              description: Client error or server error (4xx or 5xx)
              schema:
                $ref: '#/definitions/ErrorDetail'
              headers:
                x-ms-error-code:
                  description: error code
                  type: string
