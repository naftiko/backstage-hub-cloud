apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-apps-appid-versions-versionid-settings
  description: Gets the settings in a version of the application.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Apps Appid Versions Versionid Settings
      description: Gets the settings in a version of the application.
    paths:
      /apps/{appId}/versions/{versionId}/settings:
        get:
          summary: Get Apps Appid Versions Versionid Settings
          description: Gets the settings in a version of the application.
          operationId: settingsList
          responses:
            default:
              description: Error Response.
              schema:
                $ref: '#/definitions/ErrorResponse'
