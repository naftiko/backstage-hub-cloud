apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Operationroute
  description: Creates a new streaming chat completion.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Operationroute
      description: Creates a new streaming chat completion.
    paths:
      /{operationRoute}:
        post:
          summary: Post Operationroute
          description: Creates a new streaming chat completion.
          operationId: chatCreateStreaming
          responses:
            '200':
              description: The request has succeeded.
              schema:
                $ref: '#/definitions/ChatCompletionChunk'
