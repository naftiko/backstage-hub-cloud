apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: creates-a-job-request
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Creates A Job Request.
      description: Needs a more full description created.
    paths:
      /accounts/{accountId}/jobs/{jobId}:
        put:
          summary: Creates A Job Request.
          description: Needs a more full description created.
          operationId: ingestionJobCreate
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
