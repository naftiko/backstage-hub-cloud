apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Or Update The Nginx Configuration For Given Nginx Deployment
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Or Update The Nginx Configuration For Given Nginx Deployment
      description: Needs a more full description created.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Nginx.NginxPlus/nginxDeployments/{deploymentName}/configurations/{configurationName}:
        put:
          summary: Create Or Update The Nginx Configuration For Given Nginx Deployment
          description: Needs a more full description created.
          operationId: configurationsCreateOrUpdate
          responses:
            default:
              description: Default error response.
              schema:
                $ref: '#/definitions/ResourceProviderDefaultErrorResponse'
