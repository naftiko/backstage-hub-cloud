apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureScheduledjobsGet
  description: Get details about a scheduled job by ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Scheduled Job By Id
      description: Get details about a scheduled job by ID.
    paths:
      /scheduledJobs/{scheduledJobId}:
        get:
          summary: Get A Scheduled Job By Id
          description: Get details about a scheduled job by ID.
          operationId: microsoftAzureScheduledjobsGet
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
