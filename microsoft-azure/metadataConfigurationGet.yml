apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Retrieves The Openid Configuration Data For The Azure Attestation Service
  description: >-
    Retrieves metadata about the attestation signing keys in use by the
    attestation service
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieves The Openid Configuration Data For The Azure Attestation Service
      description: >-
        Retrieves metadata about the attestation signing keys in use by the
        attestation service
    paths:
      /.well-known/openid-configuration:
        get:
          summary: >-
            Retrieves The Openid Configuration Data For The Azure Attestation
            Service
          description: >-
            Retrieves metadata about the attestation signing keys in use by the
            attestation service
          operationId: metadataConfigurationGet
          responses:
            default:
              description: Error response describing why the operation failed
              schema:
                $ref: '#/definitions/CloudError'
