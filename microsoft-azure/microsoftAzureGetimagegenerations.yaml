apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureGetimagegenerations
  description: Creates an image given a prompt.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Deployments Deploymentid Images Generations
      description: Creates an image given a prompt.
    paths:
      /deployments/{deploymentId}/images/generations:
        post:
          summary: Post Deployments Deploymentid Images Generations
          description: Creates an image given a prompt.
          operationId: microsoftAzureGetimagegenerations
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
