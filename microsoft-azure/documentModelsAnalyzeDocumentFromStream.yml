apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Documentmodels Modelid:analyze
  description: Analyzes document with document model.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Documentmodels Modelid:analyze
      description: Analyzes document with document model.
    paths:
      /documentModels/{modelId}:analyze:
        post:
          summary: Post Documentmodels Modelid:analyze
          description: Analyzes document with document model.
          operationId: documentModelsAnalyzeDocumentFromStream
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/ErrorResponse'
