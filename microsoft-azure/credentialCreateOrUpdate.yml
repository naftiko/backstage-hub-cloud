apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Put Credentials Credentialname
  description: Creates an instance of a credential
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Credentials Credentialname
      description: Creates an instance of a credential
    paths:
      /credentials/{credentialName}:
        put:
          summary: Put Credentials Credentialname
          description: Creates an instance of a credential
          operationId: credentialCreateOrUpdate
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
