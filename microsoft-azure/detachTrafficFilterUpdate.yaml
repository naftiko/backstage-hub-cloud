apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: detach-traffic-filter-for-the-given-deployment
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Detach Traffic Filter For The Given Deployment.
      description: Needs a more full description created.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Elastic/monitors/{monitorName}/detachTrafficFilter:
        post:
          summary: Detach Traffic Filter For The Given Deployment.
          description: Needs a more full description created.
          operationId: detachTrafficFilterUpdate
          responses:
            default:
              description: Default error response.
              schema:
                $ref: '#/definitions/ResourceProviderDefaultErrorResponse'
