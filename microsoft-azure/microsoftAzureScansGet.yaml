apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureScansGet
  description: Gets a scan information
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Datasources Datasourcename Scans Scanname
      description: Gets a scan information
    paths:
      /datasources/{dataSourceName}/scans/{scanName}:
        get:
          summary: Get Datasources Datasourcename Scans Scanname
          description: Gets a scan information
          operationId: microsoftAzureScansGet
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
