apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: deploy-the-cluster
  description: Deploy the cluster using the rack configuration provided during creation.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deploy The Cluster.
      description: Deploy the cluster using the rack configuration provided during creation.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.NetworkCloud/clusters/{clusterName}/deploy:
        post:
          summary: Deploy The Cluster.
          description: >-
            Deploy the cluster using the rack configuration provided during
            creation.
          operationId: clustersDeploy
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: >-
                  ../../../../../common-types/resource-management/v5/types.json#/definitions/ErrorResponse
