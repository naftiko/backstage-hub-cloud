apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Batches Batchid
  description: Cancels a running spark batch job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Batches Batchid
      description: Cancels a running spark batch job.
    paths:
      /batches/{batchId}:
        delete:
          summary: Delete Batches Batchid
          description: Cancels a running spark batch job.
          operationId: sparkBatchCancelSparkBatchJob
          responses:
            '200':
              description: Success
