apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-livy-batches-batchid-state
  description: Gets a single spark batch state.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Livy Batches Batchid State
      description: Gets a single spark batch state.
    paths:
      /livy/batches/{batchId}/state:
        get:
          summary: Get Livy Batches Batchid State
          description: Gets a single spark batch state.
          operationId: jobGetSparkBatchState
          responses:
            '200':
              description: Success
              schema:
                $ref: '#/definitions/SparkJobState'
