apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: retrieve-a-list-of-tasks-for-the-provided-search-parameters
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieve A List Of Tasks For The Provided Search Parameters.
      description: Needs a more full description created.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/workspaces/{workspaceName}/tasks:
        get:
          summary: Retrieve A List Of Tasks For The Provided Search Parameters.
          description: Needs a more full description created.
          operationId: tasksList
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/ErrorResponse'
