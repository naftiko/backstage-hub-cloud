apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureGetanomalydetectionconfiguration
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Query A Single Anomaly Detection Configuration
      description: Needs a more full description created.
    paths:
      /enrichment/anomalyDetection/configurations/{configurationId}:
        get:
          summary: Query A Single Anomaly Detection Configuration
          description: Needs a more full description created.
          operationId: microsoftAzureGetanomalydetectionconfiguration
          responses:
            default:
              description: Client error or server error (4xx or 5xx)
              schema:
                $ref: '#/definitions/ErrorCode'
