apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureMultivariateDetectmultivariatelastanomaly
  description: >-
    Submit a multivariate anomaly detection task with the modelId value of a
    trained model<br>and inference data. The inference data should be put into
    the request body in<br>JSON format. The request will finish synchronously
    and return the detection<br>immediately in the response body.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Detect Anomalies In The Last Point Of The Request Body
      description: >-
        Submit a multivariate anomaly detection task with the modelId value of a
        trained model<br>and inference data. The inference data should be put into
        the request body in<br>JSON format. The request will finish synchronously
        and return the detection<br>immediately in the response body.
    paths:
      /multivariate/models/{modelId}:detect-last:
        post:
          summary: Detect Anomalies In The Last Point Of The Request Body
          description: >-
            Submit a multivariate anomaly detection task with the modelId value of a
            trained model<br>and inference data. The inference data should be put
            into the request body in<br>JSON format. The request will finish
            synchronously and return the detection<br>immediately in the response
            body.
          operationId: microsoftAzureMultivariateDetectmultivariatelastanomaly
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Multivariate.ResponseError'
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code.
