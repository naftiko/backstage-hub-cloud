apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: list-all-destinations-connected-to-the-given-export
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List All Destinations Connected To The Given Export.
      description: Needs a more full description created.
    paths:
      /dataExport/exports/{exportId}/destinations:
        get:
          summary: List All Destinations Connected To The Given Export.
          description: Needs a more full description created.
          operationId: exportsListDestinations
          responses:
            default:
              description: An error response received from the IoT Central Service.
              headers:
                x-ms-error-code:
                  type: string
                  description: Error code for specific error that occurred.
              schema:
                $ref: '#/definitions/Error'
