apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Scanrulesets Scanrulesetname
  description: Deletes a scan ruleset
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Scanrulesets Scanrulesetname
      description: Deletes a scan ruleset
    paths:
      /scanrulesets/{scanRulesetName}:
        delete:
          summary: Delete Scanrulesets Scanrulesetname
          description: Deletes a scan ruleset
          operationId: scanRulesetsDelete
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
