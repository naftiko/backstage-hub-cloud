apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-providers-microsoftresourcehealth-metadata
  description: Gets the list of metadata entities.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Providers Microsoft.resourcehealth Metadata
      description: Gets the list of metadata entities.
    paths:
      /providers/Microsoft.ResourceHealth/metadata:
        get:
          summary: Get Providers Microsoft.resourcehealth Metadata
          description: Gets the list of metadata entities.
          operationId: metadataList
          responses:
            default:
              description: DefaultErrorResponse
              schema:
                $ref: '#/definitions/ErrorResponse'
