apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-atlas-types-typedefs
  description: List all type definitions in bulk.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Atlas Types Typedefs
      description: List all type definitions in bulk.
    paths:
      /atlas/v2/types/typedefs:
        get:
          summary: Get Atlas Types Typedefs
          description: List all type definitions in bulk.
          operationId: typeList
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
