apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: retrieves-existing-exception-policies
  description: Retrieves existing exception policies.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieves Existing Exception Policies.
      description: Retrieves existing exception policies.
    paths:
      /routing/exceptionPolicies:
        get:
          summary: Retrieves Existing Exception Policies.
          description: Retrieves existing exception policies.
          operationId: jobRouterAdministrationOperationsListExceptionPolicies
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
