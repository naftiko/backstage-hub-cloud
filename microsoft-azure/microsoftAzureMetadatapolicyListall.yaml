apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureMetadatapolicyListall
  description: List or Get metadata policies
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Metadatapolicies
      description: List or Get metadata policies
    paths:
      /metadataPolicies:
        get:
          summary: Get Metadatapolicies
          description: List or Get metadata policies
          operationId: microsoftAzureMetadatapolicyListall
          responses:
            default:
              description: An error response received from the Metadata Policy Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
              headers:
                x-ms-error-code:
                  type: string
                  description: The error code
