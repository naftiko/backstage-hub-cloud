apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureTriggersGettrigger
  description: Gets trigger information
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Datasources Datasourcename Scans Scanname Triggers Default
      description: Gets trigger information
    paths:
      /datasources/{dataSourceName}/scans/{scanName}/triggers/default:
        get:
          summary: Get Datasources Datasourcename Scans Scanname Triggers Default
          description: Gets trigger information
          operationId: microsoftAzureTriggersGettrigger
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
