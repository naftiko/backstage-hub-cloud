apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: get-one-role-for-a-given-entity-in-a-version-of-the-application
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get One Role For A Given Entity In A Version Of The Application
      description: Needs a more full description created.
    paths:
      /apps/{appId}/versions/{versionId}/entities/{entityId}/roles/{roleId}:
        get:
          summary: Get One Role For A Given Entity In A Version Of The Application
          description: Needs a more full description created.
          operationId: modelGetEntityRole
          responses:
            default:
              description: Error Response.
              schema:
                $ref: '#/definitions/ErrorResponse'
