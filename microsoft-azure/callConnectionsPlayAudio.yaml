apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: play-audio-in-the-call
  description: Play audio in the call.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Play Audio In The Call.
      description: Play audio in the call.
    paths:
      /calling/callConnections/{callConnectionId}/:playAudio:
        post:
          summary: Play Audio In The Call.
          description: Play audio in the call.
          operationId: callConnectionsPlayAudio
          responses:
            '500':
              $ref: '#/responses/500'
