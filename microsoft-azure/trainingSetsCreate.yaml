apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: put-trainingsets-id
  description: Creates a new training set.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Trainingsets Id
      description: Creates a new training set.
    paths:
      /trainingsets/{id}:
        put:
          summary: Put Trainingsets Id
          description: Creates a new training set.
          operationId: trainingSetsCreate
          responses:
            default:
              description: An error occurred.
              schema:
                $ref: '#/definitions/ErrorResponse'
