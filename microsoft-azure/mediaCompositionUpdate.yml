apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Updates An Existing Media Composition
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Updates An Existing Media Composition
      description: Needs a more full description created.
    paths:
      /mediaCompositions/{mediaCompositionId}:
        patch:
          summary: Updates An Existing Media Composition
          description: Needs a more full description created.
          operationId: mediaCompositionUpdate
          responses:
            default:
              description: Failure
              schema:
                $ref: '#/definitions/CommunicationErrorResponse'
              headers:
                x-ms-error-code:
                  description: The error code.
                  type: string
              x-ms-error-response: true
