apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureIndexesAnalyze
  description: Shows how an analyzer breaks text into tokens.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Indexes Indexname  Search Analyze
      description: Shows how an analyzer breaks text into tokens.
    paths:
      /indexes('{indexName}')/search.analyze:
        post:
          summary: Post Indexes Indexname  Search Analyze
          description: Shows how an analyzer breaks text into tokens.
          operationId: microsoftAzureIndexesAnalyze
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
