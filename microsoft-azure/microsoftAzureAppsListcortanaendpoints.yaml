apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureAppsListcortanaendpoints
  description: Gets the endpoint URLs for the prebuilt Cortana applications.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Apps Assistants
      description: Gets the endpoint URLs for the prebuilt Cortana applications.
    paths:
      /apps/assistants:
        get:
          summary: Get Apps Assistants
          description: Gets the endpoint URLs for the prebuilt Cortana applications.
          operationId: microsoftAzureAppsListcortanaendpoints
          responses:
            default:
              description: Error Response.
              schema:
                $ref: '#/definitions/ErrorResponse'
