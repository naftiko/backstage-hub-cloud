apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: >-
    Generates A New Publishing Password For An App (or Deployment Slot, If
    Specified).
  description: >-
    Description for Generates a new publishing password for an app (or
    deployment slot, if specified).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: >-
        Generates A New Publishing Password For An App (or Deployment Slot, If
        Specified).
      description: >-
        Description for Generates a new publishing password for an app (or
        deployment slot, if specified).
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Web/sites/{name}/slots/{slot}/newpassword:
        post:
          summary: >-
            Generates A New Publishing Password For An App (or Deployment Slot, If
            Specified).
          description: >-
            Description for Generates a new publishing password for an app (or
            deployment slot, if specified).
          operationId: webAppsGenerateNewSitePublishingPasswordSlot
          responses:
            default:
              description: App Service error response.
              schema:
                $ref: ./CommonDefinitions.json#/definitions/DefaultErrorResponse
