apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Put Datasources Datasourcename Scans Scanname Runs Runid
  description: Runs the scan
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put Datasources Datasourcename Scans Scanname Runs Runid
      description: Runs the scan
    paths:
      /datasources/{dataSourceName}/scans/{scanName}/runs/{runId}:
        put:
          summary: Put Datasources Datasourcename Scans Scanname Runs Runid
          description: Runs the scan
          operationId: scanResultRunScan
          responses:
            default:
              description: An error response received from the Scanning Service
              schema:
                $ref: '#/definitions/ErrorResponseModel'
