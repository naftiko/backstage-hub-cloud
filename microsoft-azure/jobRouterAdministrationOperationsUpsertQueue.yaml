apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: creates-or-updates-a-queue
  description: Creates or updates a queue.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Creates Or Updates A Queue.
      description: Creates or updates a queue.
    paths:
      /routing/queues/{queueId}:
        patch:
          summary: Creates Or Updates A Queue.
          description: Creates or updates a queue.
          operationId: jobRouterAdministrationOperationsUpsertQueue
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
