apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: remove-a-connection-from-the-target-group
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Remove A Connection From The Target Group.
      description: Needs a more full description created.
    paths:
      /api/hubs/{hub}/groups/{group}/connections/{connectionId}:
        delete:
          summary: Remove A Connection From The Target Group.
          description: Needs a more full description created.
          operationId: webPubSubRemoveConnectionFromGroup
          responses:
            default:
              description: Error response
              x-ms-error-response: true
