apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: post-authoring-analyze-conversations-projects-projectname-copy
  description: Copies an existing project to another Azure resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Authoring Analyze-conversations Projects Projectname :copy
      description: Copies an existing project to another Azure resource.
    paths:
      /authoring/analyze-conversations/projects/{projectName}/:copy:
        post:
          summary: Post Authoring Analyze-conversations Projects Projectname :copy
          description: Copies an existing project to another Azure resource.
          operationId: conversationalAnalysisAuthoringCopyProject
          responses:
            default:
              description: Error response.
              schema:
                $ref: common.json#/definitions/ErrorResponse
