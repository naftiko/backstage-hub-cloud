apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Documentclassifiers:build
  description: Builds a custom document classifier.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Documentclassifiers:build
      description: Builds a custom document classifier.
    paths:
      /documentClassifiers:build:
        post:
          summary: Post Documentclassifiers:build
          description: Builds a custom document classifier.
          operationId: documentClassifiersBuildClassifier
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/ErrorResponse'
