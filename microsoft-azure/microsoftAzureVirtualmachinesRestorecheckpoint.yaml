apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureVirtualmachinesRestorecheckpoint
  description: Restores to a checkpoint in virtual machine.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Implements The Operation To Restores To A Checkpoint In A Virtual Machine '
      description: Restores to a checkpoint in virtual machine.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ScVmm/virtualMachines/{virtualMachineName}/restoreCheckpoint:
        post:
          summary: >-
            Implements The Operation To Restores To A Checkpoint In A Virtual
            Machine 
          description: Restores to a checkpoint in virtual machine.
          operationId: microsoftAzureVirtualmachinesRestorecheckpoint
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
