apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Gets The Status Of A Signing Operation.
  description: >-
    This status operation requires that a Sign request has been submitted and
    the operationId is known.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Gets The Status Of A Signing Operation.
      description: >-
        This status operation requires that a Sign request has been submitted and
        the operationId is known.
    paths:
      /codesigningaccounts/{accountName}/certificateprofiles/{certificateProfile}/sign/{operationId}:
        get:
          summary: Gets The Status Of A Signing Operation.
          description: >-
            This status operation requires that a Sign request has been submitted
            and the operationId is known.
          operationId: certificateProfileOperationsGetSigningStatus
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
