apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Or Update Private Endpoint.
  description: >-
    Create a private endpoint with specified name. If a private endpoint already
    exists, update it.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Or Update Private Endpoint.
      description: >-
        Create a private endpoint with specified name. If a private endpoint already
        exists, update it.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/migrateProjects/{migrateProjectName}/privateEndpointConnections/{peConnectionName}:
        put:
          summary: Create Or Update Private Endpoint.
          description: >-
            Create a private endpoint with specified name. If a private endpoint
            already exists, update it.
          operationId: privateEndpointConnectionControllerPutPrivateEndpointConnection
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/CloudError'
