apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureCreatemanifest
  description: >-
    Put the manifest identified by `name` and `reference` where `reference` can
    be a tag or digest.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Put  Name Manifests Reference
      description: >-
        Put the manifest identified by `name` and `reference` where `reference` can
        be a tag or digest.
    paths:
      /v2/{name}/manifests/{reference}:
        put:
          summary: Put  Name Manifests Reference
          description: >-
            Put the manifest identified by `name` and `reference` where `reference`
            can be a tag or digest.
          operationId: microsoftAzureCreatemanifest
          responses:
            default:
              description: ACR error response describing why the operation failed.
              schema:
                $ref: '#/definitions/AcrErrors'
