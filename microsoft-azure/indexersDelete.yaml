apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: delete-indexersindexername
  description: Deletes an indexer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Indexers('indexername')
      description: Deletes an indexer.
    paths:
      /indexers('{indexerName}'):
        delete:
          summary: Delete Indexers('indexername')
          description: Deletes an indexer.
          operationId: indexersDelete
          responses:
            default:
              description: Error response.
              schema:
                $ref: '#/definitions/SearchError'
