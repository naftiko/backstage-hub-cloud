apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Deletes The Specified Value Of The Named Secret Resource.
  description: >-
    Deletes the secret value resource identified by the name. The name of the
    resource is typically the version associated with that value. Deletion will
    fail if the specified value is in use.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deletes The Specified Value Of The Named Secret Resource.
      description: >-
        Deletes the secret value resource identified by the name. The name of the
        resource is typically the version associated with that value. Deletion will
        fail if the specified value is in use.
    paths:
      /Resources/Secrets/{secretResourceName}/values/{secretValueResourceName}:
        delete:
          summary: Deletes The Specified Value Of The Named Secret Resource.
          description: >-
            Deletes the secret value resource identified by the name. The name of
            the resource is typically the version associated with that value.
            Deletion will fail if the specified value is in use.
          operationId: meshSecretValueDelete
          responses:
            default:
              description: Error
              schema:
                $ref: '#/definitions/FabricError'
