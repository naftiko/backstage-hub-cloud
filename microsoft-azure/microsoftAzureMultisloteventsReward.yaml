apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: microsoftAzureMultisloteventsReward
  description: >-
    Report reward that resulted from using the action specified in
    rewardActionId for the slot.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: 'Post Multi Slot Rewards '
      description: >-
        Report reward that resulted from using the action specified in
        rewardActionId for the slot.
    paths:
      /multislot/events/{eventId}/reward:
        post:
          summary: 'Post Multi Slot Rewards '
          description: >-
            Report reward that resulted from using the action specified in
            rewardActionId for the slot.
          operationId: microsoftAzureMultisloteventsReward
          responses:
            default:
              description: Invalid reward request.
              schema:
                $ref: '#/definitions/ErrorResponse'
