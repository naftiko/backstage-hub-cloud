apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Indexes Name
  description: Get latest Index.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Indexes Name
      description: Get latest Index.
    paths:
      /indexes/{name}:
        get:
          summary: Get Indexes Name
          description: Get latest Index.
          operationId: indexesGetLatest
          responses:
            default:
              description: An unexpected error response.
              schema:
                $ref: '#/definitions/Azure.Core.Foundations.ErrorResponse'
              headers:
                x-ms-error-code:
                  type: string
                  description: String error code indicating what went wrong.
