apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Implements Virtualnetworks Put Method.
  description: Onboards the ScVmm virtual network as an Azure virtual network resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Implements Virtualnetworks Put Method.
      description: Onboards the ScVmm virtual network as an Azure virtual network resource.
    paths:
      /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ScVmm/virtualNetworks/{virtualNetworkName}:
        put:
          summary: Implements Virtualnetworks Put Method.
          description: Onboards the ScVmm virtual network as an Azure virtual network resource.
          operationId: virtualNetworksCreateOrUpdate
          responses:
            default:
              description: Error response describing why the operation failed.
              schema:
                $ref: '#/definitions/ErrorResponse'
