apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: deleteroutesettings
  description: Deletes the RouteSettings for a stage.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deleteroutesettings
      description: Deletes the RouteSettings for a stage.
    paths:
      /v2/apis/{apiId}/stages/{stageName}/routesettings/{routeKey}:
        DELETE:
          summary: Deleteroutesettings
          description: Deletes the RouteSettings for a stage.
          operationId: deleteRouteSettings
