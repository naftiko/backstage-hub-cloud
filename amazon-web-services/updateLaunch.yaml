apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: updatelaunch
  description: >-
    Updates a launch of a given feature.  Don't use this operation to update the
    tags of an existing launch. Instead, use TagResource. 
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Updatelaunch
      description: >-
        Updates a launch of a given feature.  Don't use this operation to update the
        tags of an existing launch. Instead, use TagResource. 
    paths:
      /projects/{project}/launches/{launch}:
        PATCH:
          summary: Updatelaunch
          description: >-
            Updates a launch of a given feature.  Don't use this operation to update
            the tags of an existing launch. Instead, use TagResource. 
          operationId: updateLaunch
