apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: amazonWebServicesListCodeSigningConfigs
  description: >-
    Returns a list of code signing configurations. A request returns up to
    10,000 configurations per call. You can use the MaxItems parameter to return
    fewer configurations per call. 
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Listcodesigningconfigs
      description: >-
        Returns a list of code signing configurations. A request returns up to
        10,000 configurations per call. You can use the MaxItems parameter to return
        fewer configurations per call. 
    paths:
      /2020-04-22/code-signing-configs/:
        GET:
          summary: Listcodesigningconfigs
          description: >-
            Returns a list of code signing configurations. A request returns up to
            10,000 configurations per call. You can use the MaxItems parameter to
            return fewer configurations per call. 
          operationId: amazonWebServicesListCodeSigningConfigs
