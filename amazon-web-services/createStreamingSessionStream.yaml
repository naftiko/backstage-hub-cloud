apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: createstreamingsessionstream
  description: >-
    Creates a streaming session stream for a streaming session. After invoking
    this API, invoke GetStreamingSessionStream with the returned streamId to
    poll the resource until it is in the READY state.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Createstreamingsessionstream
      description: >-
        Creates a streaming session stream for a streaming session. After invoking
        this API, invoke GetStreamingSessionStream with the returned streamId to
        poll the resource until it is in the READY state.
    paths:
      /2020-08-01/studios/{studioId}/streaming-sessions/{sessionId}/streams:
        POST:
          summary: Createstreamingsessionstream
          description: >-
            Creates a streaming session stream for a streaming session. After
            invoking this API, invoke GetStreamingSessionStream with the returned
            streamId to poll the resource until it is in the READY state.
          operationId: createStreamingSessionStream
