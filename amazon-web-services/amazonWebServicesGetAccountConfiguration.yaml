apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: amazonWebServicesGetAccountConfiguration
  description: Use to get account level configuration.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Getaccountconfiguration
      description: Use to get account level configuration.
    paths:
      /accountConfiguration/get:
        GET:
          summary: Getaccountconfiguration
          description: Use to get account level configuration.
          operationId: amazonWebServicesGetAccountConfiguration
