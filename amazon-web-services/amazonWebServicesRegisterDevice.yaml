apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: amazonWebServicesRegisterDevice
  description: >-
    Registers a device to receive push sync notifications.This API can only be
    called with temporary credentials provided by Cognito Identity. You cannot
    call this API with developer credentials.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Registerdevice
      description: >-
        Registers a device to receive push sync notifications.This API can only be
        called with temporary credentials provided by Cognito Identity. You cannot
        call this API with developer credentials.
    paths:
      /identitypools/{IdentityPoolId}/identity/{IdentityId}/device:
        POST:
          summary: Registerdevice
          description: >-
            Registers a device to receive push sync notifications.This API can only
            be called with temporary credentials provided by Cognito Identity. You
            cannot call this API with developer credentials.
          operationId: amazonWebServicesRegisterDevice
