apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: amazonWebServicesListSignalingChannels
  description: >-
    Returns an array of ChannelInfo objects. Each object describes a signaling
    channel. To retrieve only those channels that satisfy a specific condition,
    you can specify a ChannelNameCondition.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Listsignalingchannels
      description: >-
        Returns an array of ChannelInfo objects. Each object describes a signaling
        channel. To retrieve only those channels that satisfy a specific condition,
        you can specify a ChannelNameCondition.
    paths:
      /listSignalingChannels:
        POST:
          summary: Listsignalingchannels
          description: >-
            Returns an array of ChannelInfo objects. Each object describes a
            signaling channel. To retrieve only those channels that satisfy a
            specific condition, you can specify a ChannelNameCondition.
          operationId: amazonWebServicesListSignalingChannels
