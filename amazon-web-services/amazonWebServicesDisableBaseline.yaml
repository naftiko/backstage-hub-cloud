apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: amazonWebServicesDisableBaseline
  description: >-
    Disable an EnabledBaseline resource on the specified Target. This API starts
    an asynchronous operation to remove all resources deployed as part of the
    baseline enablement. The resource will vary depending on the enabled
    baseline.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Disablebaseline
      description: >-
        Disable an EnabledBaseline resource on the specified Target. This API starts
        an asynchronous operation to remove all resources deployed as part of the
        baseline enablement. The resource will vary depending on the enabled
        baseline.
    paths:
      /disable-baseline:
        POST:
          summary: Disablebaseline
          description: >-
            Disable an EnabledBaseline resource on the specified Target. This API
            starts an asynchronous operation to remove all resources deployed as
            part of the baseline enablement. The resource will vary depending on the
            enabled baseline.
          operationId: amazonWebServicesDisableBaseline
