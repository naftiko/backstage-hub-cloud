apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Updatedomainconfiguration
  description: >-
    Updates values stored in the domain configuration. Domain configurations for
    default endpoints can't be updated. Requires permission to access the
    UpdateDomainConfiguration action.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Updatedomainconfiguration
      description: >-
        Updates values stored in the domain configuration. Domain configurations for
        default endpoints can't be updated. Requires permission to access the
        UpdateDomainConfiguration action.
    paths:
      /domainConfigurations/{domainConfigurationName}:
        PUT:
          summary: Updatedomainconfiguration
          description: >-
            Updates values stored in the domain configuration. Domain configurations
            for default endpoints can't be updated. Requires permission to access
            the UpdateDomainConfiguration action.
          operationId: updateDomainConfiguration
