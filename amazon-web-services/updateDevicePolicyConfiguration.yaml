apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: updatedevicepolicyconfiguration
  description: Updates the device policy configuration for the fleet.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Updatedevicepolicyconfiguration
      description: Updates the device policy configuration for the fleet.
    paths:
      /updateDevicePolicyConfiguration:
        POST:
          summary: Updatedevicepolicyconfiguration
          description: Updates the device policy configuration for the fleet.
          operationId: updateDevicePolicyConfiguration
