apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: amazonWebServicesUpdateProbe
  description: >-
    Updates a monitor probe. This action requires both the monitorName and
    probeId parameters. Run ListMonitors to get a list of monitor names. Run
    GetMonitor to get a list of probes and probe IDs. 
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Updateprobe
      description: >-
        Updates a monitor probe. This action requires both the monitorName and
        probeId parameters. Run ListMonitors to get a list of monitor names. Run
        GetMonitor to get a list of probes and probe IDs. 
    paths:
      /monitors/{monitorName}/probes/{probeId}:
        PATCH:
          summary: Updateprobe
          description: >-
            Updates a monitor probe. This action requires both the monitorName and
            probeId parameters. Run ListMonitors to get a list of monitor names. Run
            GetMonitor to get a list of probes and probe IDs. 
          operationId: amazonWebServicesUpdateProbe
