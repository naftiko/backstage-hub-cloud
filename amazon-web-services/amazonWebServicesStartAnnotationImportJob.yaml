apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: amazonWebServicesStartAnnotationImportJob
  description: Starts an annotation import job.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Startannotationimportjob
      description: Starts an annotation import job.
    paths:
      /import/annotation:
        POST:
          summary: Startannotationimportjob
          description: Starts an annotation import job.
          operationId: amazonWebServicesStartAnnotationImportJob
