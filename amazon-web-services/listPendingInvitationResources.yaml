apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: listpendinginvitationresources
  description: >-
    Lists the resources in a resource share that is shared with you but for
    which the invitation is still PENDING. That means that you haven't accepted
    or rejected the invitation and the invitation hasn't expired.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Listpendinginvitationresources
      description: >-
        Lists the resources in a resource share that is shared with you but for
        which the invitation is still PENDING. That means that you haven't accepted
        or rejected the invitation and the invitation hasn't expired.
    paths:
      /listpendinginvitationresources:
        POST:
          summary: Listpendinginvitationresources
          description: >-
            Lists the resources in a resource share that is shared with you but for
            which the invitation is still PENDING. That means that you haven't
            accepted or rejected the invitation and the invitation hasn't expired.
          operationId: listPendingInvitationResources
