apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: amazonWebServicesGetManagedEndpointSessionCredentials
  description: 'Generate a session token to connect to a managed endpoint. '
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Getmanagedendpointsessioncredentials
      description: 'Generate a session token to connect to a managed endpoint. '
    paths:
      /virtualclusters/{virtualClusterId}/endpoints/{endpointId}/credentials:
        POST:
          summary: Getmanagedendpointsessioncredentials
          description: 'Generate a session token to connect to a managed endpoint. '
          operationId: amazonWebServicesGetManagedEndpointSessionCredentials
