apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Listpackageimportjobs
  description: Returns a list of package import jobs.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Listpackageimportjobs
      description: Returns a list of package import jobs.
    paths:
      /packages/import-jobs:
        GET:
          summary: Listpackageimportjobs
          description: Returns a list of package import jobs.
          operationId: listPackageImportJobs
