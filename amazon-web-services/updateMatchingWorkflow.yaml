apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: updatematchingworkflow
  description: >-
    Updates an existing MatchingWorkflow. This method is identical to
    CreateMatchingWorkflow, except it uses an HTTP PUT request instead of a POST
    request, and the MatchingWorkflow must already exist for the method to
    succeed.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Updatematchingworkflow
      description: >-
        Updates an existing MatchingWorkflow. This method is identical to
        CreateMatchingWorkflow, except it uses an HTTP PUT request instead of a POST
        request, and the MatchingWorkflow must already exist for the method to
        succeed.
    paths:
      /matchingworkflows/{workflowName}:
        PUT:
          summary: Updatematchingworkflow
          description: >-
            Updates an existing MatchingWorkflow. This method is identical to
            CreateMatchingWorkflow, except it uses an HTTP PUT request instead of a
            POST request, and the MatchingWorkflow must already exist for the method
            to succeed.
          operationId: updateMatchingWorkflow
