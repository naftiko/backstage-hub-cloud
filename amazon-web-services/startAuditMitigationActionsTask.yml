apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Startauditmitigationactionstask
  description: >-
    Starts a task that applies a set of mitigation actions to the specified
    target. Requires permission to access the StartAuditMitigationActionsTask
    action.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Startauditmitigationactionstask
      description: >-
        Starts a task that applies a set of mitigation actions to the specified
        target. Requires permission to access the StartAuditMitigationActionsTask
        action.
    paths:
      /audit/mitigationactions/tasks/{taskId}:
        POST:
          summary: Startauditmitigationactionstask
          description: >-
            Starts a task that applies a set of mitigation actions to the specified
            target. Requires permission to access the
            StartAuditMitigationActionsTask action.
          operationId: startAuditMitigationActionsTask
