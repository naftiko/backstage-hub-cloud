apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: acceptattachment
  description: >-
    Accepts a core network attachment request.  Once the attachment request is
    accepted by a core network owner, the attachment is created and connected to
    a core network.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Acceptattachment
      description: >-
        Accepts a core network attachment request.  Once the attachment request is
        accepted by a core network owner, the attachment is created and connected to
        a core network.
    paths:
      /attachments/{attachmentId}/accept:
        POST:
          summary: Acceptattachment
          description: >-
            Accepts a core network attachment request.  Once the attachment request
            is accepted by a core network owner, the attachment is created and
            connected to a core network.
          operationId: acceptAttachment
