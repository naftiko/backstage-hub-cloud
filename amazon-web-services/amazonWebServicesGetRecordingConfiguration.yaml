apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: amazonWebServicesGetRecordingConfiguration
  description: Gets the recording configuration for the specified ARN.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Getrecordingconfiguration
      description: Gets the recording configuration for the specified ARN.
    paths:
      /GetRecordingConfiguration:
        POST:
          summary: Getrecordingconfiguration
          description: Gets the recording configuration for the specified ARN.
          operationId: amazonWebServicesGetRecordingConfiguration
