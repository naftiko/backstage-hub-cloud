apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Describedevicepolicyconfiguration
  description: Describes the device policy configuration for the specified fleet.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Describedevicepolicyconfiguration
      description: Describes the device policy configuration for the specified fleet.
    paths:
      /describeDevicePolicyConfiguration:
        POST:
          summary: Describedevicepolicyconfiguration
          description: Describes the device policy configuration for the specified fleet.
          operationId: describeDevicePolicyConfiguration
