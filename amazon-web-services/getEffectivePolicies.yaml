apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: geteffectivepolicies
  description: >-
    Gets a list of the policies that have an effect on the authorization
    behavior of the specified device when it connects to the IoT device gateway.
    Requires permission to access the GetEffectivePolicies action.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Geteffectivepolicies
      description: >-
        Gets a list of the policies that have an effect on the authorization
        behavior of the specified device when it connects to the IoT device gateway.
        Requires permission to access the GetEffectivePolicies action.
    paths:
      /effective-policies:
        POST:
          summary: Geteffectivepolicies
          description: >-
            Gets a list of the policies that have an effect on the authorization
            behavior of the specified device when it connects to the IoT device
            gateway. Requires permission to access the GetEffectivePolicies action.
          operationId: getEffectivePolicies
