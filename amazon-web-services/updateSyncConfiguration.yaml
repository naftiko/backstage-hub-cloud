apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: updatesyncconfiguration
  description: >-
    Updates the sync configuration for your connection and a specified external
    Git repository.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Updatesyncconfiguration
      description: >-
        Updates the sync configuration for your connection and a specified external
        Git repository.
    paths:
      /:
        POST:
          summary: Updatesyncconfiguration
          description: >-
            Updates the sync configuration for your connection and a specified
            external Git repository.
          operationId: updateSyncConfiguration
