apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: amazonWebServicesListDeviceEvents
  description: >-
    Using a device ID, returns a DeviceEventsResponse object containing an<br>
    array of events for the device.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Listdeviceevents
      description: >-
        Using a device ID, returns a DeviceEventsResponse object containing an<br>
        array of events for the device.
    paths:
      /devices/{deviceId}/events:
        GET:
          summary: Listdeviceevents
          description: >-
            Using a device ID, returns a DeviceEventsResponse object containing
            an<br> array of events for the device.
          operationId: amazonWebServicesListDeviceEvents
