apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Listtransactionevents
  description: >-
    An array of TransactionEvent objects. Each object contains details about the
    transaction event.  This action will return transaction details for all
    transactions that are confirmed on the blockchain, even if they have not
    reached finality.  
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Listtransactionevents
      description: >-
        An array of TransactionEvent objects. Each object contains details about the
        transaction event.  This action will return transaction details for all
        transactions that are confirmed on the blockchain, even if they have not
        reached finality.  
    paths:
      /list-transaction-events:
        POST:
          summary: Listtransactionevents
          description: >-
            An array of TransactionEvent objects. Each object contains details about
            the transaction event.  This action will return transaction details for
            all transactions that are confirmed on the blockchain, even if they have
            not reached finality.  
          operationId: listTransactionEvents
