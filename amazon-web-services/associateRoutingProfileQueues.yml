apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Associateroutingprofilequeues
  description: Associates a set of queues with a routing profile.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Associateroutingprofilequeues
      description: Associates a set of queues with a routing profile.
    paths:
      /routing-profiles/{InstanceId}/{RoutingProfileId}/associate-queues:
        POST:
          summary: Associateroutingprofilequeues
          description: Associates a set of queues with a routing profile.
          operationId: associateRoutingProfileQueues
