apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Setidentitypoolconfiguration
  description: >-
    Sets the necessary configuration for push sync.This API can only be called
    with developer credentials. You cannot call this API with the temporary user
    credentials provided by Cognito Identity.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Setidentitypoolconfiguration
      description: >-
        Sets the necessary configuration for push sync.This API can only be called
        with developer credentials. You cannot call this API with the temporary user
        credentials provided by Cognito Identity.
    paths:
      /identitypools/{IdentityPoolId}/configuration:
        POST:
          summary: Setidentitypoolconfiguration
          description: >-
            Sets the necessary configuration for push sync.This API can only be
            called with developer credentials. You cannot call this API with the
            temporary user credentials provided by Cognito Identity.
          operationId: setIdentityPoolConfiguration
