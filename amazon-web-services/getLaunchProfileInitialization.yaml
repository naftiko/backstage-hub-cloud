apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: getlaunchprofileinitialization
  description: Get a launch profile initialization.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Getlaunchprofileinitialization
      description: Get a launch profile initialization.
    paths:
      /2020-08-01/studios/{studioId}/launch-profiles/{launchProfileId}/init:
        GET:
          summary: Getlaunchprofileinitialization
          description: Get a launch profile initialization.
          operationId: getLaunchProfileInitialization
