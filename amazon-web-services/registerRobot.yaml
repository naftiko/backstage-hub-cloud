apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: registerrobot
  description: >-
    Registers a robot with a fleet.  This API is no longer supported and will
    throw an error if used. 
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Registerrobot
      description: >-
        Registers a robot with a fleet.  This API is no longer supported and will
        throw an error if used. 
    paths:
      /registerRobot:
        POST:
          summary: Registerrobot
          description: >-
            Registers a robot with a fleet.  This API is no longer supported and
            will throw an error if used. 
          operationId: registerRobot
