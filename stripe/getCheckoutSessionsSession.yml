apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Checkout Sessions Session
  description: Retrieves a Session object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Checkout Sessions Session
      description: Retrieves a Session object.
    paths:
      /v1/checkout/sessions/{session}:
        get:
          summary: Get Checkout Sessions Session
          description: Retrieves a Session object.
          operationId: getCheckoutSessionsSession
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetCheckoutSessionsSessionRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
