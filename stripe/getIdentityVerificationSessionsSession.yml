apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Identity Verification_sessions Session
  description: >-
    Retrieves the details of a VerificationSession that was previously
    created.When the session status is requires_input, you can use this method
    to retrieve a validclient_secret or url to allow re-submission.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Identity Verification_sessions Session
      description: >-
        Retrieves the details of a VerificationSession that was previously
        created.When the session status is requires_input, you can use this method
        to retrieve a validclient_secret or url to allow re-submission.
    paths:
      /v1/identity/verification_sessions/{session}:
        get:
          summary: Get Identity Verification_sessions Session
          description: >-
            Retrieves the details of a VerificationSession that was previously
            created.When the session status is requires_input, you can use this
            method to retrieve a validclient_secret or url to allow re-submission.
          operationId: getIdentityVerificationSessionsSession
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetIdentityVerificationSessionsSessionRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
