apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Issuing Authorizations Authorization
  description: Retrieves an Issuing Authorization object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Issuing Authorizations Authorization
      description: Retrieves an Issuing Authorization object.
    paths:
      /v1/issuing/authorizations/{authorization}:
        get:
          summary: Get Issuing Authorizations Authorization
          description: Retrieves an Issuing Authorization object.
          operationId: getIssuingAuthorizationsAuthorization
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetIssuingAuthorizationsAuthorizationRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
