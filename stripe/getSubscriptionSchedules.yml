apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Subscription_schedules
  description: Retrieves the list of your subscription schedules.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Subscription_schedules
      description: Retrieves the list of your subscription schedules.
    paths:
      /v1/subscription_schedules:
        get:
          summary: Get Subscription_schedules
          description: Retrieves the list of your subscription schedules.
          operationId: getSubscriptionSchedules
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetSubscriptionSchedulesRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
