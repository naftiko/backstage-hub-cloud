apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Test_helpers Test_clocks Test_clock
  description: Deletes a test clock.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Test_helpers Test_clocks Test_clock
      description: Deletes a test clock.
    paths:
      /v1/test_helpers/test_clocks/{test_clock}:
        delete:
          summary: Delete Test_helpers Test_clocks Test_clock
          description: Deletes a test clock.
          operationId: deleteTestHelpersTestClocksTestClock
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/DeleteTestHelpersTestClocksTestClockRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
