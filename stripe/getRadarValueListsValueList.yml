apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Radar Value_lists Value_list
  description: Retrieves a ValueList object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Radar Value_lists Value_list
      description: Retrieves a ValueList object.
    paths:
      /v1/radar/value_lists/{value_list}:
        get:
          summary: Get Radar Value_lists Value_list
          description: Retrieves a ValueList object.
          operationId: getRadarValueListsValueList
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetRadarValueListsValueListRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
