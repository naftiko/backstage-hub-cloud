apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Received_credits Id
  description: >-
    Retrieves the details of an existing ReceivedCredit by passing the unique
    ReceivedCredit ID from the ReceivedCredit list.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Received_credits Id
      description: >-
        Retrieves the details of an existing ReceivedCredit by passing the unique
        ReceivedCredit ID from the ReceivedCredit list.
    paths:
      /v1/treasury/received_credits/{id}:
        get:
          summary: Get Treasury Received_credits Id
          description: >-
            Retrieves the details of an existing ReceivedCredit by passing the
            unique ReceivedCredit ID from the ReceivedCredit list.
          operationId: getTreasuryReceivedCreditsId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTreasuryReceivedCreditsIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
