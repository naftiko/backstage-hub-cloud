apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Quotes Quote Cancel
  description: Cancels the quote.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Quotes Quote Cancel
      description: Cancels the quote.
    paths:
      /v1/quotes/{quote}/cancel:
        post:
          summary: Post Quotes Quote Cancel
          description: Cancels the quote.
          operationId: postQuotesQuoteCancel
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostQuotesQuoteCancelRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
