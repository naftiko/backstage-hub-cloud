apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Webhook_endpoints Webhook_endpoint
  description: >-
    You can also delete webhook endpoints via the webhook endpoint management
    page of the Stripe dashboard.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Webhook_endpoints Webhook_endpoint
      description: >-
        You can also delete webhook endpoints via the webhook endpoint management
        page of the Stripe dashboard.
    paths:
      /v1/webhook_endpoints/{webhook_endpoint}:
        delete:
          summary: Delete Webhook_endpoints Webhook_endpoint
          description: >-
            You can also delete webhook endpoints via the webhook endpoint
            management page of the Stripe dashboard.
          operationId: deleteWebhookEndpointsWebhookEndpoint
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/DeleteWebhookEndpointsWebhookEndpointRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
