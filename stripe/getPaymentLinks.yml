apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Payment_links
  description: Returns a list of your payment links.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Payment_links
      description: Returns a list of your payment links.
    paths:
      /v1/payment_links:
        get:
          summary: Get Payment_links
          description: Returns a list of your payment links.
          operationId: getPaymentLinks
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetPaymentLinksRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
