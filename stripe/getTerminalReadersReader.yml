apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Terminal Readers Reader
  description: Retrieves a Reader object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Terminal Readers Reader
      description: Retrieves a Reader object.
    paths:
      /v1/terminal/readers/{reader}:
        get:
          summary: Get Terminal Readers Reader
          description: Retrieves a Reader object.
          operationId: getTerminalReadersReader
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTerminalReadersReaderRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
