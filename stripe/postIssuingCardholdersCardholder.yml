apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Issuing Cardholders Cardholder
  description: >-
    Updates the specified Issuing Cardholder object by setting the values of the
    parameters passed. Any parameters not provided will be left unchanged.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Issuing Cardholders Cardholder
      description: >-
        Updates the specified Issuing Cardholder object by setting the values of the
        parameters passed. Any parameters not provided will be left unchanged.
    paths:
      /v1/issuing/cardholders/{cardholder}:
        post:
          summary: Post Issuing Cardholders Cardholder
          description: >-
            Updates the specified Issuing Cardholder object by setting the values of
            the parameters passed. Any parameters not provided will be left
            unchanged.
          operationId: postIssuingCardholdersCardholder
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  billing:
                    explode: true
                    style: deepObject
                  company:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  individual:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                  preferred_locales:
                    explode: true
                    style: deepObject
                  spending_controls:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostIssuingCardholdersCardholderRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
