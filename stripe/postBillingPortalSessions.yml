apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Billing_portal Sessions
  description: Creates a session of the customer portal.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Billing_portal Sessions
      description: Creates a session of the customer portal.
    paths:
      /v1/billing_portal/sessions:
        post:
          summary: Post Billing_portal Sessions
          description: Creates a session of the customer portal.
          operationId: postBillingPortalSessions
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  flow_data:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostBillingPortalSessionsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
