apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Treasury Credit_reversals
  description: Reverses a ReceivedCredit and creates a CreditReversal object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Treasury Credit_reversals
      description: Reverses a ReceivedCredit and creates a CreditReversal object.
    paths:
      /v1/treasury/credit_reversals:
        post:
          summary: Post Treasury Credit_reversals
          description: Reverses a ReceivedCredit and creates a CreditReversal object.
          operationId: postTreasuryCreditReversals
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTreasuryCreditReversalsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
