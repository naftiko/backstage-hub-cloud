apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Customers Customer Tax_ids
  description: Creates a new tax_id object for a customer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Customers Customer Tax_ids
      description: Creates a new tax_id object for a customer.
    paths:
      /v1/customers/{customer}/tax_ids:
        post:
          summary: Post Customers Customer Tax_ids
          description: Creates a new tax_id object for a customer.
          operationId: postCustomersCustomerTaxIds
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostCustomersCustomerTaxIdsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
