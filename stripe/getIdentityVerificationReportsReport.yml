apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Identity Verification_reports Report
  description: Retrieves an existing VerificationReport
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Identity Verification_reports Report
      description: Retrieves an existing VerificationReport
    paths:
      /v1/identity/verification_reports/{report}:
        get:
          summary: Get Identity Verification_reports Report
          description: Retrieves an existing VerificationReport
          operationId: getIdentityVerificationReportsReport
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetIdentityVerificationReportsReportRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
