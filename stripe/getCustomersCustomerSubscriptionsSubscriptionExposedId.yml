apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Customers Customer Subscriptions Subscription_exposed_id
  description: Retrieves the subscription with the given ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Customers Customer Subscriptions Subscription_exposed_id
      description: Retrieves the subscription with the given ID.
    paths:
      /v1/customers/{customer}/subscriptions/{subscription_exposed_id}:
        get:
          summary: Get Customers Customer Subscriptions Subscription_exposed_id
          description: Retrieves the subscription with the given ID.
          operationId: getCustomersCustomerSubscriptionsSubscriptionExposedId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetCustomersCustomerSubscriptionsSubscriptionExposedIdRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
