apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Outbound_transfers Outbound_transfer
  description: >-
    Retrieves the details of an existing OutboundTransfer by passing the unique
    OutboundTransfer ID from either the OutboundTransfer creation request or
    OutboundTransfer list.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Outbound_transfers Outbound_transfer
      description: >-
        Retrieves the details of an existing OutboundTransfer by passing the unique
        OutboundTransfer ID from either the OutboundTransfer creation request or
        OutboundTransfer list.
    paths:
      /v1/treasury/outbound_transfers/{outbound_transfer}:
        get:
          summary: Get Treasury Outbound_transfers Outbound_transfer
          description: >-
            Retrieves the details of an existing OutboundTransfer by passing the
            unique OutboundTransfer ID from either the OutboundTransfer creation
            request or OutboundTransfer list.
          operationId: getTreasuryOutboundTransfersOutboundTransfer
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetTreasuryOutboundTransfersOutboundTransferRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
