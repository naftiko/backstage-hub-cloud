apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Financial_connections Accounts Account Unsubscribe
  description: >-
    Unsubscribes from periodic refreshes of data associated with a Financial
    Connections Account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Financial_connections Accounts Account Unsubscribe
      description: >-
        Unsubscribes from periodic refreshes of data associated with a Financial
        Connections Account.
    paths:
      /v1/financial_connections/accounts/{account}/unsubscribe:
        post:
          summary: Post Financial_connections Accounts Account Unsubscribe
          description: >-
            Unsubscribes from periodic refreshes of data associated with a Financial
            Connections Account.
          operationId: postFinancialConnectionsAccountsAccountUnsubscribe
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  features:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostFinancialConnectionsAccountsAccountUnsubscribeRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
