apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Financial_connections Accounts
  description: Returns a list of Financial Connections Account objects.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Financial_connections Accounts
      description: Returns a list of Financial Connections Account objects.
    paths:
      /v1/financial_connections/accounts:
        get:
          summary: Get Financial_connections Accounts
          description: Returns a list of Financial Connections Account objects.
          operationId: getFinancialConnectionsAccounts
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetFinancialConnectionsAccountsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
