apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Terminal Configurations Configuration
  description: Deletes a Configuration object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Terminal Configurations Configuration
      description: Deletes a Configuration object.
    paths:
      /v1/terminal/configurations/{configuration}:
        delete:
          summary: Delete Terminal Configurations Configuration
          description: Deletes a Configuration object.
          operationId: deleteTerminalConfigurationsConfiguration
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/DeleteTerminalConfigurationsConfigurationRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
