apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Issuing Tokens Token
  description: >-
    Attempts to update the specified Issuing Token object to the status
    specified.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Issuing Tokens Token
      description: >-
        Attempts to update the specified Issuing Token object to the status
        specified.
    paths:
      /v1/issuing/tokens/{token}:
        post:
          summary: Post Issuing Tokens Token
          description: >-
            Attempts to update the specified Issuing Token object to the status
            specified.
          operationId: postIssuingTokensToken
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostIssuingTokensTokenRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
