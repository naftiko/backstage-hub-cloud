apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Financial_connections Accounts Account Disconnect
  description: >-
    Disables your access to a Financial Connections Account. You will no longer
    be able to access data associated with the account (e.g. balances,
    transactions).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Financial_connections Accounts Account Disconnect
      description: >-
        Disables your access to a Financial Connections Account. You will no longer
        be able to access data associated with the account (e.g. balances,
        transactions).
    paths:
      /v1/financial_connections/accounts/{account}/disconnect:
        post:
          summary: Post Financial_connections Accounts Account Disconnect
          description: >-
            Disables your access to a Financial Connections Account. You will no
            longer be able to access data associated with the account (e.g.
            balances, transactions).
          operationId: postFinancialConnectionsAccountsAccountDisconnect
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostFinancialConnectionsAccountsAccountDisconnectRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
