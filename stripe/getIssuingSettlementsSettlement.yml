apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Issuing Settlements Settlement
  description: Retrieves an Issuing Settlement object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Issuing Settlements Settlement
      description: Retrieves an Issuing Settlement object.
    paths:
      /v1/issuing/settlements/{settlement}:
        get:
          summary: Get Issuing Settlements Settlement
          description: Retrieves an Issuing Settlement object.
          operationId: getIssuingSettlementsSettlement
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetIssuingSettlementsSettlementRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
