apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Promotion_codes
  description: Returns a list of your promotion codes.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Promotion_codes
      description: Returns a list of your promotion codes.
    paths:
      /v1/promotion_codes:
        get:
          summary: Get Promotion_codes
          description: Returns a list of your promotion codes.
          operationId: getPromotionCodes
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetPromotionCodesRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
