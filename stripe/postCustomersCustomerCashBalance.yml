apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Customers Customer Cash_balance
  description: Changes the settings on a customer’s cash balance.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Customers Customer Cash_balance
      description: Changes the settings on a customer’s cash balance.
    paths:
      /v1/customers/{customer}/cash_balance:
        post:
          summary: Post Customers Customer Cash_balance
          description: Changes the settings on a customer’s cash balance.
          operationId: postCustomersCustomerCashBalance
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  settings:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostCustomersCustomerCashBalanceRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
