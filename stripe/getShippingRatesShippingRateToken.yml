apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Shipping_rates Shipping_rate_token
  description: Returns the shipping rate object with the given ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Shipping_rates Shipping_rate_token
      description: Returns the shipping rate object with the given ID.
    paths:
      /v1/shipping_rates/{shipping_rate_token}:
        get:
          summary: Get Shipping_rates Shipping_rate_token
          description: Returns the shipping rate object with the given ID.
          operationId: getShippingRatesShippingRateToken
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  properties: {}
                  type: object
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
