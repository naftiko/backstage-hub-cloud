apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Sources Source Verify
  description: Verify a given source.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Sources Source Verify
      description: Verify a given source.
    paths:
      /v1/sources/{source}/verify:
        post:
          summary: Post Sources Source Verify
          description: Verify a given source.
          operationId: postSourcesSourceVerify
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  values:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostSourcesSourceVerifyRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
