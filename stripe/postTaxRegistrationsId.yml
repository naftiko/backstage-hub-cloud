apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Tax Registrations Id
  description: >-
    Updates an existing Tax Registration object.A registration cannot be deleted
    after it has been created. If you wish to end a registration you may do so
    by setting expires_at.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Tax Registrations Id
      description: >-
        Updates an existing Tax Registration object.A registration cannot be deleted
        after it has been created. If you wish to end a registration you may do so
        by setting expires_at.
    paths:
      /v1/tax/registrations/{id}:
        post:
          summary: Post Tax Registrations Id
          description: >-
            Updates an existing Tax Registration object.A registration cannot be
            deleted after it has been created. If you wish to end a registration you
            may do so by setting expires_at.
          operationId: postTaxRegistrationsId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  active_from:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  expires_at:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTaxRegistrationsIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
