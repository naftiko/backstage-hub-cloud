apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Setup_attempts
  description: Returns a list of SetupAttempts that associate with a provided SetupIntent.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Setup_attempts
      description: Returns a list of SetupAttempts that associate with a provided SetupIntent.
    paths:
      /v1/setup_attempts:
        get:
          summary: Get Setup_attempts
          description: >-
            Returns a list of SetupAttempts that associate with a provided
            SetupIntent.
          operationId: getSetupAttempts
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetSetupAttemptsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
