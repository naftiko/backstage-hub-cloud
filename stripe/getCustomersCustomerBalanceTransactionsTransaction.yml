apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Customers Customer Balance_transactions Transaction
  description: >-
    Retrieves a specific customer balance transaction that updated the
    customer’s balances.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Customers Customer Balance_transactions Transaction
      description: >-
        Retrieves a specific customer balance transaction that updated the
        customer’s balances.
    paths:
      /v1/customers/{customer}/balance_transactions/{transaction}:
        get:
          summary: Get Customers Customer Balance_transactions Transaction
          description: >-
            Retrieves a specific customer balance transaction that updated the
            customer’s balances.
          operationId: getCustomersCustomerBalanceTransactionsTransaction
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetCustomersCustomerBalanceTransactionsTransactionRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
