apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Climate Products
  description: Lists all available Climate product objects.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Climate Products
      description: Lists all available Climate product objects.
    paths:
      /v1/climate/products:
        get:
          summary: Get Climate Products
          description: Lists all available Climate product objects.
          operationId: getClimateProducts
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetClimateProductsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
