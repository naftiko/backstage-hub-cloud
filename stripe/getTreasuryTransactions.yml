apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Transactions
  description: Retrieves a list of Transaction objects.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Transactions
      description: Retrieves a list of Transaction objects.
    paths:
      /v1/treasury/transactions:
        get:
          summary: Get Treasury Transactions
          description: Retrieves a list of Transaction objects.
          operationId: getTreasuryTransactions
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTreasuryTransactionsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
