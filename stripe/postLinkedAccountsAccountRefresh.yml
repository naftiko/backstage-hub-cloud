apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Linked_accounts Account Refresh
  description: Refreshes the data associated with a Financial Connections Account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Linked_accounts Account Refresh
      description: Refreshes the data associated with a Financial Connections Account.
    paths:
      /v1/linked_accounts/{account}/refresh:
        post:
          summary: Post Linked_accounts Account Refresh
          description: Refreshes the data associated with a Financial Connections Account.
          operationId: postLinkedAccountsAccountRefresh
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  features:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostLinkedAccountsAccountRefreshRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
