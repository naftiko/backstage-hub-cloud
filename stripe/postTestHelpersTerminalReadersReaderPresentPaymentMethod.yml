apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Terminal Readers Reader Present_payment_method
  description: >-
    Presents a payment method on a simulated reader. Can be used to simulate
    accepting a payment, saving a card or refunding a transaction.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Terminal Readers Reader Present_payment_method
      description: >-
        Presents a payment method on a simulated reader. Can be used to simulate
        accepting a payment, saving a card or refunding a transaction.
    paths:
      /v1/test_helpers/terminal/readers/{reader}/present_payment_method:
        post:
          summary: Post Test_helpers Terminal Readers Reader Present_payment_method
          description: >-
            Presents a payment method on a simulated reader. Can be used to simulate
            accepting a payment, saving a card or refunding a transaction.
          operationId: postTestHelpersTerminalReadersReaderPresentPaymentMethod
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  card_present:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  interac_present:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTestHelpersTerminalReadersReaderPresentPaymentMethodRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
