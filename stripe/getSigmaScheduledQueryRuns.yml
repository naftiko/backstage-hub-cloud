apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Sigma Scheduled_query_runs
  description: Returns a list of scheduled query runs.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Sigma Scheduled_query_runs
      description: Returns a list of scheduled query runs.
    paths:
      /v1/sigma/scheduled_query_runs:
        get:
          summary: Get Sigma Scheduled_query_runs
          description: Returns a list of scheduled query runs.
          operationId: getSigmaScheduledQueryRuns
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetSigmaScheduledQueryRunsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
