apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Customers Customer Payment_methods
  description: Returns a list of PaymentMethods for a given Customer
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Customers Customer Payment_methods
      description: Returns a list of PaymentMethods for a given Customer
    paths:
      /v1/customers/{customer}/payment_methods:
        get:
          summary: Get Customers Customer Payment_methods
          description: Returns a list of PaymentMethods for a given Customer
          operationId: getCustomersCustomerPaymentMethods
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetCustomersCustomerPaymentMethodsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
