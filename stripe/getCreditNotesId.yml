apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Credit_notes Id
  description: Retrieves the credit note object with the given identifier.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Credit_notes Id
      description: Retrieves the credit note object with the given identifier.
    paths:
      /v1/credit_notes/{id}:
        get:
          summary: Get Credit_notes Id
          description: Retrieves the credit note object with the given identifier.
          operationId: getCreditNotesId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetCreditNotesIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
