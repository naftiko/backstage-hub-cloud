apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Webhook_endpoints Webhook_endpoint
  description: >-
    Updates the webhook endpoint. You may edit the url, the list of
    enabled_events, and the status of your endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Webhook_endpoints Webhook_endpoint
      description: >-
        Updates the webhook endpoint. You may edit the url, the list of
        enabled_events, and the status of your endpoint.
    paths:
      /v1/webhook_endpoints/{webhook_endpoint}:
        post:
          summary: Post Webhook_endpoints Webhook_endpoint
          description: >-
            Updates the webhook endpoint. You may edit the url, the list of
            enabled_events, and the status of your endpoint.
          operationId: postWebhookEndpointsWebhookEndpoint
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  description:
                    explode: true
                    style: deepObject
                  enabled_events:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostWebhookEndpointsWebhookEndpointRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
