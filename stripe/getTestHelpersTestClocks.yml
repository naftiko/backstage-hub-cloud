apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Test_helpers Test_clocks
  description: Returns a list of your test clocks.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Test_helpers Test_clocks
      description: Returns a list of your test clocks.
    paths:
      /v1/test_helpers/test_clocks:
        get:
          summary: Get Test_helpers Test_clocks
          description: Returns a list of your test clocks.
          operationId: getTestHelpersTestClocks
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTestHelpersTestClocksRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
