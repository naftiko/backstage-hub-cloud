apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Customers Customer Fund_cash_balance
  description: Create an incoming testmode bank transfer
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Customers Customer Fund_cash_balance
      description: Create an incoming testmode bank transfer
    paths:
      /v1/test_helpers/customers/{customer}/fund_cash_balance:
        post:
          summary: Post Test_helpers Customers Customer Fund_cash_balance
          description: Create an incoming testmode bank transfer
          operationId: postTestHelpersCustomersCustomerFundCashBalance
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTestHelpersCustomersCustomerFundCashBalanceRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
