apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Terminal Readers Reader
  description: Deletes a Reader object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Terminal Readers Reader
      description: Deletes a Reader object.
    paths:
      /v1/terminal/readers/{reader}:
        delete:
          summary: Delete Terminal Readers Reader
          description: Deletes a Reader object.
          operationId: deleteTerminalReadersReader
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/DeleteTerminalReadersReaderRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
