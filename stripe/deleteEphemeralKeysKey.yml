apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Ephemeral_keys Key
  description: Invalidates a short-lived API key for a given resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Ephemeral_keys Key
      description: Invalidates a short-lived API key for a given resource.
    paths:
      /v1/ephemeral_keys/{key}:
        delete:
          summary: Delete Ephemeral_keys Key
          description: Invalidates a short-lived API key for a given resource.
          operationId: deleteEphemeralKeysKey
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/DeleteEphemeralKeysKeyRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
