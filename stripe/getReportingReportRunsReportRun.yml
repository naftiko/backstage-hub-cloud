apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Reporting Report_runs Report_run
  description: Retrieves the details of an existing Report Run.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Reporting Report_runs Report_run
      description: Retrieves the details of an existing Report Run.
    paths:
      /v1/reporting/report_runs/{report_run}:
        get:
          summary: Get Reporting Report_runs Report_run
          description: Retrieves the details of an existing Report Run.
          operationId: getReportingReportRunsReportRun
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetReportingReportRunsReportRunRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
