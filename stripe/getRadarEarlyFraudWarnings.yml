apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Radar Early_fraud_warnings
  description: Returns a list of early fraud warnings.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Radar Early_fraud_warnings
      description: Returns a list of early fraud warnings.
    paths:
      /v1/radar/early_fraud_warnings:
        get:
          summary: Get Radar Early_fraud_warnings
          description: Returns a list of early fraud warnings.
          operationId: getRadarEarlyFraudWarnings
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetRadarEarlyFraudWarningsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
