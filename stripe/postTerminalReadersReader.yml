apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Terminal Readers Reader
  description: >-
    Updates a Reader object by setting the values of the parameters passed. Any
    parameters not provided will be left unchanged.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Terminal Readers Reader
      description: >-
        Updates a Reader object by setting the values of the parameters passed. Any
        parameters not provided will be left unchanged.
    paths:
      /v1/terminal/readers/{reader}:
        post:
          summary: Post Terminal Readers Reader
          description: >-
            Updates a Reader object by setting the values of the parameters passed.
            Any parameters not provided will be left unchanged.
          operationId: postTerminalReadersReader
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  label:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTerminalReadersReaderRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
