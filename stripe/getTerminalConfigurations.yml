apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Terminal Configurations
  description: Returns a list of Configuration objects.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Terminal Configurations
      description: Returns a list of Configuration objects.
    paths:
      /v1/terminal/configurations:
        get:
          summary: Get Terminal Configurations
          description: Returns a list of Configuration objects.
          operationId: getTerminalConfigurations
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTerminalConfigurationsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
