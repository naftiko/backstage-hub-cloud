apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Setup_intents Intent Verify_microdeposits
  description: Verifies microdeposits on a SetupIntent object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Setup_intents Intent Verify_microdeposits
      description: Verifies microdeposits on a SetupIntent object.
    paths:
      /v1/setup_intents/{intent}/verify_microdeposits:
        post:
          summary: Post Setup_intents Intent Verify_microdeposits
          description: Verifies microdeposits on a SetupIntent object.
          operationId: postSetupIntentsIntentVerifyMicrodeposits
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  amounts:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostSetupIntentsIntentVerifyMicrodepositsRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
