apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Financial_accounts
  description: Returns a list of FinancialAccounts.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Financial_accounts
      description: Returns a list of FinancialAccounts.
    paths:
      /v1/treasury/financial_accounts:
        get:
          summary: Get Treasury Financial_accounts
          description: Returns a list of FinancialAccounts.
          operationId: getTreasuryFinancialAccounts
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTreasuryFinancialAccountsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
