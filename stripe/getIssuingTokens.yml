apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Issuing Tokens
  description: Lists all Issuing Token objects for a given card.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Issuing Tokens
      description: Lists all Issuing Token objects for a given card.
    paths:
      /v1/issuing/tokens:
        get:
          summary: Get Issuing Tokens
          description: Lists all Issuing Token objects for a given card.
          operationId: getIssuingTokens
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetIssuingTokensRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
