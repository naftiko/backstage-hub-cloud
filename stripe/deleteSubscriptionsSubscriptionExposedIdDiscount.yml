apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Subscriptions Subscription_exposed_id Discount
  description: Removes the currently applied discount on a subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Subscriptions Subscription_exposed_id Discount
      description: Removes the currently applied discount on a subscription.
    paths:
      /v1/subscriptions/{subscription_exposed_id}/discount:
        delete:
          summary: Delete Subscriptions Subscription_exposed_id Discount
          description: Removes the currently applied discount on a subscription.
          operationId: deleteSubscriptionsSubscriptionExposedIdDiscount
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/DeleteSubscriptionsSubscriptionExposedIdDiscountRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
