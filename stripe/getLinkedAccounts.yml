apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Linked_accounts
  description: Returns a list of Financial Connections Account objects.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Linked_accounts
      description: Returns a list of Financial Connections Account objects.
    paths:
      /v1/linked_accounts:
        get:
          summary: Get Linked_accounts
          description: Returns a list of Financial Connections Account objects.
          operationId: getLinkedAccounts
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetLinkedAccountsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
