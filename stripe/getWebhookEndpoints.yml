apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Webhook_endpoints
  description: Returns a list of your webhook endpoints.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Webhook_endpoints
      description: Returns a list of your webhook endpoints.
    paths:
      /v1/webhook_endpoints:
        get:
          summary: Get Webhook_endpoints
          description: Returns a list of your webhook endpoints.
          operationId: getWebhookEndpoints
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetWebhookEndpointsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
