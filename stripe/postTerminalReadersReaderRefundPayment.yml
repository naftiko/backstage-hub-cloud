apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Terminal Readers Reader Refund_payment
  description: Initiates a refund on a Reader
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Terminal Readers Reader Refund_payment
      description: Initiates a refund on a Reader
    paths:
      /v1/terminal/readers/{reader}/refund_payment:
        post:
          summary: Post Terminal Readers Reader Refund_payment
          description: Initiates a refund on a Reader
          operationId: postTerminalReadersReaderRefundPayment
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTerminalReadersReaderRefundPaymentRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
