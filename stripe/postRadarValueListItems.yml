apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Radar Value_list_items
  description: >-
    Creates a new ValueListItem object, which is added to the specified parent
    value list.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Radar Value_list_items
      description: >-
        Creates a new ValueListItem object, which is added to the specified parent
        value list.
    paths:
      /v1/radar/value_list_items:
        post:
          summary: Post Radar Value_list_items
          description: >-
            Creates a new ValueListItem object, which is added to the specified
            parent value list.
          operationId: postRadarValueListItems
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostRadarValueListItemsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
