apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Reporting Report_types
  description: Returns a full list of Report Types.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Reporting Report_types
      description: Returns a full list of Report Types.
    paths:
      /v1/reporting/report_types:
        get:
          summary: Get Reporting Report_types
          description: Returns a full list of Report Types.
          operationId: getReportingReportTypes
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetReportingReportTypesRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
