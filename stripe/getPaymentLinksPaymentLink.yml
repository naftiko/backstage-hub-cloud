apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Payment_links Payment_link
  description: Retrieve a payment link.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Payment_links Payment_link
      description: Retrieve a payment link.
    paths:
      /v1/payment_links/{payment_link}:
        get:
          summary: Get Payment_links Payment_link
          description: Retrieve a payment link.
          operationId: getPaymentLinksPaymentLink
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetPaymentLinksPaymentLinkRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
