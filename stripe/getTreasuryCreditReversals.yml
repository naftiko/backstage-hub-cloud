apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Credit_reversals
  description: Returns a list of CreditReversals.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Credit_reversals
      description: Returns a list of CreditReversals.
    paths:
      /v1/treasury/credit_reversals:
        get:
          summary: Get Treasury Credit_reversals
          description: Returns a list of CreditReversals.
          operationId: getTreasuryCreditReversals
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTreasuryCreditReversalsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
