apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Terminal Readers
  description: Creates a new Reader object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Terminal Readers
      description: Creates a new Reader object.
    paths:
      /v1/terminal/readers:
        post:
          summary: Post Terminal Readers
          description: Creates a new Reader object.
          operationId: postTerminalReaders
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTerminalReadersRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
