apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Customers Customer Sources Id
  description: Retrieve a specified source for a given customer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Customers Customer Sources Id
      description: Retrieve a specified source for a given customer.
    paths:
      /v1/customers/{customer}/sources/{id}:
        get:
          summary: Get Customers Customer Sources Id
          description: Retrieve a specified source for a given customer.
          operationId: getCustomersCustomerSourcesId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetCustomersCustomerSourcesIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
