apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Quotes Quote Computed_upfront_line_items
  description: >-
    When retrieving a quote, there is an includable computed.upfront.line_items
    property containing the first handful of those items. There is also a URL
    where you can retrieve the full (paginated) list of upfront line items.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Quotes Quote Computed_upfront_line_items
      description: >-
        When retrieving a quote, there is an includable computed.upfront.line_items
        property containing the first handful of those items. There is also a URL
        where you can retrieve the full (paginated) list of upfront line items.
    paths:
      /v1/quotes/{quote}/computed_upfront_line_items:
        get:
          summary: Get Quotes Quote Computed_upfront_line_items
          description: >-
            When retrieving a quote, there is an includable
            computed.upfront.line_items property containing the first handful of
            those items. There is also a URL where you can retrieve the full
            (paginated) list of upfront line items.
          operationId: getQuotesQuoteComputedUpfrontLineItems
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetQuotesQuoteComputedUpfrontLineItemsRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
