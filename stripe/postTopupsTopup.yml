apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Topups Topup
  description: >-
    Updates the metadata of a top-up. Other top-up details are not editable by
    design.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Topups Topup
      description: >-
        Updates the metadata of a top-up. Other top-up details are not editable by
        design.
    paths:
      /v1/topups/{topup}:
        post:
          summary: Post Topups Topup
          description: >-
            Updates the metadata of a top-up. Other top-up details are not editable
            by design.
          operationId: postTopupsTopup
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTopupsTopupRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
