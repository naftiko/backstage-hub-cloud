apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Climate Suppliers
  description: Lists all available Climate supplier objects.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Climate Suppliers
      description: Lists all available Climate supplier objects.
    paths:
      /v1/climate/suppliers:
        get:
          summary: Get Climate Suppliers
          description: Lists all available Climate supplier objects.
          operationId: getClimateSuppliers
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetClimateSuppliersRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
