apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Charges Charge Dispute
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Charges Charge Dispute
      description: Needs a more full description created.
    paths:
      /v1/charges/{charge}/dispute:
        post:
          summary: Post Charges Charge Dispute
          description: Needs a more full description created.
          operationId: postChargesChargeDispute
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  evidence:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostChargesChargeDisputeRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
