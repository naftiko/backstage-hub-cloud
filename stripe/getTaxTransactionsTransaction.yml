apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Tax Transactions Transaction
  description: Retrieves a Tax Transaction object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Tax Transactions Transaction
      description: Retrieves a Tax Transaction object.
    paths:
      /v1/tax/transactions/{transaction}:
        get:
          summary: Get Tax Transactions Transaction
          description: Retrieves a Tax Transaction object.
          operationId: getTaxTransactionsTransaction
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTaxTransactionsTransactionRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
