apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Payment_method_domains
  description: Lists the details of existing payment method domains.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Payment_method_domains
      description: Lists the details of existing payment method domains.
    paths:
      /v1/payment_method_domains:
        get:
          summary: Get Payment_method_domains
          description: Lists the details of existing payment method domains.
          operationId: getPaymentMethodDomains
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetPaymentMethodDomainsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
