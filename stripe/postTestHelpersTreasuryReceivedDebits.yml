apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Treasury Received_debits
  description: >-
    Use this endpoint to simulate a test mode ReceivedDebit initiated by a third
    party. In live mode, you can’t directly create ReceivedDebits initiated by
    third parties.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Treasury Received_debits
      description: >-
        Use this endpoint to simulate a test mode ReceivedDebit initiated by a third
        party. In live mode, you can’t directly create ReceivedDebits initiated by
        third parties.
    paths:
      /v1/test_helpers/treasury/received_debits:
        post:
          summary: Post Test_helpers Treasury Received_debits
          description: >-
            Use this endpoint to simulate a test mode ReceivedDebit initiated by a
            third party. In live mode, you can’t directly create ReceivedDebits
            initiated by third parties.
          operationId: postTestHelpersTreasuryReceivedDebits
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  initiating_payment_method_details:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTestHelpersTreasuryReceivedDebitsRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
