apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Customers Customer Tax_ids
  description: Returns a list of tax IDs for a customer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Customers Customer Tax_ids
      description: Returns a list of tax IDs for a customer.
    paths:
      /v1/customers/{customer}/tax_ids:
        get:
          summary: Get Customers Customer Tax_ids
          description: Returns a list of tax IDs for a customer.
          operationId: getCustomersCustomerTaxIds
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetCustomersCustomerTaxIdsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
