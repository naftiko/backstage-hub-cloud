apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Terminal Readers Reader Set_reader_display
  description: Sets reader display to show cart details.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Terminal Readers Reader Set_reader_display
      description: Sets reader display to show cart details.
    paths:
      /v1/terminal/readers/{reader}/set_reader_display:
        post:
          summary: Post Terminal Readers Reader Set_reader_display
          description: Sets reader display to show cart details.
          operationId: postTerminalReadersReaderSetReaderDisplay
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  cart:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTerminalReadersReaderSetReaderDisplayRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
