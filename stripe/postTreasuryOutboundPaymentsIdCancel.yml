apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Treasury Outbound_payments Id Cancel
  description: Cancel an OutboundPayment.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Treasury Outbound_payments Id Cancel
      description: Cancel an OutboundPayment.
    paths:
      /v1/treasury/outbound_payments/{id}/cancel:
        post:
          summary: Post Treasury Outbound_payments Id Cancel
          description: Cancel an OutboundPayment.
          operationId: postTreasuryOutboundPaymentsIdCancel
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTreasuryOutboundPaymentsIdCancelRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
