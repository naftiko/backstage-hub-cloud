apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Radar Value_lists
  description: Creates a new ValueList object, which can then be referenced in rules.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Radar Value_lists
      description: Creates a new ValueList object, which can then be referenced in rules.
    paths:
      /v1/radar/value_lists:
        post:
          summary: Post Radar Value_lists
          description: Creates a new ValueList object, which can then be referenced in rules.
          operationId: postRadarValueLists
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostRadarValueListsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
