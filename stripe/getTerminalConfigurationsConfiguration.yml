apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Terminal Configurations Configuration
  description: Retrieves a Configuration object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Terminal Configurations Configuration
      description: Retrieves a Configuration object.
    paths:
      /v1/terminal/configurations/{configuration}:
        get:
          summary: Get Terminal Configurations Configuration
          description: Retrieves a Configuration object.
          operationId: getTerminalConfigurationsConfiguration
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetTerminalConfigurationsConfigurationRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
