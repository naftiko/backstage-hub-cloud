apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Terminal Readers
  description: Returns a list of Reader objects.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Terminal Readers
      description: Returns a list of Reader objects.
    paths:
      /v1/terminal/readers:
        get:
          summary: Get Terminal Readers
          description: Returns a list of Reader objects.
          operationId: getTerminalReaders
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTerminalReadersRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
