apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Credit_reversals Credit_reversal
  description: >-
    Retrieves the details of an existing CreditReversal by passing the unique
    CreditReversal ID from either the CreditReversal creation request or
    CreditReversal list
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Credit_reversals Credit_reversal
      description: >-
        Retrieves the details of an existing CreditReversal by passing the unique
        CreditReversal ID from either the CreditReversal creation request or
        CreditReversal list
    paths:
      /v1/treasury/credit_reversals/{credit_reversal}:
        get:
          summary: Get Treasury Credit_reversals Credit_reversal
          description: >-
            Retrieves the details of an existing CreditReversal by passing the
            unique CreditReversal ID from either the CreditReversal creation request
            or CreditReversal list
          operationId: getTreasuryCreditReversalsCreditReversal
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetTreasuryCreditReversalsCreditReversalRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
