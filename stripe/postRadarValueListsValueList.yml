apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Radar Value_lists Value_list
  description: >-
    Updates a ValueList object by setting the values of the parameters passed.
    Any parameters not provided will be left unchanged. Note that item_type is
    immutable.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Radar Value_lists Value_list
      description: >-
        Updates a ValueList object by setting the values of the parameters passed.
        Any parameters not provided will be left unchanged. Note that item_type is
        immutable.
    paths:
      /v1/radar/value_lists/{value_list}:
        post:
          summary: Post Radar Value_lists Value_list
          description: >-
            Updates a ValueList object by setting the values of the parameters
            passed. Any parameters not provided will be left unchanged. Note that
            item_type is immutable.
          operationId: postRadarValueListsValueList
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostRadarValueListsValueListRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
