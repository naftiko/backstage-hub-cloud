apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Issuing Cards Card
  description: Retrieves an Issuing Card object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Issuing Cards Card
      description: Retrieves an Issuing Card object.
    paths:
      /v1/issuing/cards/{card}:
        get:
          summary: Get Issuing Cards Card
          description: Retrieves an Issuing Card object.
          operationId: getIssuingCardsCard
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetIssuingCardsCardRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
