apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Climate Orders Order
  description: Updates the specified order by setting the values of the parameters passed.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Climate Orders Order
      description: Updates the specified order by setting the values of the parameters passed.
    paths:
      /v1/climate/orders/{order}:
        post:
          summary: Post Climate Orders Order
          description: >-
            Updates the specified order by setting the values of the parameters
            passed.
          operationId: postClimateOrdersOrder
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  beneficiary:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostClimateOrdersOrderRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
