apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Quotes Quote Finalize
  description: Finalizes the quote.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Quotes Quote Finalize
      description: Finalizes the quote.
    paths:
      /v1/quotes/{quote}/finalize:
        post:
          summary: Post Quotes Quote Finalize
          description: Finalizes the quote.
          operationId: postQuotesQuoteFinalize
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostQuotesQuoteFinalizeRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
