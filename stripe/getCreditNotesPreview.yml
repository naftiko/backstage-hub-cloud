apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Credit_notes Preview
  description: Get a preview of a credit note without creating it.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Credit_notes Preview
      description: Get a preview of a credit note without creating it.
    paths:
      /v1/credit_notes/preview:
        get:
          summary: Get Credit_notes Preview
          description: Get a preview of a credit note without creating it.
          operationId: getCreditNotesPreview
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetCreditNotesPreviewRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
