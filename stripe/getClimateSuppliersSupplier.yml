apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Climate Suppliers Supplier
  description: Retrieves a Climate supplier object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Climate Suppliers Supplier
      description: Retrieves a Climate supplier object.
    paths:
      /v1/climate/suppliers/{supplier}:
        get:
          summary: Get Climate Suppliers Supplier
          description: Retrieves a Climate supplier object.
          operationId: getClimateSuppliersSupplier
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetClimateSuppliersSupplierRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
