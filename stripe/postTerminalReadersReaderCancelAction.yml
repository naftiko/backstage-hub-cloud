apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Terminal Readers Reader Cancel_action
  description: Cancels the current reader action.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Terminal Readers Reader Cancel_action
      description: Cancels the current reader action.
    paths:
      /v1/terminal/readers/{reader}/cancel_action:
        post:
          summary: Post Terminal Readers Reader Cancel_action
          description: Cancels the current reader action.
          operationId: postTerminalReadersReaderCancelAction
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTerminalReadersReaderCancelActionRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
