apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Terminal Readers Reader Process_payment_intent
  description: Initiates a payment flow on a Reader.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Terminal Readers Reader Process_payment_intent
      description: Initiates a payment flow on a Reader.
    paths:
      /v1/terminal/readers/{reader}/process_payment_intent:
        post:
          summary: Post Terminal Readers Reader Process_payment_intent
          description: Initiates a payment flow on a Reader.
          operationId: postTerminalReadersReaderProcessPaymentIntent
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  process_config:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTerminalReadersReaderProcessPaymentIntentRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
