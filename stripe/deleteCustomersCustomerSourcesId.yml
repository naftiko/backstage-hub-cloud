apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Customers Customer Sources Id
  description: Delete a specified source for a given customer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Customers Customer Sources Id
      description: Delete a specified source for a given customer.
    paths:
      /v1/customers/{customer}/sources/{id}:
        delete:
          summary: Delete Customers Customer Sources Id
          description: Delete a specified source for a given customer.
          operationId: deleteCustomersCustomerSourcesId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/DeleteCustomersCustomerSourcesIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
