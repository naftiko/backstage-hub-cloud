apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Subscription_items Item
  description: Updates the plan or quantity of an item on a current subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Subscription_items Item
      description: Updates the plan or quantity of an item on a current subscription.
    paths:
      /v1/subscription_items/{item}:
        post:
          summary: Post Subscription_items Item
          description: Updates the plan or quantity of an item on a current subscription.
          operationId: postSubscriptionItemsItem
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  billing_thresholds:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                  price_data:
                    explode: true
                    style: deepObject
                  tax_rates:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostSubscriptionItemsItemRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
