apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Accounts Account Capabilities Capability
  description: Retrieves information about the specified Account Capability.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Accounts Account Capabilities Capability
      description: Retrieves information about the specified Account Capability.
    paths:
      /v1/accounts/{account}/capabilities/{capability}:
        get:
          summary: Get Accounts Account Capabilities Capability
          description: Retrieves information about the specified Account Capability.
          operationId: getAccountsAccountCapabilitiesCapability
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetAccountsAccountCapabilitiesCapabilityRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
