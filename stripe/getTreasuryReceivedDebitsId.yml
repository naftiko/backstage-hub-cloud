apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Received_debits Id
  description: >-
    Retrieves the details of an existing ReceivedDebit by passing the unique
    ReceivedDebit ID from the ReceivedDebit list
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Received_debits Id
      description: >-
        Retrieves the details of an existing ReceivedDebit by passing the unique
        ReceivedDebit ID from the ReceivedDebit list
    paths:
      /v1/treasury/received_debits/{id}:
        get:
          summary: Get Treasury Received_debits Id
          description: >-
            Retrieves the details of an existing ReceivedDebit by passing the unique
            ReceivedDebit ID from the ReceivedDebit list
          operationId: getTreasuryReceivedDebitsId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTreasuryReceivedDebitsIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
