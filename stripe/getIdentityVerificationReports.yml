apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Identity Verification_reports
  description: List all verification reports.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Identity Verification_reports
      description: List all verification reports.
    paths:
      /v1/identity/verification_reports:
        get:
          summary: Get Identity Verification_reports
          description: List all verification reports.
          operationId: getIdentityVerificationReports
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetIdentityVerificationReportsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
