apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Financial_connections Sessions Session
  description: Retrieves the details of a Financial Connections Session
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Financial_connections Sessions Session
      description: Retrieves the details of a Financial Connections Session
    paths:
      /v1/financial_connections/sessions/{session}:
        get:
          summary: Get Financial_connections Sessions Session
          description: Retrieves the details of a Financial Connections Session
          operationId: getFinancialConnectionsSessionsSession
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetFinancialConnectionsSessionsSessionRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
