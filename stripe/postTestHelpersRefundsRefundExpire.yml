apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Refunds Refund Expire
  description: Expire a refund with a status of requires_action.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Refunds Refund Expire
      description: Expire a refund with a status of requires_action.
    paths:
      /v1/test_helpers/refunds/{refund}/expire:
        post:
          summary: Post Test_helpers Refunds Refund Expire
          description: Expire a refund with a status of requires_action.
          operationId: postTestHelpersRefundsRefundExpire
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTestHelpersRefundsRefundExpireRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
