apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Test_clocks
  description: Creates a new test clock that can be attached to new customers and quotes.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Test_clocks
      description: Creates a new test clock that can be attached to new customers and quotes.
    paths:
      /v1/test_helpers/test_clocks:
        post:
          summary: Post Test_helpers Test_clocks
          description: >-
            Creates a new test clock that can be attached to new customers and
            quotes.
          operationId: postTestHelpersTestClocks
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTestHelpersTestClocksRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
