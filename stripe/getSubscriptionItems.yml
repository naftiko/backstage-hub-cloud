apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Subscription_items
  description: Returns a list of your subscription items for a given subscription.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Subscription_items
      description: Returns a list of your subscription items for a given subscription.
    paths:
      /v1/subscription_items:
        get:
          summary: Get Subscription_items
          description: Returns a list of your subscription items for a given subscription.
          operationId: getSubscriptionItems
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetSubscriptionItemsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
