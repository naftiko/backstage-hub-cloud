apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Sources Source Source_transactions Source_transaction
  description: >-
    Retrieve an existing source transaction object. Supply the unique source ID
    from a source creation request and the source transaction ID and Stripe will
    return the corresponding up-to-date source object information.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Sources Source Source_transactions Source_transaction
      description: >-
        Retrieve an existing source transaction object. Supply the unique source ID
        from a source creation request and the source transaction ID and Stripe will
        return the corresponding up-to-date source object information.
    paths:
      /v1/sources/{source}/source_transactions/{source_transaction}:
        get:
          summary: Get Sources Source Source_transactions Source_transaction
          description: >-
            Retrieve an existing source transaction object. Supply the unique source
            ID from a source creation request and the source transaction ID and
            Stripe will return the corresponding up-to-date source object
            information.
          operationId: getSourcesSourceSourceTransactionsSourceTransaction
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetSourcesSourceSourceTransactionsSourceTransactionRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
