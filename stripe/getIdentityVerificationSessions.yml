apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Identity Verification_sessions
  description: Returns a list of VerificationSessions
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Identity Verification_sessions
      description: Returns a list of VerificationSessions
    paths:
      /v1/identity/verification_sessions:
        get:
          summary: Get Identity Verification_sessions
          description: Returns a list of VerificationSessions
          operationId: getIdentityVerificationSessions
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetIdentityVerificationSessionsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
