apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Accounts Account External_accounts
  description: Create an external account for a given account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Accounts Account External_accounts
      description: Create an external account for a given account.
    paths:
      /v1/accounts/{account}/external_accounts:
        post:
          summary: Post Accounts Account External_accounts
          description: Create an external account for a given account.
          operationId: postAccountsAccountExternalAccounts
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  bank_account:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostAccountsAccountExternalAccountsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
