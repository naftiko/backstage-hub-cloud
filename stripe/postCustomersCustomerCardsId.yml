apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Customers Customer Cards Id
  description: Update a specified source for a given customer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Customers Customer Cards Id
      description: Update a specified source for a given customer.
    paths:
      /v1/customers/{customer}/cards/{id}:
        post:
          summary: Post Customers Customer Cards Id
          description: Update a specified source for a given customer.
          operationId: postCustomersCustomerCardsId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                  owner:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostCustomersCustomerCardsIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
