apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Test_clocks Test_clock Advance
  description: >-
    Starts advancing a test clock to a specified time in the future. Advancement
    is done when status changes to Ready.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Test_clocks Test_clock Advance
      description: >-
        Starts advancing a test clock to a specified time in the future. Advancement
        is done when status changes to Ready.
    paths:
      /v1/test_helpers/test_clocks/{test_clock}/advance:
        post:
          summary: Post Test_helpers Test_clocks Test_clock Advance
          description: >-
            Starts advancing a test clock to a specified time in the future.
            Advancement is done when status changes to Ready.
          operationId: postTestHelpersTestClocksTestClockAdvance
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTestHelpersTestClocksTestClockAdvanceRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
