apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Subscription_items Item
  description: Retrieves the subscription item with the given ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Subscription_items Item
      description: Retrieves the subscription item with the given ID.
    paths:
      /v1/subscription_items/{item}:
        get:
          summary: Get Subscription_items Item
          description: Retrieves the subscription item with the given ID.
          operationId: getSubscriptionItemsItem
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetSubscriptionItemsItemRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
