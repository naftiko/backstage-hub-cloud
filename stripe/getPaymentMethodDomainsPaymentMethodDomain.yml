apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Payment_method_domains Payment_method_domain
  description: Retrieves the details of an existing payment method domain.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Payment_method_domains Payment_method_domain
      description: Retrieves the details of an existing payment method domain.
    paths:
      /v1/payment_method_domains/{payment_method_domain}:
        get:
          summary: Get Payment_method_domains Payment_method_domain
          description: Retrieves the details of an existing payment method domain.
          operationId: getPaymentMethodDomainsPaymentMethodDomain
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetPaymentMethodDomainsPaymentMethodDomainRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
