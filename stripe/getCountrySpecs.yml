apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Country_specs
  description: Lists all Country Spec objects available in the API.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Country_specs
      description: Lists all Country Spec objects available in the API.
    paths:
      /v1/country_specs:
        get:
          summary: Get Country_specs
          description: Lists all Country Spec objects available in the API.
          operationId: getCountrySpecs
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetCountrySpecsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
