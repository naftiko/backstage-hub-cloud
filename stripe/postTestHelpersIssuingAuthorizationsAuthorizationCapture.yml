apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Issuing Authorizations Authorization Capture
  description: Capture a test-mode authorization.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Issuing Authorizations Authorization Capture
      description: Capture a test-mode authorization.
    paths:
      /v1/test_helpers/issuing/authorizations/{authorization}/capture:
        post:
          summary: Post Test_helpers Issuing Authorizations Authorization Capture
          description: Capture a test-mode authorization.
          operationId: postTestHelpersIssuingAuthorizationsAuthorizationCapture
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  purchase_details:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTestHelpersIssuingAuthorizationsAuthorizationCaptureRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
