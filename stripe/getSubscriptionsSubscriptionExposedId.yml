apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Subscriptions Subscription_exposed_id
  description: Retrieves the subscription with the given ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Subscriptions Subscription_exposed_id
      description: Retrieves the subscription with the given ID.
    paths:
      /v1/subscriptions/{subscription_exposed_id}:
        get:
          summary: Get Subscriptions Subscription_exposed_id
          description: Retrieves the subscription with the given ID.
          operationId: getSubscriptionsSubscriptionExposedId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetSubscriptionsSubscriptionExposedIdRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
