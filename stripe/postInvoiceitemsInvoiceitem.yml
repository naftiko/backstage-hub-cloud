apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Invoiceitems Invoiceitem
  description: >-
    Updates the amount or description of an invoice item on an upcoming invoice.
    Updating an invoice item is only possible before the invoice it’s attached
    to is closed.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Invoiceitems Invoiceitem
      description: >-
        Updates the amount or description of an invoice item on an upcoming invoice.
        Updating an invoice item is only possible before the invoice it’s attached
        to is closed.
    paths:
      /v1/invoiceitems/{invoiceitem}:
        post:
          summary: Post Invoiceitems Invoiceitem
          description: >-
            Updates the amount or description of an invoice item on an upcoming
            invoice. Updating an invoice item is only possible before the invoice
            it’s attached to is closed.
          operationId: postInvoiceitemsInvoiceitem
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  discounts:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                  period:
                    explode: true
                    style: deepObject
                  price_data:
                    explode: true
                    style: deepObject
                  tax_code:
                    explode: true
                    style: deepObject
                  tax_rates:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostInvoiceitemsInvoiceitemRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
