apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Climate Reservations Order Confirm
  description: >-
    Confirms a Climate order. When you confirm your order, we immediately deduct
    the funds from yourStripe balance.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Climate Reservations Order Confirm
      description: >-
        Confirms a Climate order. When you confirm your order, we immediately deduct
        the funds from yourStripe balance.
    paths:
      /v1/climate/reservations/{order}/confirm:
        post:
          summary: Post Climate Reservations Order Confirm
          description: >-
            Confirms a Climate order. When you confirm your order, we immediately
            deduct the funds from yourStripe balance.
          operationId: postClimateReservationsOrderConfirm
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostClimateReservationsOrderConfirmRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
