apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Billing_portal Configurations
  description: >-
    Returns a list of configurations that describe the functionality of the
    customer portal.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Billing_portal Configurations
      description: >-
        Returns a list of configurations that describe the functionality of the
        customer portal.
    paths:
      /v1/billing_portal/configurations:
        get:
          summary: Get Billing_portal Configurations
          description: >-
            Returns a list of configurations that describe the functionality of the
            customer portal.
          operationId: getBillingPortalConfigurations
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetBillingPortalConfigurationsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
