apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Issuing Cardholders
  description: Creates a new Issuing Cardholder object that can be issued cards.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Issuing Cardholders
      description: Creates a new Issuing Cardholder object that can be issued cards.
    paths:
      /v1/issuing/cardholders:
        post:
          summary: Post Issuing Cardholders
          description: Creates a new Issuing Cardholder object that can be issued cards.
          operationId: postIssuingCardholders
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  billing:
                    explode: true
                    style: deepObject
                  company:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  individual:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                  preferred_locales:
                    explode: true
                    style: deepObject
                  spending_controls:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostIssuingCardholdersRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
