apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Sources Source Source_transactions
  description: List source transactions for a given source.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Sources Source Source_transactions
      description: List source transactions for a given source.
    paths:
      /v1/sources/{source}/source_transactions:
        get:
          summary: Get Sources Source Source_transactions
          description: List source transactions for a given source.
          operationId: getSourcesSourceSourceTransactions
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetSourcesSourceSourceTransactionsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
