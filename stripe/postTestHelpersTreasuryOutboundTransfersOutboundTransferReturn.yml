apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Treasury Outbound_transfers Outbound_transfer Return
  description: >-
    Transitions a test mode created OutboundTransfer to the returned status. The
    OutboundTransfer must already be in the processing state.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Treasury Outbound_transfers Outbound_transfer Return
      description: >-
        Transitions a test mode created OutboundTransfer to the returned status. The
        OutboundTransfer must already be in the processing state.
    paths:
      /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return:
        post:
          summary: Post Test_helpers Treasury Outbound_transfers Outbound_transfer Return
          description: >-
            Transitions a test mode created OutboundTransfer to the returned status.
            The OutboundTransfer must already be in the processing state.
          operationId: postTestHelpersTreasuryOutboundTransfersOutboundTransferReturn
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  returned_details:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTestHelpersTreasuryOutboundTransfersOutboundTransferReturnRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
