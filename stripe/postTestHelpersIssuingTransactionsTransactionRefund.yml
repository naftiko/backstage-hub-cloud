apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Issuing Transactions Transaction Refund
  description: Refund a test-mode Transaction.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Issuing Transactions Transaction Refund
      description: Refund a test-mode Transaction.
    paths:
      /v1/test_helpers/issuing/transactions/{transaction}/refund:
        post:
          summary: Post Test_helpers Issuing Transactions Transaction Refund
          description: Refund a test-mode Transaction.
          operationId: postTestHelpersIssuingTransactionsTransactionRefund
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTestHelpersIssuingTransactionsTransactionRefundRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
