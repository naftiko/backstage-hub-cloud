apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Tax Calculations
  description: Calculates tax based on input and returns a Tax Calculation object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Tax Calculations
      description: Calculates tax based on input and returns a Tax Calculation object.
    paths:
      /v1/tax/calculations:
        post:
          summary: Post Tax Calculations
          description: Calculates tax based on input and returns a Tax Calculation object.
          operationId: postTaxCalculations
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  customer_details:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  line_items:
                    explode: true
                    style: deepObject
                  shipping_cost:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTaxCalculationsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
