apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Financial_connections Accounts Account Owners
  description: Lists all owners for a given Account
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Financial_connections Accounts Account Owners
      description: Lists all owners for a given Account
    paths:
      /v1/financial_connections/accounts/{account}/owners:
        get:
          summary: Get Financial_connections Accounts Account Owners
          description: Lists all owners for a given Account
          operationId: getFinancialConnectionsAccountsAccountOwners
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetFinancialConnectionsAccountsAccountOwnersRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
