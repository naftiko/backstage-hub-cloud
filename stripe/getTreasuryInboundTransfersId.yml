apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Inbound_transfers Id
  description: Retrieves the details of an existing InboundTransfer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Inbound_transfers Id
      description: Retrieves the details of an existing InboundTransfer.
    paths:
      /v1/treasury/inbound_transfers/{id}:
        get:
          summary: Get Treasury Inbound_transfers Id
          description: Retrieves the details of an existing InboundTransfer.
          operationId: getTreasuryInboundTransfersId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTreasuryInboundTransfersIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
