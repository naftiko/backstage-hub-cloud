apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Issuing Authorizations
  description: Create a test-mode authorization.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Issuing Authorizations
      description: Create a test-mode authorization.
    paths:
      /v1/test_helpers/issuing/authorizations:
        post:
          summary: Post Test_helpers Issuing Authorizations
          description: Create a test-mode authorization.
          operationId: postTestHelpersIssuingAuthorizations
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  amount_details:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  merchant_data:
                    explode: true
                    style: deepObject
                  network_data:
                    explode: true
                    style: deepObject
                  verification_data:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTestHelpersIssuingAuthorizationsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
