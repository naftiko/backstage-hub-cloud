apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Payment_method_configurations
  description: List payment method configurations
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Payment_method_configurations
      description: List payment method configurations
    paths:
      /v1/payment_method_configurations:
        get:
          summary: Get Payment_method_configurations
          description: List payment method configurations
          operationId: getPaymentMethodConfigurations
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetPaymentMethodConfigurationsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
