apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Customers Customer Bank_accounts Id Verify
  description: Verify a specified bank account for a given customer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Customers Customer Bank_accounts Id Verify
      description: Verify a specified bank account for a given customer.
    paths:
      /v1/customers/{customer}/bank_accounts/{id}/verify:
        post:
          summary: Post Customers Customer Bank_accounts Id Verify
          description: Verify a specified bank account for a given customer.
          operationId: postCustomersCustomerBankAccountsIdVerify
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  amounts:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostCustomersCustomerBankAccountsIdVerifyRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
