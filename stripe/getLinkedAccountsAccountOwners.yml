apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Linked_accounts Account Owners
  description: Lists all owners for a given Account
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Linked_accounts Account Owners
      description: Lists all owners for a given Account
    paths:
      /v1/linked_accounts/{account}/owners:
        get:
          summary: Get Linked_accounts Account Owners
          description: Lists all owners for a given Account
          operationId: getLinkedAccountsAccountOwners
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetLinkedAccountsAccountOwnersRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
