apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Invoiceitems Invoiceitem
  description: Retrieves the invoice item with the given ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Invoiceitems Invoiceitem
      description: Retrieves the invoice item with the given ID.
    paths:
      /v1/invoiceitems/{invoiceitem}:
        get:
          summary: Get Invoiceitems Invoiceitem
          description: Retrieves the invoice item with the given ID.
          operationId: getInvoiceitemsInvoiceitem
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetInvoiceitemsInvoiceitemRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
