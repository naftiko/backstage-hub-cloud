apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Treasury Inbound_transfers Inbound_transfer Cancel
  description: Cancels an InboundTransfer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Treasury Inbound_transfers Inbound_transfer Cancel
      description: Cancels an InboundTransfer.
    paths:
      /v1/treasury/inbound_transfers/{inbound_transfer}/cancel:
        post:
          summary: Post Treasury Inbound_transfers Inbound_transfer Cancel
          description: Cancels an InboundTransfer.
          operationId: postTreasuryInboundTransfersInboundTransferCancel
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTreasuryInboundTransfersInboundTransferCancelRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
