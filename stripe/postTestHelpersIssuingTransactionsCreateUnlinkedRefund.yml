apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Issuing Transactions Create_unlinked_refund
  description: >-
    Allows the user to refund an arbitrary amount, also known as a unlinked
    refund.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Issuing Transactions Create_unlinked_refund
      description: >-
        Allows the user to refund an arbitrary amount, also known as a unlinked
        refund.
    paths:
      /v1/test_helpers/issuing/transactions/create_unlinked_refund:
        post:
          summary: Post Test_helpers Issuing Transactions Create_unlinked_refund
          description: >-
            Allows the user to refund an arbitrary amount, also known as a unlinked
            refund.
          operationId: postTestHelpersIssuingTransactionsCreateUnlinkedRefund
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  merchant_data:
                    explode: true
                    style: deepObject
                  purchase_details:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTestHelpersIssuingTransactionsCreateUnlinkedRefundRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
