apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Invoices Invoice Mark_uncollectible
  description: >-
    Marking an invoice as uncollectible is useful for keeping track of bad debts
    that can be written off for accounting purposes.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Invoices Invoice Mark_uncollectible
      description: >-
        Marking an invoice as uncollectible is useful for keeping track of bad debts
        that can be written off for accounting purposes.
    paths:
      /v1/invoices/{invoice}/mark_uncollectible:
        post:
          summary: Post Invoices Invoice Mark_uncollectible
          description: >-
            Marking an invoice as uncollectible is useful for keeping track of bad
            debts that can be written off for accounting purposes.
          operationId: postInvoicesInvoiceMarkUncollectible
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostInvoicesInvoiceMarkUncollectibleRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
