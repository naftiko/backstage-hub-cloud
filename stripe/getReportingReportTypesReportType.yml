apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Reporting Report_types Report_type
  description: >-
    Retrieves the details of a Report Type. (Certain report types require a
    live-mode API key.)
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Reporting Report_types Report_type
      description: >-
        Retrieves the details of a Report Type. (Certain report types require a
        live-mode API key.)
    paths:
      /v1/reporting/report_types/{report_type}:
        get:
          summary: Get Reporting Report_types Report_type
          description: >-
            Retrieves the details of a Report Type. (Certain report types require a
            live-mode API key.)
          operationId: getReportingReportTypesReportType
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetReportingReportTypesReportTypeRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
