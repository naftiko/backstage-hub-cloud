apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Topups Topup Cancel
  description: Cancels a top-up. Only pending top-ups can be canceled.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Topups Topup Cancel
      description: Cancels a top-up. Only pending top-ups can be canceled.
    paths:
      /v1/topups/{topup}/cancel:
        post:
          summary: Post Topups Topup Cancel
          description: Cancels a top-up. Only pending top-ups can be canceled.
          operationId: postTopupsTopupCancel
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTopupsTopupCancelRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
