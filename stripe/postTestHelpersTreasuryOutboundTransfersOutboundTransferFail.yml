apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Treasury Outbound_transfers Outbound_transfer Fail
  description: >-
    Transitions a test mode created OutboundTransfer to the failed status. The
    OutboundTransfer must already be in the processing state.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Treasury Outbound_transfers Outbound_transfer Fail
      description: >-
        Transitions a test mode created OutboundTransfer to the failed status. The
        OutboundTransfer must already be in the processing state.
    paths:
      /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail:
        post:
          summary: Post Test_helpers Treasury Outbound_transfers Outbound_transfer Fail
          description: >-
            Transitions a test mode created OutboundTransfer to the failed status.
            The OutboundTransfer must already be in the processing state.
          operationId: postTestHelpersTreasuryOutboundTransfersOutboundTransferFail
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTestHelpersTreasuryOutboundTransfersOutboundTransferFailRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
