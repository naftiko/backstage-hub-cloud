apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Issuing Cards
  description: Creates an Issuing Card object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Issuing Cards
      description: Creates an Issuing Card object.
    paths:
      /v1/issuing/cards:
        post:
          summary: Post Issuing Cards
          description: Creates an Issuing Card object.
          operationId: postIssuingCards
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                  shipping:
                    explode: true
                    style: deepObject
                  spending_controls:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostIssuingCardsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
