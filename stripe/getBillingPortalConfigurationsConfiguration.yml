apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Billing_portal Configurations Configuration
  description: >-
    Retrieves a configuration that describes the functionality of the customer
    portal.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Billing_portal Configurations Configuration
      description: >-
        Retrieves a configuration that describes the functionality of the customer
        portal.
    paths:
      /v1/billing_portal/configurations/{configuration}:
        get:
          summary: Get Billing_portal Configurations Configuration
          description: >-
            Retrieves a configuration that describes the functionality of the
            customer portal.
          operationId: getBillingPortalConfigurationsConfiguration
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetBillingPortalConfigurationsConfigurationRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
