apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Credit_notes
  description: Returns a list of credit notes.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Credit_notes
      description: Returns a list of credit notes.
    paths:
      /v1/credit_notes:
        get:
          summary: Get Credit_notes
          description: Returns a list of credit notes.
          operationId: getCreditNotes
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetCreditNotesRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
