apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Link_account_sessions Session
  description: Retrieves the details of a Financial Connections Session
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Link_account_sessions Session
      description: Retrieves the details of a Financial Connections Session
    paths:
      /v1/link_account_sessions/{session}:
        get:
          summary: Get Link_account_sessions Session
          description: Retrieves the details of a Financial Connections Session
          operationId: getLinkAccountSessionsSession
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetLinkAccountSessionsSessionRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
