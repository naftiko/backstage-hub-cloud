apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Transaction_entries Id
  description: Retrieves a TransactionEntry object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Transaction_entries Id
      description: Retrieves a TransactionEntry object.
    paths:
      /v1/treasury/transaction_entries/{id}:
        get:
          summary: Get Treasury Transaction_entries Id
          description: Retrieves a TransactionEntry object.
          operationId: getTreasuryTransactionEntriesId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTreasuryTransactionEntriesIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
