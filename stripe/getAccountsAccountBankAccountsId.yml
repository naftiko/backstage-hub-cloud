apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Accounts Account Bank_accounts Id
  description: Retrieve a specified external account for a given account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Accounts Account Bank_accounts Id
      description: Retrieve a specified external account for a given account.
    paths:
      /v1/accounts/{account}/bank_accounts/{id}:
        get:
          summary: Get Accounts Account Bank_accounts Id
          description: Retrieve a specified external account for a given account.
          operationId: getAccountsAccountBankAccountsId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetAccountsAccountBankAccountsIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
