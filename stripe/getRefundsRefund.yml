apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Refunds Refund
  description: Retrieves the details of an existing refund.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Refunds Refund
      description: Retrieves the details of an existing refund.
    paths:
      /v1/refunds/{refund}:
        get:
          summary: Get Refunds Refund
          description: Retrieves the details of an existing refund.
          operationId: getRefundsRefund
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetRefundsRefundRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
