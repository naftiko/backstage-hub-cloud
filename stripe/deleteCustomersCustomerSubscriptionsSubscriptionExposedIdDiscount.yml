apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Customers Customer Subscriptions Subscription_exposed_id Discount
  description: Removes the currently applied discount on a customer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Customers Customer Subscriptions Subscription_exposed_id Discount
      description: Removes the currently applied discount on a customer.
    paths:
      /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount:
        delete:
          summary: Delete Customers Customer Subscriptions Subscription_exposed_id Discount
          description: Removes the currently applied discount on a customer.
          operationId: deleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
