apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Debit_reversals Debit_reversal
  description: Retrieves a DebitReversal object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Debit_reversals Debit_reversal
      description: Retrieves a DebitReversal object.
    paths:
      /v1/treasury/debit_reversals/{debit_reversal}:
        get:
          summary: Get Treasury Debit_reversals Debit_reversal
          description: Retrieves a DebitReversal object.
          operationId: getTreasuryDebitReversalsDebitReversal
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetTreasuryDebitReversalsDebitReversalRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
