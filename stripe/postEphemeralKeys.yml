apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Ephemeral_keys
  description: Creates a short-lived API key for a given resource.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Ephemeral_keys
      description: Creates a short-lived API key for a given resource.
    paths:
      /v1/ephemeral_keys:
        post:
          summary: Post Ephemeral_keys
          description: Creates a short-lived API key for a given resource.
          operationId: postEphemeralKeys
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostEphemeralKeysRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
