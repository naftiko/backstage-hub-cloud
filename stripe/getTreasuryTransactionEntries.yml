apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Transaction_entries
  description: Retrieves a list of TransactionEntry objects.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Transaction_entries
      description: Retrieves a list of TransactionEntry objects.
    paths:
      /v1/treasury/transaction_entries:
        get:
          summary: Get Treasury Transaction_entries
          description: Retrieves a list of TransactionEntry objects.
          operationId: getTreasuryTransactionEntries
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTreasuryTransactionEntriesRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
