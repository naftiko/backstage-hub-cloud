apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Accounts Account External_accounts Id
  description: Delete a specified external account for a given account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Accounts Account External_accounts Id
      description: Delete a specified external account for a given account.
    paths:
      /v1/accounts/{account}/external_accounts/{id}:
        delete:
          summary: Delete Accounts Account External_accounts Id
          description: Delete a specified external account for a given account.
          operationId: deleteAccountsAccountExternalAccountsId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/DeleteAccountsAccountExternalAccountsIdRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
