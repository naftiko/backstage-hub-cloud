apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Tax_rates
  description: Creates a new tax rate.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Tax_rates
      description: Creates a new tax rate.
    paths:
      /v1/tax_rates:
        post:
          summary: Post Tax_rates
          description: Creates a new tax rate.
          operationId: postTaxRates
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTaxRatesRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
