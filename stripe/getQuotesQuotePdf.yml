apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Quotes Quote Pdf
  description: Download the PDF for a finalized quote
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Quotes Quote Pdf
      description: Download the PDF for a finalized quote
    paths:
      /v1/quotes/{quote}/pdf:
        get:
          summary: Get Quotes Quote Pdf
          description: Download the PDF for a finalized quote
          operationId: getQuotesQuotePdf
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetQuotesQuotePdfRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
