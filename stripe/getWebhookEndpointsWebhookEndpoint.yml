apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Webhook_endpoints Webhook_endpoint
  description: Retrieves the webhook endpoint with the given ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Webhook_endpoints Webhook_endpoint
      description: Retrieves the webhook endpoint with the given ID.
    paths:
      /v1/webhook_endpoints/{webhook_endpoint}:
        get:
          summary: Get Webhook_endpoints Webhook_endpoint
          description: Retrieves the webhook endpoint with the given ID.
          operationId: getWebhookEndpointsWebhookEndpoint
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetWebhookEndpointsWebhookEndpointRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
