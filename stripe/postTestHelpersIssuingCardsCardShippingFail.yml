apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Issuing Cards Card Shipping Fail
  description: Updates the shipping status of the specified Issuing Card object to failure.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Issuing Cards Card Shipping Fail
      description: Updates the shipping status of the specified Issuing Card object to failure.
    paths:
      /v1/test_helpers/issuing/cards/{card}/shipping/fail:
        post:
          summary: Post Test_helpers Issuing Cards Card Shipping Fail
          description: >-
            Updates the shipping status of the specified Issuing Card object to
            failure.
          operationId: postTestHelpersIssuingCardsCardShippingFail
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTestHelpersIssuingCardsCardShippingFailRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
