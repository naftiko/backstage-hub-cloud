apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Payment_method_domains Payment_method_domain
  description: Updates an existing payment method domain.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Payment_method_domains Payment_method_domain
      description: Updates an existing payment method domain.
    paths:
      /v1/payment_method_domains/{payment_method_domain}:
        post:
          summary: Post Payment_method_domains Payment_method_domain
          description: Updates an existing payment method domain.
          operationId: postPaymentMethodDomainsPaymentMethodDomain
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostPaymentMethodDomainsPaymentMethodDomainRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
