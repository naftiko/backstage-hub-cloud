apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Received_credits
  description: Returns a list of ReceivedCredits.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Received_credits
      description: Returns a list of ReceivedCredits.
    paths:
      /v1/treasury/received_credits:
        get:
          summary: Get Treasury Received_credits
          description: Returns a list of ReceivedCredits.
          operationId: getTreasuryReceivedCredits
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTreasuryReceivedCreditsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
