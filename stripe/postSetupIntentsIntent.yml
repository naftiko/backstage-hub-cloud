apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Setup_intents Intent
  description: Updates a SetupIntent object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Setup_intents Intent
      description: Updates a SetupIntent object.
    paths:
      /v1/setup_intents/{intent}:
        post:
          summary: Post Setup_intents Intent
          description: Updates a SetupIntent object.
          operationId: postSetupIntentsIntent
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  flow_directions:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                  payment_method_data:
                    explode: true
                    style: deepObject
                  payment_method_options:
                    explode: true
                    style: deepObject
                  payment_method_types:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostSetupIntentsIntentRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
