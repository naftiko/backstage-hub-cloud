apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Tokens
  description: >-
    Creates a single-use token that represents a bank account’s details.You can
    use this token with any API method in place of a bank account dictionary.
    You can only use this token once. To do so, attach it to a Custom account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Tokens
      description: >-
        Creates a single-use token that represents a bank account’s details.You can
        use this token with any API method in place of a bank account dictionary.
        You can only use this token once. To do so, attach it to a Custom account.
    paths:
      /v1/tokens:
        post:
          summary: Post Tokens
          description: >-
            Creates a single-use token that represents a bank account’s details.You
            can use this token with any API method in place of a bank account
            dictionary. You can only use this token once. To do so, attach it to a
            Custom account.
          operationId: postTokens
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  account:
                    explode: true
                    style: deepObject
                  bank_account:
                    explode: true
                    style: deepObject
                  card:
                    explode: true
                    style: deepObject
                  cvc_update:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  person:
                    explode: true
                    style: deepObject
                  pii:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTokensRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
