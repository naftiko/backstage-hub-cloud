apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Accounts Account People Person
  description: Retrieves an existing person.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Accounts Account People Person
      description: Retrieves an existing person.
    paths:
      /v1/accounts/{account}/people/{person}:
        get:
          summary: Get Accounts Account People Person
          description: Retrieves an existing person.
          operationId: getAccountsAccountPeoplePerson
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetAccountsAccountPeoplePersonRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
