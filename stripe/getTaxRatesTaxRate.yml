apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Tax_rates Tax_rate
  description: Retrieves a tax rate with the given ID
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Tax_rates Tax_rate
      description: Retrieves a tax rate with the given ID
    paths:
      /v1/tax_rates/{tax_rate}:
        get:
          summary: Get Tax_rates Tax_rate
          description: Retrieves a tax rate with the given ID
          operationId: getTaxRatesTaxRate
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTaxRatesTaxRateRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
