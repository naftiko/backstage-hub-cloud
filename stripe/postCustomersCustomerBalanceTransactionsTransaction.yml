apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Customers Customer Balance_transactions Transaction
  description: >-
    Most credit balance transaction fields are immutable, but you may update its
    description and metadata.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Customers Customer Balance_transactions Transaction
      description: >-
        Most credit balance transaction fields are immutable, but you may update its
        description and metadata.
    paths:
      /v1/customers/{customer}/balance_transactions/{transaction}:
        post:
          summary: Post Customers Customer Balance_transactions Transaction
          description: >-
            Most credit balance transaction fields are immutable, but you may update
            its description and metadata.
          operationId: postCustomersCustomerBalanceTransactionsTransaction
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostCustomersCustomerBalanceTransactionsTransactionRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
