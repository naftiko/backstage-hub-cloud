apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Subscription_schedules
  description: >-
    Creates a new subscription schedule object. Each customer can have up to 500
    active or scheduled subscriptions.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Subscription_schedules
      description: >-
        Creates a new subscription schedule object. Each customer can have up to 500
        active or scheduled subscriptions.
    paths:
      /v1/subscription_schedules:
        post:
          summary: Post Subscription_schedules
          description: >-
            Creates a new subscription schedule object. Each customer can have up to
            500 active or scheduled subscriptions.
          operationId: postSubscriptionSchedules
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  default_settings:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                  phases:
                    explode: true
                    style: deepObject
                  start_date:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostSubscriptionSchedulesRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
