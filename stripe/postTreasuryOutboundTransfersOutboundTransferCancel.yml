apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Treasury Outbound_transfers Outbound_transfer Cancel
  description: An OutboundTransfer can be canceled if the funds have not yet been paid out.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Treasury Outbound_transfers Outbound_transfer Cancel
      description: An OutboundTransfer can be canceled if the funds have not yet been paid out.
    paths:
      /v1/treasury/outbound_transfers/{outbound_transfer}/cancel:
        post:
          summary: Post Treasury Outbound_transfers Outbound_transfer Cancel
          description: >-
            An OutboundTransfer can be canceled if the funds have not yet been paid
            out.
          operationId: postTreasuryOutboundTransfersOutboundTransferCancel
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTreasuryOutboundTransfersOutboundTransferCancelRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
