apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Treasury Debit_reversals
  description: Reverses a ReceivedDebit and creates a DebitReversal object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Treasury Debit_reversals
      description: Reverses a ReceivedDebit and creates a DebitReversal object.
    paths:
      /v1/treasury/debit_reversals:
        post:
          summary: Post Treasury Debit_reversals
          description: Reverses a ReceivedDebit and creates a DebitReversal object.
          operationId: postTreasuryDebitReversals
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTreasuryDebitReversalsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
