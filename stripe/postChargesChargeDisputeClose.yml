apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Charges Charge Dispute Close
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Charges Charge Dispute Close
      description: Needs a more full description created.
    paths:
      /v1/charges/{charge}/dispute/close:
        post:
          summary: Post Charges Charge Dispute Close
          description: Needs a more full description created.
          operationId: postChargesChargeDisputeClose
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostChargesChargeDisputeCloseRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
