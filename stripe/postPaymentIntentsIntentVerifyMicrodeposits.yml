apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Payment_intents Intent Verify_microdeposits
  description: Verifies microdeposits on a PaymentIntent object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Payment_intents Intent Verify_microdeposits
      description: Verifies microdeposits on a PaymentIntent object.
    paths:
      /v1/payment_intents/{intent}/verify_microdeposits:
        post:
          summary: Post Payment_intents Intent Verify_microdeposits
          description: Verifies microdeposits on a PaymentIntent object.
          operationId: postPaymentIntentsIntentVerifyMicrodeposits
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  amounts:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostPaymentIntentsIntentVerifyMicrodepositsRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
