apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Subscription_items
  description: >-
    Adds a new item to an existing subscription. No existing items will be
    changed or replaced.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Subscription_items
      description: >-
        Adds a new item to an existing subscription. No existing items will be
        changed or replaced.
    paths:
      /v1/subscription_items:
        post:
          summary: Post Subscription_items
          description: >-
            Adds a new item to an existing subscription. No existing items will be
            changed or replaced.
          operationId: postSubscriptionItems
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  billing_thresholds:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                  price_data:
                    explode: true
                    style: deepObject
                  tax_rates:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostSubscriptionItemsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
