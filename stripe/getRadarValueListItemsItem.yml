apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Radar Value_list_items Item
  description: Retrieves a ValueListItem object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Radar Value_list_items Item
      description: Retrieves a ValueListItem object.
    paths:
      /v1/radar/value_list_items/{item}:
        get:
          summary: Get Radar Value_list_items Item
          description: Retrieves a ValueListItem object.
          operationId: getRadarValueListItemsItem
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetRadarValueListItemsItemRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
