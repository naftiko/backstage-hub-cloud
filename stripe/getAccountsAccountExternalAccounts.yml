apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Accounts Account External_accounts
  description: List external accounts for an account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Accounts Account External_accounts
      description: List external accounts for an account.
    paths:
      /v1/accounts/{account}/external_accounts:
        get:
          summary: Get Accounts Account External_accounts
          description: List external accounts for an account.
          operationId: getAccountsAccountExternalAccounts
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetAccountsAccountExternalAccountsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
