apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Payment_intents Intent Apply_customer_balance
  description: >-
    Manually reconcile the remaining amount for a customer_balance
    PaymentIntent.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Payment_intents Intent Apply_customer_balance
      description: >-
        Manually reconcile the remaining amount for a customer_balance
        PaymentIntent.
    paths:
      /v1/payment_intents/{intent}/apply_customer_balance:
        post:
          summary: Post Payment_intents Intent Apply_customer_balance
          description: >-
            Manually reconcile the remaining amount for a customer_balance
            PaymentIntent.
          operationId: postPaymentIntentsIntentApplyCustomerBalance
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostPaymentIntentsIntentApplyCustomerBalanceRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
