apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Setup_intents
  description: Returns a list of SetupIntents.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Setup_intents
      description: Returns a list of SetupIntents.
    paths:
      /v1/setup_intents:
        get:
          summary: Get Setup_intents
          description: Returns a list of SetupIntents.
          operationId: getSetupIntents
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetSetupIntentsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
