apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Customers Customer Tax_ids Id
  description: Deletes an existing tax_id object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Customers Customer Tax_ids Id
      description: Deletes an existing tax_id object.
    paths:
      /v1/customers/{customer}/tax_ids/{id}:
        delete:
          summary: Delete Customers Customer Tax_ids Id
          description: Deletes an existing tax_id object.
          operationId: deleteCustomersCustomerTaxIdsId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/DeleteCustomersCustomerTaxIdsIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
