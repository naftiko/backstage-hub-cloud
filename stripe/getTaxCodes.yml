apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Tax_codes
  description: >-
    A list of all tax codes available to add to Products in order to allow
    specific tax calculations.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Tax_codes
      description: >-
        A list of all tax codes available to add to Products in order to allow
        specific tax calculations.
    paths:
      /v1/tax_codes:
        get:
          summary: Get Tax_codes
          description: >-
            A list of all tax codes available to add to Products in order to allow
            specific tax calculations.
          operationId: getTaxCodes
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTaxCodesRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
