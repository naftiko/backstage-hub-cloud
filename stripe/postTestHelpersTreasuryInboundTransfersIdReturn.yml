apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Treasury Inbound_transfers Id Return
  description: >-
    Marks the test mode InboundTransfer object as returned and links the
    InboundTransfer to a ReceivedDebit. The InboundTransfer must already be in
    the succeeded state.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Treasury Inbound_transfers Id Return
      description: >-
        Marks the test mode InboundTransfer object as returned and links the
        InboundTransfer to a ReceivedDebit. The InboundTransfer must already be in
        the succeeded state.
    paths:
      /v1/test_helpers/treasury/inbound_transfers/{id}/return:
        post:
          summary: Post Test_helpers Treasury Inbound_transfers Id Return
          description: >-
            Marks the test mode InboundTransfer object as returned and links the
            InboundTransfer to a ReceivedDebit. The InboundTransfer must already be
            in the succeeded state.
          operationId: postTestHelpersTreasuryInboundTransfersIdReturn
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTestHelpersTreasuryInboundTransfersIdReturnRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
