apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Customers Customer Tax_ids Id
  description: Retrieves the tax_id object with the given identifier.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Customers Customer Tax_ids Id
      description: Retrieves the tax_id object with the given identifier.
    paths:
      /v1/customers/{customer}/tax_ids/{id}:
        get:
          summary: Get Customers Customer Tax_ids Id
          description: Retrieves the tax_id object with the given identifier.
          operationId: getCustomersCustomerTaxIdsId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetCustomersCustomerTaxIdsIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
