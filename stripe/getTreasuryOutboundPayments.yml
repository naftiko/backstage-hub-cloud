apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Outbound_payments
  description: Returns a list of OutboundPayments sent from the specified FinancialAccount.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Outbound_payments
      description: Returns a list of OutboundPayments sent from the specified FinancialAccount.
    paths:
      /v1/treasury/outbound_payments:
        get:
          summary: Get Treasury Outbound_payments
          description: >-
            Returns a list of OutboundPayments sent from the specified
            FinancialAccount.
          operationId: getTreasuryOutboundPayments
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTreasuryOutboundPaymentsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
