apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Issuing Disputes Dispute
  description: Retrieves an Issuing Dispute object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Issuing Disputes Dispute
      description: Retrieves an Issuing Dispute object.
    paths:
      /v1/issuing/disputes/{dispute}:
        get:
          summary: Get Issuing Disputes Dispute
          description: Retrieves an Issuing Dispute object.
          operationId: getIssuingDisputesDispute
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetIssuingDisputesDisputeRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
