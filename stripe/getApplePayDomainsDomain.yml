apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Apple_pay Domains Domain
  description: Retrieve an apple pay domain.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Apple_pay Domains Domain
      description: Retrieve an apple pay domain.
    paths:
      /v1/apple_pay/domains/{domain}:
        get:
          summary: Get Apple_pay Domains Domain
          description: Retrieve an apple pay domain.
          operationId: getApplePayDomainsDomain
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  properties: {}
                  type: object
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
