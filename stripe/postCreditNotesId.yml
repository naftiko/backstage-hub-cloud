apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Credit_notes Id
  description: Updates an existing credit note.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Credit_notes Id
      description: Updates an existing credit note.
    paths:
      /v1/credit_notes/{id}:
        post:
          summary: Post Credit_notes Id
          description: Updates an existing credit note.
          operationId: postCreditNotesId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostCreditNotesIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
