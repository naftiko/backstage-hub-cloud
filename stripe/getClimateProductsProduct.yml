apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Climate Products Product
  description: Retrieves the details of a Climate product with the given ID.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Climate Products Product
      description: Retrieves the details of a Climate product with the given ID.
    paths:
      /v1/climate/products/{product}:
        get:
          summary: Get Climate Products Product
          description: Retrieves the details of a Climate product with the given ID.
          operationId: getClimateProductsProduct
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetClimateProductsProductRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
