apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Accounts Account Capabilities Capability
  description: >-
    Updates an existing Account Capability. Request or remove a capability by
    updating its requested parameter.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Accounts Account Capabilities Capability
      description: >-
        Updates an existing Account Capability. Request or remove a capability by
        updating its requested parameter.
    paths:
      /v1/accounts/{account}/capabilities/{capability}:
        post:
          summary: Post Accounts Account Capabilities Capability
          description: >-
            Updates an existing Account Capability. Request or remove a capability
            by updating its requested parameter.
          operationId: postAccountsAccountCapabilitiesCapability
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostAccountsAccountCapabilitiesCapabilityRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
