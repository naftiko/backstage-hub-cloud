apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Customers Customer Sources Id Verify
  description: Verify a specified bank account for a given customer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Customers Customer Sources Id Verify
      description: Verify a specified bank account for a given customer.
    paths:
      /v1/customers/{customer}/sources/{id}/verify:
        post:
          summary: Post Customers Customer Sources Id Verify
          description: Verify a specified bank account for a given customer.
          operationId: postCustomersCustomerSourcesIdVerify
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  amounts:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostCustomersCustomerSourcesIdVerifyRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
