apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Treasury Outbound_transfers
  description: Creates an OutboundTransfer.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Treasury Outbound_transfers
      description: Creates an OutboundTransfer.
    paths:
      /v1/treasury/outbound_transfers:
        post:
          summary: Post Treasury Outbound_transfers
          description: Creates an OutboundTransfer.
          operationId: postTreasuryOutboundTransfers
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  destination_payment_method_options:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  metadata:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostTreasuryOutboundTransfersRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
