apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Sigma Scheduled_query_runs Scheduled_query_run
  description: Retrieves the details of an scheduled query run.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Sigma Scheduled_query_runs Scheduled_query_run
      description: Retrieves the details of an scheduled query run.
    paths:
      /v1/sigma/scheduled_query_runs/{scheduled_query_run}:
        get:
          summary: Get Sigma Scheduled_query_runs Scheduled_query_run
          description: Retrieves the details of an scheduled query run.
          operationId: getSigmaScheduledQueryRunsScheduledQueryRun
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetSigmaScheduledQueryRunsScheduledQueryRunRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
