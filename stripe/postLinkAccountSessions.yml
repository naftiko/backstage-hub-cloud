apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Link_account_sessions
  description: >-
    To launch the Financial Connections authorization flow, create a Session.
    The session’s client_secret can be used to launch the flow using Stripe.js.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Link_account_sessions
      description: >-
        To launch the Financial Connections authorization flow, create a Session.
        The session’s client_secret can be used to launch the flow using Stripe.js.
    paths:
      /v1/link_account_sessions:
        post:
          summary: Post Link_account_sessions
          description: >-
            To launch the Financial Connections authorization flow, create a
            Session. The session’s client_secret can be used to launch the flow
            using Stripe.js.
          operationId: postLinkAccountSessions
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                encoding:
                  account_holder:
                    explode: true
                    style: deepObject
                  expand:
                    explode: true
                    style: deepObject
                  filters:
                    explode: true
                    style: deepObject
                  permissions:
                    explode: true
                    style: deepObject
                  prefetch:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostLinkAccountSessionsRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
