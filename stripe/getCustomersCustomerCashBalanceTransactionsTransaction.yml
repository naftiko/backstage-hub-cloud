apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Customers Customer Cash_balance_transactions Transaction
  description: >-
    Retrieves a specific cash balance transaction, which updated the customer’s
    cash balance.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Customers Customer Cash_balance_transactions Transaction
      description: >-
        Retrieves a specific cash balance transaction, which updated the customer’s
        cash balance.
    paths:
      /v1/customers/{customer}/cash_balance_transactions/{transaction}:
        get:
          summary: Get Customers Customer Cash_balance_transactions Transaction
          description: >-
            Retrieves a specific cash balance transaction, which updated the
            customer’s cash balance.
          operationId: getCustomersCustomerCashBalanceTransactionsTransaction
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/GetCustomersCustomerCashBalanceTransactionsTransactionRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
