apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Exchange_rates Rate_id
  description: >-
    Retrieves the exchange rates from the given currency to every supported
    currency.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Exchange_rates Rate_id
      description: >-
        Retrieves the exchange rates from the given currency to every supported
        currency.
    paths:
      /v1/exchange_rates/{rate_id}:
        get:
          summary: Get Exchange_rates Rate_id
          description: >-
            Retrieves the exchange rates from the given currency to every supported
            currency.
          operationId: getExchangeRatesRateId
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetExchangeRatesRateIdRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
