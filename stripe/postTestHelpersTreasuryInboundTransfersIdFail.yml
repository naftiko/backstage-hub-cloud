apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Test_helpers Treasury Inbound_transfers Id Fail
  description: >-
    Transitions a test mode created InboundTransfer to the failed status. The
    InboundTransfer must already be in the processing state.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Test_helpers Treasury Inbound_transfers Id Fail
      description: >-
        Transitions a test mode created InboundTransfer to the failed status. The
        InboundTransfer must already be in the processing state.
    paths:
      /v1/test_helpers/treasury/inbound_transfers/{id}/fail:
        post:
          summary: Post Test_helpers Treasury Inbound_transfers Id Fail
          description: >-
            Transitions a test mode created InboundTransfer to the failed status.
            The InboundTransfer must already be in the processing state.
          operationId: postTestHelpersTreasuryInboundTransfersIdFail
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                  failure_details:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: >-
                    #/components/schemas/PostTestHelpersTreasuryInboundTransfersIdFailRequest
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
