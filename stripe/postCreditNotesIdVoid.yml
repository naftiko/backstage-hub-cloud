apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Credit_notes Id Void
  description: Marks a credit note as void. Learn more about voiding credit notes.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Credit_notes Id Void
      description: Marks a credit note as void. Learn more about voiding credit notes.
    paths:
      /v1/credit_notes/{id}/void:
        post:
          summary: Post Credit_notes Id Void
          description: Marks a credit note as void. Learn more about voiding credit notes.
          operationId: postCreditNotesIdVoid
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding:
                  expand:
                    explode: true
                    style: deepObject
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/PostCreditNotesIdVoidRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
