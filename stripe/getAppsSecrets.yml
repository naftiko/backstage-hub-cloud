apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Apps Secrets
  description: List all secrets stored on the given scope.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Apps Secrets
      description: List all secrets stored on the given scope.
    paths:
      /v1/apps/secrets:
        get:
          summary: Get Apps Secrets
          description: List all secrets stored on the given scope.
          operationId: getAppsSecrets
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  properties: {}
                  type: object
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
