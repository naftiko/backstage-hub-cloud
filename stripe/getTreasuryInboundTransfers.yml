apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Treasury Inbound_transfers
  description: Returns a list of InboundTransfers sent from the specified FinancialAccount.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Treasury Inbound_transfers
      description: Returns a list of InboundTransfers sent from the specified FinancialAccount.
    paths:
      /v1/treasury/inbound_transfers:
        get:
          summary: Get Treasury Inbound_transfers
          description: >-
            Returns a list of InboundTransfers sent from the specified
            FinancialAccount.
          operationId: getTreasuryInboundTransfers
          requestBody:
            content:
              application/x-www-form-urlencoded:
                encoding: {}
                schema:
                  additionalProperties: false
                  $ref: '#/components/schemas/GetTreasuryInboundTransfersRequest'
          responses:
            default:
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/error'
              description: Error response.
