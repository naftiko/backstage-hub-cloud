apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Pipeline
  description: >-
    Delete a given pipeline from your organization.This endpoint takes no JSON
    arguments.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Pipeline
      description: >-
        Delete a given pipeline from your organization.This endpoint takes no JSON
        arguments.
    paths:
      /api/v1/logs/config/pipelines/{pipeline_id}:
        delete:
          summary: Delete A Pipeline
          description: >-
            Delete a given pipeline from your organization.This endpoint takes no
            JSON arguments.
          operationId: deleteapipeline
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
