apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Team Link
  description: Get a single link for a team.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Team Link
      description: Get a single link for a team.
    paths:
      /api/v2/team/{team_id}/links/{link_id}:
        get:
          summary: Get A Team Link
          description: Get a single link for a team.
          operationId: getateamlink
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
