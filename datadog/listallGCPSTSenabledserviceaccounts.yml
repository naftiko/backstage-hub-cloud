apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List All Gcp Sts-enabled Service Accounts
  description: >-
    List all GCP STS-enabled service accounts configured in your Datadog
    account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List All Gcp Sts-enabled Service Accounts
      description: >-
        List all GCP STS-enabled service accounts configured in your Datadog
        account.
    paths:
      /api/v2/integration/gcp/accounts:
        get:
          summary: List All Gcp Sts-enabled Service Accounts
          description: >-
            List all GCP STS-enabled service accounts configured in your Datadog
            account.
          operationId: listallGCPSTSenabledserviceaccounts
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
