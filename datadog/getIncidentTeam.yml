apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Details Of An Incident Team
  description: >-
    Get details of an incident team. If the `include[users]` query parameter is
    provided,the included attribute will contain the users related to these
    incident teams.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Details Of An Incident Team
      description: >-
        Get details of an incident team. If the `include[users]` query parameter is
        provided,the included attribute will contain the users related to these
        incident teams.
    paths:
      /api/v2/teams/{team_id}:
        get:
          summary: Get Details Of An Incident Team
          description: >-
            Get details of an incident team. If the `include[users]` query parameter
            is provided,the included attribute will contain the users related to
            these incident teams.
          operationId: getIncidentTeam
          responses:
            '429':
              $ref: '#/components/responses/TooManyRequestsResponse'
