apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Cloud Cost Enabled
  description: Get the Cloud Cost Management activity.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Cloud Cost Enabled
      description: Get the Cloud Cost Management activity.
    paths:
      /api/v2/cost/enabled:
        get:
          summary: Cloud Cost Enabled
          description: Get the Cloud Cost Management activity.
          operationId: cloudCostEnabled
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
