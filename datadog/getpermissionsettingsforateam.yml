apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Permission Settings For A Team
  description: Get all permission settings for a given team.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Permission Settings For A Team
      description: Get all permission settings for a given team.
    paths:
      /api/v2/team/{team_id}/permission-settings:
        get:
          summary: Get Permission Settings For A Team
          description: Get all permission settings for a given team.
          operationId: getpermissionsettingsforateam
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
