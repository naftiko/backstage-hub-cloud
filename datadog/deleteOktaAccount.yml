apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Okta Account
  description: Delete an Okta account.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Okta Account
      description: Delete an Okta account.
    paths:
      /api/v2/integrations/okta/accounts/{account_id}:
        delete:
          summary: Delete Okta Account
          description: Delete an Okta account.
          operationId: deleteOktaaccount
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
