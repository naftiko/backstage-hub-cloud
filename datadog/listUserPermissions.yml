apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A User Permissions
  description: >-
    Get a user permission set. Returns a list of the users permissionsgranted by
    the associated user's roles.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A User Permissions
      description: >-
        Get a user permission set. Returns a list of the users permissionsgranted by
        the associated user's roles.
    paths:
      /api/v2/users/{user_id}/permissions:
        get:
          summary: Get A User Permissions
          description: >-
            Get a user permission set. Returns a list of the users
            permissionsgranted by the associated user's roles.
          operationId: listUserPermissions
          responses:
            '429':
              $ref: '#/components/responses/TooManyRequestsResponse'
