apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Your Managed Organizations
  description: This endpoint returns data on your top-level organization.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Your Managed Organizations
      description: This endpoint returns data on your top-level organization.
    paths:
      /api/v1/org:
        get:
          summary: List Your Managed Organizations
          description: This endpoint returns data on your top-level organization.
          operationId: listyourmanagedorganizations
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
