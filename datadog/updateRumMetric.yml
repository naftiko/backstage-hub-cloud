apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update A Rum-based Metric
  description: >-
    Update a specific rum-based metric from your organization.Returns the
    rum-based metric object from the request body when the request is
    successful.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update A Rum-based Metric
      description: >-
        Update a specific rum-based metric from your organization.Returns the
        rum-based metric object from the request body when the request is
        successful.
    paths:
      /api/v2/rum/config/metrics/{metric_id}:
        patch:
          summary: Update A Rum-based Metric
          description: >-
            Update a specific rum-based metric from your organization.Returns the
            rum-based metric object from the request body when the request is
            successful.
          operationId: updateRumMetric
          requestBody:
            description: New definition of the rum-based metric.
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/RumMetricUpdateRequest'
          responses:
            '429':
              $ref: '#/components/responses/TooManyRequestsResponse'
