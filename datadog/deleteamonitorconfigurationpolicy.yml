apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Monitor Configuration Policy
  description: Delete a monitor configuration policy.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Monitor Configuration Policy
      description: Delete a monitor configuration policy.
    paths:
      /api/v2/monitor/policy/{policy_id}:
        delete:
          summary: Delete A Monitor Configuration Policy
          description: Delete a monitor configuration policy.
          operationId: deleteamonitorconfigurationpolicy
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
