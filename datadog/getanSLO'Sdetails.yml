apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get An Slo's Details
  description: Get a service level objective object.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get An Slo's Details
      description: Get a service level objective object.
    paths:
      /api/v1/slo/{slo_id}:
        get:
          summary: Get An Slo's Details
          description: Get a service level objective object.
          operationId: getanSLO'Sdetails
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
