apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Grant Role To An Archive
  description: >-
    Adds a read role to an archive. ([Roles
    API](https://docs.datadoghq.com/api/v2/roles/))
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Grant Role To An Archive
      description: >-
        Adds a read role to an archive. ([Roles
        API](https://docs.datadoghq.com/api/v2/roles/))
    paths:
      /api/v2/logs/config/archives/{archive_id}/readers:
        post:
          summary: Grant Role To An Archive
          description: >-
            Adds a read role to an archive. ([Roles
            API](https://docs.datadoghq.com/api/v2/roles/))
          operationId: grantroletoanarchive
          requestBody:
            content:
              application/json:
                schema:
                  type: object
                  example:
                    data:
                      id: 3653d3c6-0c75-11ea-ad28-fb5701eabc7d
                      type: roles
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
