apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Workload Protection Agent Rules (us1-fed)
  description: >-
    Get the list of agent rules.**Note**: This endpoint should only be used for
    the Government (US1-FED) site.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Workload Protection Agent Rules (us1-fed)
      description: >-
        Get the list of agent rules.**Note**: This endpoint should only be used for
        the Government (US1-FED) site.
    paths:
      /api/v2/security_monitoring/cloud_workload_security/agent_rules:
        get:
          summary: Get All Workload Protection Agent Rules (us1-fed)
          description: >-
            Get the list of agent rules.**Note**: This endpoint should only be used
            for the Government (US1-FED) site.
          operationId: listCloudWorkloadSecurityAgentRules
          responses:
            '429':
              $ref: '#/components/responses/TooManyRequestsResponse'
