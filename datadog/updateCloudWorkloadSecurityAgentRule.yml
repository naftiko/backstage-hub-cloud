apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update A Workload Protection Agent Rule (us1-fed)
  description: >-
    Update a specific agent rule.Returns the agent rule object when the request
    is successful.**Note**: This endpoint should only be used for the Government
    (US1-FED) site.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update A Workload Protection Agent Rule (us1-fed)
      description: >-
        Update a specific agent rule.Returns the agent rule object when the request
        is successful.**Note**: This endpoint should only be used for the Government
        (US1-FED) site.
    paths:
      /api/v2/security_monitoring/cloud_workload_security/agent_rules/{agent_rule_id}:
        patch:
          summary: Update A Workload Protection Agent Rule (us1-fed)
          description: >-
            Update a specific agent rule.Returns the agent rule object when the
            request is successful.**Note**: This endpoint should only be used for
            the Government (US1-FED) site.
          operationId: updateCloudWorkloadSecurityAgentRule
          requestBody:
            description: New definition of the agent rule
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/CloudWorkloadSecurityAgentRuleUpdateRequest'
          responses:
            '429':
              $ref: '#/components/responses/TooManyRequestsResponse'
