apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update Scanning Group
  description: >-
    Update a group, including the order of the rules.Rules within the group are
    reordered by including a rules relationship. If the rulesrelationship is
    present, its data section MUST contain linkages for all of the
    rulescurrently in the group, and MUST NOT contain any others.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update Scanning Group
      description: >-
        Update a group, including the order of the rules.Rules within the group are
        reordered by including a rules relationship. If the rulesrelationship is
        present, its data section MUST contain linkages for all of the
        rulescurrently in the group, and MUST NOT contain any others.
    paths:
      /api/v2/sensitive-data-scanner/config/groups/{group_id}:
        patch:
          summary: Update Scanning Group
          description: >-
            Update a group, including the order of the rules.Rules within the group
            are reordered by including a rules relationship. If the
            rulesrelationship is present, its data section MUST contain linkages for
            all of the rulescurrently in the group, and MUST NOT contain any others.
          operationId: updateScanningGroup
          requestBody:
            content:
              application/json:
                schema:
                  type: object
                  example:
                    data:
                      attributes:
                        description: sunt magna de
                        filter:
                          query: commodo sit elit
                        is_enabled: false
                        name: deserunt
                        product_list:
                          - logs
                          - logs
                      id: adipisicing in occaecat enim
                      relationships:
                        configuration:
                          data:
                            id: cup
                            type: sensitive_data_scanner_configuration
                        rules:
                          data:
                            - id: sunt pariatur sed
                              type: sensitive_data_scanner_rule
                            - id: ex veniam
                              type: sensitive_data_scanner_rule
                      type: sensitive_data_scanner_group
                    meta:
                      version: 0
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
