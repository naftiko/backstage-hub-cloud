apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Remove Host Tags
  description: This endpoint allows you to remove all user-assigned tagsfor a single host.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Remove Host Tags
      description: This endpoint allows you to remove all user-assigned tagsfor a single host.
    paths:
      /api/v1/tags/hosts/{host_name}:
        delete:
          summary: Remove Host Tags
          description: >-
            This endpoint allows you to remove all user-assigned tagsfor a single
            host.
          operationId: removehosttags
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
