apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Quick List Of Security Signals
  description: >-
    The list endpoint returns security signals that match a search query.Both
    this endpoint and the POST endpoint can be used interchangeably when
    listingsecurity signals.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Quick List Of Security Signals
      description: >-
        The list endpoint returns security signals that match a search query.Both
        this endpoint and the POST endpoint can be used interchangeably when
        listingsecurity signals.
    paths:
      /api/v2/security_monitoring/signals:
        get:
          summary: Get A Quick List Of Security Signals
          description: >-
            The list endpoint returns security signals that match a search
            query.Both this endpoint and the POST endpoint can be used
            interchangeably when listingsecurity signals.
          operationId: listSecurityMonitoringSignals
          responses:
            '429':
              $ref: '#/components/responses/TooManyRequestsResponse'
