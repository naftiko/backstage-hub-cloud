apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Disable A User
  description: >-
    Disable a user. Can only be used with an application key belongingto an
    administrator user.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Disable A User
      description: >-
        Disable a user. Can only be used with an application key belongingto an
        administrator user.
    paths:
      /api/v2/users/{user_id}:
        delete:
          summary: Disable A User
          description: >-
            Disable a user. Can only be used with an application key belongingto an
            administrator user.
          operationId: disableauser
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
