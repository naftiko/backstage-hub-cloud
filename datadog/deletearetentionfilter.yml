apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Retention Filter
  description: Delete a specific retention filter from your organization.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Retention Filter
      description: Delete a specific retention filter from your organization.
    paths:
      /api/v2/apm/config/retention-filters/{filter_id}:
        delete:
          summary: Delete A Retention Filter
          description: Delete a specific retention filter from your organization.
          operationId: deletearetentionfilter
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
