apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Details Of An Incident Service
  description: >-
    Get details of an incident service. If the `include[users]` query parameter
    is provided,the included attribute will contain the users related to these
    incident services.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Details Of An Incident Service
      description: >-
        Get details of an incident service. If the `include[users]` query parameter
        is provided,the included attribute will contain the users related to these
        incident services.
    paths:
      /api/v2/services/{service_id}:
        get:
          summary: Get Details Of An Incident Service
          description: >-
            Get details of an incident service. If the `include[users]` query
            parameter is provided,the included attribute will contain the users
            related to these incident services.
          operationId: getIncidentService
          responses:
            '429':
              $ref: '#/components/responses/TooManyRequestsResponse'
