apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Archive Order
  description: >-
    Get the current order of your archives.This endpoint takes no JSON
    arguments.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Archive Order
      description: >-
        Get the current order of your archives.This endpoint takes no JSON
        arguments.
    paths:
      /api/v2/logs/config/archive-order:
        get:
          summary: Get Archive Order
          description: >-
            Get the current order of your archives.This endpoint takes no JSON
            arguments.
          operationId: getarchiveorder
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
