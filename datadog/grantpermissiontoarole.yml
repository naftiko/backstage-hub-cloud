apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Grant Permission To A Role
  description: Adds a permission to a role.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Grant Permission To A Role
      description: Adds a permission to a role.
    paths:
      /api/v2/roles/{role_id}/permissions:
        post:
          summary: Grant Permission To A Role
          description: Adds a permission to a role.
          operationId: grantpermissiontoarole
          requestBody:
            content:
              application/json:
                schema:
                  type: object
                  example:
                    data:
                      id: elit aliq
                      type: permissions
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
