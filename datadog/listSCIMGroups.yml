apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Groups
  description: >-
    List groups in the organization.Results are paginated by `startIndex` and
    `count` parameters.Results can be narrowed down by the `filter` parameter.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Groups
      description: >-
        List groups in the organization.Results are paginated by `startIndex` and
        `count` parameters.Results can be narrowed down by the `filter` parameter.
    paths:
      /api/v2/scim/Groups:
        get:
          summary: List Groups
          description: >-
            List groups in the organization.Results are paginated by `startIndex`
            and `count` parameters.Results can be narrowed down by the `filter`
            parameter.
          operationId: listSCIMGroups
          responses:
            '429':
              $ref: '#/components/responses/TooManyRequestsResponse'
