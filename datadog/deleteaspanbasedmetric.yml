apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Span-based Metric
  description: Delete a specific span-based metric from your organization.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Span-based Metric
      description: Delete a specific span-based metric from your organization.
    paths:
      /api/v2/apm/config/metrics/{metric_id}:
        delete:
          summary: Delete A Span-based Metric
          description: Delete a specific span-based metric from your organization.
          operationId: deleteaspanbasedmetric
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
