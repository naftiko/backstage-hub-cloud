apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update Cloud Cost Management Azure Config
  description: Update the status of an  Azure config (active/archived).
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update Cloud Cost Management Azure Config
      description: Update the status of an  Azure config (active/archived).
    paths:
      /api/v2/cost/azure_uc_config/{cloud_account_id}:
        patch:
          summary: Update Cloud Cost Management Azure Config
          description: Update the status of an  Azure config (active/archived).
          operationId: updateCloudCostManagementAzureconfig
          requestBody:
            content:
              application/json:
                schema:
                  type: object
                  example:
                    data:
                      attributes:
                        is_enabled: true
                      type: azure_uc_config_patch_request
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
