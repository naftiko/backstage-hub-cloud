apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get The On-demand Concurrency Cap
  description: Get the on-demand concurrency cap.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get The On-demand Concurrency Cap
      description: Get the on-demand concurrency cap.
    paths:
      /api/v2/synthetics/settings/on_demand_concurrency_cap:
        get:
          summary: Get The On-demand Concurrency Cap
          description: Get the on-demand concurrency cap.
          operationId: gettheondemandconcurrencycap
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
