apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get An Slo Correction For An Slo
  description: Get an SLO correction.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get An Slo Correction For An Slo
      description: Get an SLO correction.
    paths:
      /api/v1/slo/correction/{slo_correction_id}:
        get:
          summary: Get An Slo Correction For An Slo
          description: Get an SLO correction.
          operationId: getanSLOcorrectionforanSLO
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
