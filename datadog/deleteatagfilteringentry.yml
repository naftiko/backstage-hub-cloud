apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Tag Filtering Entry
  description: Delete a tag filtering entry.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Tag Filtering Entry
      description: Delete a tag filtering entry.
    paths:
      /api/v1/integration/aws/filtering:
        delete:
          summary: Delete A Tag Filtering Entry
          description: Delete a tag filtering entry.
          operationId: deleteatagfilteringentry
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
