apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Custom Destination
  description: Delete a specific custom destination in your organization.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Custom Destination
      description: Delete a specific custom destination in your organization.
    paths:
      /api/v2/logs/config/custom-destinations/{custom_destination_id}:
        delete:
          summary: Delete A Custom Destination
          description: Delete a specific custom destination in your organization.
          operationId: deleteacustomdestination
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
