apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Check If A Monitor Can Be Deleted
  description: Check if the given monitors can be deleted.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Check If A Monitor Can Be Deleted
      description: Check if the given monitors can be deleted.
    paths:
      /api/v1/monitor/can_delete:
        get:
          summary: Check If A Monitor Can Be Deleted
          description: Check if the given monitors can be deleted.
          operationId: checkifamonitorcanbedeleted
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
