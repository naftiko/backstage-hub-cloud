apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete An Sts Enabled Gcp Account
  description: Delete an STS enabled GCP account from within Datadog.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete An Sts Enabled Gcp Account
      description: Delete an STS enabled GCP account from within Datadog.
    paths:
      /api/v2/integration/gcp/accounts/{account_id}:
        delete:
          summary: Delete An Sts Enabled Gcp Account
          description: Delete an STS enabled GCP account from within Datadog.
          operationId: deleteanSTSenabledGCPAccount
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
