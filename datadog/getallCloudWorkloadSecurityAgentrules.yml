apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Cloud Workload Security Agent Rules
  description: Get the list of Agent rules.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Cloud Workload Security Agent Rules
      description: Get the list of Agent rules.
    paths:
      /api/v2/security_monitoring/cloud_workload_security/agent_rules:
        get:
          summary: Get All Cloud Workload Security Agent Rules
          description: Get the list of Agent rules.
          operationId: getallCloudWorkloadSecurityAgentrules
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
