apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Corrections For An Slo
  description: Get corrections applied to an SLO
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Corrections For An Slo
      description: Get corrections applied to an SLO
    paths:
      /api/v1/slo/{slo_id}/corrections:
        get:
          summary: Get Corrections For An Slo
          description: Get corrections applied to an SLO
          operationId: getCorrectionsForanSLO
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
