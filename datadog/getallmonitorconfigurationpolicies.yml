apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Monitor Configuration Policies
  description: Get all monitor configuration policies.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Monitor Configuration Policies
      description: Get all monitor configuration policies.
    paths:
      /api/v2/monitor/policy:
        get:
          summary: Get All Monitor Configuration Policies
          description: Get all monitor configuration policies.
          operationId: getallmonitorconfigurationpolicies
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
