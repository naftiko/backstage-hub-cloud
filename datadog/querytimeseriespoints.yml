apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Query Timeseries Points
  description: Query timeseries points.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Query Timeseries Points
      description: Query timeseries points.
    paths:
      /api/v1/query:
        get:
          summary: Query Timeseries Points
          description: Query timeseries points.
          operationId: querytimeseriespoints
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
