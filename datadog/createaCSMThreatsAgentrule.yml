apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Csm Threats Agent Rule
  description: >-
    Create a new Cloud Security Management Threats Agent rule with the given
    parameters.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Csm Threats Agent Rule
      description: >-
        Create a new Cloud Security Management Threats Agent rule with the given
        parameters.
    paths:
      /api/v2/remote_config/products/cws/agent_rules:
        post:
          summary: Create A Csm Threats Agent Rule
          description: >-
            Create a new Cloud Security Management Threats Agent rule with the given
            parameters.
          operationId: createaCSMThreatsAgentrule
          requestBody:
            content:
              application/json:
                schema:
                  type: object
                  example:
                    data:
                      attributes:
                        name: my_agent_rule
                        expression: exec.file.name == \"sh\"
                        description: My Agent rule
                        enabled: true
                      type: agent_rule
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
