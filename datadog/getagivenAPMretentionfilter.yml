apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Given Apm Retention Filter
  description: Get an APM retention filter.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Given Apm Retention Filter
      description: Get an APM retention filter.
    paths:
      /api/v2/apm/config/retention-filters/{filter_id}:
        get:
          summary: Get A Given Apm Retention Filter
          description: Get an APM retention filter.
          operationId: getagivenAPMretentionfilter
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
