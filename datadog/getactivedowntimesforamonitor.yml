apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Active Downtimes For A Monitor
  description: Get all active downtimes for the specified monitor.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Active Downtimes For A Monitor
      description: Get all active downtimes for the specified monitor.
    paths:
      /api/v2/monitor/{monitor_id}/downtime_matches:
        get:
          summary: Get Active Downtimes For A Monitor
          description: Get all active downtimes for the specified monitor.
          operationId: getactivedowntimesforamonitor
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
