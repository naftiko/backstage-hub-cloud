apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Webhook Integration
  description: Gets the content of the webhook with the name ``.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Webhook Integration
      description: Gets the content of the webhook with the name ``.
    paths:
      /api/v1/integration/webhooks/configuration/webhooks/{webhook_name}:
        get:
          summary: Get A Webhook Integration
          description: Gets the content of the webhook with the name ``.
          operationId: getawebhookintegration
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
