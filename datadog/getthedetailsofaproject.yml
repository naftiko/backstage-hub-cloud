apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get The Details Of A Project
  description: Get the details of a project by `project_id`.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get The Details Of A Project
      description: Get the details of a project by `project_id`.
    paths:
      /api/v2/cases/projects/{project_id}:
        get:
          summary: Get The Details Of A Project
          description: Get the details of a project by `project_id`.
          operationId: getthedetailsofaproject
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
