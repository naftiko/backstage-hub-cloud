apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Remove A User From A Role
  description: Removes a user from a role.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Remove A User From A Role
      description: Removes a user from a role.
    paths:
      /api/v2/roles/{role_id}/users:
        delete:
          summary: Remove A User From A Role
          description: Removes a user from a role.
          operationId: removeauserfromarole
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
