apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Teams
  description: >-
    Get all teams.Can be used to search for teams using the `filter[keyword]`
    and `filter[me]` query parameters.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Teams
      description: >-
        Get all teams.Can be used to search for teams using the `filter[keyword]`
        and `filter[me]` query parameters.
    paths:
      /api/v2/team:
        get:
          summary: Get All Teams
          description: >-
            Get all teams.Can be used to search for teams using the
            `filter[keyword]` and `filter[me]` query parameters.
          operationId: listTeams
          responses:
            '429':
              $ref: '#/components/responses/TooManyRequestsResponse'
