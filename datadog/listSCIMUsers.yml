apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: List Users
  description: >-
    List users in the organization.Results are paginated by `startIndex` and
    `count` parameters.Results can be narrowed down by the `filter` parameter.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: List Users
      description: >-
        List users in the organization.Results are paginated by `startIndex` and
        `count` parameters.Results can be narrowed down by the `filter` parameter.
    paths:
      /api/v2/scim/Users:
        get:
          summary: List Users
          description: >-
            List users in the organization.Results are paginated by `startIndex` and
            `count` parameters.Results can be narrowed down by the `filter`
            parameter.
          operationId: listSCIMUsers
          responses:
            '429':
              $ref: '#/components/responses/TooManyRequestsResponse'
