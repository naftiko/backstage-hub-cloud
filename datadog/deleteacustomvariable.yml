apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Custom Variable
  description: Deletes the endpoint with the name ``.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Custom Variable
      description: Deletes the endpoint with the name ``.
    paths:
      /api/v1/integration/webhooks/configuration/custom-variables/{custom_variable_name}:
        delete:
          summary: Delete A Custom Variable
          description: Deletes the endpoint with the name ``.
          operationId: deleteacustomvariable
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
