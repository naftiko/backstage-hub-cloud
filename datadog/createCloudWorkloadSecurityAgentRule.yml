apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create A Workload Protection Agent Rule (us1-fed)
  description: >-
    Create a new agent rule with the given parameters.**Note**: This endpoint
    should only be used for the Government (US1-FED) site.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create A Workload Protection Agent Rule (us1-fed)
      description: >-
        Create a new agent rule with the given parameters.**Note**: This endpoint
        should only be used for the Government (US1-FED) site.
    paths:
      /api/v2/security_monitoring/cloud_workload_security/agent_rules:
        post:
          summary: Create A Workload Protection Agent Rule (us1-fed)
          description: >-
            Create a new agent rule with the given parameters.**Note**: This
            endpoint should only be used for the Government (US1-FED) site.
          operationId: createCloudWorkloadSecurityAgentRule
          requestBody:
            description: The definition of the new agent rule
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/CloudWorkloadSecurityAgentRuleCreateRequest'
          responses:
            '429':
              $ref: '#/components/responses/TooManyRequestsResponse'
