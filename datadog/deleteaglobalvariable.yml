apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete A Global Variable
  description: Delete a Synthetic global variable.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete A Global Variable
      description: Delete a Synthetic global variable.
    paths:
      /api/v1/synthetics/variables/{variable_id}:
        delete:
          summary: Delete A Global Variable
          description: Delete a Synthetic global variable.
          operationId: deleteaglobalvariable
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
