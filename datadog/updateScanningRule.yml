apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update Scanning Rule
  description: >-
    Update a scanning rule.The request body MUST NOT include a standard_pattern
    relationship, as that relationshipis non-editable. Trying to edit the regex
    attribute of a rule with a standard_patternrelationship will also result in
    an error.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update Scanning Rule
      description: >-
        Update a scanning rule.The request body MUST NOT include a standard_pattern
        relationship, as that relationshipis non-editable. Trying to edit the regex
        attribute of a rule with a standard_patternrelationship will also result in
        an error.
    paths:
      /api/v2/sensitive-data-scanner/config/rules/{rule_id}:
        patch:
          summary: Update Scanning Rule
          description: >-
            Update a scanning rule.The request body MUST NOT include a
            standard_pattern relationship, as that relationshipis non-editable.
            Trying to edit the regex attribute of a rule with a
            standard_patternrelationship will also result in an error.
          operationId: updateScanningRule
          requestBody:
            content:
              application/json:
                schema:
                  type: object
                  example:
                    data:
                      attributes:
                        description: quis enim
                        excluded_namespaces:
                          - admin.name
                        included_keyword_configuration:
                          keywords:
                            - credit card
                            - cc
                          character_count: 30
                        is_enabled: false
                        name: incididunt
                        namespaces:
                          - admin
                        pattern: v
                        priority: 4
                        tags:
                          - elit dolor labore Excepteur deserunt
                          - incididunt mollit reprehenderit
                        text_replacement:
                          number_of_chars: 86464354
                          replacement_string: ipsum elit
                          type: none
                      id: ex esse sunt eiusmod Excepteur
                      relationships:
                        group:
                          data:
                            id: labore sed exercitation amet
                            type: sensitive_data_scanner_group
                        standard_pattern:
                          data:
                            id: sed anim Lorem ut in
                            type: sensitive_data_scanner_standard_pattern
                      type: sensitive_data_scanner_rule
                    meta:
                      version: 0
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
