apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Custom Destination
  description: Get a specific custom destination in your organization.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Custom Destination
      description: Get a specific custom destination in your organization.
    paths:
      /api/v2/logs/config/custom-destinations/{custom_destination_id}:
        get:
          summary: Get A Custom Destination
          description: Get a specific custom destination in your organization.
          operationId: getacustomdestination
          responses:
            '429':
              description: Too Many Requests
              headers:
                Content-Type:
                  schema:
                    type: string
                    example: application/json
              content:
                application/json:
                  schema:
                    type: object
                  example:
                    errors:
                      - Bad Request
                      - Bad Request
