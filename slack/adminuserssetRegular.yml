apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Admin.users.setregular
  description: Set an existing guest user, admin user, or owner to be a regular user.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Admin.users.setregular
      description: Set an existing guest user, admin user, or owner to be a regular user.
    paths:
      /admin.users.setRegular:
        post:
          summary: Post Admin.users.setregular
          description: Set an existing guest user, admin user, or owner to be a regular user.
          operationId: adminuserssetRegular
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                schema:
                  required:
                    - team_id
                    - user_id
                  type: object
                  properties:
                    team_id:
                      type: string
                      description: The ID (`T1234`) of the workspace.
                    user_id:
                      type: string
                      description: The ID of the user to designate as a regular user.
              application/json:
                schema:
                  required:
                    - team_id
                    - user_id
                  type: object
                  properties:
                    team_id:
                      type: string
                      description: The ID (`T1234`) of the workspace.
                    user_id:
                      type: string
                      description: The ID of the user to designate as a regular user.
          responses:
            default:
              description: Typical error response
              content:
                application/json:
                  schema:
                    title: Default error template
                    required:
                      - ok
                    type: object
                    properties:
                      ok:
                        $ref: '#/components/schemas/defs_ok_false'
                    additionalProperties: true
                    description: >-
                      This method either only returns a brief _not OK_ response or a
                      verbose schema is not available for this method.
                  example:
                    error: invalid_auth
                    ok: false
