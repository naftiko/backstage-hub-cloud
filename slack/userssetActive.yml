apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Users.setactive
  description: Marked a user as active. Deprecated and non-functional.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Users.setactive
      description: Marked a user as active. Deprecated and non-functional.
    paths:
      /users.setActive:
        post:
          summary: Post Users.setactive
          description: Marked a user as active. Deprecated and non-functional.
          operationId: userssetActive
          responses:
            default:
              description: Typical error response
              content:
                application/json:
                  schema:
                    title: users.setActive error schema
                    required:
                      - error
                      - ok
                    type: object
                    properties:
                      callstack:
                        type: string
                        description: 'Note: PHP callstack is only visible in dev/qa'
                      error:
                        type: string
                        enum:
                          - not_authed
                          - invalid_auth
                          - account_inactive
                          - token_revoked
                          - no_permission
                          - org_login_required
                          - ekm_access_denied
                          - missing_scope
                          - invalid_arguments
                          - invalid_arg_name
                          - invalid_charset
                          - invalid_form_data
                          - invalid_post_type
                          - missing_post_type
                          - team_added_to_org
                          - request_timeout
                          - fatal_error
                          - internal_error
                      ok:
                        $ref: '#/components/schemas/defs_ok_false'
                    additionalProperties: false
                    description: Schema for error response from users.setActive method
                  example:
                    error: invalid_auth
                    ok: false
