apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Admin.users.session.invalidate
  description: Invalidate a single session for a user by session_id
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Admin.users.session.invalidate
      description: Invalidate a single session for a user by session_id
    paths:
      /admin.users.session.invalidate:
        post:
          summary: Post Admin.users.session.invalidate
          description: Invalidate a single session for a user by session_id
          operationId: adminuserssessioninvalidate
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                schema:
                  required:
                    - session_id
                    - team_id
                  type: object
                  properties:
                    team_id:
                      type: string
                      description: ID of the team that the session belongs to
                    session_id:
                      type: integer
                      format: int32
              application/json:
                schema:
                  required:
                    - session_id
                    - team_id
                  type: object
                  properties:
                    team_id:
                      type: string
                      description: ID of the team that the session belongs to
                    session_id:
                      type: integer
                      format: int32
          responses:
            default:
              description: Typical error response
              content:
                application/json:
                  schema:
                    title: Default error template
                    required:
                      - ok
                    type: object
                    properties:
                      ok:
                        $ref: '#/components/schemas/defs_ok_false'
                    additionalProperties: true
                    description: >-
                      This method either only returns a brief _not OK_ response or a
                      verbose schema is not available for this method.
                  example:
                    error: invalid_auth
                    ok: false
