apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Admin.users.session.reset
  description: Wipes all valid sessions on all devices for a given user
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Admin.users.session.reset
      description: Wipes all valid sessions on all devices for a given user
    paths:
      /admin.users.session.reset:
        post:
          summary: Post Admin.users.session.reset
          description: Wipes all valid sessions on all devices for a given user
          operationId: adminuserssessionreset
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                schema:
                  required:
                    - user_id
                  type: object
                  properties:
                    user_id:
                      type: string
                      description: The ID of the user to wipe sessions for
                    mobile_only:
                      type: boolean
                      description: 'Only expire mobile sessions (default: false)'
                    web_only:
                      type: boolean
                      description: 'Only expire web sessions (default: false)'
              application/json:
                schema:
                  required:
                    - user_id
                  type: object
                  properties:
                    user_id:
                      type: string
                      description: The ID of the user to wipe sessions for
                    mobile_only:
                      type: boolean
                      description: 'Only expire mobile sessions (default: false)'
                    web_only:
                      type: boolean
                      description: 'Only expire web sessions (default: false)'
          responses:
            default:
              description: Typical error response
              content:
                application/json:
                  schema:
                    title: Default error template
                    required:
                      - ok
                    type: object
                    properties:
                      ok:
                        $ref: '#/components/schemas/defs_ok_false'
                    additionalProperties: true
                    description: >-
                      This method either only returns a brief _not OK_ response or a
                      verbose schema is not available for this method.
                  example:
                    error: invalid_auth
                    ok: false
