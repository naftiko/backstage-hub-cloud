apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Files.revokepublicurl
  description: Revokes public/external sharing access for a file
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Files.revokepublicurl
      description: Revokes public/external sharing access for a file
    paths:
      /files.revokePublicURL:
        post:
          summary: Post Files.revokepublicurl
          description: Revokes public/external sharing access for a file
          operationId: filesrevokePublicURL
          requestBody:
            content:
              application/x-www-form-urlencoded:
                schema:
                  type: object
                  properties:
                    file:
                      type: string
                      description: File to revoke
              application/json:
                schema:
                  type: object
                  properties:
                    file:
                      type: string
                      description: File to revoke
          responses:
            default:
              description: Typical error response
              content:
                application/json:
                  schema:
                    title: files.revokePublicURL error schema
                    required:
                      - error
                      - ok
                    type: object
                    properties:
                      callstack:
                        type: string
                        description: 'Note: PHP callstack is only visible in dev/qa'
                      error:
                        type: string
                        enum:
                          - file_not_found
                          - not_authed
                          - invalid_auth
                          - account_inactive
                          - token_revoked
                          - no_permission
                          - org_login_required
                          - user_is_bot
                          - user_is_restricted
                          - invalid_arg_name
                          - invalid_array_arg
                          - invalid_charset
                          - invalid_form_data
                          - invalid_post_type
                          - missing_post_type
                          - team_added_to_org
                          - invalid_json
                          - json_not_object
                          - request_timeout
                          - upgrade_required
                          - fatal_error
                      ok:
                        $ref: '#/components/schemas/defs_ok_false'
                    additionalProperties: false
                    description: Schema for error response from files.revokePublicURL method
                  example:
                    error: invalid_auth
                    ok: false
