apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Post Users.setpresence
  description: Manually sets user presence.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Post Users.setpresence
      description: Manually sets user presence.
    paths:
      /users.setPresence:
        post:
          summary: Post Users.setpresence
          description: Manually sets user presence.
          operationId: userssetPresence
          requestBody:
            required: true
            content:
              application/x-www-form-urlencoded:
                schema:
                  required:
                    - presence
                  type: object
                  properties:
                    presence:
                      type: string
                      description: Either `auto` or `away`
              application/json:
                schema:
                  required:
                    - presence
                  type: object
                  properties:
                    presence:
                      type: string
                      description: Either `auto` or `away`
          responses:
            default:
              description: Typical error response
              content:
                application/json:
                  schema:
                    title: users.setPresence error schema
                    required:
                      - error
                      - ok
                    type: object
                    properties:
                      callstack:
                        type: string
                        description: 'Note: PHP callstack is only visible in dev/qa'
                      error:
                        type: string
                        enum:
                          - invalid_presence
                          - not_authed
                          - invalid_auth
                          - account_inactive
                          - token_revoked
                          - no_permission
                          - org_login_required
                          - invalid_arg_name
                          - invalid_array_arg
                          - invalid_charset
                          - invalid_form_data
                          - invalid_post_type
                          - missing_post_type
                          - team_added_to_org
                          - invalid_json
                          - json_not_object
                          - request_timeout
                          - upgrade_required
                          - fatal_error
                      ok:
                        $ref: '#/components/schemas/defs_ok_false'
                    additionalProperties: false
                    description: Schema for error response from users.setPresence method
                  example:
                    error: invalid_auth
                    ok: false
