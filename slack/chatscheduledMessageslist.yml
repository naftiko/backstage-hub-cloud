apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Chat.scheduledmessages.list
  description: Returns a list of scheduled messages.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Chat.scheduledmessages.list
      description: Returns a list of scheduled messages.
    paths:
      /chat.scheduledMessages.list:
        get:
          summary: Get Chat.scheduledmessages.list
          description: Returns a list of scheduled messages.
          operationId: chatscheduledMessageslist
          responses:
            default:
              description: Typical error response if the channel passed is invalid
              content:
                application/json:
                  schema:
                    title: chat.scheduledMessages.list error schema
                    required:
                      - error
                      - ok
                    type: object
                    properties:
                      callstack:
                        type: string
                        description: 'Note: PHP callstack is only visible in dev/qa'
                      error:
                        type: string
                        enum:
                          - invalid_channel
                          - not_authed
                          - invalid_auth
                          - account_inactive
                          - token_revoked
                          - no_permission
                          - org_login_required
                          - ekm_access_denied
                          - missing_scope
                          - invalid_arguments
                          - invalid_arg_name
                          - invalid_charset
                          - invalid_form_data
                          - invalid_post_type
                          - missing_post_type
                          - team_added_to_org
                          - invalid_json
                          - json_not_object
                          - request_timeout
                          - upgrade_required
                          - fatal_error
                      ok:
                        $ref: '#/components/schemas/defs_ok_false'
                    additionalProperties: false
                    description: >-
                      Schema for error response from chat.scheduledMessages.list
                      method
                  example:
                    error: invalid_channel
                    ok: false
