apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Retrieve Bounce Classification Totals
  description: >-
    This endpoint will return the total number of bounces by classification in
    descending order for each day. You can retrieve the bounce classification
    totals in CSV format by specifying `"text/csv"` in the Accept header.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieve Bounce Classification Totals
      description: >-
        This endpoint will return the total number of bounces by classification in
        descending order for each day. You can retrieve the bounce classification
        totals in CSV format by specifying `"text/csv"` in the Accept header.
    paths:
      /v3/suppression/bounces/classifications:
        get:
          summary: Retrieve Bounce Classification Totals
          description: >-
            This endpoint will return the total number of bounces by classification
            in descending order for each day. You can retrieve the bounce
            classification totals in CSV format by specifying `"text/csv"` in the
            Accept header.
          operationId: listSuppressionBouncesClassifications
          responses:
            '401':
              description: ''
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ErrorResponse'
