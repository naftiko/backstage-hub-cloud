apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Create Segment
  description: >-
    Segment `name` has to be unique. A user can not create a new segment with an
    existing segment name.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Create Segment
      description: >-
        Segment `name` has to be unique. A user can not create a new segment with an
        existing segment name.
    paths:
      /v3/marketing/segments/2.0:
        post:
          summary: Create Segment
          description: >-
            Segment `name` has to be unique. A user can not create a new segment
            with an existing segment name.
          operationId: createSegment
          requestBody: {}
          responses:
            '500':
              description: ''
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ErrorsSegmentV2'
