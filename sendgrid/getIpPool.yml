apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Retrieve All The Ips In A Specified Pool
  description: >-
    **This endpoint allows you to get all of the IP addresses that are in a
    specific IP pool.**
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieve All The Ips In A Specified Pool
      description: >-
        **This endpoint allows you to get all of the IP addresses that are in a
        specific IP pool.**
    paths:
      /v3/ips/pools/{pool_name}:
        get:
          summary: Retrieve All The Ips In A Specified Pool
          description: >-
            **This endpoint allows you to get all of the IP addresses that are in a
            specific IP pool.**
          operationId: getIpPool
          responses:
            '404':
              description: ''
              content:
                application/json:
                  schema:
                    type: object
                    properties:
                      errors:
                        type: array
                        items:
                          type: object
                          properties:
                            field:
                              type: string
                              description: The name of the error.
                            message:
                              type: string
                              description: >-
                                A message explaining why the IP addresses could not
                                be listed.
                  examples:
                    response:
                      value:
                        errors:
                          - field: error
                            message: Unable to locate specified IPs Pool
