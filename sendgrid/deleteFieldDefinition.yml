apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Custom Field Definition
  description: >-
    **This endpoint deletes a defined Custom Field.**You can delete only Custom
    Fields; Reserved Fields cannot be deleted.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Custom Field Definition
      description: >-
        **This endpoint deletes a defined Custom Field.**You can delete only Custom
        Fields; Reserved Fields cannot be deleted.
    paths:
      /v3/marketing/field_definitions/{custom_field_id}:
        delete:
          summary: Delete Custom Field Definition
          description: >-
            **This endpoint deletes a defined Custom Field.**You can delete only
            Custom Fields; Reserved Fields cannot be deleted.
          operationId: deleteFieldDefinition
          responses:
            '404':
              description: ''
              content:
                application/json:
                  schema:
                    type: object
                    required:
                      - errors
                    properties:
                      errors:
                        type: array
                        uniqueItems: true
                        items:
                          $ref: '#/components/schemas/CustomFieldsError'
