apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Stop Warming Up An Ip Address
  description: >-
    **This endpoint allows you to remove an IP address from warmup mode.**Your
    request will return a 204 status code if the specified IP was successfully
    removed from warmup mode. To retrieve details of the IP’s warmup status
    *before* removing it from warmup mode, call the  "Retrieve the warmpup
    status for a specific IP address" endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Stop Warming Up An Ip Address
      description: >-
        **This endpoint allows you to remove an IP address from warmup mode.**Your
        request will return a 204 status code if the specified IP was successfully
        removed from warmup mode. To retrieve details of the IP’s warmup status
        *before* removing it from warmup mode, call the  "Retrieve the warmpup
        status for a specific IP address" endpoint.
    paths:
      /v3/ips/warmup/{ip_address}:
        delete:
          summary: Stop Warming Up An Ip Address
          description: >-
            **This endpoint allows you to remove an IP address from warmup
            mode.**Your request will return a 204 status code if the specified IP
            was successfully removed from warmup mode. To retrieve details of the
            IP’s warmup status *before* removing it from warmup mode, call the 
            "Retrieve the warmpup status for a specific IP address" endpoint.
          operationId: stopIpWarmUp
          responses:
            '404':
              description: ''
              content:
                application/json:
                  schema:
                    type: object
                    properties:
                      errors:
                        type: array
                        description: The errors that were encountered.
                        items:
                          type: object
                          properties:
                            field:
                              type: string
                              nullable: true
                            message:
                              type: string
                              description: >-
                                A message explaining why the IP couldn't be removed
                                from warmup.
                  examples:
                    response:
                      value:
                        errors:
                          - field: null
                            message: resource not found
