apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Deny Access Request
  description: >-
    **This endpoint allows you to deny an attempt to access your
    account.****Note:** Only teammate admins may delete a teammate's access
    request.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Deny Access Request
      description: >-
        **This endpoint allows you to deny an attempt to access your
        account.****Note:** Only teammate admins may delete a teammate's access
        request.
    paths:
      /v3/scopes/requests/{request_id}:
        delete:
          summary: Deny Access Request
          description: >-
            **This endpoint allows you to deny an attempt to access your
            account.****Note:** Only teammate admins may delete a teammate's access
            request.
          operationId: denyScopeRequest
          responses:
            '404':
              description: ''
              content:
                application/json:
                  schema:
                    type: object
                    properties:
                      errors:
                        type: array
                        items:
                          type: object
                          properties:
                            message:
                              type: string
                            field:
                              type: string
                  examples:
                    response:
                      value:
                        errors:
                          - message: Cannot find request
                            field: request_id
