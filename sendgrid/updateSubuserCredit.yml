apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Update The Credits For A Subuser
  description: '**This endpoint allows you to update the Credits for a Subuser.**'
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Update The Credits For A Subuser
      description: '**This endpoint allows you to update the Credits for a Subuser.**'
    paths:
      /v3/subusers/{subuser_name}/credits:
        put:
          summary: Update The Credits For A Subuser
          description: '**This endpoint allows you to update the Credits for a Subuser.**'
          operationId: updateSubuserCredit
          requestBody:
            required: true
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/SubuserCreditsRequest'
          responses:
            '400':
              description: ''
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ErrorResponse'
                  examples:
                    response:
                      value:
                        errors:
                          - field: type
                            message: >-
                              Type should be set to 'recurring', 'nonrecurring', or
                              'unlimited'
