apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Single Send Click Tracking Stats By Id
  description: >-
    **This endpoint lets you retrieve click-tracking stats for one Single
    Send**.The stats returned list the URLs embedded in the specified Single
    Send and the number of clicks each one received.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Single Send Click Tracking Stats By Id
      description: >-
        **This endpoint lets you retrieve click-tracking stats for one Single
        Send**.The stats returned list the URLs embedded in the specified Single
        Send and the number of clicks each one received.
    paths:
      /v3/marketing/stats/singlesends/{id}/links:
        get:
          summary: Get Single Send Click Tracking Stats By Id
          description: >-
            **This endpoint lets you retrieve click-tracking stats for one Single
            Send**.The stats returned list the URLs embedded in the specified Single
            Send and the number of clicks each one received.
          operationId: listSingleSendTrackingStat
          responses:
            '404':
              description: ''
              content:
                application/json:
                  schema:
                    type: object
