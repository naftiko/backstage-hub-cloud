apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Retrieve The Warmup Status For A Specific Ip Address
  description: >-
    **This endpoint allows you to retrieve the warmup status for a specific IP
    address.**You can retrieve all of your warming IPs using the "Retrieve all
    IPs currently in warmup" endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieve The Warmup Status For A Specific Ip Address
      description: >-
        **This endpoint allows you to retrieve the warmup status for a specific IP
        address.**You can retrieve all of your warming IPs using the "Retrieve all
        IPs currently in warmup" endpoint.
    paths:
      /v3/ips/warmup/{ip_address}:
        get:
          summary: Retrieve The Warmup Status For A Specific Ip Address
          description: >-
            **This endpoint allows you to retrieve the warmup status for a specific
            IP address.**You can retrieve all of your warming IPs using the
            "Retrieve all IPs currently in warmup" endpoint.
          operationId: getWarmUpIp
          responses:
            '404':
              description: ''
              content:
                application/json:
                  schema:
                    type: object
                    properties:
                      errors:
                        type: array
                        description: The errors that were encountered.
                        items:
                          type: object
                          properties:
                            field:
                              type: string
                              nullable: true
                            message:
                              type: string
                              description: >-
                                A message explaining why the warmup status could not
                                be retrieved.
                  examples:
                    response:
                      value:
                        errors:
                          - field: null
                            message: resource not found
