apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: This Request Lists All Of A User's Bulk Email Validation Jobs.
  description: >-
    **This endpoint returns a list of all of a user's Bulk Email Validation
    Jobs.**
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: This Request Lists All Of A User's Bulk Email Validation Jobs.
      description: >-
        **This endpoint returns a list of all of a user's Bulk Email Validation
        Jobs.**
    paths:
      /v3/validations/email/jobs:
        get:
          summary: This Request Lists All Of A User's Bulk Email Validation Jobs.
          description: >-
            **This endpoint returns a list of all of a user's Bulk Email Validation
            Jobs.**
          operationId: getValidationsEmailJobs
          responses:
            '500':
              description: ''
              content:
                application/json:
                  schema:
                    $ref: '#/components/schemas/ErrorResponse'
