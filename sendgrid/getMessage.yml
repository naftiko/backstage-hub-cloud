apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Filter Messages By Message Id
  description: Get all of the details about the specified message.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Filter Messages By Message Id
      description: Get all of the details about the specified message.
    paths:
      /v3/messages/{msg_id}:
        get:
          summary: Filter Messages By Message Id
          description: Get all of the details about the specified message.
          operationId: getMessage
          responses:
            '429':
              description: ''
              content:
                application/json:
                  schema:
                    type: object
                    properties:
                      errors:
                        type: array
                        items:
                          type: object
                          properties:
                            message:
                              type: string
                  examples:
                    response:
                      value:
                        errors:
                          - message: too many requests
