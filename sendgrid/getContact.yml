apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Contact By Id
  description: >-
    **This endpoint returns the full details and all fields for the specified
    contact**.The "Get Contacts by Identifier" endpoint can be used to get the
    ID of a contact.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Contact By Id
      description: >-
        **This endpoint returns the full details and all fields for the specified
        contact**.The "Get Contacts by Identifier" endpoint can be used to get the
        ID of a contact.
    paths:
      /v3/marketing/contacts/{id}:
        get:
          summary: Get A Contact By Id
          description: >-
            **This endpoint returns the full details and all fields for the
            specified contact**.The "Get Contacts by Identifier" endpoint can be
            used to get the ID of a contact.
          operationId: getContact
          responses:
            '500':
              $ref: '#/components/responses/MarketingContacts500'
