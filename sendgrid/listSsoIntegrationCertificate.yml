apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get All Sso Certificates By Integration
  description: >-
    **This endpoint allows you to retrieve all your IdP configurations by
    configuration ID.**The `integration_id` expected by this endpoint is the
    `id` returned in the response by the "Get All SSO Integrations" endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get All Sso Certificates By Integration
      description: >-
        **This endpoint allows you to retrieve all your IdP configurations by
        configuration ID.**The `integration_id` expected by this endpoint is the
        `id` returned in the response by the "Get All SSO Integrations" endpoint.
    paths:
      /v3/sso/integrations/{integration_id}/certificates:
        get:
          summary: Get All Sso Certificates By Integration
          description: >-
            **This endpoint allows you to retrieve all your IdP configurations by
            configuration ID.**The `integration_id` expected by this endpoint is the
            `id` returned in the response by the "Get All SSO Integrations"
            endpoint.
          operationId: listSsoIntegrationCertificate
          responses:
            '500':
              $ref: '#/components/responses/Sso500'
