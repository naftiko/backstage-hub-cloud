apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Listintegration
  description: This endpoint returns all the Integrations for the user making this call.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Listintegration
      description: This endpoint returns all the Integrations for the user making this call.
    paths:
      /v3/marketing/integrations:
        get:
          summary: Listintegration
          description: >-
            This endpoint returns all the Integrations for the user making this
            call.
          operationId: getIntegrationsByUser
          responses:
            '500':
              description: Internal Error
              content:
                application/json:
                  schema:
                    properties:
                      errors:
                        type: array
                        items:
                          $ref: '#/components/schemas/InternalError'
