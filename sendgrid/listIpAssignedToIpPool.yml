apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get Ips Assigned To An Ip Pool
  description: >-
    This operation returns the IP addresses that are assigned to the specified
    IP pool.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get Ips Assigned To An Ip Pool
      description: >-
        This operation returns the IP addresses that are assigned to the specified
        IP pool.
    paths:
      /v3/send_ips/pools/{poolid}/ips:
        get:
          summary: Get Ips Assigned To An Ip Pool
          description: >-
            This operation returns the IP addresses that are assigned to the
            specified IP pool.
          operationId: listIpAssignedToIpPool
          responses:
            '500':
              $ref: '#/components/responses/IpAddressManagement500'
