apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Retrieve Paged Transactional Templates.
  description: '**This endpoint allows you to retrieve all transactional templates.**'
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieve Paged Transactional Templates.
      description: '**This endpoint allows you to retrieve all transactional templates.**'
    paths:
      /v3/templates:
        get:
          summary: Retrieve Paged Transactional Templates.
          description: '**This endpoint allows you to retrieve all transactional templates.**'
          operationId: listTemplate
          responses:
            '400':
              description: ''
              content:
                application/json:
                  schema:
                    type: object
                    properties:
                      errors:
                        type: array
                        items:
                          type: object
                          properties:
                            message:
                              type: string
                            error_id:
                              type: string
