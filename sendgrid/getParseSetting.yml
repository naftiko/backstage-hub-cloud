apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Retrieve A Specific Parse Setting
  description: >-
    **This endpoint allows you to retrieve a specific inbound parse setting by
    hostname.**You can retrieve all your Inbound Parse settings and their
    associated host names with the "Retrieve all parse settings" endpoint.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Retrieve A Specific Parse Setting
      description: >-
        **This endpoint allows you to retrieve a specific inbound parse setting by
        hostname.**You can retrieve all your Inbound Parse settings and their
        associated host names with the "Retrieve all parse settings" endpoint.
    paths:
      /v3/user/webhooks/parse/settings/{hostname}:
        get:
          summary: Retrieve A Specific Parse Setting
          description: >-
            **This endpoint allows you to retrieve a specific inbound parse setting
            by hostname.**You can retrieve all your Inbound Parse settings and their
            associated host names with the "Retrieve all parse settings" endpoint.
          operationId: getParseSetting
          responses:
            '500':
              $ref: '#/components/responses/Webhook500'
