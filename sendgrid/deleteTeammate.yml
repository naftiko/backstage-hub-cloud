apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Delete Teammate
  description: >-
    **This endpoint allows you to delete a teammate.****Only the parent user or
    an admin teammate can delete another teammate.**
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Delete Teammate
      description: >-
        **This endpoint allows you to delete a teammate.****Only the parent user or
        an admin teammate can delete another teammate.**
    paths:
      /v3/teammates/{username}:
        delete:
          summary: Delete Teammate
          description: >-
            **This endpoint allows you to delete a teammate.****Only the parent user
            or an admin teammate can delete another teammate.**
          operationId: deleteTeammate
          responses:
            '404':
              description: ''
              content:
                application/json:
                  schema:
                    type: object
                    properties:
                      errors:
                        type: array
                        items:
                          type: object
                          properties:
                            message:
                              type: string
                            field:
                              type: string
                  examples:
                    response:
                      value:
                        errors:
                          - message: username not found
                            field: username
