apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Get A Specific Sender
  description: >-
    **This endpoint allows you to get the details for a specific Sender by
    `id`.**
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Get A Specific Sender
      description: >-
        **This endpoint allows you to get the details for a specific Sender by
        `id`.**
    paths:
      /v3/marketing/senders/{id}:
        get:
          summary: Get A Specific Sender
          description: >-
            **This endpoint allows you to get the details for a specific Sender by
            `id`.**
          operationId: getSender
          responses:
            '404':
              $ref: '#/components/responses/Senders404'
