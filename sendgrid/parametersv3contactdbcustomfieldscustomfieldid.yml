apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Parameters  Contactdb Custom_fields Custom_field_id
  description: Needs a more full description created.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Parameters  Contactdb Custom_fields Custom_field_id
      description: Needs a more full description created.
    paths:
      /v3/contactdb/custom_fields/{custom_field_id}:
        parameters:
          summary: Parameters  Contactdb Custom_fields Custom_field_id
          description: Needs a more full description created.
          operationId: parametersv3contactdbcustomfieldscustomfieldid
