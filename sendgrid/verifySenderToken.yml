apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: Verify Sender Request
  description: >-
    **This endpoint allows you to verify a sender requests.**The token is
    generated by SendGrid and included in a verification email delivered to the
    address that's pending verification.
  tags: []
  links:
    - title: Link
      icon: code
      url: https://example.com
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition: |
    openapi: 3.1.0
    info:
      title: Verify Sender Request
      description: >-
        **This endpoint allows you to verify a sender requests.**The token is
        generated by SendGrid and included in a verification email delivered to the
        address that's pending verification.
    paths:
      /v3/verified_senders/verify/{token}:
        get:
          summary: Verify Sender Request
          description: >-
            **This endpoint allows you to verify a sender requests.**The token is
            generated by SendGrid and included in a verification email delivered to
            the address that's pending verification.
          operationId: verifySenderToken
          responses:
            '500':
              $ref: '#/components/responses/VerifiedSenders500'
